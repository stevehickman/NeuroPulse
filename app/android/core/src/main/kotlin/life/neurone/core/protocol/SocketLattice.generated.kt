// Generated by scripts/sync-socket-map.ts — DO NOT EDIT BY HAND.
// Regenerate with: bun scripts/sync-socket-map.ts
//
// The PHYSICAL LATTICE only: how many sockets exist and how they are
// numbered. This file carries NO zone content, by design — a zone is defined
// by a `zone` block in a .npps file and nowhere else (NP-NPPS-REF-001 §8).
// Resolve zone names through NPZoneRegistry, which reads the loaded files.

package life.neurone.core.protocol

/**
 * The socket lattice: a hardware fact, changing only on an inner-bowl re-tool.
 *
 * Not a zone table. Which sockets a zone contains is authored in a `.npps`
 * file and answered by [NPZoneRegistry].
 */
object SocketLattice {

    /** Number of sockets on this lattice. */
    const val COUNT = 80

    /** Socket ids are 1-based — the numbering base every consumer honours. */
    const val NUMBERING_BASE = 1

    /** Inclusive lowest valid socket id. */
    const val MIN = 1

    /** Inclusive highest valid socket id. */
    const val MAX = 80

    /** Inclusive id range, for error messages and UI hints. */
    val rangeLabel: String get() = "$MIN–$MAX"

    fun isValid(id: Int): Boolean = id in MIN..MAX
}
