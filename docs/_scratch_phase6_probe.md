# Scratch — phase 6 skipped-check probe

Temporary. Exists only so a pull request can touch `docs/` and nothing else,
so NP-SW-CI-001 phase 6 can measure whether the firmware checks report
`skipped` rather than not reporting at all. Deleted with the scratch branch.

No section references, deliberately, so the section-ref guard has nothing to
resolve here.

Second docs-only commit so the push and pull_request events agree on scope.
