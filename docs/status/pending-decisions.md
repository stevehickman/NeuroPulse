# Remaining Weaknesses + Open Items

> Relocated from CLAUDE.md Rev 32 §13.1–§13.4 to slim the always-loaded core. This is the authoritative open-items log. Referenced from CLAUDE.md → Document Map.
>
> **Note on overlap:** much of the completed detail here and in `completed-decisions.md` also appears in the DHF index (`docs/np_dhf_001.md`) and git history. Content was relocated faithfully, not deduped — a dedup pass against the DHF is flagged as a follow-up (deleting authoritative memory should be a reviewed, per-item decision).

## 13.1 Critical — action required immediately

| Issue | Action | Cost/Timeline |
|-------|--------|---------------|
| **Zero published clinical trials** — 35-trial gap vs Vielight | Commission SBIR Phase I at company formation. First contacts: Rashidi-Ranjbar → Jog → Naeser | 2–3 years to published data |
| **400 mW/cm² regulatory opinion not obtained (RISK-03)** — cannot appear in ANY public material until cleared | Commission outside regulatory counsel (PBM/digital health specialist). Also assess Vielight comparison claim under FTC implied claim doctrine | $8,000–15,000 · 3–5 weeks |
| **"NeurOne" is an uncleared placeholder** — trademark not searched | Trademark search and clearance: US, EU, Canada, Australia. Required before ANY external conversation. | $15,000–25,000 |
| **CFRP shell slot rim Ra ≤ 1.6 µm unconfirmed (RISK-20)** — BLOCKING for tooling release | Obtain written confirmation from CFRP shell tooling supplier (letter + Ra measurement data from representative coupon). Supplier qualification item SUP-M-07 in NP-PROC-SUP-001. If Ra > 1.6 µm unavoidable: escalate to ME + EE; gasket or shell geometry must be revised. | $0 — supplier engagement required |
| **PDMS CAT-C supplier not selected (RISK-04)** — BLOCKING for production start | Select PDMS bonding supplier per NP-PROC-SUP-001 CAT-C criteria. Supplier must confirm IEC 60068-2-14 thermal cycling qualification capability. 200-cycle qualification (FAI-TC02) must pass before any production FPCs are built. | Supplier lead time 8 weeks from selection |

## 13.2 Moderate

| Issue | Status |
|-------|--------|
| SAB not formed — no scientific credentialing | Tsai outreach at 617-324-0305 (SAB role only). Jeffery and Naeser natural SAB candidates. Budget $50,000–80,000/yr for 5-person SAB. |
| Vulnerable population withdrawal edge case in research consent | **Resolved and locked** — irreversibility notice added at L3 blanket consent screen AND at per-project invitation (step 4). Forward-effectiveness guarantee added: on-device fresh-per-study anonymization makes consent withdrawal fully effective for all data periods. |
| 45W charger in box | **Decided and locked** — included in BOM across all configurations at appropriate wattage. Weakness resolved. |
| Zone module mold complexity (RISK-23) | NP-TOOL-ZM-001 created consolidating all 8 molded features (F-01 through F-08). 12-item mold design review checklist must be completed before steel is cut (NP-COORD-001 G1-05). |
| **clinical-03 treated area is ~8× the evidence aperture** | **Decided 2026-07-27** — evidence-grade claim gated behind REG-1 (option iii); duty/dose shortfall resolved by honest relabeling, not a firmware change. See §13.2c below. |
| **NIR PBM wavelength: 808–830nm in-window vs. extend to 850–860nm (OI-LED-W1)** | **OPEN 2026-07-28** — decision brief written, not yet decided. Two viable paths, real tradeoffs on both sides, no clear-cut answer. See §13.2d below. |

### 13.2c clinical-03 dose-fidelity gap (raised 2026-07-20, DECIDED 2026-07-27)

Resolving the clinical-03 lateralization question required pinning down what `docs/pbm_neuro_protocols.md` §3 means by "60 J/cm² **per site**". The doc's own arithmetic settles it, and the answer is larger than the lateralization issue that prompted the work.

**The evidence "site" is a single ~13.6 cm² aperture.** §3 states "Dose: 60 J/cm² per site (≈816–1632 J total over 1–2 sites)". 816 J ÷ 60 J/cm² = **13.6 cm²** per site — the aperture of the laser used across the Gonzalez-Lima / Barrett lineage. So "60 J/cm²/site" is **per-site-on-target**, an areal energy density over that one spot, and "site" means one ~4 cm-diameter spot over right lateral PFC (Fp2/F4), not a region.

**One hex module is one site.** A 40 mm flat-to-flat hexagon has area (√3/2)·W² = **13.86 cm²** (NP-HEX-ZM-001 §3 design point). The evidence aperture and one NeurOne module are the same size to within 2%. That is a coincidence worth knowing, and it makes the mismatch easy to state:

| Zone | Modules | Area | vs one evidence site |
|---|---|---|---|
| Evidence protocol | — | 13.6 cm² (1 site) or 27.2 cm² (2 sites) | 1× |
| `Frontal Right` (as originally shipped) | 11 | 152 cm² | **11.2×** |
| `Frontal Right (excl. midline)` (as now shipped) | 8 | 111 cm² | **8.2×** |

Narrowing the zone fixed lateralization; it did **not** fix this. clinical-03 still illuminates roughly eight times the evidence footprint.

**Two further fidelity gaps in the same protocol, resolved below:**

1. **Irradiance — RESOLVED 2026-07-27 (honest relabel, not a firmware change).** The evidence is 0.25 W/cm² **CW**. NP-BIB-1064-001 records NeurOne 1064nm running pulsed at 25% duty → **0.10 W/cm² average**, 2.5× below the gold standard. The protocol file declared `frequency: 0Hz` (CW) *and* `duty_cycle: 25%` simultaneously, self-contradictory as authored. Investigation found this is not just an authoring slip: `duty_cycle: 25%` is the **firmware-enforced ceiling** for the 1064nm channel (`np_pbm1064_drive_set_duty()`, NP-FW-PBM1064-001 §5 — "cannot be bypassed by the session layer"), and the npps schema itself only accepts `duty_cycle: 1–25`; true CW (100% duty) is not an option the protocol file can request. Separately, firmware caps this channel at **36 J/cm²/session** (OI-SES-01), below the literature's 60 J/cm²/site regardless of duty. **Given the choice between (a) relabeling the protocol to honestly describe what firmware actually delivers within existing safety ceilings, or (b) raising the firmware duty/dose ceilings to actually hit the gold standard — the owner chose (a).** Raising firmware safety ceilings is out of scope for a documentation pass and was explicitly declined; it would require its own safety/regulatory review (RISK-03, FAI-SM re-test, OI-SES-01 re-confirmation). `protocols/predefined/clinical-03-pbm-cognitive-1064.npps` (Rev 1.1) now states both the duty shortfall (2.5×) and the dose ceiling (36 vs. 60 J/cm²) explicitly, and withholds the "gold-standard"/CW claim rather than asserting parameters the hardware cannot produce.
2. **A doc inconsistency at the source protocol database, still open.** §3 of `docs/pbm_neuro_protocols.md` states "Duration: 8 min (240 s/site × 2 sites, **or 480 s single site**)", which is internally inconsistent: 480 s at 0.25 W/cm² is 120 J/cm², double the stated 60 J/cm²/site. The two-site reading is the self-consistent one. Left unedited deliberately — this is a clinical evidence bibliography document (not NeurOne's own delivered-dose claim, which is addressed in item 1 above) and the correction should be made by whoever owns that document.

**Decision (2026-07-27) — option (iii): gate the evidence-grade claim behind REG-1.** Narrowing clinical-03 to the 1–2 modules the evidence actually describes means naming the modules over Fp2/F4 — which depends on gate **REG-1** (socket lattice registers to 10-20 within tolerance, NP-HEX-ZM-001 §7), still open. Until REG-1 closes there is no honest way to author an Fp2/F4-precise zone. Rather than (i) hold clinical-03 at 8 modules and accept an ~8× area deviation from a protocol carrying a **Grade A / "gold-standard parameter set"** description, or (ii) reduce module count now on the provisional lattice and accept placement uncertainty, the decision is: **withhold the evidence-grade / "Grade A" / gold-standard characterization from `clinical-03-pbm-cognitive-1064.npps` until REG-1 lands and the zone is re-authored to the true 1–2 module Fp2/F4 footprint.** This is the conservative reading of the existing marketing-claims gate posture (cf. RISK-03 / NP-REG-PBM1064-001 §7). **Important:** REG-1 landing alone does not automatically lift the gate — the duty/dose shortfall in item 1 above is independent of zone geometry and must be separately re-evaluated (or explicitly accepted as a permanent, disclosed limitation) before any "gold-standard" claim is reinstated. Protocol file updated (`protocols/predefined/clinical-03-pbm-cognitive-1064.npps` Rev 1.1); decision logged in `docs/status/completed-decisions.md`. REG-1 (NP-HEX-ZM-001 §7) updated to list this claim gate as a consumer.

### 13.2d NIR wavelength decision (OI-LED-W1): stay in 808–830nm vs. extend to 850–860nm (raised 2026-07-28, OPEN — decision owner: SAB / Regulatory Counsel / EE Lead)

> **⚠ ELECTRICAL PREMISE CORRECTED 2026-08-16 — read §13.2e before using the Problem statement below.**
> The sentence *"vs. the ~1.6–2.2V the existing 660nm string/driver architecture assumes"* is **not
> sourced to any controlled document**, is not a 660 nm figure, and the incompatibility it asserts was
> anchored on a **15 V** rail that no longer exists. Two further facts found in the same pass:
> **SFH 4703AS's centroid wavelength is 810 nm** (peak 820 nm) per its own datasheet, and the part is
> **listed discontinued**. §13.2e states what replaces the premise. **OI-LED-W1 remains OPEN and
> undecided** — this correction changes the facts the owners decide against, nothing else.

**Problem** *(as written 2026-07-28; the Vf clause is superseded by §13.2e)*. No high-power (≥350mA-class, thermal-pad ceramic SMD) LED shortlisted for the NIR channel actually sits inside the CLAUDE.md §3 "808–830nm" spec. NP-PROC-FPC-001 §2.6.2's two electrically-workable candidates — Lumileds L1IZ-0850 (850nm) and ams-OSRAM SFH 4718A (860nm) — are 20nm and 30nm over the window respectively. The one candidate that **is** in-window, ams-OSRAM SFH 4703AS (820nm), has a Vf of ~3.0–3.6V (AlGaAs epitaxy) vs. the ~1.6–2.2V the existing 660nm string/driver architecture assumes — incompatible without a separate driver rail. Pulse-current rating (closed above) is not the blocker for either path; wavelength-vs-electrical-compatibility is.

**This is bigger than a component-sourcing question.** Extending the window is entangled with claims already made elsewhere in the doc set:

- CLAUDE.md §3 states "808–830nm" in eight separate places (PBM base module, intranasal probes, VNS/HRV PPG, visual-stimulation lens, mastoid roadmap) — a window change is cross-cutting, not FPC-local.
- `docs/reference/competitive-position.md` states the marketed wavelength as **"810nm"**, not a range, and one headline claim reads *"300 LEDs per wavelength — matching Neuronic's total LED count at each of the two CCO absorption peaks they don't cover."* That asserts 810nm **is** a CCO absorption peak, in direct comparison to Vielight (competitor, also 810nm). Shipping 850–860nm doesn't widen a spec, it falsifies a stated comparative claim.
- `docs/np_bib_001.docx`'s NIR clinical evidence is almost entirely Vielight Neuro Gamma studies run **at 810nm specifically** (dementia RCTs, gamma-entrainment RCT, cognition systematic review) — one entry states the literature "validates NeurOne's 808–830nm wavelength choice." Moving to 850–860nm weakens the "we replicate the clinically-tested protocol" argument that underwrites those citations. This is the same shape of problem already decided once in §13.2c above (clinical-03 dose-fidelity gap): the standing precedent there was to **withhold the matching-evidence claim rather than quietly redefine parameters to fit available hardware**, not to reinterpret the evidence to fit the part.
- NP-PROC-FPC-001 §2.6.2 already gates this: 860nm is noted as "slightly further from 808–830nm CCO absorption peaks... acceptable if SAB confirms" — OI-LED-W1 already requires Scientific Advisory Board sign-off before either 850 or 860nm can be adopted. Extending the window doesn't remove that gate.
- RISK-03 (400 mW/cm² regulatory opinion, BLOCKING for all public material, §13.1) is scoped to specific claimed parameters — a wavelength change on the headline NIR channel likely needs the regulatory opinion's scope re-checked, not assumed to still cover it.
- **It doesn't even solve the electrical problem it's trying to avoid.** L1IZ-0850 (850nm) has the *same* elevated-Vf issue as SFH 4703AS (2.7–3.7V vs. ~3.0–3.6V) — confirmed from its real datasheet (DS190 Table 2), not estimated. Extending the wavelength window trades away clinical/marketing fidelity without buying back string/driver compatibility.

**Option A — extend spec to 850nm or 860nm (L1IZ-0850 or SFH 4718A).**
Pros: current-current-rating already verified with wide margin (see closed item above); both are distributor-stocked, established suppliers, real Vf-binning capability.
Cons: requires SAB sign-off (OI-LED-W1 as currently scoped); contradicts the "810nm" claim already published in competitive-position.md; weakens the clinical-evidence-alignment argument in the bibliography; likely reopens RISK-03 regulatory scope; touches CLAUDE.md in 8 places; **still requires the string/driver rework** below since real Vf (2.7–3.7V for L1IZ-0850) is well above the current architecture's assumption — this option does not avoid OI-LED-01.

**Option B — stay in-window with SFH 4703AS (820nm) + separate driver rail.**
Pros: preserves the "810nm-class CCO peak" claim (820nm is 10nm from 810, still inside the evidence-cited 808–830nm band); no SAB gate required; no rewrite of competitive-position.md or the bibliography's evidence-alignment framing; no RISK-03 rescoping.
Cons: Vf ~3.0–3.6V requires a separate higher-voltage driver stage, string topology change, and a Hub PCB revision (real NRE and schedule cost, not paperwork); SFH 4703AS's own pulse-current rating at 120–180mA/≤25% duty was **not independently verified in this pass** — it was not a shortlisted candidate when OI-LED-04 was closed, precisely because of the Vf issue, so this is residual work if Option B is chosen.

**Option C — keep searching for an alternate in-window, high-power part.** Low-probability based on two research passes: true 800–830nm AlGaAs LEDs at ≥350mA in a thermal-pad ceramic package appear to be a genuinely under-served market segment — vendors in that exact wavelength band (Ushio/Epitex SMT810N, Marktech MTE-8xxx series) top out at 60–100mA (sensor/pulse-oximetry-class parts, not illumination-class), while the high-power ceramic NIR market has concentrated at 850nm/940nm (biometrics/ToF volume). Not recommended as the primary path; could run in parallel as a low-cost contingency via OI-LED-07 (Seoul Semiconductor outreach, already open).

**Not decided here.** This brief hands the tradeoff to whoever owns OI-LED-W1 (SAB for the science call, Regulatory Counsel for the RISK-03 scope question, EE Lead for the driver-rework cost) — it does not pick a path. Whichever option is chosen, update NP-PROC-FPC-001 §2.6.2/§2.6.3 (OI-LED-W1, OI-LED-01) and this entry with the decision and date.

### 13.2e The "~1.6–2.2 V" premise under §13.2d — traced, and it is not a specified constraint (2026-08-16)

**Question asked.** §13.2d rules out the only in-window NIR candidate on one stated ground: its Vf is
incompatible with *"the ~1.6–2.2V the existing 660nm string/driver architecture assumes."* Since that
sentence is the sole electrical reason for the exclusion, it was traced to source.

#### (a) The figure is an estimate, and it is not a 660 nm figure

There is **no controlled document that specifies a 1.6–2.2 V forward-voltage constraint.** The number
is a span welded from two different columns of one estimate table, and from two different channels:

| Source | Value | How the source labels it |
|---|---|---|
| `NP-PROC-FPC-001` §2.3, **660 nm** column | **2.0–2.2 V** at 150 mA | *"(confirm from datasheet)"* |
| `NP-PROC-FPC-001` §2.3, **808–830 nm** column | **1.6–1.8 V** at 150 mA | *"(confirm from datasheet)"* |
| `NP-HW-HEXTILE-001` §4.3, CH_A **660–670 nm** | **2.10 V** at 150 mA | design target |
| `NP-HW-HEXTILE-001` §4.3, CH_B **808–830 nm** | **1.60 V** at 150 mA | design target |

`1.6` is the **NIR** channel's lower bound. `2.2` is the **660 nm** channel's upper bound. Nothing in
the document set asserts `1.6–2.2 V` as a property of the 660 nm string, and the 660 nm figure taken
alone is `2.0–2.2 V`. **The answer to §13.2d's own framing is (b):** the typical Vf of unselected
AlGaInP/AlGaAs candidates, carried openly as an estimate, generalised into an architectural constraint
that was never specified.

Both owning documents say so in their own words, and neither was contradicted:

- `NP-HW-HEXTILE-001` **OI-HEXTILE-02**: *"§4.3's V_f and radiant-flux figures are **design targets,
  not datasheet values**."*
- `NP-HW-HEXTILE-001` §10: *"**Values deliberately NOT asserted:** … final emitter part numbers and
  their V_f/flux (§4.3)."*
- `NP-PROC-FPC-001` §2.6.1 action row: *"Pull full If-Vf curves from datasheets and confirm at 150mA.
  **All estimates.**"*

**The 660 nm primary's Vf has still never been read off a datasheet.** OI-LED-04 (closed 2026-07-28)
pulled *current* ratings for all three shortlisted parts; it did not pull Vf for GH CSSRM5.24, which
`NP-PROC-FPC-001` §2.6.1 still carries as *"~1.9 V (est. from ~2.0–2.1V @700mA DC; GaAlInP curve
shape)"*. So the 660 nm half of the range is an estimate of an estimate.

#### (b) The real constraint was a 15 V rail, and that rail is gone

The incompatibility claim §13.2d inherited comes from `NP-PROC-FPC-001` §2.6.2, whose actual wording
names a **rail**, not a Vf window:

> *"This is fundamentally incompatible with the 1.6–1.8V Vf assumed in §2.3 **and with driving red
> (660nm) and NIR strings on the same 15V VLED rail at similar string depth**."*

That is the load-bearing half, and it is stale. `NP-HW-HEXTILE-001` **D-6** sets `VLED` = **24 V**,
adopted programme-wide as **OI-HUB-C17b**, propagated into `NP-DRV-SHELL-002` Rev 2 §5.4 and
`NP-HW-HUB-001` §7.4, closing OI-SHELL2-01 — and merged to `main` (commit `c6b5dfa`, verified as an
ancestor of `origin/main`, not an open branch). An independent corroboration that §8.1 is a 24 V-era
rule while §2.6.2 is a 15 V-era leftover: §8.1's residual budget is 1.6 V, and **1.6 / 24 = 6.7 %**
(satisfying its own "≤7 %"), whereas **1.6 / 15 = 10.7 %** (violating it). §8.1's numbers are
self-consistent only at 24 V.

#### (c) The actual forward-voltage budget, derived from the current rail and tile spec

`NP-HW-HEXTILE-001` §8.1 states the governing rule: *"the residual dropped across the FET and sense
resistor is ≤1.6 V, so linear-loss overhead stays under 7 %."* On a 24 V rail that is:

> **N · V_f ≥ 22.4 V** (thermal/efficiency allocation) **and N · V_f ≤ 24 V − V_dropout(min)**
> (hard functional ceiling — below it the constant-current stage falls out of regulation).

The two bounds are **not the same kind of constraint** and should not be quoted as one. The upper is
functional: violate it and the string does not regulate. The lower is a **budget allocation** whose
dissipation lands on the tile, inside the §9.3 thermal argument — violating it produces heat, not
failure, but it is not a free preference either.

Against that budget, the currently specified strings and the in-window candidate:

| String | N | N · V_f | Residual | Overhead | Meets §8.1 |
|---|---|---|---|---|---|
| 660 nm at the §4.3 design target 2.10 V | 11 | 23.10 V | 0.90 V | 3.8 % | ✓ |
| 808–830 nm at the §4.3 design target 1.60 V | 14 | 22.40 V | 1.60 V | 6.7 % | ✓ (at the ceiling) |
| SFH 4703AS at the low end of its datasheet bracket, 2.80 V | **8** | 22.40 V | 1.60 V | 6.7 % | ✓ |
| SFH 4703AS at its 1 A datasheet typical, 3.30 V | **7** | 23.10 V | 0.90 V | 3.8 % | ✓ |
| SFH 4703AS anywhere in 3.00 < V_f < 3.20 | 7 or 8 | 21.0–22.1 / 24.0–25.6 V | — | >7 % / none | ✗ **dead band** |

**A 3.0–3.6 V part is therefore not categorically incompatible with a 24 V rail.** At both ends of the
bracket a compliant string exists, and each lands on a residual the tile spec has already accepted
somewhere. What remains is a **divisibility** question with an interior dead band at
**3.00 < V_f < 3.20 V** (≥0.20 V wide, and wider once driver dropout is subtracted) — not a
driver-architecture question.

**Three limits on that conclusion, stated because they bound it:**

1. **The 150 mA Vf is still unknown, for every candidate.** The datasheet tabulates Vf only at 1 A and
   2 A. The bracket above (2.80–3.30 V) comes from the datasheet's own `IF = f(VF)` curve, whose
   abscissa spans **2.8–3.6 V** across 10⁻²–10¹ A, plus the 3.3 V typical at 1 A — i.e. it is read
   from **axis limits, not from the trace**. Whether the part lands in the dead band or outside it is
   exactly the open question, and it is one curve-read away. This is OI-LED-01's standing complaint
   (*"not the 700mA–1A datasheet test points cited here"*) applied to a third part.
2. **Unbinned, no fixed N survives — for any candidate.** SFH 4703AS's Vf max is **4.0 V**; 7 × 4.0 =
   28 V, over the rail. The typ→max spread of 0.7 V per die is 4.9 V across seven in series, **three
   times the entire 1.6 V residual budget.** Fixed-N string construction on a 24 V rail is viable only
   because `NP-PROC-FPC-001` §2.1 mandates **±0.10 V Vf binning within a purchase order**. That spec
   is load-bearing for the whole architecture, not just for RISK-08 current matching — worth stating,
   because §2.1 currently justifies itself only on current-hogging grounds.
3. **Two inputs the budget needs are not specified anywhere.** Neither `NP-HW-HEXTILE-001`,
   `NP-HW-HUB-001` nor `NP-DRV-SHELL-002` states **(i)** a minimum dropout for the constant-current
   stage, or **(ii)** a tolerance on the 24 V rail. At ±5 % the low corner is 22.8 V, which against a
   22.4 V floor leaves ~0.4 V of window *before* subtracting dropout. Until both are stated, every
   string-length result above is a nominal-point calculation, not a corner analysis. Raised as
   **OI-HEXTILE-18** and **OI-HEXTILE-19** in `NP-HW-HEXTILE-001` Rev 4 §11.

#### (d) Two facts about SFH 4703AS that change §13.2d independently of the Vf question

Pulled from the ams-OSRAM datasheet (*Produktdatenblatt* Version 1.1, part footer Version 0.3 /
2019-07-22) rather than from a distributor parametric field:

| Parameter | Datasheet | What §13.2d / NP-PROC-FPC-001 §2.6.2 carries |
|---|---|---|
| **Centroid wavelength** | **810 nm** typ — series is titled *"OSLON Black Series (810 nm)"* | not recorded anywhere; the doc set calls the part "820nm" throughout |
| Peak wavelength | 820 nm typ | 820 nm ✓ |
| V_F at I_F = 1 A, t_p = 10 ms | **typ 3.3 V**, max 4.0 V | *"Vf=3.55V @1A"* — **not a datasheet value** |
| V_F at I_F = 2 A, t_p = 100 µs | typ 3.7 V, max 4.9 V | — |
| **I_F max (DC)** | **1000 mA** | *"2000mA DC (11× DC margin)"* — **wrong** |
| I_FSM (surge) | 2 A at t_p ≤ 200 µs, **D = 0.005** | conflated into the DC row above |
| P_tot / T_j / R_thJS / TC_V | 4 W / 145 °C / 16 K/W / −2 mV/K | not recorded |

- **The part's centroid is 810 nm.** `docs/reference/competitive-position.md` markets the wavelength as
  **810 nm** and asserts it *is* a CCO absorption peak. The one in-window candidate matches that
  published claim **exactly, on the manufacturer's own centroid specification** — a materially stronger
  position than §13.2d's "820nm is 10nm from 810, still inside the band." This is a fact for the SAB,
  not a decision taken here.
- **The §2.6.2 "2000mA DC / 11× margin" row is wrong** — 2 A is the surge rating at 0.5 % duty, not a
  DC maximum. This is the same error class already caught once in this document set (GH CSSRM5.24's
  "700 mA", which was the optical binning current). **It happens to correct in the safe direction:**
  §13.2d's residual item — SFH 4703AS's pulse rating at 120–180 mA / ≤25 % duty, never independently
  verified — **now resolves PASS at 5.6× margin, not the 11× the table claims.** I_F max = 1000 mA is a
  DC rating, so it bounds any duty cycle up to CW; no interpolation on the duty-cycle-derated pulse
  family is needed. Same argument the set already accepted for SFH 4718A.

#### (e) …and the part is listed discontinued

The ams-OSRAM product page, DigiKey and RS Components all list **SFH 4703AS as discontinued /
end-of-life**; the datasheet PDF carries a *"Preliminary datasheet version Discontinued"* status
stamp on every page. `NP-PROC-FPC-001` §2.6.2 records it as *"DigiKey, Mouser stocked"* — that row
is stale (it dates from Issue #9, 2026-05-08).

**Stated at the strength of the evidence actually held:** this is three vendor status fields, **not a
retrieved PCN**. Enough to stop designing the part in; not enough to assert as fact in a controlled
document. Confirming it against a Product Discontinuation Notice, and confirming the EOL applies to
the exact orderable variant rather than a sibling suffix, is residual work below.

#### (f) What this correction does and does not license

**It does not make Option B viable, and it is not a decision on OI-LED-W1.** It removes one stated
objection and replaces it with a narrower, better-evidenced one:

| §13.2d said | Now |
|---|---|
| SFH 4703AS is *"incompatible without a separate driver rail"* | **Not supported by the constraint it cites.** That constraint was a 15 V rail; the rail is 24 V |
| Option B costs *"a separate higher-voltage driver stage, string topology change, and a Hub PCB revision (real NRE and schedule cost)"* | **The Hub-side cost is not established.** The rail already exists. What is established is a **per-tile** consequence: a ~3 V part needs 7–8 emitters per string against the 14 the NIR channel is specified at, roughly **doubling parallel strings and sense resistors on the tile** — which is the same objection §8.1 raises against a 12 V rail, on a tile it says *"has no room for them."* A rigidizer-layout and RISK-08 current-matching question, not a Hub PCB question |
| Option B's residual risk is an unverified pulse rating | **Resolved PASS** (5.6× margin, §13.2e(d)). Replaced by two harder residuals: the part is **EOL**, and its 150 mA Vf may sit in the dead band |
| Option C (keep searching in-window) is *"low-probability"*, partly because in-window parts carry the Vf problem | **The search filter was wrong.** If elevated Vf is not disqualifying at 24 V, the candidate set is wider than the two passes assumed. The ams-OSRAM **OSLON Black 810 nm** family is the specific place to look — the EOL part's own series, named for the wavelength the marketing claim asserts |

**Nothing here weakens the 810 nm claims, and no claim is strengthened on this evidence either.** The
conservative-claim precedent from §13.2c holds: the point of this correction is that the 810 nm-class
claim may be *keepable more cheaply than §13.2d assumed*, not that it has been vindicated. Per §13.2d,
OI-LED-W1 still requires **SAB** on the science, **Regulatory Counsel** on RISK-03 scope, and **EE
Lead** on cost. All three now decide against a corrected electrical premise.

#### (g) Residual work created by this correction

| # | Item | Owner |
|---|---|---|
| 1 | **Read Vf at 120–180 mA off the `IF = f(VF)` curve** for GH CSSRM5.24 (660 nm), SFH 4718A, L1IZ-0850 and any in-window candidate. Every string-length number in the set — including the specified 11 × 2.10 and 14 × 1.60 — currently rests on design targets, not datasheet values (OI-HEXTILE-02, OI-LED-01) | EE Lead |
| 2 | **Confirm SFH 4703AS EOL against a PCN**, and confirm it applies to the exact orderable variant | Procurement |
| 3 | **Re-run the Option C search with the Vf filter removed**, starting with the ams-OSRAM OSLON Black 810 nm family. Feeds OI-LED-07 | EE Lead + Procurement |
| 4 | **`NP-PROC-FPC-001` §2.6.2 corrections** (`.docx` — not hand-edited here, per standing practice): the "2000mA DC / 11× margin" row is the surge rating and must read **1000 mA DC / 5.6×**; "Vf=3.55V @1A" is not a datasheet value (**typ 3.3 V, max 4.0 V**); add **centroid 810 nm**; the "DigiKey, Mouser stocked" row is stale; and the §2.6.2 CRITICAL FINDING's **15 V rail** anchor must be restated at 24 V or withdrawn | Hardware Engineering |
| 5 | **State a minimum driver dropout and a 24 V rail tolerance** — OI-HEXTILE-18 / OI-HEXTILE-19 | EE Lead |
| 6 | **`NP-PROC-FPC-001` §2.1's ±0.10 V binning spec is load-bearing for string construction**, not only for RISK-08 current matching. Worth stating in §2.1 itself | Hardware Engineering |

**Documents changed by this pass:** this entry (§13.2d banner, §13.2e new); `NP-HW-HEXTILE-001` Rev 3
→ **Rev 4** (§8.1 string budget generalised; OI-HEXTILE-18/17 raised). **No decision was taken on
OI-LED-W1, OI-LED-01, OI-HEXTILE-02 or RISK-03.**

## 13.2b tFUS / LIFU — modality watch (elevated to MEDIUM 2026-05-13)

Science basis confirmed: PIEZO1 mechanosensitive ion channel mechanism (PNAS 2023); strong safety record (n > 400 human subjects, no serious AEs at MI < 1.9); first wearable coupling breakthrough (MiniUlTra bioadhesive hydrogel, Nature Communications 2025). Cannot enter T1 (no wellness regulatory pathway for brain-targeted therapeutic ultrasound). T2 add-on requires 510(k) predicate, which does not yet exist.

**Re-evaluation trigger:** SPIRE DIADEM FDA clearance (pivotal trial submission anticipated 2026, decision ~2027–2028). On clearance: evaluate T2 add-on 510(k) for DLPFC-targeting LIFU pod at 500–650 kHz, targeting frontal cortex (~15–20 mm depth, no skull correction required). Natural synergy with existing tDCS/tACS/sLORETA-HD-tDCS stack and EEG-guided closed-loop architecture.

**Active watch items:**

| Item | Why it matters | Expected timing |
|------|---------------|----------------|
| SPIRE DIADEM FDA pivotal trial + clearance | First US 510(k) acoustic neuromodulation predicate | Submission 2026, decision 2027–2028 |
| Sonomind CE mark (EU, depression, acoustic metalens) | First EU CE mark for focused tFUS neuromodulation; EU regulatory precedent | 2027 |
| Hair-compatible hydrogel coupling | MiniUlTra demonstrated 35-day adhesion on shaved scalp — hair-gap solution is the remaining consumer engineering blocker | Watch UT Austin + Stanford Wearable Electronics Initiative |
| Li-Huei Tsai lab (SAB candidate) on 40 Hz ultrasound | If GENUS multi-sensory protocol extends to ultrasound as 40 Hz entrainment channel, adds strategic synergy to existing mastoid pad program | Ongoing; flag at SAB formation outreach |
| ITRUSST standards consolidation | Standardised reporting enables cross-study comparison; affects which clinical evidence is citable | Published 2024–2025; monitor updates |

**Not feasible under any current regulatory framework for T1.** Do not include in T1 product claims, BOM, or marketing.

## 13.3 Structural (accepted, managed)

| Issue | Mitigation |
|-------|-----------|
| Fluxgate calibration requires Tier B service visit (every 3–5 years) | Geomagnetic comparison detects severe drift. Fleet SHDR predicts need with months of lead time. 3–5 year interval means Tier B network established before first visits needed. |
| iOS/Android OS update dependency | Apple/Google developer beta participation. 7-day OS compatibility SLA. Autonomous Mode 3 as structural fallback. |

## 13.4 Pending decisions — must resolve before tooling is cut

- [ ] Product name trademark clearance
- [ ] 400 mW/cm² regulatory opinion letter (RISK-03 — BLOCKING for all public material)
- [x] **LED emitter pulse current rating verification (660nm + 808–830nm FPC candidates at 120–180mA)** — **CLOSED 2026-07-28:** pulled the actual manufacturer datasheets for all three parts in the NP-PROC-FPC-001 §2.6 shortlist (Rev B) and confirmed current-handling margin directly from published Absolute Maximum Ratings / pulse-handling curves, not the estimates the shortlist had been carrying:
  - **660nm primary — ams-OSRAM GH CSSRM5.24** (datasheet v1.1, 2023-01-18): I_F max = **1400 mA continuous** (min 100 mA), I_FS surge = 1500 mA at ≤10 µs/D=0.005. No separate duty-cycle-derated pulse table exists — Osram's "Max. Permissible Forward Current" chart holds the full 1400 mA flat up to ~120 °C case temperature, i.e. thermal (not electrical pulse) limits govern at our operating point. **180 mA operating ceiling sits at 7.8× margin below I_F max.** Closes the current-rating half of OI-LED-04.
  - **808–830nm primary candidate — Lumileds L1IZ-0850 (850nm)** (DS190, 2018-06-19): I_F(DC) max = 1000 mA, Peak Pulsed I_F max = 3000 mA, and the datasheet publishes an actual duty-cycle-family pulse-handling curve (Fig. 9). At ~1 ms pulse width and ≤25% duty (between the D=0.10 and D=0.20 traces) the chart shows ≈2.5–3 A permissible — **180 mA sits at >13× margin** inside the manufacturer's own derated envelope.
  - **808–830nm secondary candidate — ams-OSRAM SFH 4718A (860nm)** (datasheet v1.5, 2026-01-09): I_F(DC) max = 1000 mA, I_F pulsed max = 1 A at D≤1 (covers any duty cycle up to CW). **180 mA sits at 5.6× margin.**
  - **Not closed by this item — two adjacent, larger problems surfaced and remain separately tracked, do NOT let this checkbox read as "NIR emitter selection is done":** (1) **OI-LED-W1 (wavelength, BLOCKING, unchanged):** neither NIR candidate actually falls in the required 808–830nm window — L1IZ-0850 is 850nm (+20nm over), SFH 4718A is 860nm (+30nm over); the one part that does land in-window (SFH 4703AS, 820nm) has the Vf-incompatibility problem already logged in NP-PROC-FPC-001 §2.6.2, not a current-rating problem. **Decision brief written 2026-07-28 — see §13.2d below** (options: extend spec to 850–860nm vs. stay in-window with SFH 4703AS + driver rework; not yet decided). (2) **OI-LED-01 (V_f assumptions, BLOCKING for string/RSET design, now confirmed materially worse than tracked):** L1IZ-0850's real V_f is 2.7–3.7 V typ 3.2 V at 1000 mA (Table 2) — higher than both the original NP-PROC-FPC-001 §2.3 estimate (1.6–1.8 V) and the procurement doc's already-revised estimate (~2.0–2.2 V est.); SFH 4718A's real V_f (1.75 V typ / 2.05 V max, §Characteristics) is close to the *original* 1.6–1.8 V estimate but was itself under-revised in NP-PROC-FPC-001 §2.6.2 (guessed ~1.4–1.5 V). Hub PCB VLED string depth and BCR421W RSET values cannot be finalized until OI-LED-01 is re-closed against these real numbers. **Residual:** update NP-PROC-FPC-001 §2.6.1/§2.6.2 tables and OI-LED-01/OI-LED-04 status directly (docx, not hand-edited here) with these datasheet citations; SFH 4703AS current rating was not independently pulled (not a shortlisted candidate — Vf-blocked regardless of current rating).
- [x] Zone module FPC layout specification — freeze layout so PD2 aperture position (F-04 in NP-TOOL-ZM-001) can be specified in mold design
- [ ] Zone module mold design review — NP-TOOL-ZM-001 §5 checklist (all 8 features F-01 through F-08) signed off before steel cut
- [ ] **CFRP shell slot rim Ra ≤ 1.6 µm — written supplier confirmation (RISK-20 BLOCKING)**
- [ ] **PDMS CAT-C supplier selection + 200-cycle IEC 60068-2-14 thermal cycling qualification (RISK-04 BLOCKING)**
- [x] ZONE_ID firmware debounce spec written into firmware requirements document (3×ADC at 100ms, ≥2/3 pass) — **CLOSED 2026-07-27:** requirement already implemented identically in NP-FW-ZA-001 §6.2 and NP-FW-PBM1064-001 §4 (RISK-18); now also stated at the software-development-plan level in NP-SW-001 Rev 2 §5.2.1 so it is traceable from the master firmware requirements document itself.
- [ ] HFE formative study for sliding eject lever — 5 subjects with Parkinson's/post-stroke (NP-TOOL-ZM-001 OI-4, NP-FAI-ZM-001 FAI-A15)
- [x] EEG cable routing path in shell CAD model — OI-09 in NP-DRV-SHELL-001 (required for DRC-18 verification, RISK-21)
- [x] **Hub tooling: probe dock + anchor posts + large-radius Boa cable channel + tool-free fan (quarter-turn captive fastener)** — **NP-TOOL-HUB-001 Rev 1 written 2026-07-27:** F-01 probe dock (Y-junction saddle + bilateral silicone-padded receptacles, ≤2N retention); F-02 two tethered port-cover anchor posts (≤20mm tether, bounded so it cannot reach the fan intake grille); F-03 Boa cable channel through the hub housing at ≥12×-lace-OD minimum bend radius, PTFE-lined continuation of the shell channel; F-04 tool-free fan/heatsink access door (two diagonally-opposed quarter-turn captive fasteners, ≤6mm louvre slot finger-probe safety). Hub placement (occipital arch, external to the shielded shell) inferred from the already-locked EEG routing origin (NP-DRV-SHELL-001 Rev 2) plus the hub-resident antennas (CLAUDE.md §4.1) and external fan-cooled heatsink (NP-THERM-CFD-R1-001 §5) — flagged as an open item (OI-HTOOL-01) pending CAD lock, not yet a locked mechanical interface. 11-item mold design review checklist; 6 open items (OI-HTOOL-01–06); no separate mold tool required (features of the single hub housing mold). See `docs/np_tool_hub_001.md`.
- [ ] Shell tooling: anchor posts for all interface covers (5 zone + 3 port positions, color-coded for zones) + EEG cable channel (§2.4) — **REOPENED 2026-08-18. This was closed against a shell that no longer exists.** `NP-TOOL-SHELL-001` Rev 1 was written 2026-05-10 and specified exactly what this line asks for; opening it on 2026-08-18 confirmed that the *5 zone positions* and the *EEG cable channel* — i.e. both halves of this item — are retired by `NP-HEX-ZM-001` and `NP-DRV-SHELL-002` respectively. The 3 port positions survive but now conflict with `NP-TOOL-HUB-001` F-02 (**OI-ART-07**). What actually remains open: the L0 outer bowl's clamp latches, posterior blind-mate boss and parting-plane lip (**MECH-1**), the L1 inner bowl's tooling (no document at all), and whether unpopulated hex sockets need plugs — a design question, not a renumbering (**OI-ART-01**). See the banner in `docs/np_tool_shell_001.docx` and NP-TOOL-SHELL-001 Rev 2.
- [x] Lens tooling: sliding rail + N42 magnet positions (≥1mm polymer wall all faces) + AgNW spec + hard coat + EC driver contacts — **NP-TOOL-LENS-001 Rev 2 (2026-05-10)**
- [x] Goggle arm tooling: anchor hook for lens rim guard tether — **NP-TOOL-LENS-001 Rev 2 F-07: hook at superior-posterior corner, 4mm ID, shade clearance ≥4mm (MR-19), tether ≤45mm posterior routing no FOV crossing (MR-20), OI-10 tracks CAD confirmation. Addresses Issue #18.**
- [x] **1064nm smart module hardware — FPC layout variant (NP-HW-FPC-001 Rev 5, NP-FPC-ZM-SM-01), mold variant (NP-TOOL-ZM-SM-001 Rev 1), InGaAs PD (Hamamatsu G12180-010A), 1064nm LED emitters (EPITEX via Marubeni), on-module driver (ATtiny402 + 3× IRLML6344 N-FETs, BOM $1.00), 550 LEDs (200×660nm + 200×808nm + 150×1064nm) — Issue #54 CLOSED (PR #58)**
- [x] **1064nm smart module — on-module I2C LED driver IC selected: Microchip ATtiny402 (I2C slave, SOT-23-8) + 3× Infineon IRLML6344TRPBF N-channel MOSFETs (SOT-23); total driver BOM $1.00 — hardware decision LOCKED (Issue #53, PR #57)**
- [x] **OI-PBM-HW-01 (SPECIFIED — Issue #62): Hub PCB Rev B adds Vishay DG2788A per-slot analog switch (×5, $0.75–1.25 BOM) to reduce TIA Rf 47kΩ→22kΩ for InGaAs PD slots (2× higher responsivity saturates TIA at current gain). ZONE_ID debounce triggers gain switch (GAIN_SEL GPIO HIGH) BEFORE I2C mux enable. NXP PCA9546A I2C isolation switch added (OI-PBM-HW-02 also closed). Hub PCB Rev B Gerber build required before FAI-SM-04/06 optical bench tests. See NP-HW-HUB-001 Rev 2, firmware/pbm_1064nm/ (np_pbm1064_hal_tia_gain_set, np_pbm1064_hal_tia_gain_boot_init, np_pbm1064_detect.c). ⚠ SUPERSEDED 2026-07-28 — this closure was for 5 zone slots; SMART-1 (§13.2d, `docs/np_hex_zm_001.md` §4a/§7) now requires every socket I2C/TIA-capable, reopening the Hub PCB TIA-gain design as a Rev 3 item, not resolved by this closure.**
- [x] **RISK-03 regulatory opinion scope expansion: must include 1064nm irradiance, three-channel aggregate irradiance (600 mW/cm² limit OI-PBM-05), and any T2 combined 1064+1170nm claim (Issue #56)** — **NP-REG-PBM1064-001 Rev 1 (2026-05-13): scope expansion brief delivered to counsel; 12 questions covering 1064nm FDA pathway (Q1–Q3), aggregate irradiance IEC 62471 + FTC (Q4–Q6), T2 combined 1064+1170nm (Q7–Q9), depth-tier penetration claim FTC substantiation (Q10–Q12). Marketing copy gate maintained until opinion letter received. OI-PBM-05 resolution pending counsel Q5/Q6. See `docs/np_reg_pbm1064_001.md`.**
- [x] **Factory calibration procedure for InGaAs PD coefficients (K_PD1_1064, K_PD2_1064): integrating sphere reference at 1064nm; write to Config partition at manufacture (OI-PBM-04 in NP-FW-PBM1064-001)** — **CLOSED 2026-07-27:** procedure specified in NP-FW-PBM1064-001 Rev 2 §6.6 (integrating-sphere bench procedure, per-zone/per-wavelength coefficient computation, out-of-band quarantine rule, Config partition write). Residual: FAI-SM-06/07/08 hardware bench execution (manufacturing-line fixture build + first-article run) still pending, unchanged by this closure.
- [x] **1064nm FTC bibliography extension: add Yao et al. Science Advances 2022 + UT Dallas follow-ons to np_bib_001.docx (Issue #56)** — **NP-BIB-1064-001 Rev 1 (2026-05-13): six entries added — Yao et al. (2022) Science Advances DOI:10.1126/sciadv.abj7390 (primary working memory evidence), UT Dallas follow-on search guidance, tissue optics penetration depth refs (Bashkatov 2005, Yaroslavsky 2002, Strangman 2002), CCO mechanism at 1064nm (Rojas & Gonzalez-Lima 2013), safety literature (Hamblin 2016), multi-modal combination evidence gap. Marketing copy gate remains until RISK-03 opinion letter also received. See `docs/np_bib_1064_001.md`.**
- [ ] **QMS — hire Quality Lead (VP Quality)** — Month 6 target; interim authority CEO. First action: gap assessment against 21 CFR Part 820 + ISO 13485:2016 with external QMS consultant (Month 3).
- [x] **QMS — author Human Factors Engineering Plan (NP-HFE-001)** — **CLOSED (plan authored) 2026-07-27:** NP-HFE-001 Rev 1 — use specification, 7-critical-task URRA (CT-01..CT-07), formative plan including the named eject-lever Parkinson's/post-stroke study (n=5), summative plan (n=15, Month 14–18), traceability to DI-USE-01..07/DI-PERF-24. See `docs/np_hfe_001.md`. Residual: the formative and summative testing itself has not been performed — tracked separately at this file's eject-lever line item and at NP-DT-001 VE-12.
- [x] **QMS — author Post-Market Surveillance Plan (NP-PMS-001)** — **CLOSED (plan authored) 2026-07-27:** NP-PMS-001 Rev 1 — complaint handling, SHDR fleet trend cadence, MDR reporting decision tree (T2 post-510(k)), CAPA/risk-management linkage, breach-response handoff, annual review cycle. See `docs/np_pms_001.md`. Residual: operational execution (first annual review, eQMS complaint-tracking tool selection) remains for Year 1–2 per the plan's own open items.
- [x] **QMS — author Design and Development Plan (NP-DP-001)** — **NP-DP-001 Rev 1 (2026-05-17):** 20-section plan per 21 CFR §820.30(b) and ISO 13485:2016 §7.3.1. Six design phases (Phase 0 complete; Phase 1–5 T1; Phase 6 T2). Relative-month milestone schedule. Responsibilities (Steve Hickman CEO/Design Authority; all other leads TBD). Gate reviews (G1 Month 6, G2 Month 10, G3 Month 14, T1 launch Month 18). Verification, validation, transfer, change control, risk integration, software integration, supplier management, interim doc control (GitHub + CLAUDE.md → eQMS Month 9). DHF index updated to NP-DHF-001 Rev 4. See `docs/np_dp_001.md`.
- [x] **QMS — author Design Input/Output Traceability Matrix (NP-DT-001 Rev 1, 2026-06-07, PR #122)** — 57 design inputs (DI-PERF/SAFE/USE/REG/INT), 35 design outputs, 13 verification evidence entries; full DI→DO→VE traceability matrix; DHF completeness upgraded from Partial to Good. See `docs/np_dt_001.md`.
- [ ] **SHDR warranty token architecture** — opaque 256-bit TRNG token replaces warranty owner ID as device-to-person linkage in SHDR. Spec in NP-FW-EMMC-002 Rev 1 §A. BLOCKING for warranty registration system build and any SHDR fleet DB schema freeze. No-join CI test required (OI-EMMC2-06). **App-side PARTIAL (2026-06-05, PR #112):** `SHDRUploader` now uses a Keychain-stored 32-byte random token instead of `identifierForVendor`; upgrade to hub-provisioned TRNG token pending GATT warranty characteristic (hub firmware work remaining).
- [x] **Device factory reset procedure** — full SANITIZE + SHDR wipe + warranty token clear + new TRNG salt + new warranty token. Spec in NP-FW-EMMC-002 Rev 1 §B. **Firmware IMPLEMENTED + host-tested (2026-07-13):** `firmware/factory_reset/` — `np_factory_reset_execute()` runs the 12-step R-1..R-12 sequence (§B.3); `np_factory_reset_resume_after_powerloss()` re-runs the idempotent R-5..R-10 core on interrupted-reset detection (§B.4, SNVS_LPGPR1 bit 0). Fail-safe invariant: on ANY HAL step failure the sequence returns the error WITHOUT clearing `reset_in_progress`, so the next boot re-wipes and the device never comes up half-erased. New salt + warranty token generated via TRNG (§A.6); sensitive stack buffers `memset_explicit`-zeroed on every path. Bounded HAL retry (`NP_FR_HAL_MAX_RETRIES`=3) guards transient eMMC/TRNG faults without unbounded looping. **24 host tests** (`tests/np_factory_reset_tests.c`) — happy path + abort-safety (flag STAYS SET on each of R-5/R-6/R-7/R-8/R-9/R-10 failure) + bounded-retry (always-fail called exactly 3×) + retry recovery + resume-path invariant; host-only failure injection via `include/np_factory_reset_test_hal.h` (`np_fr_host_hal`, entirely `#ifdef NPTEST_HOST`). Builds clean under `-Wall -Wextra -Werror` on host AND Cortex-M7 (`nm` confirms no host symbols in the target object). Wired into root `firmware/CMakeLists.txt` (`add_subdirectory` + `add_test`) and CI `firmware-host-tests.yml`. **Residual (hardware, external):** target-side platform HAL `OI-RESET-01..05` (real eMMC SANITIZE/zero, TRNG, Config write) and `FAI-RESET-01` SANITIZE-timing bench on the i.MX RT1062 (per OI-EMMC2-03) before any device ships outside the company.
- [ ] **UHDR two-layer key scheme** — master key wrapper (UKMD + WKMD). Spec in NP-FW-EMMC-002 Rev 1 §C. BLOCKING for UHDR firmware implementation; supersedes single-layer Argon2id spec in NP-FW-EMMC-001 Rev 1 §6. Cryptography reviewer sign-off required (OI-EMMC2-02). **Firmware IMPLEMENTED + host-tested (2026-07-08):** `firmware/uhdr_key/` — `np_ukmd_record_t` (§C.3), `np_uhdr_key_provision` / `_unlock` (§C.4) / `_background` (§C.4 step 7) / `_change_credential` (§C.5 — re-wraps the 32-byte UKMD only, UHDR ciphertext untouched, no remount). WKMD is a function-local, never in the context struct or any header, `memset_explicit`-zeroed before every return; only the GCM ciphertext+tag reach Config. HAL boundary (OI-UHDRK-01..07): TRNG, Argon2id, AES-256-GCM wrap/unwrap, Config read/atomic-write, UHDR XTS mount — real CAAM/DCP + Argon2id + RNGB backing pending. Host stubs model Argon2id/hw_bind/GCM with `np_crypto` SHA-256 so wrong-credential rejection (`NP_UHDR_ERR_AUTH`) is a genuine probe. The Argon2 `version` is plumbed through the KDF HAL (`np_uhdr_hal_argon2id`) and the host TRNG is counter-seeded so nonce-uniqueness is exercised (code-review CR-2/CR-3). 13 host test functions, `ctest` 9/9 green; wired into `firmware/CMakeLists.txt` (host + cross) and `firmware-host-tests.yml`. **Privacy-review hardening (2026-07-08):** (F1) **hardware binding** — `WKMD = hw_bind(Argon2id(credential, salt), device-fused HUK)` via new HAL **OI-UHDRK-09** (CAAM/OTPMK on target), so an eMMC image can't be brute-forced offline (defeats the §C.6 PIN-fallback offline vector); (F2) the salt + KDF params are **authenticated as GCM AAD** and stored params are **validated against a floor** (`NP_UHDR_ARGON2ID_*_MIN`) → `NP_UHDR_ERR_PARAM` on rollback; (F3) **every unlock failure path scrubs the UKMD**. (F4) UKMD key-material (incl. per-device salt) added to the SHDR-schema CI prohibition (`ci/test_shdr_schema.py` KEYMAT-01, 7 checks green) and a fleet-export prohibition documented in `np_uhdr_key_types.h`. **OI-UHDRK-08:** §C.3 struct is self-inconsistent — fields sum to 152 B but the comment says "Pad to 192 bytes"; resolved to the stated 192 B (`reserved[84]`), pending spec-author confirmation. Remaining before checkbox close: real AES-GCM/XTS/Argon2id + CAAM-HUK HAL implementations, and **cryptography-reviewer sign-off (OI-EMMC2-02) — scope MUST cover F1 (hardware binding / offline-attack resistance) and F2 (key-wrap construction; consider AES-KW / AES-GCM-SIV vs the current AES-GCM+AAD)**.
- [ ] **Scratch partition encryption for research anonymization** — per-task AES-256-CTR key, SRAM-only, memset_explicit on completion, eMMC SANITIZE post-run. Spec in NP-FW-EMMC-002 Rev 1 §D. **Corrected 2026-07-27 (was stale — text below is unchanged from before implementation):** the firmware is already implemented and host-tested. `firmware/anon/` — `np_anon_scratch.c/.h` (per-run AES-256-CTR key + 16-byte TRNG nonce, SRAM-only, `memset_explicit` zeroing, eMMC SANITIZE on completion, power-loss resume path per §D.6, disjoint CTR counter-window guard against adjacent-block keystream reuse) plus `np_anon_pipeline.c/.h` (atomic six-step §D.5 transaction: init → stage_uhdr → transform → validate → emit → complete, with the two §D.5 invariants enforced — no partial extract, always tear down). 13 host tests pass; wired into `firmware/CMakeLists.txt` and `firmware-host-tests.yml`; cross-builds clean under `-Wall -Wextra -Werror`. HAL stubs (OI-ANON-AES-01..05: TRNG, AES-256-CTR, eMMC scratch read/write, SANITIZE) still need real backing. **Remains open because it needs cryptography-reviewer sign-off (OI-EMMC2-02) — not because the firmware is unwritten.** This checkbox and the UHDR two-layer key scheme checkbox above share the same OI-EMMC2-02 reviewer engagement; one sign-off covering both scopes would close both.
- [x] **EDF+ patient header policy** — patient code = 16-char UHDR token; sex/birthdate/name = 'X'. Header validator in research anonymization pipeline. Spec in NP-FW-EMMC-002 Rev 1 §E. **Firmware IMPLEMENTED + host-tested (commit 81f5bf5, verified 2026-07-14):** `firmware/edf/` — `np_edf_write_header()` fills a byte-exact 256-byte EDF+ main header (packed `np_edf_header_t`, compile-time size-checked) per §E.2/§E.3: `local_patient_id` = `"NP"` + 14 lowercase-hex chars from UHDR token bytes 0–6 (16-char opaque patient code) + ` X` (sex) + ` X` (birthdate) + ` NeurOne_User` (fixed name); `local_recording_id` = `"Startdate <DD-MMM-YYYY> NeurOne X NeurOne_v<fw>"`; `startdate`/`starttime` from `session_ts`. Fully self-contained — file-local integer `gmtime`/hex/field-pad helpers, no libc string/time, no heap, fixed-width non-NUL-terminated fields (identical bytes on host + target). `np_edf_validate_privacy_header()` (§E.4) rejects any non-`'X'` sex/birthdate, any real patient name (`np_edf_header_contains_real_name()` — compliant only for empty or exactly `NeurOne_User`), and any code not prefixed `"NP"` → `NP_EDF_ERR_PRIVACY_HEADER`/`NP_EDF_ERR_REAL_NAME`; null-safe fail-closed. **7 host tests** (`tests/np_edf_tests.c`) — OI-EMMC2-05 100-headers-generate-and-validate loop + 3 validator rejection paths (sex/DOB/name) + `"NP"` prefix + 256-byte size + fw-version-in-recording-id; **0 failures**. Builds clean under `-Wall -Wextra -Werror` on host AND cross-compiles clean for Cortex-M7 (`arm-none-eabi-gcc` 14.2.1, `-mcpu=cortex-m7 -mfpu=fpv5-d16 -ffreestanding`). Wired into root `firmware/CMakeLists.txt` (`add_subdirectory(edf)` + `add_test(np_edf_tests)`) and CI `firmware-host-tests.yml` (`firmware/edf/**` trigger). **Anonymization-pipeline gate ADDED (2026-07-14):** `np_edf_anon_gate_filter()` (`np_edf_anon_gate.{h,c}`) implements §E.4 as the per-file, exclude-not-abort staging filter — validates each candidate EDF+ header with `np_edf_validate_privacy_header()`, marks each `included[i]` true/false, and on a violation fires an injected SHDR-fault callback (`NP_EDF_SHDR_EV_PRIVACY_HEADER_VIOLATION` = 0xE1, distinct from the CVNS 0xC1..0xC7 range) then CONTINUES — a bad header excludes one file, never aborts the extract (returns `NP_EDF_OK` even when files were excluded; asserted by tests). Deliberately NOT the anon pipeline's extract-level `validate` callback (which does k/l/DP thresholds and DOES abort) — routing a per-file header check there would kill the whole extract on one bad file. Decoupled from `hub_control`: the gate never calls the SHDR logger itself; the fault is delivered through the injected callback (mirrors the anon pipeline's dependency-injection pattern). **4 new host tests** (mixed pass/exclude with correct fault indices+code, all-pass, all-fail-does-not-abort, null/empty/NULL-fault_cb) → 11 EDF tests total, 0 failures; gate source cross-compiles clean for Cortex-M7 under `-Werror`. **Residual (integration, external):** (1) writer still has no caller — wires in at the first EEG session recorder (target-side, unwritten); (2) the study-extract runner that iterates the real staged EDF+ files, calls `np_edf_anon_gate_filter()`, and maps its fault callback onto `np_log_shdr_fault(NP_EDF_SHDR_EV_PRIVACY_HEADER_VIOLATION, ...)` is the remaining hub/anon integration seam (the gate primitive it needs is now done).
- [x] **SHDR accelerometer data reclassification** — raw accelerometer series never written to any partition; only `drop_detected: bool` and `maintenance_alert: bool` written to SHDR. **Stale entry corrected 2026-07-27:** §G was already authored 2026-06-03 (NP-FW-EMMC-002 Rev 1 §G is fully written, not "to be authored" as this line previously stated) and the associated CI schema gate (OI-EMMC2-07) is already closed and passing per the entry below — this checkbox was simply never updated to match. No new work performed; this is a bookkeeping fix only.
- [ ] **NP-SEC-BR-001 annual tabletop exercise** — before T1 launch and annually thereafter. See `docs/np_sec_br_001.md` §9.
- [ ] **EU-US Data Privacy Framework self-certification** — before any EU resident's personal data is transmitted to US infrastructure. Month 3 target. See NP-PRIV-REM-001 STEP-12 and §4.1.
- [ ] **Clinician BAA template (NP-LEGAL-BAA-001)** — must include consent revocation deletion cascade clause (30-day deletion obligation). Before any clinician subscription is sold. See NP-PRIV-REM-001 STEP-13.
- [ ] **Research anonymization upgrade (NP-FW-ANON-001)** — l-diversity l≥3 + differential privacy ε≤1.0. Before first study descriptor deployment. External DP reviewer sign-off required. See NP-PRIV-REM-001 STEP-11.
- [ ] **Mode F regulatory opinion (Q-13)** — add Q-13 to NP-REG-PBM1064-001 Rev 2 covering Mode F cumulative retinal dose under RISK-03 existing counsel engagement. See NP-FW-EMMC-002 Rev 1 §F and NP-PRIV-REM-001 STEP-18. **Firmware gate IMPLEMENTED (2026-06-17):** `NP_MODE_F_REGULATORY_CLEARED = 0` compile-time guard in `firmware/hub_control/include/np_hub_config.h` (line 141) + `#if NP_MODE_F_REGULATORY_CLEARED` blocks in `firmware/hub_control/modules/np_mod_visual.c`. Mode F code path is compiled-in but cannot execute until opinion letter received and flag set to 1 in np_hub_config.h.
- [ ] **IRB protocol (NP-IRB-001)** — before first study descriptor is sent to any device. Month 9 target. See NP-PRIV-REM-001 STEP-20 and §4.4. **Protocol template authored 2026-07-27** (`docs/np_irb_001.md`, DRAFT): primary pathway (Rashidi-Ranjbar/Unity Health REB), fallback pathway (WCG/Advarra + OHRP FWA), Type A/B study distinction, consent mechanism, anonymization architecture reference, risk/benefit template. **Remains open** — this checkbox tracks actual IRB/REB approval (or exemption determination) being in effect, not the template's existence; outreach (OI-IRB-01) has not yet been initiated.
- [x] **App privacy audit (NP-PRIV-AUDIT-001 Rev 1, 2026-06-03)** — 16 findings (0 Critical, 5 High, 7 Medium, 4 Low); 16-item compliance checklist AUDIT-01 through AUDIT-16 (all OPEN — blocking external beta). See NP-PRIV-REM-001 STEP-21. **Follow-on analysis NP-PRIV-ANALYSIS-002 (2026-06-05, PR #112):** second-pass code review surfaced 12 additional findings (1 Critical, 3 High, 5 Medium, 3 Low); 6 repaired on PR #112; remaining 6 repaired in session 2026-06-05: MEDIUM-07 (sessionDay coarsening), MEDIUM-08 (WC bridge epoch exclusion), LOW-10 (contact redaction), LOW-11 (SPKI cert pinning), LOW-12 (Keychain load query filter). OI-PA-03 (BIPA locale detection) resolved: locale gate removed, biometric consent shown to all users. OI-PA-04 (Adaptive Adjustments copy sign-off) remains open. All code-level findings in NP-PRIV-ANALYSIS-002 now resolved. **Consumable tracker analysis NP-PRIV-ANALYSIS-003 Rev 1 (2026-06-08):** consumable tracker feature audited. 0 Critical, 0 High, 0 Medium, 2 Low findings. LOW-1: notification lock-screen title exposes device ownership (recommended: generic title "NeurOne" on lock screen, consumable detail revealed after unlock). LOW-2: `requestNotificationPermission()` called at app init before onboarding (recommended: defer to `ConsumableView.onAppear` for in-context permission request). See `docs/np_priv_analysis_003.md`.
- [x] **FHIR ImplementationGuide (NP-INT-FHIR-001)** — before any T2 EHR integration code is written. G1 gate item NP-COORD-001 G3-09. See NP-PRIV-REM-001 STEP-14. **Stale entry corrected 2026-07-27:** NP-INT-FHIR-001 Rev 1 was already released 2026-06-03 (`docs/np_int_fhir_001.md`) and is already marked COMPLETE in NP-DHF-001 §8 — this checkbox was simply never updated to match. No new work performed; this is a bookkeeping fix only. No T2 EHR integration code has been written yet, so the gate this item protects still holds.
- [x] **T2 scripting API spec (NP-API-001 Rev 1, 2026-06-07, PR #121)** — REST + WebSocket API specification; UHDR three-condition gate; SHDR-only default; 8 REST endpoints; rate limits (1,000/hr, 10,000/day); 256-bit key / 90-day expiry; audit log; 7 open items (OI-API-01..07). G1 gate item. See `docs/np_api_001.md`.
- [ ] **App Store privacy nutrition label and Google Play Data Safety** — before first TestFlight / Play internal test. Requires NP-APP-TELEMETRY-001 complete. See NP-PRIV-REM-001 STEP-16, STEP-17 and §4.2, §4.3.
- [ ] **EU data residency for T2 clinical cloud** — when T2 cloud infrastructure vendor is selected. Month 9 target. See NP-PRIV-REM-001 STEP-19.
- [ ] **T2 scripting API independent security audit (NP-SEC-PENTEST-002)** — before any clinical API key is issued. See NP-PRIV-REM-001 STEP-26.
- [x] **BIPA biometric written release — shipped to ALL users (universalized 2026-07-10)** — Per the durable privacy principle ([[privacy-strictest-globally]]: most privacy-protecting approach; any jurisdiction's *user protection* is applied to all users everywhere, never geo-gated), the BIPA-specific biometric written-release screen AND the public biometric retention/destruction policy are shown to EVERY NeurOne user, not gated to Illinois residents. iOS `BIPADisclosureView` (unconditional, `!bipaShown` gate in `NeurOneApp.swift`) + Android `BipaConsentScreen` (locale gate removed; OI-PA-03 resolved). `RegionHelper.isLikelyIllinois` deleted (locale is not a reliable jurisdiction proxy — a Chicago user with a non-IL locale would have been missed). EEG patterns qualify as biometric information under BIPA 740 ILCS 14; GDPR Art. 9 and WA MHMD reinforce the same control. **Open — confirms scope, does NOT gate shipping the protection:** BIPA legal opinion on whether the UHDR encryption architecture creates a "possession" defense (BIPA 740 ILCS 14/10). See NP-PRIV-001 HIGH-01, NP-PRIV-REM-001 STEP-34.
- [x] **Washington MHMD protections — applied to ALL users (universalized 2026-07-10)** — Per the same durable principle ([[privacy-strictest-globally]]), the MHMD *user protections* on SHDR behavioral data (consumable counts, session counts) apply to every user, not gated to Washington activations: (1) **no-sale** — NeurOne does not sell, lease, or trade SHDR behavioral data (or any user data), a universal commitment stated unconditionally in the privacy notice; (2) **standalone authorization** — the warranty-owner authorization for SHDR uploads is separate and distinct from HIPAA consent and is obtained from every warranty owner, not just Washington registrants. MHMD RCW 70.372 is cited as the WHY; the control is global. **Open — confirms scope, does NOT gate shipping the protection:** WA-qualified counsel analysis of whether SHDR session/consumable counts are "consumer health data" under RCW 70.372.010(2). See NP-PRIV-001 HIGH-02, NP-PRIV-REM-001 STEP-35.
- [x] **Minimum age declaration in app consent flow** — `AgeGateView.swift` implemented (Issue #51, PR #106, 2026-06-04). "I confirm I am 16 years of age or older." unchecked checkbox; Continue disabled until checked; persisted to `np.onboarding.age-confirmed` in UserDefaults; presented before all consent layers; `Under16View.swift` explains feature availability for under-16 users. OI-PA-01 (legal counsel 16-year threshold confirmation) still open. See NP-PRIV-001 Rev 2 MEDIUM-03.
- [ ] **HIPAA Expert Determination certifier engagement (STEP-31)** — before NP-FW-ANON-001 is finalised; before any study descriptor is authored. Named certifier (individual or firm) under standing engagement. NP-ANON-CERT document template required. See NP-PRIV-REM-001 STEP-31 and 45 CFR §164.514(b)(1).
- [ ] **NP-ANON-CERT-[study_id] per study (STEP-32)** — signed Expert Determination certification required before each study descriptor is cryptographically signed and deployed. BLOCKING gate enforced in deployment pipeline. See NP-PRIV-REM-001 STEP-32.
- [x] **Adaptive stimulation transparency (STEP-33, Issue #98, 2026-06-03):** np_adapt_trigger_t (17 values) + np_adaptation_event_t + API in firmware/hub_control/include/np_adaptation_log.h; NP_LOG_TAG_UHDR_ADAPT_EVENT 0x18 + np_log_adapt_event() in np_session_log; AdaptationEvent.swift + AdaptTrigger enum (plainLanguageDescription) + CompletedSessionSummary; AdaptiveAdjustmentsCard.swift (≤5 inline + "View all"); SessionHistoryView.swift; GDPR Art. 13(2)(f) §4 in docs/np_priv_notice_001.md. T2 NP-API-001 clinical report deferred to G3. OI-PA-04 OPEN — card must not ship without Privacy Lead copy sign-off.
- [x] **SHDR fleet DB schema freeze gate (OI-EMMC2-07)** — CI schema test implemented and passing (7/7 checks). `ci/test_shdr_schema.py` parses `ci/shdr/shdr_fleet_schema.sql` and enforces: ACCEL-01 no prohibited accelerometer columns (g-force, accel_[xyz], orientation_*, drop_count, drop_timestamp, impact_*, fall_*, rms_g/peak_g/average_g, raw_accel); ACCEL-02 no numeric column types in any `accel` table (only BOOLEAN accel-derived fields permitted); ACCEL-03 required booleans `drop_detected`/`maintenance_alert` present; PII-01 no personal-data columns; KEYMAT-01 no UHDR key-material columns (Finding 4); TIME-01 no wall-clock timestamp columns (no TIMESTAMP/TIMESTAMPTZ/TIME type anywhere); NOJOIN-01 no cross-DB warranty-registration references; TOKEN-01 `warranty_token` is opaque BYTEA. Wired into CI via `.github/workflows/shdr-schema-ci.yml` (triggers on changes to the schema or test; BLOCKING). The gate is now armed and green (8/8); the actual SHDR fleet DB schema *freeze* remains a downstream project milestone that this test guards. Spec in NP-FW-EMMC-002 §G.5. **Schema Rev 2 (2026-07-13) — timestamp minimization:** every per-row `TIMESTAMPTZ` wall clock removed and replaced with the largest granularity that still serves a real function — per-session/event tables carry a month-granular `ingest_month DATE` (retention/TTL only; row ordering was already an integer counter — `session_index`/`gap_index`/`*_at_*`/`*_count`); `devices.updated_at`→`last_seen_month DATE` (liveness+retention); `devices.created_at` removed (device age = `manufacture_date`; activation date lives only in the consented warranty_db, no-join); `consumable_counts.updated_at` removed (reminders are count-triggered — no function needed it). This deletes a per-row docking-time correlator keyed to `warranty_token` and aligns the schema with the §5.1 "no timestamps in SHDR" rule; TIME-01 makes it durable.
- [ ] **QMS — select and implement eQMS platform** — Month 9 target. Candidates: MasterControl, Greenlight Guru, SimplerQMS, Veeva Vault QMS. Must be 21 CFR Part 11 compliant with DHF and IEC 62304 modules.
- [x] **SW-01 Safety MCU unit-level FMEA (NP-FMEA-001 Rev 2, 2026-06-15)** — 43 failure modes across SW01-M01..M08; ISO 14971 S×P scoring; two initial UNACCEPTABLE risks (FMEA-M03-01 charge accumulator overflow, FMEA-M05-02 HR delta underflow) mitigated to ACCEPTABLE by architectural controls (64-bit accumulator, int16_t signed arithmetic, MISRA C:2012 enforcement); 5 open items (hardware bench OI-FMEA-01/02 pending G2 prototype). Rev 2 (OI-SW01-M07-02 CLOSED, PR #132): §3.7 description updated to reflect np_crypto/Monocypher 4.0.2 backing; FMEA-M07-05 mitigation updated to reference Monocypher ct_memcmp. See `docs/np_fmea_001.md`.
- [x] **Build infrastructure established (2026-06-08):** `firmware/CMakeLists.txt` super-project (host-test mode `-DNP_BUILD_TESTS=ON` + ARM cross-compile mode); `firmware/cmake/stm32g071.cmake` Cortex-M0+ toolchain; `firmware/safety_mcu/` full build system (STM32G071, produces `.elf/.bin/.hex`, linker script with `.fault_latch` NOLOAD section, startup assembly for 36 KB SRAM); `.github/workflows/firmware-host-tests.yml` (CMake host tests, Ubuntu); `.github/workflows/web-ci.yml` (Bun + tsc + Vitest + Vite, Ubuntu); `.github/workflows/ios-ci.yml` (xcodebuild test, macOS-14); `.gitignore` (project-appropriate rules replacing Java boilerplate).
- [x] **FreeRTOS-Kernel V11.3.0 vendored + integrated — SW-02 RTOS (2026-07-11):** Closes the SW-02 "FreeRTOS listed as SOUP but absent from tree; cannot cross-compile" gap. **Vendored** byte-exact subset of FreeRTOS-Kernel V11.3.0 (from FreeRTOS-LTS `202604.00-LTS`) into `firmware/vendor/freertos/` (mirrors the Monocypher SOUP pattern): `kernel/` (tasks, list, queue, event_groups, timers, stream_buffer + all 21 headers), `portable/GCC/ARM_CM7/r0p1/` (shipped target port), `portable/MemMang/heap_4.c` (shipped heap), `portable/ThirdParty/GCC/Posix/` (**host-test-only**, never in the device image), MIT `LICENSE.md`, and `VERSION` SOUP record. `croutine.c` excluded (deprecated; `configUSE_CO_ROUTINES 0`); all other ~60 CPU ports excluded. All 34 kernel files sha256-verified byte-exact vs upstream. **First-party (not SOUP):** `firmware/hub_control/include/FreeRTOSConfig.h` (600 MHz, 1 ms tick, `configMAX_PRIORITIES 6`, heap_4 64 KiB, `configUSE_PORT_OPTIMISED_TASK_SELECTION` on target, `configCHECK_FOR_STACK_OVERFLOW=2` + `configUSE_MALLOC_FAILED_HOOK=1` — substantiates the NP-SW-001 §9.4 stack-overflow-detection claim — `configASSERT`→`np_freertos_assert_failed`, Cortex-M NVIC priorities with `configPRIO_BITS 4`, and `SVC_/PendSV_/SysTick_Handler` aliases; a single `NP_FREERTOS_POSIX_HOST` guard makes the same file serve target + host); `firmware/hub_control/src/np_hub_freertos_hooks.c` (the three hooks — fail-safe by halting the main processor, which stops the SPI heartbeat so the STM32G071 safety MCU independently cuts stimulation <50 ms). **Build wiring:** `np_freertos` static library builds for Cortex-M7 (`-mcpu=cortex-m7 -mfpu=fpv5-d16 -mfloat-abi=hard`); root `NP_PLATFORM_INCLUDE_DIRS` populated with kernel/port/config dirs; `np_hub_control` links `np_freertos`; hooks added to hub sources. **Verification:** `np_freertos_smoke_tests` compiles the real kernel + this config + hooks against the POSIX port and runs the scheduler (prioritised tasks + event group + `vTaskDelayUntil`, mirroring the hub's `NP_EV_SESSION_START` pattern) → PASS; registered in root CMake ctest and `firmware-host-tests.yml` (trigger path `firmware/vendor/**`); full host suite **10/10 green**. **NOT done (separate platform bring-up, not FreeRTOS-kernel work):** NXP MCUXpresso SDK (MIMXRT1062 CMSIS device header, clock init), startup/vector table, SRAM/LPSDR4 linker script, and the final linked application target; plus the hub platform-HAL `OI-*` stubs (SPI, transport, module drivers, eMMC log). On-target ARM_CM7 compile requires `arm-none-eabi-gcc` (not installed on the dev host). See `firmware/vendor/freertos/README-NEURONE.md`, NP-SW-001 §4.2/§9.4, NP-DHF-001 Rev 17.
- [x] **SW-01 Safety MCU firmware source authored + 10 critical bugs corrected (2026-06-08):** All 8 IEC 62304 Class C modules implemented: np_gpio_mgr (M01), np_spi_watchdog (M02), np_charge_monitor (M03), np_thermal_interlock (M04), np_cardiac_interlock (M05), np_impedance_check (M06), np_session_sig (M07), np_fault_latch (M08). 7-angle code review surfaced 10 confirmed bugs; all fixed: **(1) CRITICAL — watchdog dead path:** `np_spi_watchdog_check()` was never called; added extern + call every main-loop iteration. **(2) Session transition wiring:** `np_session_sig_reset()`, `np_charge_monitor_reset_session()`, `np_impedance_check_request()` now called on `session_active` 0→1 transition. **(3) Impedance poll wired:** `np_impedance_check_poll()` now called every main-loop iteration. **(4) CUTOFF bit cleared unconditionally:** `np_spi_watchdog_tick()` cleared `NP_SAFETY_STATUS_CUTOFF` on any valid heartbeat, masking active thermal/charge/cardiac faults from main processor — now only clears when no other interlock active. **(5) `s_cutoff_active` never reset:** After 30s lockout expiry, cardiac interlock was permanently disabled for the session — now reset when lockout expires. **(6) Lockout wrap at ~49 days:** Absolute-timestamp `>=` comparison wraps at `UINT32_MAX` — replaced with elapsed subtraction `(now - start) >= period`. **(7) First RR phantom interval:** `s_last_capture=0` produced a phantom RR on first beat — guarded with `s_first_beat_seen` flag. **(8) 5s observation window not implemented:** `NP_CARDIAC_OBS_MS` defined but never used — rolling baseline now refreshed every observation window. **(9) `NP_SAFETY_STATUS_CARDIAC` sticky:** Never cleared, blocking CVNS re-enable permanently — cleared in new `np_cardiac_interlock_reenable()` called by main when hub asserts `NP_SESSION_STATUS_CVNS_REENABLE`. **(10) CVNS re-enable protocol:** New `NP_SESSION_STATUS_CVNS_REENABLE` bit (bit 1 of `session_status` frame byte) allows hub to signal explicit re-enable after cardiac cutoff; triggers reenable + fresh impedance check. See `firmware/safety_mcu/`.
- [x] **SW-01 Safety MCU second-pass code review — 9 additional bugs corrected (2026-06-08):** 7-angle second-pass review of all 8 Class C modules surfaced 9 additional bugs (8 fixed in code; 3 documented as open items requiring design work — see OI-CHARGE-01, OI-CHARGE-02 below (OI-SW01-M07-02 subsequently CLOSED 2026-06-11)): **(1) `.fault_latch` linker placement bug:** `.fault_latch` section floated immediately after `.bss` mid-SRAM rather than at the top-of-SRAM address where `_fault_latch_start` symbol pointed — warm-reset persistence silently broken; fixed by adding `. = _fault_latch_start;` inside the section body (`startup/stm32g071_flash.ld`). **(2) `rr_to_bpm()` int16_t overflow:** RR intervals < 1831µs (noise spikes, contact bounce, > 32767 BPM) silently overflow int16_t, producing phantom cardiac cutoffs or missed cutoffs; now saturates to INT16_MAX (`np_cardiac_interlock.c`). **(3) `current_hr_bpm()` double-call ISR race:** function called twice per main-loop tick with no atomic guard — an R-peak ISR arriving between calls produces an inconsistent HR delta that could both miss a true event and false-trigger; now called once into a local variable and reused. **(4) Baseline refresh desensitization after cutoff:** `s_baseline_bpm` refreshed unconditionally while `s_cutoff_active`, adopting elevated post-event HR as the new baseline and desensitizing cardiac detection for any subsequent event in the same session; refresh now gated on `!s_cutoff_active`. **(5) `NP_SAFETY_STATUS_IMPEDANCE` permanent lock:** IMPEDANCE status bit set on a failed check but never cleared on successful recheck — any single impedance measurement failure would permanently disable the affected channel for the device's operational lifetime; fixed: bit now cleared when re-check passes (`np_impedance_check.c`). **(6) Fault latch overwrite on first loop after warm reset:** `np_fault_latch_commit()` executing on the first main-loop iteration after a warm reset overwrote the preserved latch data (specific fault slot + detailed status bits) with the generic `FAULT=0x01 / slot=0xFF` from freshly-zeroed `s_state` — destroying the diagnostic forensic record; guarded by new `s_prior_latch_reported` flag (`np_safety_main.c`). **(7) GPIO startup comment inversion:** comment stated "All stimulation GPIO is driven LOW (cutoff) at startup" — incorrect for active-LOW system (LOW = enabled, HIGH = disabled) — would mislead HAL implementer and cause bring-up error; corrected to "HIGH (disabled) at startup; LOW = enabled". **(8) Double-evaluated fault flags in `np_spi_watchdog_tick()`:** four-flag OR expression evaluated twice with no critical section — if any interlock migrates to ISR, a fault asserting between the two evaluations clears CUTOFF while simultaneously granting the full requested mask (unsafe state); extracted to single local `uint8_t active_faults`. **(9) Fault latch count saturation:** `np_fault_latch_commit()` incremented `s_latch.count` every main-loop iteration while FAULT bit set — UINT16_MAX (65535) reached in ~65 seconds, freezing the event counter and hiding all subsequent distinct fault events in SHDR audit trail; now change-gated: count increments only when status or fault_slot actually changes (`np_fault_latch.c`). See `firmware/safety_mcu/`.
- [x] **OI-SW01-M07-01 CLOSED: Session signature delivery mechanism — 102-byte SPI command frame (2026-06-16)** — Two-frame SPI protocol designed and wired. New `np_safety_sig_cmd_t` (102 bytes, magic `0xC0 0xDE`): 2-byte magic + 1-byte cmd type + 1 reserved + 32-byte SHA-256 hash + 64-byte Ed25519 sig + 2-byte additive checksum. HAL distinguishes from 8-byte heartbeat by NSS-delineated transfer length. `NP_SAFETY_STATUS_SIG_PENDING` (bit 7) added to `np_safety_status_t`: set by `np_session_sig_reset(state*)` on session 0→1 transition; cleared by `np_session_sig_verify()` on valid sig; persists (+ FAULT + CUTOFF, fault_slot=0xFD) on bad sig. `NP_SAFETY_STATUS_SIG_PENDING` added to `active_faults` mask in `np_spi_watchdog_tick()` — blocks `granted_mask` until hub delivers signature. Hub-side `np_safety_spi_send_session_sig(hash, sig)` builds and sends the 102-byte frame; must be called after first heartbeat with `session_active=1` and before first non-zero enable request. Sequencing: heartbeat(session_active=1, mask=0) → MCU sets SIG_PENDING → hub sends 102-byte cmd → MCU verifies → hub checks SIG_PENDING in next heartbeat reply → if cleared, sends heartbeat(mask=channels). Files modified: `np_safety_protocol.h`, `np_session_sig.c`, `np_spi_watchdog.c`, `np_safety_main.c`, `np_safety_spi.h/.c`, `np_hub_config.h`, `np_hub_types.h`. **38 host tests added** (`firmware/safety_mcu/tests/np_safety_spi_proto_tests.c`): frame sizes + field offsets, magic distinctness, checksum round-trip + corruption detection, SIG_PENDING lifecycle, watchdog grant gating, constant values, heartbeat unaffected. All 38 pass. `np_safety_spi_proto_tests` added to root `firmware/CMakeLists.txt` host test suite (now 6 test binaries). Session sig verification now has full safety effect on `granted_mask`.
- [x] **OI-CVNS-HUB-01 CLOSED: hub-side `NP_SESSION_STATUS_CVNS_REENABLE` logic (2026-07-08)** — New pure-C, host-tested three-gate state machine `firmware/hub_control/src/np_cvns_reenable.c` (+ `include/np_cvns_reenable.h`) asserts the re-enable bit ONLY when ALL three conditions hold: (1) `NP_CVNS_REENABLE_LOCKOUT_MS`=30000 elapsed since the hub OBSERVED `NP_SAFETY_STATUS_CARDIAC` in a heartbeat reply (the hub window ⊇ the MCU's own 30s lockout because hub observation lags MCU cutoff by ≤1 beat); (2) explicit app confirmation via `np_cvns_reenable_confirm()`; (3) a fresh hub-side impedance check (HAL `np_cvns_hal_impedance_start`/`_poll`, OI-CVNS-HUB-02) PASSED. States IDLE→LOCKOUT→AWAIT_CONFIRM→IMPEDANCE→ASSERTING; only ASSERTING emits the bit; impedance fail/timeout and a 2s assert-timeout all fail-closed to AWAIT_CONFIRM; any session-boundary or external CARDIAC clear resets to IDLE. **Single-writer concurrency design (Forge audit findings 1+2):** `confirm()` (transport task) only posts a one-shot intent flag; the higher-priority heartbeat task is the SOLE state mutator and consumes/re-validates the flag from AWAIT_CONFIRM — race-free without a lock, and a stale confirmation is discarded on every reset and new-cutoff rising edge. Wired into `task_safety_heartbeat` (`np_hub_control_main.c`): drives the machine and sets the bit BEFORE building each TX frame (Forge finding 3 — guarantees CVNS_REENABLE and ACTIVE are consistent in the same frame, never REENABLE=1 with ACTIVE=0 during teardown). Recoverable-cutoff policy: a cardiac-only fault status (CARDIAC set, none of `NP_CVNS_NONRECOVERABLE_FAULTS`) no longer aborts the session — the hub holds it so re-enable is possible. **Two blocking SPI bugs fixed:** (a) `np_safety_spi_heartbeat()` was casting the `np_session_state_t` enum directly into `session_status` (RUNNING=3 spuriously set CVNS_REENABLE, PAUSED=4 set GEOM_REQUIRED) — replaced with pure host-tested helper `np_safety_session_status_bits()` (ACTIVE for RUNNING/PAUSED/STOPPING); (b) `task_safety_heartbeat` used a stale 2-arg signature and echoed the granted mask — now calls the 4-arg form and transmits the REQUESTED mask via new `np_safety_spi_get_requested_mask()`. Defense-in-depth: safety MCU `np_safety_main.c` re-enable now additionally gates on `s_state.session_active`. Session runner resets the manager at session start. SHDR lifecycle logging via `np_log_shdr_fault()` with `NP_CVNS_SHDR_EV_*` flag codes and suppressed timestamp (no HR/RR biology). **76 host tests** (`firmware/hub_control/tests/np_cvns_reenable_tests.c`) wired into `firmware/CMakeLists.txt` + `hub_control/CMakeLists.txt` NP_BUILD_TESTS path; full host suite **8/8 binaries, 244 checks, 0 failures**. CI `firmware-host-tests.yml` trigger paths extended to `firmware/hub_control/**`, `firmware/safety_mcu/**`, `firmware/common/**`. **RESIDUAL — OI-CVNS-HUB-02** (hub-side impedance HAL driver) and **OI-CVNS-HUB-03** (app-transport wiring of `np_hub_cvns_reenable_confirm()`) are stubbed integration points. FAI-CV02 hardware bench timing still required at T2 prototype. OI-CVNS-HUB-01 in NP-FW-HUB-001.
- [x] **OI-SW01-M07-02 CLOSED: `np_ed25519_verify` shared crypto library — ARM link error resolved (2026-06-11)** — Created `firmware/crypto/` static library (`np_crypto`) backed by **Monocypher 4.0.2** (RFC 8032, SHA-512, vendored in `firmware/crypto/vendor/monocypher/`). `np_session_sig.c` `extern` replaced with `#include "np_crypto.h"`; `firmware/safety_mcu/CMakeLists.txt` links `np_crypto`. All 11 host tests pass (`np_crypto_tests`: 5 SHA/CRC + 6 Ed25519 including TV1/TV2 RFC 8032 vectors + all-zero pubkey guard). Bootloader intentionally retains self-contained `np_signature.c` (uses `-nostdlib/-nodefaultlibs`; Monocypher requires libc symbols unavailable in that context). SOUP record: `firmware/crypto/vendor/monocypher/VERSION` (version 4.0.2, BSD-2-Clause OR CC0-1.0, IEC 62304 §8.1.2). Build system: `C_EXTENSIONS OFF` via CMake property (prevents GNU extensions, MISRA C:2012 compliant); Monocypher SOUP files compiled with `-w` to isolate from first-party `-Wall -Wextra`. Code review: 3 findings resolved. Privacy analysis: clean — Ed25519 verify operates on manufacturing root key + session descriptor hash (SHDR-class, no UHDR). OI-SW01-M07-01 (SPI delivery frame for 96-byte hash+sig payload) CLOSED 2026-06-16. See `firmware/crypto/`, `firmware/safety_mcu/`.
- [x] **OI-CHARGE-01 CLOSED: `np_charge_monitor_accumulate()` wired — 40µC/cm² charge density limit now enforced (2026-06-16)** — Extended heartbeat frame `np_safety_rx_ext_frame_t` (38 bytes) introduced in `firmware/common/include/np_spi_wire_types.h`: 8-byte base (magic + session_status + enables + channel_count + base_checksum) + 28-byte `current_ua[14]` (NP_SAFETY_MAX_CHANNELS × uint16_t, commanded µA per channel) + 2-byte `ext_checksum`. Privacy gate satisfied: `current_ua[]` is SHDR "commanded current from session descriptor" — explicitly NOT ADC-measured actual current (which would be UHDR) — classification comment in all 5 affected production files. Accumulate loop wired in `np_safety_main.c` after all interlocks reduce `granted_mask` (thermal → impedance → cardiac → accumulate → charge_tick — order critical). Per-channel `s_limit_nc[14]` replaces global constant in `tick()`. `np_charge_monitor_set_channel_limit()` API hook added for geometry override (OI-CHARGE-02). Hub-side `np_safety_spi_heartbeat()` extended with `current_ua` + `channel_count` params; builds 38-byte ext frame; receives MCU reply as first 8 bytes of 38-byte full-duplex transfer. Array bounds use `NP_SAFETY_MAX_CHANNELS` throughout (code-review fix). **26 np_charge_monitor_tests + 12 new np_safety_spi_proto_tests pass (76 total / 0 failures).** FAI bench test with calibrated current source still required at G2 prototype. OI-CHARGE-01 in NP-SW-001.
- [x] **OI-CHARGE-02 CLOSED: HD-tDCS electrode geometry wired from hub session runner to safety MCU charge monitor (2026-07-07)** — The hub session runner (`np_session_runner.c`) scans the verified session descriptor at session start; for any `NP_MOD_HD_TDCS` command with a `RING_4X1`/`BILATERAL_4X1` montage (3.5mm Ag/AgCl electrodes) it sends the electrode AREA (96 milli-cm² = floor(0.0962cm²×1000), `NP_HD_SMALL_ELECTRODE_AREA_MCM2`) for the CLIN_STIM channel (index 13, `NP_SAFETY_CH_CLIN_STIM`) to the safety MCU via a new 34-byte command frame `np_safety_chan_limit_cmd_t` (magic 0xC0/0xDE, `cmd_type=NP_SAFETY_CMD_CHAN_LIMIT` 0x02; `firmware/common/include/np_spi_wire_types.h`). Delivered by `np_safety_spi_send_channel_limits()` (`hub_control/src/np_safety_spi.c`). The safety MCU main loop reads it AFTER the session-start reset block (`np_safety_main.c`) and calls the new `np_charge_monitor_set_channel_area_mcm2()`, which derives `limit_nc = NP_CHARGE_LIMIT_UC_CM2 (40) × area_mcm2` → 3840nC = **3.84µC** on CLIN_STIM (vs the 1000µC pad default). **Design invariant:** the hub transmits electrode AREA only — the 40µC/cm² density constant stays resident on the Class C safety MCU. Area is floored so the derived limit never exceeds the true 40µC/cm² ceiling. **Tests:** +3 charge-monitor cases (HD-tDCS trip at 3.84µC, 25cm²-via-area = 1000µC, other-channel isolation) and +3 SPI-proto cases (34-byte frame size/offsets, checksum round-trip + corruption); host suite 7/7 green. **RESIDUAL — OI-CHARGE-03 CLOSED (2026-07-07):** the fail-open hole is now closed by a fail-safe geometry gate (see next bullet). FAI bench with a calibrated current source still required at G2 prototype. OI-CHARGE-02 in NP-SW-001.
- [x] **OI-CHARGE-03 CLOSED: fail-safe electrode-geometry gate blocks CLIN_STIM until HD-tDCS area applied (2026-07-07)** — Closes the OI-CHARGE-02 fail-open residual: if the electrode-area command is lost/corrupt, CLIN_STIM would keep the permissive 1000µC pad default. New shared heartbeat bit `NP_SESSION_STATUS_GEOM_REQUIRED` (bit 2, `firmware/common/include/np_spi_wire_types.h`) is asserted by the hub every 200ms while an HD-tDCS ring/bilateral montage is in the session (`np_safety_spi_set_geom_required()` in `hub_control/src/np_safety_spi.c`; armed by the session runner in `np_session_runner.c` before any CLIN_STIM enable; cleared on abort/end). The safety MCU reads it into `np_safety_state_t.geom_required` and `np_charge_monitor_geom_gate()` clears `NP_SAFETY_EN_CLIN_STIM` from `granted_mask` whenever `geom_required && !geom_applied` — where `geom_applied` latches true only when a valid area command sets CLIN_STIM's limit (`np_charge_monitor_set_channel_area_mcm2()`), and resets each session. The gate runs among the interlocks **before** the charge-accumulate loop (a gated channel accrues no charge). **Fail-CLOSED:** a dropped area command keeps HD-tDCS disabled until a retry lands, rather than running at the wrong limit. **Scoped to CLIN_STIM only** — clinical tACS shares enable bit 13 but never sets GEOM_REQUIRED, so it is never blocked. **No new MCU status bit** (the 8-bit status byte is full); the block is observable via CLIN_STIM absent from granted_mask, and the hub needs no prompt since it already sends the area command. **Irreducible limit (documented):** a hub that never declares GEOM_REQUIRED for an HD-tDCS session still runs at the default — HD-tDCS and clinical tACS share enable bit 13, so full closure would need per-electrode enable lines (hardware). **Tests:** +5 charge-monitor gate cases (blocked-until-applied, granted-after-applied, tACS-unaffected, CLIN_STIM-only, re-arm-per-session) and +4 SPI-proto session-status-bit assertions; host suite 7/7 green. OI-CHARGE-03 in NP-SW-001.
- [x] **Fault latch extended SPI command privacy gate — RE-RESOLVED (2026-08-12), reversing the conditional suppression below.** The 2026-07-14 resolution (retained verbatim after this paragraph as the record of what was believed then) suppressed `tick_ms` to 0 **only** for `NP_SAFETY_STATUS_CARDIAC`. That is now understood to be a defect, not a protection. Because `count` was reported unconditionally by an *independent* helper, the observable pair `count > 0 && tick_ms == 0` identified a cardiac fault **with certainty and with no correlation work** — a one-bit oracle that is strictly worse than no redaction at all, since a bare relative SysTick value only becomes sensitive when joined to a session record SHDR does not hold, whereas the redaction *pattern* is self-interpreting. **General rule now binding: a redaction applied conditionally on a sensitive predicate leaks that predicate; the redaction must be unconditional, or the predicate must not be inferable from the pattern of redaction.** Two further findings established during the fix: (1) **the suppression never protected the cardiac predicate in the first place** — the hub already writes the cardiac cutoff to SHDR as a first-class named event (`NP_CVNS_SHDR_EV_CUTOFF` 0xC1 → `fault_log.fault_type = 'CVNS_HR_CUTOFF'`, `ci/shdr/shdr_fleet_schema.sql`), by the locked "safety interlock log → SHDR" rule; (2) **fault event timing was already classified UHDR unconditionally** — `fault_log` has no timing column and states "Precise fault event timing, where it exists at all, is UHDR under the user's key", and `np_log_shdr_fault()` discards its `session_ms` argument for *every* caller. **New resolution:** `tick_ms` is **not SHDR-reportable at all** (device-internal only); `status`, `slot` and `count` are reported unconditionally through ONE fixed-shape marshaller `np_fault_latch_build_report()` → `np_fault_latch_report_t {status, slot, count}`. The retired per-field accessors `np_fault_latch_report_tick_ms()` / `_report_count()` are deleted — the oracle existed *because* they were independent, so the fix is structural (a fixed struct shape cannot have a field present for one fault kind and absent for another) rather than a different constant. `tick_ms` remains in the SRAM latch for MCU-internal timing. **23 host assertions** (`np_fault_latch_privacy_tests.c`, was 18), including a falsified regression guard: with identical history at the same tick, the reported records for a cardiac and a non-cardiac fault must differ **only** in `status` (the deliberate, already-published fault-kind channel) — verified to FAIL when the retired conditional is reintroduced, and the `count`/`slot` equalities are pinned to positive expected values so a do-nothing marshaller cannot satisfy it. Firmware host suite 28/28 green. **IEC 62304 Class C:** reporting-path change only — no stimulation enable, interlock threshold, cutoff timing or latch-commit behaviour altered; `np_fault_latch_commit()`/`_clear()`/`_init()` untouched. Development-mode posture: no SHDR fault records exist, so no fleet data was encoded under the retired rule and no consumer needed migration. See `docs/status/completed-decisions.md` (2026-08-12 entry).
- [x] **Fault latch extended SPI command privacy gate — RESOLVED (2026-07-14) — SUPERSEDED, see the entry immediately above:** The classification of the fault-latch `tick_ms` (event timestamp) and `count` fields — which are NOT in the current 8-byte `np_safety_tx_frame_t` reply and never leave SRAM today — is now locked for any future dedicated fault-latch SPI read command. **`count` → SHDR** unconditionally (distinct-fault-transition tally; device-condition, no biology, no timestamp; mirrors the SHDR device session count). **`tick_ms` → SHDR** in general (a device fault time, consistent with "safety interlock log → SHDR"), **but SUPPRESSED (reported as 0) when the latched fault carries `NP_SAFETY_STATUS_CARDIAC`** — a cardiac cutoff during cervical VNS co-locates the event time with a UHDR-class health event (the wearer's cardiac response), and even the relative SysTick value could be linked to a session record (mirrors the OI-CVNS-HUB-11 divergence flag's suppressed timestamp). Resolution baked into enforceable firmware, not prose: any future read command MUST marshal these fields through `np_fault_latch_report_tick_ms()` (CARDIAC→0) and `np_fault_latch_report_count()` in `firmware/safety_mcu/src/np_fault_latch.c` rather than reading `s_latch` directly. **18 host tests** (`firmware/safety_mcu/tests/np_fault_latch_privacy_tests.c`) assert the suppression + change-gated count, wired into `firmware/safety_mcu/CMakeLists.txt` + root `firmware/CMakeLists.txt` `add_test` + CI `firmware-host-tests.yml` (already triggers on `firmware/safety_mcu/**`); `np_fault_latch.c` cross-compiles clean for Cortex-M0+ under `-Werror` (section attribute guarded by `NPTEST_HOST` for host builds — Mach-O rejects the bare `.fault_latch` name; target image unchanged). Both elements added to the NP-FW-EMMC-001 §12 session data classification table (`docs/np_fw_emmc_001.docx`, now 32 elements; revision-history amendment logged). No change required to current firmware behavior — the 8-byte reply frame is untouched.
- [ ] **SBOM (Software Bill of Materials)** — NP-SBOM-001 planned Year 2; required for 510(k) cybersecurity submission.
- [x] **SessionHistoryStore session timestamp migration (NP-PRIV-ANALYSIS-002 MEDIUM-07, 2026-06-05)** — `SessionRecord.completedAt: Date` replaced with `sessionDay: String` ("yyyy-MM-dd" local calendar day) + `insertionIndex: Int` (same-day ordering tiebreaker). Exact timestamps no longer written to UserDefaults or iCloud/iTunes backups. No v1 migration — this is pre-production with no deployed user data. Synthesized `Codable` (no custom init/encode). One new test: `testPersistedBlobContainsNoCompletedAtKey` (privacy invariant). Display formatters updated to omit time. `CompletedSessionSummary(record:)` reconstructs start-of-day Date from sessionDay for display. Resolved before US beta.
- [x] **Watch bridge payload review — `SessionState.toWCMessage()` (NP-PRIV-ANALYSIS-002 MEDIUM-08, 2026-06-05)** — Payload reviewed. No raw EEG or HRV time-series values present. `coherenceX100` and `rmssd` are derived display metrics (same values in `SessionRecord`). `epoch` (Unix ms timestamp, UHDR-class) removed from payload — Watch elapsed timer is driven by a local `Timer`, not `epoch`. `from(wcMessage:)` defaults `epoch: 0` on reconstruction. `WatchSessionManager` is entirely in-memory; no Watch-side UserDefaults persistence. Resolved before Watch beta.
- [x] **Certificate pinning for SHDR fleet endpoint (NP-PRIV-ANALYSIS-002 LOW-11, 2026-06-05)** — `SHDRUploader` now uses a dedicated private `URLSession` with `SHDRFleetPinningDelegate: URLSessionDelegate` implementing SPKI SHA-256 pinning. Algorithm: OS trust store evaluation first, then leaf-certificate SPKI hash compared against `pinnedHashes: Set<Data>`. Covers RSA-2048/4096 and EC P-256/P-384 via TrustKit-compatible DER SPKI headers. Session uses `.ephemeral` configuration. Two placeholder hashes installed — replace with actual derived hashes before production deployment. `CryptoKit.SHA256` used for hash computation. `SecTrustCopyCertificateChain` used (iOS 15+).
- [x] **SessionProtocolSigner Keychain load query — add `kSecAttrAccessible` filter (NP-PRIV-ANALYSIS-002 LOW-12, 2026-06-05)** — `kSecAttrAccessible: kSecAttrAccessibleWhenUnlockedThisDeviceOnly` added to the `SecItemCopyMatching` read query in `loadOrCreateSigningKey()`. Prevents an attacker-written key with a different accessibility class but matching `kSecAttrApplicationTag` from being returned by the query. Write/add query already had the filter; now read and write are consistent.
- [x] **ConsentDashboardView contact email redaction (NP-PRIV-ANALYSIS-002 LOW-10, 2026-06-05)** — `redacted(_:)` helper added to `ConsentDashboardView`. Email → `•••@domain.com`; phone → `••• ••• ••XX`. Summary row uses redacted form; full value remains in Research Preferences sheet.
- [ ] Partner optician network contract (S3 Rx program)
- [x] eMMC partition architecture + separate UHDR/SHDR encryption in firmware specification (before any storage code is written) — **NP-FW-EMMC-001 Rev 1 (2026-05-11); see `docs/np_fw_emmc_001.docx`**
- [x] Dual-bank OTA bootloader (must be from first firmware line) — **NP-FW-EMMC-001 Rev 1 §8.1–§8.4; firmware/bootloader/ (Issue #20)**
- [x] Cervical VNS safety interlock specification + safety MCU cardiac interlock (NP-FW-CVNS-001 Rev 1 — Issue #24, G3-08 SOFTWARE BASELINED)
- [x] Cervical VNS 510(k) Q-Sub substantial equivalence argument (NP-REG-CVNS-001 Rev 1)
- [x] 1064nm smart zone module firmware specification (NP-FW-PBM1064-001 Rev 1 — Issue #53, firmware/pbm_1064nm/)
- [x] 1064nm multi-wavelength session protocol (NP-SES-1064-001 Rev 1 — Issue #53/#55, docs/np_ses_1064_001.md)
- [ ] FAI-CV01 electrode placement bench (anatomical phantom — T2 prototype required)
- [ ] FAI-CV02 cardiac interlock timing bench (oscilloscope, R-peak signal generator — T2 prototype required)
- [ ] FAI-CV03 tolerability (3 healthy adults — IRB approval required)
- [ ] **OI-BLE-01: SHDRUploader upgrade path to hub-provisioned TRNG warranty token** — **App-side IMPLEMENTED (iOS + Android, 2026-07-14); residual is external hub firmware OI-WA-03.** `SHDRUploader` now upgrades from its interim Keychain token to the hub-provisioned 256-bit TRNG token as soon as the hub delivers it over GATT. Wiring: the SHDR-facing `SHDRUploadTriggering` protocol gained `warrantyTokenPublisher: AnyPublisher<Data?, Never>` (backed by `NeurOneGATTManager.$warrantyToken`), keeping the uploader's least-privilege boundary (it still never sees the full UHDR-carrying GATT interface; the warranty token is SHDR-class device identity). `SHDRUploader.observeWarrantyToken()` subscribes and `compactMap`s out `nil` so a disconnect never rolls back an established upgrade; `upgradeDeviceToken(_:)` rejects any payload ≠ 32 bytes; `currentDeviceToken()` prefers the hub token (hex) for the `X-NP-Device-Token` header and falls back to the Keychain token before provisioning. The full app-side chain is closed (discovery → `readValue(for: warrantyTokenChar)` → `applyWarrantyToken` → `$warrantyToken` → uploader). +4 tests in `SHDRUploaderTests`. Until hub firmware ships the `warrantyToken` GATT characteristic (OI-WA-03, UUID `4E455550-0010-…`) the publisher stays `nil` and the Keychain fallback is used — the app has no work pending. BLOCKING for the full SHDR fleet DB no-join architecture (NP-FW-EMMC-002 Rev 1 §A) only insofar as OI-WA-03 must land. **Android — collector WIRED (2026-07-14, PR #204):** the Android SHDR upload pipeline is now composed to match iOS (superseding the earlier "not-yet-composed" status). `NeurOneGattManager` exposes `shdrUploadPending: StateFlow<Boolean>` (set true when `shdrUploadStatus` notifies 0x01, reset on disconnect), mirroring iOS `$shdrUploadPending` / `SHDRUploadTriggering`. `NeurOneApplication.composeShdrUploadPipeline()` instantiates `ShdrUploader` (gated by `WarrantyAnalyticsGate`) and launches two `bleScope` collectors: (a) `shdrUploadPending` → reads the hub-staged `shdr_staging.bin` payload on `Dispatchers.IO` and uploads when the warranty gate is open (deletes on success); (b) `warrantyToken` → `ShdrUploadWiring.applyWarrantyToken(it, …)` which drops nulls so a disconnect never downgrades the token. Token storage sits behind a `DeviceTokenStore` seam (`EncryptedPrefsDeviceTokenStore` in production — identical Keystore-encrypted CSPRNG behavior) so the token logic is JVM-testable without the Android Keystore; pure collector rules are in `ShdrUploadWiring`. Kotlin tests (`app/src/test/kotlin/life/neurone/app/data/ShdrUploaderTests.kt`) cover the four OI-BLE-01 cases (64-hex fallback, 32-byte upgrade, null-does-not-downgrade, non-32-byte rejected) plus two wiring-gate cases, build-verified by the new Android CI (`.github/workflows/android-ci.yml` → `:app:testDebugUnitTest`). Both platforms stay `nil` and use the CSPRNG/Keychain fallback until hub firmware ships `warrantyToken` (OI-WA-03) — the collectors then upgrade automatically. Tracked in app/ios/ISA.md Decisions as OI-BLE-01.
- [ ] SBIR Phase I application
- [ ] First researcher contacts
- [ ] SAB formation (Tsai outreach first)
- [ ] **A second power inlet alongside USB-C is a third TMS power-path option nobody assessed — and CLAUDE.md §2.2 may already assume it (NP-PWR-BUDGET-001 Rev 3, 2026-08-21)** — `OI-PWR-04` framed the TMS power path as shared-rail vs. mains-tethered. A third option exists: **keep USB-C for data, programming and all T1 power; add a second power-only inlet for high-draw T2 sessions.** It dominates both because it **scopes the Mode 3 loss to the modality that forces it** rather than to the whole device — CLAUDE.md §1's *"wired-first USB-C"* and Mode 3 autonomy both survive for T1. Four decisions:
  - **(a) `OI-PWR-11` — principal, and it comes first. §2.2 (LOCKED) ships Pro Full two 65 W chargers ($26 BOM); §4.5 negotiates a single 100 W EPR contract. 130 W is not 100 W.** §2.2's table is otherwise strictly keyed to *"peak draw of configuration"* and prices spares nowhere else. Either the second brick is an undocumented spare, or **a dual-inlet architecture was assumed and never specified**, or it powers a **separate T2 accessory that has no port, enclosure or power spec in any document** — the last being consistent with the finding that TMS is absent from the electrical model entirely. **The answer decides whether §4.4 is a proposal or a specification.** Nothing in §2.2 has been modified.
  - **(b) Prefer a second USB-C PD sink over a barrel or proprietary DC inlet — and the reason is IEC 60601-1, not cost.** A PD sink stays SELV behind a certified charger; a mains-derived inlet makes **NeurOne the isolation barrier** on a device with conductive applied parts (tDCS, tACS, VNS), at the 510(k) tier, with `NP-DT-001` **VE-11** (accredited-lab standards testing, G3 Month 10–14) still **Open**. It also reuses the already-specified USB-C Layer 5 filter and preserves §2.2's *"any PD-compliant charger must work"* EU stance. `NP-TOOL-HUB-001` HUB-MDR-04 already specifies two hub port openings with tethered covers, so the mechanical feature exists.
  - **(c) `OI-PWR-12` — EMF must be bench-verified, not argued.** An inlet carrying up to 5 A of switched current is a materially worse aggressor than a data port, and the hub PCB sits at or adjacent to the **occipital arch**, inside the assembly whose **measured** shielding is the product's primary technical claim. **Gates the option regardless of how (a) resolves.**
  - **(d) `OI-PWR-13` — dual-source arbitration is new IEC 62304 surface.** OR-ing, inrush, and defined behaviour on mid-session hot-plug or removal; a brownout during active stimulation is a safety event. **The safety MCU has no representation of total available power today.** Interacts with D-4 — the governor's budget becomes the sum of two negotiated contracts, recomputed on hot-plug.
  **It does NOT raise T1 PBM concurrency**, and should not be justified on that basis: §3.2's thermal ceiling (4–8 tiles) and the power-derived one (~6) are coincident, so relieving power just moves the binding constraint to heat. Revisit only if `OI-PWR-01`'s multi-tile CFD returns a materially higher thermal ceiling. See `docs/np_pwr_budget_001.md` §4.4.

- [ ] **17 of 20 predefined PBM protocols exceed the power envelope, and all 20 compile clean (NP-SES-PWR-001 Rev 1 + NP-HW-HEXTILE-001 Rev 8, 2026-08-21)** — `OI-HEXTILE-09` was raised as a hypothetical gap in the v2 wire format. It is an actual one: audited against `protocols/predefined/`, **2 protocols fit the 40 W emitter budget, 17 exceed it by 1.25× to 40×** (Vascular Baseline needs 1,600 W), **1 is operator-scoped**. `app/web/src/lib/hubCompiler.ts` has **no power or budget check of any kind**. Audit: `bun scripts/check-pbm-power.ts`. Four decisions follow:
  - **(a) `OI-SESPWR-01` — the zones are lobe-scale; the evidence is electrode-scale. This is a clinical-validity defect, not a power one.** `clinical-04-pbm-depression.npps` is titled *"PBM — Depression (DLPFC)"* and targets **37 sockets**; its cited evidence specifies *bilateral DLPFC (F3/F4)*. **No DLPFC zone, no F3/F4 zone, and no single-10-20-site zone exists** — the finest frontal targeting available is a 20-socket half-lobe. One tile is ~40 mm FWHM at cortex (`NP-OPT-PSF-001` §3.2), so ~2 sockets/hemisphere is both physically meaningful and evidence-faithful. **This would need fixing even with an unlimited power budget** — a protocol titled DLPFC that irradiates the frontal lobe cannot support a DLPFC claim, which is exactly the hazard `clinical-09-pbm-stroke-rehab.npps` already names in its own comments and resolves with `clinician_selected`. **Blocked on REG-1** (socket-to-10-20 registration). **Largest single reduction in the over-budget condition, and it is a data edit.**
  - **(b) `OI-SESPWR-03` — `frequency: 0Hz` with `duty_cycle:` is undefined and worth 4×.** Four protocols set both. `NP-NPPS-REF-001` §4.1 says `frequency: 0` selects CW and CW is 100 % duty; `hubCompiler.ts` emits `freqCode(0)` and `dutyReg(25)` independently and leaves the hub to decide. Depression draws **740 W or 185 W** depending on the answer. **This blocks `OI-HEXTILE-09`** — a governor cannot be written against an undefined input. Preferred resolution: make it a parser error.
  - **(c) `OI-SESPWR-02` — possible R-4 CW breach, independent of power.** `Vascular Baseline` at 80 % CW implies ~322 mW/cm² against R-4's 200 mW/cm² CW ceiling. No mode-dependent clamp was found in the compiler, the validator, or on the firmware searches run — **which is not proof of absence.** Needs a firmware read.
  - **(d) `OI-SESPWR-04` — cascading cannot rescue the protocols that most need it.** Rotating socket groups preserves dose and multiplies session length by the group count (40 m to **20 h**). It survives for dose-driven CW protocols — and **fast cascading is the same mechanism as `NP-PWR-BUDGET-001` §3.6's whole-vault mode**, in the time domain rather than the current domain. It **invalidates every 40 Hz entrainment protocol**, including Grade A Alzheimer's, because the claimed mechanism is network-wide *coherent* rhythmic drive; it also breaks closed-loop adaptation and the cross-modal 40 Hz phase-lock in `15-full-t1-immersive`. **Any cascade primitive must carry a per-protocol admissibility flag defaulting to *not admissible*** — without it, the primitive silently invalidates the strongest protocols in the library.
  **Sequence matters (`NP-SES-PWR-001` §5): fix the zones, then the CW/duty semantic, then the CW ceiling, THEN build the governor, and cascade only the residue.** A governor built against today's library would reject 17 protocols when the correct response to 15 of them is to fix the protocol. See `docs/np_ses_pwr_001.md`.

- [ ] **PBM has an efficacy FLOOR, not just a ceiling — and three things follow (NP-PWR-BUDGET-001 Rev 2 + NP-HW-HEXTILE-001 Rev 7, 2026-08-21)** — The power-budget study classified every modality by its *ceiling* (dose-capped or heat-removal-bound). **A budget bounded only from above cannot say what the power is for.** `docs/pbm_neuro_protocols.md` supplies the floor: **0.02–0.3 W/cm² at scalp, 10–120 J/cm², 6–30 min**, with *"under-dosing, not mechanism failure, explains most nulls"* (NEST-1/2/3; Iosifescu 2022). Three consequences now need decisions:
  - **(a) `OI-HEXTILE-06` has arguments on both sides, and the cost model sees only one.** *Populated* is not *driven* — a populated socket costs 2 mA / 25 mW standby plus BOM, not 25 W, so a fully populated helmet under the ~6-tile governor draws what a 20-tile build draws. Options 2/3 (silicon PD, per-cluster PD) **trade away the dual-PD dose metering**, which is the structural answer to the **−79 % declared-vs-measured** gap independently measured on a marketed 1070 nm helmet — dropping it to save ~$9/tile makes NeurOne a better-specified competitor rather than a differently-verified one. And a whole-vault mode wants *more* sockets populated. `NP-COST-001` §6 evaluates only the BOM axis. **Still a principal decision, and `OI-COST-10` makes it a precondition on setting retail price.**
  - **(b) A whole-vault low-irradiance mode is affordable and unspecified — `OI-PWR-07`.** ~30 W lights all 80 sockets at ~6 mW/cm², inside the existing 45–50 W envelope, and Grade A Alzheimer's specifies *whole-head* geometry. **But 7.2 J/cm² in a 20-min session is below the ≥10 J/cm² threshold** — it is coverage, not a therapeutic dose, and must never be presented as one. Product + Clinical decision on whether it is a feature at all.
  - **(c) `OI-HEXTILE-21` — the 1064 nm channel is 9× below its own flagship protocol.** §4.3.2 gives CH_C **28 mW/cm²**; Grade A cognitive enhancement specifies **0.25 W/cm²**. A 90-site 1064-only tile still reaches only ~85 mW/cm². **η_wp ≈ 4.8 % emitter wall — more watts and more sites cannot close it.** Options: better emitter (extends `OI-HEXTILE-02` to CH_C), accept CW/long sessions, or **claim 1064 nm against the AD protocol (0.1–0.3 W/cm²) rather than the cognitive one**. **Do not make comparative 1064 nm claims until this resolves.**
  See `docs/np_pwr_budget_001.md` §3.4–§3.7, `docs/np_hw_hextile_001.md` §6.4/§9.3/§11, `docs/reference/competitive-position.md`.

- [ ] **`OI-HEXTILE-20` — the 25.0 W/tile peak may not be a legal operating point, and it sets a tooling-blocking contact count (NP-HW-HEXTILE-001 Rev 7, 2026-08-21)** — §4.3.1 puts each T1-A channel at **403 mW/cm²** at full drive, so **both channels together is 806 mW/cm² against R-5's 600 mW/cm² aggregate ceiling.** Either R-5 binds only the three-channel case its source addresses — in which case say so, because §4.3.2 presents the check as general — or the true per-tile peak is **~18.6 W**, and the rail current, §9's whole concurrency table, and the **3-contact `VLED` count that D-5 calls *"load-bearing and now tooling-blocking"*** are all derived from the wrong number. Resolve with `NP-FW-PBM1064-001` as R-5's owner.

- [ ] **`OI-HEXTILE-09` scoping — the concurrent-power governor must be denominated in watts, not tiles (NP-HW-HEXTILE-001 Rev 7, 2026-08-21)** — A "~6 tiles" rule is wrong in **both** directions: it forbids the 80-tile / ~30 W whole-vault case and permits **six tiles at full dual-channel drive = 150 W**, three times R-10's peak. Per-tile drive is an input to the check, not a constant. Nothing in the delivered v2 wire format bounds this today, and `NP-PWR-BUDGET-001` §3.5 puts the unbounded worst case at **~2.0 kW**. `NP-DRV-SHELL-002` **SH2-DRC-02b**'s pass condition reads *"global governor present"* with no unit and needs one.

- [ ] **Power budget (CLAUDE.md §4.5) — TMS has no electrical spec, T2-peak figure never included it (NP-PWR-BUDGET-001 Rev 1, 2026-08-02)** — Design study reversed the direction every other power/thermal document uses: instead of taking CLAUDE.md §4.5 as a given input, derived what the budget should be from actual safety ceilings. **T1 (PBM):** the existing "~6 concurrent tiles" rule (NP-HW-HEXTILE-001 §9) is power-budget-derived, not thermally derived; an independent first-order thermal estimate off the existing single-tile CFD model (NP-THERM-CFD-R1-001) brackets 4–8 tiles — close enough that T1's figures can likely stand pending real multi-tile CFD (OI-PWR-01), but flags that clustered-montage activation (e.g. bilateral DLPFC) concentrates residual cavity heat more than the flat rule assumes. **T2 (the bigger finding):** TMS coil drive has **zero** power/energy specification anywhere in the document tree, and is absent from the hub power architecture and the thermal power-budget table — T2-peak (70–74W) is explicitly sized for the 1170nm laser zone only. An order-of-magnitude physics estimate (E∝B² scaling from general clinical rTMS device figures) puts average power during a 10Hz pulse train at ~15–400W depending where in NeurOne's own stated 0.1–0.5T field range the coil runs — at the top of that range, TMS alone exceeds the entire current T2-peak envelope. Also flags that 0.1–0.5T may itself be below what commercial figure-8 coils need for the 120% RMT protocols the evidence base cites (separate EM coil-design question, OI-PWR-02). **Routed to principal decision (OI-PWR-04):** does TMS share the main battery-buffered rail (needs a purpose-built capacitor bank + likely >100W EPR PD contract) or run external/mains-tethered for T2 sessions (excludes TMS from Mode 3 autonomous operation)? Does not modify CLAUDE.md §4.5 pending that decision. See `docs/np_pwr_budget_001.md`.
- [ ] **OI-CHARGE-04: tDCS electrode-area assumption diverges between the app pre-flight check and the safety MCU enforcer (raised 2026-08-05, OPEN — decision owner: Regulatory / Safety SW)** — The 40 µC/cm² charge-density limit is computed against **two different electrode areas**. iOS `NPHardwareLimits.tdcsDefaultElectrodeAreaCm2 = 35.0` (`app/ios/NeurOne/Protocol/NPHardwareLimits.swift`, per DHF Rev 11, "a standard tDCS sponge pad") drives the app's pre-flight validation; the safety MCU's `NP_ELECTRODE_AREA_CM2 = 25` (`firmware/safety_mcu/include/np_safety_config.h`, per OI-CHARGE-02, "correct for a standard 25 cm² tDCS electrode") drives the hard cutoff. **This is not a safety hole** — the MCU owns the enable GPIO and terminates regardless of what the app believed. It is a *fidelity* defect: the app validates against an assumption 40% more permissive than the enforcer, so it will accept and start a protocol the MCU cuts short at ~71% (25/35) of its authored duration, presenting as an unexplained mid-session stop. Nothing in the codebase ties the two constants together, so neither can drift into agreement by accident. **Two candidate resolutions, both requiring a design decision — do NOT change either number unilaterally:** (1) **Harmonize downward to 25 cm²** — change the iOS constant to match the enforcer, making the pre-flight conservative and the session honest. Cheapest, changes no Class C code, but contradicts the DHF Rev 11 record and would need a DHF change note explaining why the 35 cm² sponge assumption was abandoned. (2) **Make the area a per-protocol authored parameter** rather than a global default, transmitted in the signed session protocol so app and MCU validate against the *same* declared geometry. Correct in principle (electrode area genuinely is a per-montage property, and HD-tDCS already uses 3.5 mm electrodes, nothing like either constant) but touches the signed-protocol wire format and the Class C charge monitor, so it is real firmware work, not a documentation fix. **Additional gap found while raising this:** the web table (`app/web/src/lib/hardwareLimits.ts`) carries `tdcsMaxChargeDensityUCcm2: 40.0` but has **no electrode-area constant at all**, so it performs no charge-density pre-flight whatsoever — whichever resolution is chosen must also decide whether web gains the check or documents that it deliberately defers entirely to the MCU. Divergence documented in-place at both app sites; `np_safety_config.h` deliberately left untouched (in-flight work).
- [ ] **OI-DOC-01: NP-FW-HUB-001 is registered and cited as governing, but the spec was never written (raised 2026-08-05, OPEN — decision owner: Design Authority)** — `docs/np_fw_hub_001.md` does not exist. NP-FW-HUB-001 Rev 1 is nonetheless registered in `docs/status/document-register.md` as the Hub Control Program specification, cited as the governing document at the top of `firmware/CMakeLists.txt` and `app/web/src/lib/hubCompiler.ts`, and referenced from ~48 files across `firmware/hub_control/`. The firmware is therefore its own only description, and `hubCompiler.ts` — which must produce a wire format the hub accepts — has no specification to compile against, only the C it is trying to match. **This is a design-control gap, not a code defect:** 21 CFR §820.30(d) expects design outputs to be documented such that conformance to design input can be verified, and a 510(k) reviewer following the register lands on nothing. Its register entry was additionally stale in two ways, both corrected 2026-08-05: it described the module registry as probing "all 11 zone + accessory slots" (retired — the lattice is 80 sockets addressed by UID auto-inventory via `np_module_map`) and as driving "zone_announce for bone conduction" (retired — the bone-conduction announce tone was removed per NP-HFE-002 Rev 1 §7.2). **Resolution options:** (1) author NP-FW-HUB-001 Rev 1 properly against the shipped `firmware/hub_control/` behaviour, back-dating nothing and treating it as a new design output; (2) formally retire the document number and re-scope the hub control program under NP-SW-001, updating the ~48 citations. Option (1) is expected but is genuine specification work, not a documentation tidy — deliberately **not** attempted as part of the 2026-08-05 consistency pass, which recorded the absence honestly instead of writing a placeholder.

- [x] **`OI-NPPS-MOBILE-01`: neither mobile NPPS parser implemented `zone` or `condition` top-level blocks — RESOLVED 2026-08-23 (NP-NPPS-REF-001 Rev 10)** — Both mobile parsers now implement §8 `zone` and §9 `condition` blocks, build the §1.6 single namespace, and validate a protocol's zone and condition references against it; the shipped library resolves with zero unresolved references. Resolution (1) was taken — implement the blocks — rather than (2), declaring zone expansion a hub-side concern, because the parsers could then never validate a reference and §1.6's loader guarantee would have become web-only. Four further gaps were found and closed in the same change: neither parser read a protocol's `conditions:`/`references:`; **Android's PBM targeting was still the retired five-slot `ZoneSelection` model** and could not express a named zone; `scripts/sync-conditions.ts` no longer transcribes the registry into Swift and Kotlin (Windows keeps its table — it has no NPPS parser); and §8's socket range was documented as `1..30` against a lattice of 80. Android `:core` verified green at 172 tests; the iOS half is unbuilt for want of an Xcode toolchain in the authoring environment.
