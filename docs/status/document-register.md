# Documents Generated

> Relocated from CLAUDE.md Rev 32 §14 to slim the always-loaded core. This is the authoritative document/firmware register.
>
> **Note on overlap:** this register substantially overlaps the formal DHF index (`docs/np_dhf_001.md`). Content was relocated faithfully, not deduped — a dedup pass (making the DHF the single index and reducing this to a pointer) is flagged as a follow-up. Referenced from CLAUDE.md → Document Map.

| Document | Location | Contents |
|----------|----------|---------|
| Design Brief Revision 1 | `docs/superseded/np_db_001.docx` | SUPERSEDED by R5 — Initial complete design specification |
| Design Brief Revision 2 | `docs/superseded/np_db_002.docx` | SUPERSEDED by R5 — Updated with LED count, irradiance, EEG, EMF decisions |
| Design Brief Revision 3 | `docs/superseded/np_db_003.docx` | SUPERSEDED by R5 — Adds UHDR/SHDR, consent systems, durability, service network |
| Design Brief Revision 4 | `docs/superseded/np_db_004.docx` | SUPERSEDED by R5 — zone module engineering package; dual-PD, keying, gasket, lever, PDMS qual, 24-risk register |
| Master Design Brief | `docs/np_db_005.docx` | **NP-DB-005 Rev 6 — DRAFT, PENDING APPROVAL** (authored 2026-08-11; 21 CFR §820.40(a) requires approval prior to issue, so Rev 5 remains the last approved revision). Retirement-led re-issue. **§0.1 is a retirement ledger**: the fixed 5-slot/600-LED zone-module architecture, ZONE_ID resistor detection, per-slot LPI2C3 addressing, Hub PCB Rev B's DG2788A per-slot gain switch, RISK-15 keying layers 2–5, RISK-17 multi-FPC bundle management, the ~500-line safety-MCU figure, the per-zone `NP_SAFETY_EN_PBM_ZONE_0..4` bits, the 16-channel T2 tACS driver, the 25-risk NP-RISK-001 register, the ≥150 N/m PDMS peel figure and ACC-as-focal-target are each named as retired next to their replacement and the reason. Then current state: hex-tile lattice (80 sockets, 40 mm, T1-A/B/C, CLUSTER-1/SYM-1/CONTIG-1, 18 clusters); Hub PCB Rev C cluster-controller tier; NP-DRV-SHELL-002 five-network split and three EMI prohibitions; BN-boss thermal export and RISK-26; four UHDR/SHDR boundary resolutions new since Rev 5; two-consent-subjects table; SOUP vendoring and NP-SW-CI-001; OI-LED-W1 OPEN with all three options; clinical-03 evidence-grade claim GATED; every marketing claim carried with its gate. **First issue under NP-CONV-001 Rev 6** — Rev 6 is a new *revision* of serial NP-DB-005, not a new serial NP-DB-006, written in place per §4.0/§4.3 with Rev 5 retained by version control. Generator: `scripts/create_np_db_005.py`. |
| Clinical Evidence Bibliography | `docs/np_bib_001.docx` | 39 entries, 12 modality sections (A–L), DOI links, NeurOne-specific summaries. Section L (entries 34–39): 1064nm PBM — Yao et al. Science Advances 2022 (primary RCT), UT Dallas follow-ons (open item), tissue optics penetration depth basis, CCO mechanism at 1064nm, safety summary, multi-modal combination evidence gap |
| Researcher Candidate List | `docs/neurone_researchers.docx` | 12 researchers, 7 modalities, contact info, cost estimates, funding sources |
| SBIR Phase I Draft | `docs/np_sbir_001.docx` | NIH SBIR Phase I proposal draft; references Hirose FH34S (not Molex SlimStack) |
| FPC Zone Module Specification | `docs/superseded/np_hw_fpc_001.docx` | NP-HW-FPC-001 **Rev 4** — 20-pin pinout, dual-PD architecture (§8.4), PDMS bonding (§9), thermal cycling qualification (§9.3), multi-FPC routing (§11.3), five-layer keying (§11.4), gasket (§12). **⚠ SUPERSEDED 2026-07-28 — retired large-format/5-slot architecture; see `docs/superseded/np_hw_fpc_001.md` supersession note.** | **Revision corrected 2026-08-11: this row said Rev 3 (written Rev C) while `NP-DHF-001` said Rev 4 (Rev D). The document's own header reads `Revision: D (Layout Frozen — Released for Mold Specification)`, so the DHF was right and this register was wrong.** The disagreement was settled from the artifact *after* it had been deleted from the tree, which is the concrete return on `NP-CONV-001` §4.0.4's precondition that retrievability be established before deletion — the file was recoverable byte-identical from git and could still be opened and read.
| FPC Procurement Requirements | `docs/np_proc_fpc_001.docx` | NP-PROC-FPC-001 Rev 1 — LED Vf binning, Hirose FH34S exclusions, BCR421W spec, RA copper |
| Zone Module Risk Register | `docs/superseded/np_risk_001.docx` | **⚠ SUPERSEDED 2026-08-11 — this was the ISO 14971 baseline risk file (NP-RISK-001 Rev 3), not merely a stale specification.** Replaced by `NP-RISK-002` (index + disposition of all 26 RISK IDs), `NP-RISK-003` (hex-tile) and `NP-RISK-004` (shell/socket/interconnect/hub). Retained in `docs/superseded/` as the record for 2026-05-13 → 2026-08-11. 25 risks (RISK-01 through RISK-25); 23 MITIGATED; RISK-03 and RISK-20 OPEN; RISK-25 (cervical VNS cardiac reflex) MITIGATED — pending FAI-CV02 hardware bench |
| Shell FPC Routing Review | `docs/superseded/np_drv_shell_001.docx` | **⚠ SUPERSEDED — architecture replaced by `NP-DRV-SHELL-002`; the design-review *record* function restored as `NP-REV-SHELL-001` (2026-08-11).** NP-DRV-SHELL-001 Rev 2 (2026-05-10) — bend radius, multi-FPC bundle mgmt (§2.3), EEG cable routing (§2.4), 23-item DRC checklist; §2.4.5 locked routing path (OI-09 CLOSED, DRC-22 CLOSED, DRC-23 CLOSED) **⚠ SUPERSEDED 2026-07-28 — retired 5-zone-slot FPC routing architecture; replacement per-socket interconnect for the ~80-socket hex-tile lattice not yet designed, see `docs/np_hex_zm_001.md` §7 MECH-1/MECH-2/SMART-1.** **➡ REPLACED BY `NP-DRV-SHELL-002` Rev 1 (2026-07-29, `docs/np_drv_shell_002.md`) — new document number because the architecture is replaced, not revised.** |
| EEG Electrode Net | `docs/np_hw_eegnet_001.md` | **NP-HW-EEGNET-001 Rev 3 (2026-08-20)** — **Rev 1's central premise was WRONG and is corrected.** Rev 1 modelled shell-to-head mapping by **arc length**, giving ~50 mm of tangential 10-20 error at Oz and concluding the hex-tile architecture was unrescuable. A standoff shell with radial pods maps by **normal projection**, which for concentric similar surfaces preserves *angle* — and a 10-20 fraction is an angular invariant across similar heads. **Size very nearly cancels: ~1.6 mm, not ~50 mm.** Shape (vault height +/-10%) contributes ~3.4 mm and does not cancel. The dominant term is **seating concentricity, ~0.9 mm of landing error per 1 mm of head displacement**, which is unspecified and unmeasured anywhere in the document set (**OI-EEGNET-14**, now the blocking measurement). `NP-HEX-ZM-001` §3.2's claim that the lattice "registers to anatomy by construction" is **correct** and Rev 1's refutation is withdrawn. **Both named defects are fixable in-tile:** true Oz is 18 mm from socket 74's centre and the tile's active-field circumradius is 20.21 mm, so Oz lies inside socket 74's own field; Fp1/Fp2 at ~35-40 mm apart are reachable from two adjacent tiles at +/-18 mm offsets. The electrode sits at tile centre by **convention** (`NP-HW-HEXTILE-001` D-1 "site 0 reserved"), not physics, and §4.5 states T1-B *"needs no new FPC outline, no new lattice, and no new socket interface - only a different placement file."* New **§1.4** specifies in-tile electrode offset as the lead option: offset range = 20.21 mm minus pod radius, so **OI-HEXTILE-05 (pod diameter, unspecified) IS the tangential registration budget in disguise** (**OI-EEGNET-16**) - +/-6.9 mm at the current ~half-LED pod, +/-14.5 mm at a 11.4 mm pod. **§1.5: in-tile offset addresses ALL FOUR of §7.2's adverse consequences and dissolves the §0 dual-rated fork**, at zero tooling and +3.9% emitters instead of +18%. **The net is demoted from recommendation to fallback**; §§2-6 are retained as a valid net specification for the case where concentricity or T2 tolerance (**OI-EEGNET-15**) requires it. Rev 1 content below. — **Rev 1 (2026-08-18)** — **DRAFT.** Establishes that EEG electrode misregistration is **not** a pod-travel problem: the radial margin is only ~-4 mm (15.9 mm mean-radius delta across 520-620 mm vs +/-12 mm travel), but the **tangential** divergence between a fixed socket position and a 10-20 site defined as a fraction of the wearer's own 331 mm nasion-inion arc reaches **~50 mm at Oz** and **~28 mm at Cz**, and pod travel is a radial degree of freedom that reduces it by exactly zero. Corollary: `REG-1` as posed is unsatisfiable for EEG, and the Fp1/Fp2 shared-socket defect and Oz-18 mm-anterior caveat are the premise showing through rather than tuning problems. Specifies an equal-tension geodesic strut net with four palpable landmark anchors, contact force relocated from the helmet pod into a per-puck compliant element, fluoropolymer struts chosen for **absence of C-H bonds** (no NIR overtone absorption at 660/810/1064/**1170** nm), and a per-session placement check reusing the tiles' existing **PD2** dose-metering photodiodes as a pose encoder. Sizing is an **output of gate NET-1**: N = ceil(ln(620/520)/ln(1+D)) on measured strain-tracking fidelity eta, giving 1 size at eta<=0.084, 2 at <=0.175, **3 at <=0.266 (baseline)**, 4 at <=0.357. Answers the adjustable-vs-sized question as **both on orthogonal axes** - sized on overall scale, adjustable on a sagittal take-up, because cephalic index is a shape axis no number of sizes can address. Reframes the antenna question: inside the Faraday envelope a 300 mm lead is lambda/2500 at 400 kHz and **cannot radiate**; the real mechanisms are near-field mutual inductance (loop area <=2 cm^2) and mutual capacitance (resistive DRL-driven guard). 19 `REQ-NET-` requirements. **Explicitly NOT a cost saving:** nine T1-B -> nine T1-A takes Home Standard 2,286 -> ~2,700 emitters (+18.1 %), inflating `NP-COST-001`'s uncosted term **U** against a configuration already at -41 % to -51 % GM. Four things get worse and are stated: `SH2-DRC-16` may **regress** (the net sits closer to the emitting face than L1's guarded face and loses the L1 DRL guard plane); `RISK-21` **reverts** ("there are no EEG cables to route" becomes false); the retired `EEG-ROUTE-CHANNEL` shell feature returns; and the photoparoxysmal Oz halt plus tES montage presence gates re-home from a discrete keyed-socket check onto a continuous analog precondition on a soft user-placed part, requiring a new Class C presence primitive. `SYM-1`/`CONTIG-1` survive and the 18-cluster tier does **not** shrink. Reopens `NP-HELMET-GEOM-001` §0's abandoned single inner transparent shield (its sole stated ground - electrodes needing galvanic contact - is removed) **without deciding it**. Blast radius measured: 30 files, 102 references, 6 code files. 13 open items; **OI-EEGNET-01 (the EEG-only vs EEG+tES vs tES-deleted fork) is a principal decision that blocks everything else**. Recommends deciding T1-B **before** spending more on REG-1. **Rev 3 (2026-08-20) adds §1.6 (spatial sampling density as an alternative to placement tolerance — absent from the whole document set), §1.7 (electrodes per tile, gated by a socket contact budget that is a FORCE budget, not an area one) and §1.8 (per-configuration tile population is an undecided assumption per OI-COST-01, and the only argument against full population is an emitter-power one that does not reach an electrode; proposes an electrode-only T1-E type). Raises OI-EEGNET-18/19/20/21. Decides nothing; four positions raised into §7.3 and routed to their owning documents.** |
| Naming and Notation Conventions | `docs/np_conv_001.md` | **NP-CONV-001 Rev 2 (2026-08-11)** — **ACTIVE.** **Rev 2 makes document revisions positive integers**, replacing Rev 1's single-uppercase-letter rule: the alphabet had run out (`NP-DHF-001` reached `Rev AA`, which text-sorts before `B`), letters carry no magnitude, `NP-DHF-001` §5.1 was already numbering design briefs 1–4 in a table whose other rows used letters, and `NP-COORD-001` had a third, dotted scheme. Mapping is positional and total (`A`=1 … `AA`=27) and published, so citations in git history stay resolvable. §4.2 records the boundary the conversion did **not** cross — PCB revisions, SHDR database schema revisions and vendor/standards revisions keep letters, each with its reason, established by enumerating every token preceding `Rev <letter>` in the tree. §4.3 forbids revision information in filenames. Raises OI-CONV-03 (NP-COORD-001's dotted scheme mapped, not resolved) and OI-CONV-04 (eleven active `.docx` retain letters internally — their revision text is split across Word runs, so partial conversion was refused). Rev 1: First consolidated statement of NeurOne naming and notation; rules previously lived inline in `NP-HW-HEXTILE-001` §7.4 and `NP-DRV-SHELL-002` §5.1.7, which now point here. **Binding: every active-low NeurOne signal name terminates with `#`** (§1.1) — `ALERT#`, `SEAT#`, and newly `ATTN#`; active-high signals take no suffix, so absence of `#` is meaningful. §1.2 records the five suffixes and one prefix **unavailable** for polarity, each with the evidence that took it: `_N` = cardinality (`PBM_TILE_N`), `_L`/`_R` = Left/Right (`CVNS_ENABLE_L`), `_B` = channel B (`CH_B`, `NP_BANK_B`), `_LOW` = threshold (`NP_TIA_GAIN_LOW`), leading `/` = EDA hierarchical path separator. §1.3 introduces `SIGNAL[n]` index notation, retiring `SAFE_EN_n` → **`SAFE_EN[n]`**. **§2 corrects the firmware mapping from `_L` to `_ACTIVE_LOW`** (the `_L` rule shipped 2026-08-11 and was wrong — `_L` is Left). §3 states the **Class C boundary**: no firmware identifier is renamed, and `NP-FMEA-001` OI-FMEA-01 keeps ownership of the unmarked safety-MCU enable lines. §4–§8 capture document-ID, `§N` section marking, identifier families (OI/DRC/REQ/D/gates, append-only), revision practice (state what was replaced and why; outweighed ≠ refuted), and **verification by mechanical diff rather than review**. **Raises OI-CONV-01** — applying §1.1 exposed an **inverted `SAFE_EN[n]` polarity** between `NP-DRV-SHELL-002` §6 (LOW = disabled) and the safety MCU (`np_safety_config.h:7-8`, LOW = enabled); SH2-DRC-13's "defaults LOW at reset" would flip from safe to *enabled at power-on* under the firmware's convention. Safety-architecture question, not a naming one — unresolved, assess with OI-FMEA-01 / OI-HUB-C07. OI-CONV-02 tracks sweeping the remaining document set. |
| Shell Socket Interconnect Architecture | `docs/np_drv_shell_002.md` | **NP-DRV-SHELL-002 Rev 4 (2026-08-18)** — **§6 and §7.1/§7.1a reconciled to the OI-HUB-C07 decision.** §7.1's Safety-MCU enable GPIO row resolves **18 (20 provisioned) → 1 broadcast Class C line**; §6's layer table gains an explicit IEC 62304 column and splits into four tiers (per-socket software B / per-cluster hardware **B** / whole-cranial-lattice hardware **C** / whole-vault C), with the 18 `SAFE_EN[n]` gates **retained in full on the cluster controllers** — nothing leaves the §3.2 BOM. **§7.1a is ADOPTED**, its condition having been satisfied. **Two things deliberately NOT done:** the 12 → 11 conductor reduction and the connector count are **left to OI-HEXTILE-14** (§7.1a's build-against-20-positions-and-12-conductor-tails instruction stands, now as a baseline rather than a conditional); and `RISK-SHELL-03` — the programme's only CRITICAL risk — is **recorded as basis-changed but not resolved and not re-scored**, since the conflicted line is now a Class B availability gate with a Class C line in series, but a Class B gate defaulting to enabled at reset still energises a rail and the safety claim now rests on the Class C line's own unresolved reset polarity (`OI-CONV-01`, `OI-FMEA-01`, `OI-RISK4-01`). Rev 1's withdrawn *"ample GPIO"* claim is annotated as **moot rather than answered** — 1 GPIO here, ~23 total. New inherited requirement **HUB-REQ-C05** (`NP-HW-HUB-001` §7.2.2): the Class B per-cluster gate must be commanded from a tier above the cluster controller that carries it. **No BOM, network, socket or firmware change.** — Rev 2 (2026-08-11) — **DRAFT, not a tooling baseline** (input lattice is PROVISIONAL pending REG-1/ACT-1). Replaces NP-DRV-SHELL-001 Rev 2's retired 5-zone-slot FPC routing with a **cluster-controller** interconnect for the ~80-socket hex lattice. Five networks split by physics (§3): N1 power (broadside tree, **24 V**), N2 control (segmented I2C), N3 local sense (**≤50 mm, never leaves the cluster — survives**), N4 electrode (sized by channel count 8/21, not socket count), N5 per-cluster hardware safety enable. Key moves: TIA + gain switch relocated from Hub PCB to the cluster boards, which collapses SMART-1's residual (Rev C needs **no** 80× DG2788A, no 80-way analog fan-out, no cascaded muxing); modules lose their FPC tail for a back-face pad array, so **module swap actuates zero flex** (§8.2). Finds ≥15 mm PBM-to-EEG separation **unachievable** in the hex architecture (electrodes now live inside T1-B tiles on L1) and replaces the mechanism while retaining the <5 µVpp threshold (§9.1); prohibits shell/DRL structure as a current return (REQ-EMI-08) and closed-ring bus topology (REQ-EMI-09); prohibits spread-spectrum PWM in favour of deterministic phase-locked traffic with sense-quiet windows (REQ-EMI-03/04). **33-item DRC checklist (SH2-DRC-01–28 + 02a/02b/10a/10b); 11 open items (OI-SHELL2-01 CLOSED, 02/03/04/05 re-scoped, 11 NEW).** Addresses SMART-1, OI-HUB-SOCKET-01 (hardware half) and OI-HEXMAP-02. **Rev B (2026-08-11) replaces five Rev 1 positions, each stated with its predecessor and basis:** (1) the cluster **carrier becomes a cluster controller** — STM32G071 UFQFPN32 + PCA9548A + 16:1 PD mux + shared switched-gain TIA + 8:1 NTC mux/ADC + 24 V power gate (principal direction; NP-HW-HUB-001 Rev 3 §3.1/§8.3 prevails) — with **HUB-001 §3.2's LED-drive rationale explicitly not carried forward** (falsified by NP-HW-HEXTILE-001 **D-3**; OI-HUB-C15 already directs its removal) and the surviving analog-scan justification recorded instead; this **effectively resolves OI-HUB-C17c against D-4** (conservative on the FAI-SM-06 ADC-drift half; C17c's on-tile-dissipation half still open); (2) **N1 rail 12 V → 24 V** per OI-HUB-C17b/D-6, vault bus ~2.9 A → ~1.46 A, **OI-SHELL2-01 CLOSED**, boost siting at OI-HUB-C19; (3) **12 → 18 clusters** under CLUSTER-1 + SYM-1 + CONTIG-1 (provably minimal, NP-HW-HEXTILE-001 §8.2.1), so the Rev 1 `8 × ≤2 = 16` tree and 16 connector positions both fail — **replaced by D-7's 32-segment tree and 20 provisioned positions** (OI-HEXTILE-14, §8.2.2 options 1+3), with option 4's broadcast-enable 11-conductor trunk recorded as **conditional on OI-HUB-C07**; **Hub PCB Rev C interface contract is now 18 connectors × 12 pins, 20 positions provisioned, 216 interface pins** (§7); (4) **bezel 1.0 mm** where it binds (NP-THERM-BEZEL-001 §4.5, the only calculated value); (5) **OI-SHELL2-11 NEW** — 18 continuously-dissipating boards into NP-THERM-CFD-C2-001 §7's stagnant inter-bowl air (0.231 m²K/W, ~59 % of the outward path). **Socket contact count reconciled against NP-HW-HEXTILE-001 §7.1–7.2 (D-5) and CLOSED at 19**: `SEAT_N` adopted, `SYNC`/`DGND` retained with reasons, 2 reserved dropped, N3 retained → 17, then **`VLED+`/`PGND` = 3+3 (principal decision 2026-08-11)** under the rule *loss of any one contact must still leave ≥2× derating* → **19**; **HUB-001 §7.5.2's 14–15 figures voided** (conditional on D-4) and D-5's 16 superseded, so **NP-HW-HEXTILE-001 §7.1–7.2 must be co-revised (OI-SHELL2-09(i), blocks socket tooling)**. New **REQ-SKT-01**: two staggered rows, binding (a 19-contact single row spans 38 mm at 2.00 mm pitch). Plate load *falls* despite two more contacts — 19 on a 6-tile plate (34.2–57.0 N) is below Rev 1's 18 on a 7-tile plate. **33-item DRC** (adds SH2-DRC-05a). BOM $125–216 → **$175–225**. | **OI-HEXTILE-14 closed 2026-08-18:** every cluster-count-dependent figure re-sized off 12 (and off `ceil(n/8)` = 10) to **18**, with **20** connector positions provisioned so a REG-1 re-cut needs no PCB re-spin; `NP-HW-HUB-001` §7.4/§6.3/§5.2/§8.2/§8.5 and HUB-DRC-C02 corrected, HT-DRC-20 now passes. The 12 → 11 conductor reduction that OI-HUB-C07's closure unblocks is **not** taken — HUB-REQ-C05 leaves open how the Class B gate is commanded.
| FAI Zone Module Checklist | `docs/superseded/np_fai_zm_001.docx` | **⚠ SUPERSEDED 2026-08-11 — replaced by `NP-FAI-001` (programme + surviving process qualifications), `NP-ART-001` (artifact readiness) and `NP-FAI-HUB-001` (first delivered checklist).** NP-FAI-ZM-001 Rev 1 — §3d PDMS adhesion (FAI-M01–M03), §3e PDMS thermal cycling qualification (FAI-TC01–TC06, TC02 BLOCKING), §4a accessibility (FAI-A09–A15), §4c IPX4 (FAI-IPX-01–04, IPX-02 BLOCKING), §5 lifecycle, §6 system test, §9 risk cross-reference |
| Engineering Coordination Checklist | `docs/np_coord_001.docx` | NP-COORD-001 Rev 1.9 (2026-06-28) — G1 (16 items), G2 (14 items), G3 (8 items) gate structure. Rev 1.9: fixed 8 detail-table rows with incorrect 5-column format (G1-13, G1-14, G2-10, G2-11, G2-12, G2-14, G3-07, G3-08) — all now 7-column matching headers; fixed G1-06 status placement; abbreviated summary dashboard rows G2-02/G2-13/G2-14; added status color coding key (BLOCKING=red, OPEN=amber, PARTIAL/BASELINED=light amber, CLOSED=green); G1-06 summary shading corrected. Prior status: G1-16 CLOSED; G2-10/11/12 CLOSED; G2-13 OPEN; G2-14 PARTIAL; G3-07/G3-08 SOFTWARE BASELINED (hardware FAI pending T2 prototype). |
| Zone Module Tooling Specification | `docs/superseded/np_tool_zm_001.docx` | **⚠ SUPERSEDED 2026-08-11 — replaced by `NP-TOOL-HEXTILE-001`; six position-unique moulds become one universal tool.** NP-TOOL-ZM-001 Rev 1 — 8 mandatory molded features (F-01 through F-08), critical dimensions, 12-item mold design review checklist, FAI cross-reference |
| Supplier Selection Checklist | `docs/np_proc_sup_001.docx` | NP-PROC-SUP-001 Rev 1 — CAT-A (moulding), CAT-B (CFRP shell), CAT-C (PDMS bonding); SUP-M-07 and SUP-B-01 BLOCKING for RISK-20; SUP-C-08 BLOCKING for RISK-04; §9 RISK-20 tracking table |
| Clinical Trials Strategy | `docs/np_clin_001.docx` | NP-CLIN-001 Rev 1 — all-in-one commercial rationale; Vielight trials scope/limitation; individual modality evidence (9 modalities); multi-modal combination evidence; researcher profiles (9 researchers); 6-trial priority plan; key bibliography |
| fNIRS Feasibility Assessment | `docs/np_feas_fnirs_001.md` | NP-FEAS-FNIRS-001 Rev 1 (2026-07-13) — **EXPLORATORY** competitive-analysis feasibility (Neurode Labs comparison), NOT a §820.30 design record. Assesses adding fNIRS hemodynamic brain monitoring on NeurOne's existing NIR optics (600 PBM LEDs 660/808–830nm + 1064nm smart module; dual PD1/PD2 detectors; DG2788A switchable-gain TIA; i.MX RT1062). Findings: Tier-1 blood-volume/total-Hb "brain-activity visualization" feasible on stock hardware (firmware + cross-zone long-separation detector read); Tier-2 quantitative HbO₂/HbR oximetry NOT clean on the 660+808nm pair (808nm sits on the ~805nm isosbestic point) — needs 660+1064nm or an added ~850nm emitter. Four gating risks: (A) wavelength pair chromophore separation, (B) co-located PD geometry needs cm S-D separation, (C) far-field SNR, (D) hair coupling (largest empirical unknown). Recommends a breath-hold/Valsalva bench on existing modules as kill-early test before any spec; fNIRS raw+hemodynamic series → UHDR. Registered in NP-DHF-001 Rev 20 §5.3 (FEAS category). |
| Additional Modalities | `docs/superseded/np_mod_ext_001.docx` | SUPERSEDED — NP-MOD-EXT-001 Rev 1 — all 6 modalities now individually specified: HRV biofeedback (NP-FW-HRV-001 Rev 1), 1064nm PBM (NP-FW-PBM1064-001 Rev 1), sLORETA HD-tDCS (NP-FW-HD-001 Rev 1), cervical VNS (NP-FW-CVNS-001 Rev 1), 40Hz vibrotactile (CLAUDE.md §3 / accessories-roadmap.md), Apple Watch sync (accessories-roadmap.md + NP-APP-ROADMAP-001 Rev 1); tFUS watch program → pending-decisions.md §13.2b |
| Shell Tooling Specification | `docs/np_tool_shell_001.docx` | **NP-TOOL-SHELL-001 Rev 2 — ⚠ PARTIALLY SUPERSEDED 2026-08-18, releasable for F-04 only.** Opened and read in full for the first time since the hex-tile decision (PRs #223/#224 flagged it by inference and never opened it). **F-01 retired outright** — `NP-HEX-ZM-001` §3.4/§4a/§5.1: no zone slots, no zone identity to colour-code, and sockets moved to the *inner* bowl while F-01 specifies posts on the outer CFRP surface; SH-01–SH-05, SH-23, FAI-CV-01–04 and OI-04 go with it. **F-03's premise retired** — EEG electrodes live inside T1-B tiles and `NP-DRV-SHELL-002` §5.3 absorbs the EEG harness, so the 8×5 mm channel has no cable to route; this merely brings the document into line with `cad/CAD_PARTS_LIST.md`, which already carried EEG-ROUTE-CHANNEL as a RETIRED tombstone, and with `NP-RISK-002`'s RISK-21 disposition. The <5 µVpp threshold survives as SH2-DRC-16. Reopening condition recorded (`NP-HW-EEGNET-001` Rev 2 §7.3 — fallback net option only). **F-02 conflicts with `NP-TOOL-HUB-001` Rev 1 F-02** on the same hub ports: 50 mm tether vs a safety-derived ≤20 mm maximum (fan-ingestion hazard, FAI-HTOOL-02 BLOCKING) — **OI-ART-07**. **F-04 survives unchanged.** Scope also understated — this governs L0 only; L1 has no tooling spec, and the clamp latches, blind-mate boss and parting-plane lip are absent pending MECH-1. Re-scope is **OI-ART-01**. Original content, unchanged and retained: 4 mandatory shell features: F-01 zone slot plug anchor posts (×5, color-coded), F-02 accessory port cover anchor posts (×3), F-03 EEG cable routing channel (8×5mm, outer CFRP surface, DRC-18 cross-ref), F-04 temporal wing anchor boss (×2 bilateral, PROVISIONAL pending HOPE Phase 3). 23-item design review checklist (SH-01–SH-23), critical dimensions, FAI cross-reference. Addresses Issue #16 and §13.4 pending decision. |
| Lens and Goggle Assembly Tooling Specification | `docs/np_tool_lens_001.docx` | NP-TOOL-LENS-001 Rev 2 — 7 features (F-01 through F-07 + P-01 through P-03) across lens rim, goggle arm, shade assembly tooling; sliding rail geometry (F-01/F-02); N42 magnet pockets (F-03/F-04, ≥1mm wall BLOCKING); EC driver contacts (F-05/F-06, hard gold, pogo pin); AgNW coating spec (P-01, replaces ITO); hard coat spec (P-02, 3–5µm silicone); inner PDMS diffuser (P-03, plasma-activated); lens rim guard anchor hooks (F-07) — expanded in Rev 2 with shade clearance ≥4mm (MR-19), tether ≤45mm posterior routing no FOV crossing (MR-20), OI-10 CAD confirmation; 20-item mold design review checklist; 10 open items; FAI cross-reference; risk register cross-reference. Addresses Issue #17 (Rev A) and Issue #18 (Rev B). |
| eMMC Partition Architecture and Storage Encryption | `docs/np_fw_emmc_001.docx` | NP-FW-EMMC-001 Rev 1 (2026-05-11) — 18 sections, 13 tables. eMMC hardware spec (§3); 9-partition GPT layout (§4, 8,192 MiB total, UHDR 6,903 MiB dominant); LittleFS config per partition (§5); UHDR AES-256-XTS + Argon2id key derivation + key lifecycle + session record EDF+ format + HMAC integrity (§6); SHDR AES-256-XTS + HKDF manufacturing key + SNVS OTPMK + file structure (§7); dual-bank OTA bootloader + Ed25519 verification + 9-step OTA sequence + USB-C DFU recovery + Safety MCU firmware update protocol (§8); Config/Calibration partition (§9); Scratch partition zero-on-boot + SANITIZE (§10); boot sequence B-1 through B-9 (§11); 27-element session data classification table UHDR vs SHDR (§12); UHDR nightly incremental backup protocol (§13); write endurance monitoring + predictive maintenance thresholds (§14); research anonymization Scratch workspace (§15); processor ownership table (§16). Closes two docs/status/pending-decisions.md §13.4 pending decisions: eMMC partition architecture + dual-bank OTA bootloader. Addresses Issue #19. |
| HRV Biofeedback Protocol Firmware | `firmware/hrv_biofeedback/` | Issue #21 (2026-05-11) — Six-module static library (np_hrv_ppg, np_hrv_coherence, np_hrv_pacer, np_hrv_tavns_sync, np_hrv_eeg_biofeedback, np_hrv_session). Coherence algorithm: 256-pt Welch PSD on 4 Hz interpolated RR series, LF peak/(LF+HF) × 10. taVNS timing: RSA slope method, 6-interval dRR window, ±5 ms/beat hysteresis, 6 s failsafe, safety MCU veto path. Dual EEG+HRV: 1024-pt EEG Welch bands, frontal alpha/theta ratio, adaptive pacer rate every 30 s. Spec: NP-FW-HRV-001 Rev 1 (docs/np_fw_hrv_001.md). G2-12 CLOSED. |
| Zone Module Bone Conduction Announcement Firmware | `firmware/zone_announce/` | Issue #22 (2026-05-11) — Three-module static library (np_zone_detect, np_zone_audio, np_zone_announce). Implements RISK-15 Layer 5 (five-layer keying). Zone detection: LPADC1 ch 12, 10 kΩ pull-up, 1% resistor ladder (ZM-01=10kΩ through ZM-05=220kΩ), midpoint ADC thresholds. Debounce: 3×100 ms reads, ≥2/3 majority (RISK-18). Audio: 256-entry Q15 sine LUT, DDS phase accumulator, SAI3 I2S at 8 kHz, eDMA ping-pong (64 samples/half). Zone sequences: N beeps (440 Hz A4) + rising confirmation (880 Hz A5); total durations 550–1270 ms. Error cue: descending 200→150 Hz pair. Five parallel slot state machines (IDLE→SETTLING→DEBOUNCING→ANNOUNCING→ACTIVE→REMOVING). Announcement queue serialises simultaneous insertions (ZM-01→05 order). SHDR auth logging: zone_id, auth_result, debounce_reads_passed per insertion. Platform HAL stubs OI-ZA-01–04 (LPADC + SAI3 + eDMA). Spec: NP-FW-ZA-001 Rev 1 (docs/superseded/np_fw_za_001.md). **G2-10 CLOSED.** **⚠ SUPERSEDED 2026-07-28 — ZONE_ID detection retired by np_module_map UID-based auto-inventory; audio/debounce logic reusable, needs re-triggering. See `docs/superseded/np_fw_za_001.md` supersession note.** |
| Dual-Bank OTA Bootloader Firmware | `firmware/bootloader/` | Issue #20 (2026-05-11) — NXP i.MX RT1062 (Cortex-M7) bootloader implementing B-1 through B-9 boot sequence from NP-FW-EMMC-001 Rev 1. Components: np_main.c (IVT, vector table, boot flow), np_boot_selector.c (SNVS_LPGPR0 bank flag + attempt counter), np_emmc.c (USDHC2 eMMC driver, partition switching, write-verify), np_signature.c (Ed25519 verify + SHA-256 + SHA-512 + CRC32, **self-contained — intentionally NOT using np_crypto**; uses `-nostdlib/-nodefaultlibs` which makes Monocypher unavailable), np_ota.c (9-step OTA state machine, streaming SHA-256 over 128 MiB banks, Config partition state record), np_dfu.c (USB DFU 1.1 class, USB1 peripheral, DFU_DNLOAD/MANIFEST with Ed25519 verification before eMMC write). Linker script targets OCRAM 0x20200000 with 48 KiB bootloader limit. CMake build system with arm-none-eabi toolchain. Public key placeholder in np_signature.c must be replaced at secure build time. |
| Vendored FreeRTOS-Kernel (SW-02 RTOS) | `firmware/vendor/freertos/` | FreeRTOS-Kernel **V11.3.0** (FreeRTOS-LTS 202604.00-LTS), vendored 2026-07-11 — Class B SOUP (NP-SW-001 §9.4). Byte-exact subset: `kernel/` (tasks/list/queue/event_groups/timers/stream_buffer + 21 headers), `portable/GCC/ARM_CM7/r0p1/` (shipped target port), `portable/MemMang/heap_4.c` (shipped heap), `portable/ThirdParty/GCC/Posix/` (HOST-TEST-ONLY), MIT `LICENSE.md`, `VERSION` SOUP record. `croutine.c` and all non-CM7 ports excluded. Config + hooks are first-party in `firmware/hub_control/` (`FreeRTOSConfig.h`, `np_hub_freertos_hooks.c`) — not part of this SOUP component. Build: `np_freertos` static lib for Cortex-M7 (cross) + `np_freertos_smoke_tests` (host POSIX port — compiles kernel+config+hooks and runs the scheduler; PASS, wired into ctest + `firmware-host-tests.yml`). On-target link (MCUX SDK + startup + linker script + app target) is separate platform bring-up, still pending. See `firmware/vendor/freertos/README-NEURONE.md`. |
| Vendored ARM CMSIS-Core(M) (SW-01 core access layer) | `firmware/vendor/cmsis_core/` | ARM **CMSIS-Core(M) 5.6.0**, from CMSIS_5 tag **`5.9.0`**, vendored 2026-08-09 — **Class C SOUP** (NP-SW-001 §9.4). Closes half of Defect A in NP-SW-CI-001 §4.1. Byte-exact subset, five headers, **no translation unit**: `Include/{core_cm0plus.h, cmsis_version.h, cmsis_compiler.h, cmsis_gcc.h, mpu_armv7.h}` + Apache-2.0 `LICENSE.txt` + `VERSION` SOUP record (per-file provenance + SHA-256; byte-exactness verified by independent re-download and `cmp`). Present because `stm32g071xx.h:115` includes `core_cm0plus.h` unconditionally — **not** for `SysTick`, which SW-01 never accesses through CMSIS (every `SysTick` string in `firmware/safety_mcu` is a comment or a NeurOne identifier). `mpu_armv7.h` required transitively via `__MPU_PRESENT 1U`, established by deletion rather than by reading the `#if`. Excluded: DSP, NN, RTOS/RTOS2, DAP, Driver, Pack, all other cores, and the non-GCC compiler headers (`cmsis_armcc.h`, `cmsis_armclang*.h`, `cmsis_iccarm.h`, `cmsis_ccs.h`) whose `#elif` branches a `__GNUC__` build never takes. Tag choice: `5.9.0` is the terminal 5.x release, so its anomaly list is closed and will not churn under the Class C record; CMSIS_6 not taken (different major series, not what ST's v1.4.5 device headers are validated against). **IEC 62304 §7.1.2 anomaly evaluation: NP-SOUP-CMSIS-001 Rev 1 §3.** No `CMakeLists.txt` — headers only, added as a `SYSTEM` include dir by `firmware/safety_mcu/CMakeLists.txt` so `-Wall -Wextra -Werror` stays in force on first-party code. See `firmware/vendor/cmsis_core/README-NEURONE.md`. |
| Vendored ST CMSIS-Device STM32G0 (SW-01 device headers) | `firmware/vendor/cmsis_device_g0/` | STMicroelectronics **CMSIS-Device STM32G0 `v1.4.5`** (27-February-2026), vendored 2026-08-09 — **Class C SOUP** (NP-SW-001 §9.4). Supplies `GPIOA`/`GPIOB` and the STM32G071 peripheral map; closes Defect A in NP-SW-CI-001 §4.1. Byte-exact subset, three headers, **no translation unit**: `Include/{stm32g0xx.h, stm32g071xx.h, system_stm32g0xx.h}` + Apache-2.0 `LICENSE.md` + `VERSION` SOUP record (per-file provenance + SHA-256; verified by independent re-download and `cmp`). **Resolves OI-SWCI-06 as device headers only — the ST HAL and LL drivers are deliberately NOT vendored:** SW-01 makes zero `HAL_*` and zero `LL_*` calls (word-boundary matched; a naive `LL_` substring search yields only `NP_SAFETY_EN_ALL_MASK` and `NP_THERMAL_POLL_MS`), and the HAL would add tens of thousands of lines of third-party *logic* to Class C SOUP surface for no benefit. `USE_HAL_DRIVER` consequently removed from `firmware/safety_mcu/CMakeLists.txt` — `stm32g0xx.h:229` includes `stm32g0xx_hal.h` under it, so it had become a hard build break the moment a device header existed in-tree. `system_stm32g0xx.c` excluded: nothing calls `SystemInit`, read from `startup_stm32g071xx.s` (whose header comment wrongly claims it does — OI-SWCI-19). Licence read from `LICENSE.md` at the tag, not from an SPDX identifier (the ST headers carry none), and it is a property of the pinned tag. **IEC 62304 §7.1.2 anomaly evaluation: NP-SOUP-CMSIS-001 Rev 1 §4** — ST publishes "Known Limitations: None" for v1.4.5; the one plausibly-relevant v1.4.5 correction (`TIMx_CCR5`) is a bit-mask SW-01 does not reference, with a v1.4.4→v1.4.5 struct diff confirming no peripheral layout moved. See `firmware/vendor/cmsis_device_g0/README-NEURONE.md`. |
| CMSIS SOUP Anomaly Evaluation (SW-01, Class C) | `docs/np_soup_cmsis_001.md` | **NP-SOUP-CMSIS-001 Rev 1 (2026-08-09, DRAFT)** — the IEC 62304 §7.1.2 anomaly-list evaluation for the two CMSIS SOUP components above. The **first** such evaluation performed for Class C software at NeurOne (FreeRTOS is Class B; Monocypher was handled as B + C on a far smaller surface). Records the method (vendor release notes for the exact tag, upstream issue tracker filtered to the vendored files, and a v1.4.4→v1.4.5 peripheral-struct differential), the per-item hazard assessment, and — deliberately — the limitations: a keyword-driven issue search is not exhaustive, and ARM publishes no numbered errata for CMSIS-Core. Principal finding: the dominant result is a *scoping* one — CMSIS_5 carried 188 open issues at review, essentially all in components not vendored (DSP/NN/RTOS/DAP/SVDConv/Pack), which is the return on the narrow-vendoring decision in NP-SW-CI-001 §9.4. Conclusion for both components: no anomaly in the vendored subset can contribute to a hazardous situation; the enable-GPIO path depends on these components only for two peripheral base addresses, and those were verified on the compiled artifact (`0x50000000`/`0x50000400`) rather than trusted. Evaluation is bound to the pinned tags — moving either tag voids it and requires a new revision. Review cadence: on any tag change, and at G2. |
| Shared Cryptographic Primitives Library | `firmware/crypto/` | OI-SW01-M07-02 CLOSED (2026-06-11) — `np_crypto` static library providing `np_ed25519_verify`, `np_sha256`, `np_sha512`, `np_crc32`. Ed25519 backed by **Monocypher 4.0.2** optional SHA-512 module (RFC 8032 §5.1.7; vendored in `firmware/crypto/vendor/monocypher/`). SOUP record: `firmware/crypto/vendor/monocypher/VERSION` (version 4.0.2, BSD-2-Clause OR CC0-1.0, IEC 62304 §8.1.2). Small-subgroup guard: all-zero pubkey rejected before `crypto_ed25519_check` call (prevents low-order degenerate verification). Build: no CPU flags in library CMakeLists — consuming project supplies them (multi-target: Cortex-M0+, Cortex-M7, x86/arm64 host). Monocypher SOUP sources compiled with `-w`; first-party `np_crypto.c` compiled with `-Wall -Wextra`; `C_EXTENSIONS OFF` enforced on all targets via CMake property. Host tests: 11/11 pass (SHA-256 TV1/TV2, SHA-512 TV1/TV2, CRC32, Ed25519 RFC 8032 TV1/TV2, Ed25519 invalid sig/key/length, all-zero pubkey rejection). IEC 62304 Class C (consumed by SW-01 Safety MCU) + Class B (consumed by SW-02 Main processor bootloader path). |
| sLORETA-guided HD-tDCS Firmware | `docs/np_fw_hd_001.md` + `firmware/sloreta_hdtdcs/` | NP-FW-HD-001 Rev 1 (2026-05-11) — Issue #23, G3-07 software baselined. Scalar sLORETA: 2447 voxels × 21 channels weight matrix (≈205KB, LPSDR4), 64-epoch 21×21 covariance accumulation, per-voxel quadratic form P[v] = W_v^T C W_v. Electrode mapping: Jurcak 2007 MNI scalp atlas for 21-ch T2 cap; nearest-electrode center anode; 4×1 ring cathodes with ≥2 quadrant angular spread. Current: charge-balanced (anode I, each cathode −I/4). 30s ramp up/down, 100ms tick. Safety MCU SPI for impedance check + enable; software charge density monitor (95% of 40µC/cm² trips abort). Concurrent EEG: 200µs blanking, ≥20dB SNR spec. 7 clinical targets (DLPFC_L/R, VLPFC_L, ACC, MPFC, M1_L/R); bilateral 4×1 and 2-electrode fallback. FAI-HD01 (phantom 15mm accuracy), FAI-HD02 (software PASS all 6 criteria), FAI-HD03 (FWHM ≤25mm phantom), FAI-HD04 (SNR ≥20dB bench). Hardware FAI BLOCKING for full G3-07 closure. BOM delta $0. UHDR: target MNI, montage, current, charge, impedance. SHDR: device metrics only. |
| Cervical VNS Safety Interlock Firmware | `docs/np_fw_cvns_001.md` + `firmware/cervical_vns/` | NP-FW-CVNS-001 Rev 3 (2026-08-12) — Issue #24, G3-08 software baselined. Safety MCU (STM32G071) owns CVNS_ENABLE_L/R GPIO; monitors R-peak via RPEAK_IN edge GPIO; TIM6 ISR at 200Hz (5ms); HR change >15 BPM within 5s observation window → GPIO LOW within <5.1ms worst-case (spec: <100ms). Baseline cross-validation: main processor PPG-derived HR vs safety MCU GPIO-timer-derived HR must agree within 5 BPM before enable granted. Re-enable requires 30s lockout + explicit app confirmation + repeat impedance check + baseline re-established. Three firmware modules: np_cvns_interlock (Pan-Tompkins R-peak detection, RR buffer, baseline, SPI exchange); np_cvns_stim (biphasic charge-balanced, 10s ramp-up, 5s ramp-down, 100ms tick); np_cvns_session (session orchestration, UHDR/SHDR record). FAI-CV01 (electrode placement, phantom bench), FAI-CV02 (cardiac interlock <100ms, oscilloscope bench — software constants PASS), FAI-CV03 (3 healthy adults, IRB required). Hardware FAI BLOCKING for T2 clinical release. RISK-25 MITIGATED. UHDR: RR series, baseline HR, HR at cutoff, **time-of-cutoff offset**, timestamps, impedance raw. SHDR: cutoff flag only (no HR values), impedance pass/fail, fault log (session ordinal + reason only — **no HR and no event timing**). **Rev 3 (2026-08-12):** §8.2 had classified the time-of-cutoff offset UHDR while simultaneously routing it to SHDR inside §5.6's fault-log struct as `cutoff_offset_ms` at finer (ms) resolution than the UHDR original; resolved to UHDR — for a cardiac cutoff that offset is the wearer's autonomic response latency, a measurement of the person. Field removed from `np_cvns_fault_log_entry_t` (12 → 8 bytes), unconditionally, so no per-reason redaction pattern can leak the reason. Struct had no producer or consumer. Element added to NP-FW-EMMC-001 Rev 2 §12, which had been silent on it. BOM delta +$35–55 (cervical accessory module). |
| Cervical VNS 510(k) Pre-Submission | `docs/np_reg_cvns_001.md` | NP-REG-CVNS-001 Rev 1 (2026-05-11) — Issue #24. Q-Sub package for FDA CDRH DNPMD. Predicate: electroCore gammaCore K163334 (cluster headache, 2016) + K173323 (migraine, 2017). Proposed SE argument: same intended use (cluster headache + migraine) + same or safer technology (biphasic charge-balanced, ≤2 mA vs predicate ≤24 mA, 25 Hz within 1–25 Hz range). Key differences mitigated: lower current (more conservative); bilateral option (literature review + bench at ≤2 mA); cardiac interlock (safety enhancement). 6 Q-Sub questions for FDA. Proposed Type B meeting at Month 20 from T1 launch. Standards: IEC 60601-1, IEC 60601-2-10, IEC 62471, IEC 62304 Class C (safety MCU) + Class B (main), IEC 62366-1, 21 CFR 820. Clinical evidence: FAI-CV03 tolerability (3 adults) + predicate program + literature review. Expanded indications (depression, PTSD, post-stroke) planned for subsequent submissions via T2 seeding studies. |
| 1064nm Smart Zone Module Firmware | `docs/np_fw_pbm1064_001.md` + `firmware/pbm_1064nm/` | NP-FW-PBM1064-001 **Rev B (2026-07-27)** — adds §6.6 Factory Calibration Procedure (integrating sphere @ 1064nm/808nm/660nm → `K_PD1`/`K_PD2`/`K_ratio_nom` per zone per wavelength written to Config partition at manufacture, `cal_source = NP_CAL_FACTORY`); closes OI-PBM-04 (procedure spec only — FAI-SM-06/07/08 hardware bench execution still pending). Rev 1 (2026-05-12) — Issues #53, #54, #55, #56. Smart module detection (3.3kΩ ZONE_ID, ADC < 1100 counts, 3×100ms debounce RISK-18). I2C bus: LPI2C3, 400kHz fast mode, per-slot independent buses, ATtiny402 address 0x30. Three-channel LED control (CH_A=660nm, CH_B=808nm, CH_C=1064nm): registers 0x00–0x0D (STATUS, CH_ENABLE, CUR, PWM_FREQ, DUTY, THERMAL, FAULT_LATCH, CONFIG). Duty ceiling 0x32 (25%) enforced unconditionally. InGaAs PD (Hamamatsu G12180-010A) dose-metering: K_PD1, K_PD2, K_ratio_nom per zone per wavelength from Config partition or firmware defaults (default values: 660nm K_PD1=0.120, 808nm K_PD1=0.105, 1064nm K_PD1=0.088). PD1/PD2 ratio: fouling vs aging disambiguation (RISK-14 Option B). Aggregate irradiance ceiling 600 mW/cm² three-channel combined (OI-PBM-05). Session dose limits: 60 J/cm² for 660/808nm, 36 J/cm² for 1064nm. T2 combined session: np_pbm1064_t2_combined coordinates 1064nm zone modules + 1170nm laser; thermal throttle priority 1170nm first → CH_C → CH_B. FAI status: SM-01,-02,-03,-05,-09,-10,-11 = Software PASS (PR #57); SM-04,-06,-07,-08 = Hardware bench PENDING (require calibrated 1064nm optical bench + hub PCB Rev B with TIA gain switch OI-PBM-HW-01). 8 HAL stubs OI-PBM-01 through OI-PBM-08. **⚠ SUPERSEDED 2026-07-28 — per-slot addressing (5 zones × 3 wavelengths coefficient indexing, 5 LPI2C3 buses) retired; register-map and calibration-model concepts remain relevant. See `docs/np_fw_pbm1064_001.md` supersession note.** |
| 1064nm Multi-Wavelength Session Protocol | `docs/np_ses_1064_001.md` | NP-SES-1064-001 Rev 1 (2026-05-12) — Issues #53, #55, #56. Session descriptor v4 with smart_module_mask, per-zone np_pbm1064_preset_t, t2_combined_enable, Ed25519 signature extension. T1 three-wavelength session lifecycle (preflight, ramp, steady, ramp-down, UHDR/SHDR). EEG-adaptive frequency: uniform mode (default) and cortical gradient mode (experimental — Research mode label, OI-SES-02). T2 combined 1064+1170nm session: sLORETA coordination for depression protocol (§4.4, requires T2 prototype). Dose limits enforced per channel per zone. UHDR: irradiance time series, dose, NTC, throttle events, EEG-adaptive events, 1170nm dose, sLORETA MNI coordinate. SHDR: PD1/PD2 ratio final, fouling/aging flags, driver fault count, peak NTC (no timestamp), cal source. App display requirements: per-zone per-wavelength irradiance + dose bars, depth tier illustration, module management screen. Clinical evidence: Yao et al. Science Advances 2022 (UT Dallas/Harvard, right prefrontal 1064nm, working memory CDA). 5 open items OI-SES-01 through OI-SES-05. **⚠ SUPERSEDED 2026-07-28 — session descriptor v4 hardcodes zone[5]/8-bit smart_module_mask, retired by the ~30–80-socket hex lattice. See `docs/np_ses_1064_001.md` supersession note.** |
| 1064nm PBM Clinical Evidence Bibliography Addendum | `docs/np_bib_1064_001.md` | NP-BIB-1064-001 Rev 1 (2026-05-13) — Issue #56. Addendum to existing 33-entry bibliography. Six entries: Yao et al. Science Advances 2022 (primary — n=20 RCT, right PFC 1064nm, CDA EEG working memory improvement); UT Dallas follow-on search protocol (open item); tissue optics penetration depth refs (Bashkatov 2005, Yaroslavsky 2002, Strangman 2002 — biophysical basis for depth-tier claim); CCO mechanism at 1064nm (Rojas & Gonzalez-Lima 2013); safety literature (Hamblin 2016); multi-modal combination evidence gap noted. FTC substantiation basis for "deeper than 810nm" and working memory claims. Both this document AND NP-REG-PBM1064-001 Rev 1 opinion letter required before any 1064nm marketing claim is lifted. |
| Hexagonal Standardized Zone-Module Design Brief | `docs/np_hex_zm_001.md` | NP-HEX-ZM-001 **Rev B (2026-08-04)** — **DESIGN STUDY, not a locked tooling baseline**; gated by the curvature-scan go/no-go (§7) and REG-1 10-20 registration. Replaces position-unique zone modules (a distinct SKU per helmet pin location) with a **single universal 40 mm hexagonal module SKU** tiling the helmet interior — one mould, one part number, one inventory line. Option A (rigid, median-curved) committed as the baseline; Option B (semi-flex tile + shell-socket-defined curvature) recorded as a future module swap into the unchanged socket interface, no shell re-tool. **Principal content:** socket lattice geometry (§3 — `ROW_WIDTHS = [3,6,7,8,9,8,9,8,7,6,5,4]`, 80 sockets over 12 coronal rows, six midline sockets {2,13,29,46,62,74} on the odd-width rows, single point of truth `scripts/sync-socket-map.ts`); module-type taxonomy and **SMART-1** (§4a — every socket I2C- and TIA-capable, so any tile type inserts in any socket, identity by UID self-report not mechanical keying); addressing and the v2 wire format (§4/§4b); the two-nested-bowl shell and EMF seam (§5); and the **module cluster clamps** (§5.4a — one over-center lever-throw actuator per cluster driving a plate of per-module spring plungers, replacing 30–42 per-module levers, carrying the RISK-22 ≤1 N one-handed accessibility intent). **This document is the home of the three principal directions that govern the cluster partition:** **CLUSTER-1** (2026-07-30 — the 7-hex flower is the cluster unit wherever the lattice allows one, partial flower where it does not; justified on clamp-plate stress and stored curvature, both of which scale off the cluster's longest dimension); **SYM-1** (2026-08-04 — the partition is mirror-symmetric about the sagittal midline, which forces six midline-centred clusters and yields **18 clusters, provably minimal**, against 12 unconstrained); and **CONTIG-1** (2026-08-04 — petals must form a contiguous arc, no pendant petal, because the clamp plate cannot bridge a gap owned by an independently-actuated neighbouring cluster and would need a 40 mm cantilever arm necked to ≤23.09 mm). Parent document of **NP-HW-HEXTILE-001** (electrical/FPC) and **NP-DRV-SHELL-002** (interconnect). **Rev B (2026-08-04)** additionally: brought the front matter to `docs/FRONT_MATTER_TEMPLATES.md` and added a revision history (Rev A had the revision in its title and lacked eight required fields; CLUSTER-1 had been inserted 2026-07-30 with no revision bump); **registered the document into the DHF index (§5.2) and this register for the first time** — it had been the unregistered parent of two indexed specifications; annotated §5.4a's origin figures ("30 tiles ≈ 4 clusters", "4–10 cluster clamps") as **retired-30-socket-lattice values that do not rescale**, the error NP-HW-HEXTILE-001 Rev 1 propagated; and reconciled §4a's `ceil(n/8)` cluster-board cost model as a **capacity floor rather than a board count** (board count = cluster count; tier BOM **$114.12** at n=80, not $63.40), with the §5.4a MECH-2 table marked pre-SYM-1 and "do not size hardware off this table". Peer documents deliberately not modified — stale counts routed to their own revisions via OI-HEXTILE-14. |
| Hex-Tile Module Electrical / FPC Specification (T1-A, T1-C) | `docs/np_hw_hextile_001.md` | NP-HW-HEXTILE-001 **Rev 8 (2026-08-21)** — **§9's concurrency ceiling measured against the authored protocol library; `OI-HEXTILE-09` confirmed as an actual gap.** `NP-SES-PWR-001` audited `protocols/predefined/`: **2 of 20 protocols fit the 40 W emitter budget as authored, 17 exceed it by 1.25×–40×, 1 is operator-scoped — and all 20 compile clean**, because `hubCompiler.ts` has no power check of any kind. **§9.2's "~6 tiles" is corrected to a range:** it assumes 6.25 W/tile and **no authored protocol runs there**; real per-tile draw spans **1.3–20.0 W**, so concurrency spans **2–32 tiles**. A tile-count governor would permit 6 CW tiles (120 W, 2.7× R-10) while forbidding 32 low-intensity ones that fit — `NP-PWR-BUDGET-001` D-4 confirmed against data. **The binding constraint is CW, not the lattice.** `OI-HEXTILE-09` gains the measured result and a new blocker (`OI-SESPWR-03`). No decision, derivation, count or interface changed. — Rev 7 (2026-08-21) — **Four decision inputs recorded against existing open items; two new open items raised. No decision, count, derivation or interface changes.** `NP-PWR-BUDGET-001` Rev 2 ran §9's arithmetic in directions this document had not. **`OI-HEXTILE-06`** gains three inputs, two pushing *against* de-population: ***populated* is not *driven*** (a populated socket costs 2 mA / 25 mW standby plus BOM, not 25 W); **options 2/3 trade away the dual-PD dose metering**, which is the structural answer to the −79 % declared-vs-measured gap documented in this product category (new Rev 7 paragraph at §6.4); and **§3.6 of the power study wants *more* sockets populated**, since whole-vault illumination at ~30 W is a capability six tiles cannot produce. **`OI-HEXTILE-09` is scoped: the governor must be denominated in watts against the negotiated PD contract, never a tile count** — a count rule forbids the 80-tile/30 W case *and* permits 6 tiles at full drive (150 W, 3× R-10); `NP-DRV-SHELL-002` SH2-DRC-02b's *"global governor present"* needs the unit. **§9.3 gains a fourth consequence:** its *"placement options, not capability"* is **too strong** — but the affordable whole-vault mode delivers 7.2 J/cm² in 20 min, **below** the efficacy threshold, so it is coverage, not dose. **New `OI-HEXTILE-20`: §8.1's 25.0 W/tile peak may not be a legal operating point** — both T1-A channels at full drive is **806 mW/cm² against R-5's 600 mW/cm²**; if R-5 binds, the true peak is ~18.6 W and the `VLED` contact count (D-5, **tooling-blocking**) is derived from the wrong number. **New `OI-HEXTILE-21`: the 1064 nm channel reaches 28 mW/cm² against its Grade A cognitive protocol's 0.25 W/cm² — 9× short**, an η_wp ≈ 4.8 % emitter wall that more watts and more sites cannot close; split from `OI-HEXTILE-03`, which framed it as session length. — Rev 6 (2026-08-18) — **`OI-HEXTILE-14` CLOSED**; §8.2.2's options 1 + 3 (20 connector positions, D-7's 32-segment tree) applied across every peer, `NP-HW-HUB-001` Rev 6 bringing the last into line at **18** clusters. Option 4 unblocked but deliberately deferred pending **HUB-REQ-C05**. Status only; no derivation changed. — Rev 5 (2026-08-16) — **§8.4.1 ACCEPTED by safety review; OI-HEXTILE-13 CLOSED.** Per-cluster safety *policy* is decided against (`NP-HW-HUB-001` Rev 4 §7.2.1). **D-8's policy-bit count resolves to 1**; its **18 switches are retained** and are now explicitly IEC 62304 **Class B**. §8.4's three findings dispose as: finding 1 (package unspecified) **unblocked** rather than closed — demand ~40 → **~23 I/O**, selection moves to `NP-HW-HUB-001` **OI-HUB-C20**; finding 2 superseded; finding 4 (the word *can* be widened cheaply) correct but **deliberately not exercised** — the enable word is unchanged, so **§8.4.2's re-layout is not taken and its time-box is moot**. §8.4.1 gains a *How the falsifier resolved* subsection recording that it could not be produced and that the absence is **structural**: hazard extents in this tree are the tile, the modality, or the whole device — never the cluster — so a per-cluster Class C control would match no hazard's extent. §8.2.2 **option 4's condition is now satisfied** (broadcast `SAFE_EN[n]` unlocks the 11-conductor multi-drop trunk) but **the count is explicitly left to OI-HEXTILE-14** and `NP-DRV-SHELL-002` §7.1a's build-against-20-positions-and-12-conductors instruction stands. HT-DRC-13 and HT-DRC-19 updated. New requirement inherited: **HUB-REQ-C05**. **No firmware change, no hardware interface change; nothing in §4, §5, §6, §7 or §9 is affected.** — Rev C (2026-08-11) — **Rev C re-cuts the socket interface (§7.1–7.2, §7.3, §8.1) from 16 to 19 positions**, closing `NP-DRV-SHELL-002` OI-SHELL2-09(i), which had flagged the 16-position count as **blocking socket tooling**. Two independent causes: **OI-HUB-C17c resolved against D-4** (principal, 2026-08-11 — the TIA + ADC stay on the cluster controller, so network **N3 survives** and `PD1_K`/`PD2_K`/`NTC` return to the socket), plus `SYNC` and `DGND` added for REQ-EMI-03 (deterministic phase reference — also closes the ask in OI-HUB-C05) and REQ-EMI-07 (`PGND` must be LED return only for the broadside pair to cancel). Against those five, **`VLED`/`PGND` drops 4+4 → 3+3** by principal decision under the rule *loss of any one contact must still leave ≥2× derating* (0.35 A nominal / 0.52 A degraded vs a ≥1.0 A rating). **Pad array becomes two staggered rows** (~18 mm, inside the 20.0 mm inradius) — forced, since 19 at 2.00 mm pitch spans 38 mm in one row and fits a 40 mm hex only along the vertex diagonal. **§7.2 now agrees pin for pin with `NP-DRV-SHELL-002` §5.1.4**; new **HT-DRC-22/23** keep them in step. D-4's *reasoning* is explicitly not withdrawn — it was outweighed, not refuted — and **§5.3/§6 still read as though D-4 holds (OI-HEXTILE-15, module BOM, not tooling-blocking)**; **OI-HEXTILE-16 (raised and CLOSED 2026-08-11)** settled two conductors that carried two names each: pin 18 is **`ELEC_SHLD`** (over `GUARD`, which collides with the DRL-driven *guard plane* on L1 that `NP-DRV-SHELL-002` §3.5/§4.1/REQ-EMI-02 specify) and pin 12 is **`ALERT#`** (over `/ALERT`: a leading `/` is the EDA hierarchical-path separator, so the net can be silently re-scoped on import) — propagated through HEXTILE §7.4, `NP-DRV-SHELL-002` §5.1.7 and `NP-HW-HUB-001`; **OI-HEXTILE-17 also raised and CLOSED 2026-08-11**, and the first residual proved not to be cosmetic: **`SEAT_N` → `SEAT#`**, because `_N` already means *cardinality* in this codebase (`PBM_TILE_N` / `EEG_TILE_N` are `sizeof(x)/sizeof(x[0])`), so `SEAT_N` read as "number of seats" — which settles the convention **toward** `#` and confirms `ALERT#` rather than reversing it (SMBus `SMBALERT#` precedent). **`ELEC_SIG` → `ELEC`** on ownership (the two pin tables define the interface; HUB-001 §7.4 adopts it) and accuracy (the pin carries tES **stimulation current**, so `_SIG` misdescribes it). **Rule of record: active-low interface signals take `#`, never `_N`, never a leading `/`; doc→firmware mapping is `<SIGNAL>#` → `<SIGNAL>_L`, never `_N`.** Related, NOT closed: `NP-FMEA-001` OI-FMEA-01 (firmware's ten active-LOW enables carry polarity only in comments). No naming residual remains in this document set. Nothing in §4, §8.2 or §9 is affected. **Rev B (2026-08-04) — **DESIGN STUDY, not a tooling baseline.** **Rev B is a cluster-count correction, documentation only, no firmware change.** Rev 1 §8.2 carried NP-HEX-ZM-001 §5.4a's "4–10 clusters" across to the ~80-socket lattice, but 4–10 was that document's figure for the **retired 30-socket** lattice; 4 clusters at n=80 implies 20 tiles/cluster, exceeding both CLUSTER-1's 7-tile unit and §8.2's own 10–19-modules-per-segment capacitance limit. Rev 2 derives the count from the lattice (`ROW_WIDTHS` in `scripts/sync-socket-map.ts`) under the two standing principal decisions — **CLUSTER-1** (7-hex flower) and **SYM-1** (2026-08-04, mirror-symmetric partition) — and obtains **18 clusters, provably minimal**: a cluster holding a midline socket must equal its own mirror image, so its centre is on the midline; only odd-width rows carry one (sockets {2,13,29,46,62,74}), so **6 midline clusters are forced** and absorb exactly 30 sockets, leaving two mirror-image lateral bands of 25 whose exhaustive minimum cover is 6 each → 6 + 2×6 = 18. **SYM-1 costs 6 clusters and ~$38 over the unconstrained 12**, accepted because the cluster is simultaneously the D-8 safety-cut domain and the D-7 I2C segment, so an asymmetric partition would make a safety cut asymmetric across hemispheres and split the inclusive-midline zone-membership rule across two bus segments. Propagated: **D-8 VLED high-side switches 4–10 → 18**; I2C pull-ups 20 → **36**; segments used **18 of 32** (56 %, so D-7's one-tier PCA9548A muxing over LPI2C1–4 is confirmed unaffected); cluster-tier BOM $63.40 → **$114.12**. Two new open items raised rather than absorbed: **OI-HEXTILE-13** — the safety-MCU GPIO budget does **not** demonstrably close at 18 (the STM32G071 **package is unspecified anywhere in the tree**, demand is ~40 I/O which excludes every ≤32-pin option; NP-HW-HUB-001 Rev 3 §7.2 and NP-DRV-SHELL-002 §6 specify **incompatible enable architectures** differing by 17 GPIO — one `NP_SAFETY_EN_PBM_CRANIAL` bit with an all-or-nothing cranial cut vs per-cluster `SAFE_EN_n`; and `np_safety_config.h` provisionally double-assigns PA4 to SPI1 NSS and `NP_EN_PBM_ZONE4_PIN`) — **with a proposed resolution recorded at §8.4.1 (PROPOSED, safety review arbitrates): split the enable by IEC 62304 class**, retaining per-cluster gates under Class B for availability with a single Class C `NP_SAFETY_EN_PBM_CRANIAL` in series as the hard interlock, which preserves R-11, keeps the topological socket→cluster map outside the certified boundary, and drops safety-MCU demand from ~40 to ~23 I/O — and **OI-HEXTILE-14** — peer documents still sized off 10 or 12 (NP-DRV-SHELL-002 §7.1 provisions 12 connectors/16 positions, which **18 exceeds**, and its `8 branches × ≤2 clusters = 16` tree cannot reach 18; NP-HW-HUB-001 §6.3 sizes DG2788A at 10). **Interconnect options and a recommendation are recorded at §8.2.2 (PROPOSED, not decided):** two independent limits bind — **C1** the 16 provisioned cluster-tail connector positions, and **C2** the `8 branches × <=2 clusters = 16` I2C tree. **C2 does not exist under NP-HW-HEXTILE-001's own D-7** (4 x LPI2C x PCA9548A = 32 segments, 18 used, 14 spare), so it resolves alongside OI-HUB-C17, which NP-HW-HUB-001 §7 already recommends deciding in D-7's favour; **C1 does not self-resolve and must be fixed before Rev 3 layout**. Recommended: adopt D-7's tree and provision **20** connector positions rather than 18 (adding tails is the cheap axis — SHELL-002 §7.3 notes adding a *conductor* costs 16 hub pins — and 20 absorbs a REG-1 re-cut without a second re-spin); and **if the §8.4.1 single-cranial-enable proposal survives safety review, remove `SAFE_EN_n` from the 12-conductor tail** — it is the only star component (N1 power is already a broadside tree, N2 control already a two-level tree), so a broadcast enable permits a multi-drop trunk and makes connector count insensitive to future cluster-count changes. Four further options assessed and not recommended: raise branch fan-out to 8 x 3 = 24 (no new silicon, but only relevant if the cluster-MCU tree survives); decouple electrical from mechanical clusters via partial socket population (legitimate, but trades D-7's one-physical-grouping principle for connector count); re-cut the lattice to <=16 clusters (**not available without breaking a principal decision** — the 6 midline clusters are forced by SYM-1 and 18 is provably minimal at n=80); and pairing clusters onto shared tails (breaks the single-FRU property and forces an asymmetric pairing under a symmetric partition). **Corrections applied in the same revision, after review of NP-HW-HUB-001 §7.4 and the Class C wire format:** (i) the claim that NP-HW-HUB-001 §7.2 and NP-DRV-SHELL-002 §6 specify **incompatible** enable architectures is **withdrawn** — §7.4 explicitly reconciles them as *"12–16 physical enable lines fanned out from one policy bit"*, and per-cluster physical gates exist in both designs; the genuinely open question is whether per-cluster **policy** (independently commanded cluster bits) is wanted, which §7.4 already routes to **OI-HUB-C07**, so OI-HEXTILE-13 is a cross-reference to that question rather than a new conflict. (ii) The claim that 27 enable bits (18 cluster + 9 modality) **cannot** fit the 16-bit Class C enable word is downgraded from a ceiling to a **cost**: per **standing principal instruction (2026-08-04) no SHDR fault records have been generated, and none are to be assumed until stated otherwise**, so §7.2's "bits 1–4 reserved, not reused" rule does not bind and the word can be widened to `uint32_t` pre-production with no migration and no ambiguous historical logs. Recorded as **§8.4.2**, which is explicitly **time-boxed** — the window closes at the first fleet fault record, after which bit positions become permanently load-bearing for analytics; collapsing the five zone bits to one cranial bit and re-laying out the word with no reserved holes is free **now** and worth taking regardless of how OI-HUB-C07 resolves. The §8.4.1 recommendation is unchanged but now rests on a single sufficient argument — per-cluster policy puts a **topological** socket→cluster map behind a Class C certification boundary, so a MECH-2 or REG-1 change becomes a recertification rather than a regenerated table. (iii) The **PA4** double-assignment (SPI1 NSS vs `NP_EN_PBM_ZONE4_PIN`) is **re-homed** out of OI-HEXTILE-13: `NP_SAFETY_EN_PBM_ZONE_0..4` encode the **retired 5-module-slot** meaning of "zone", whereas a zone is now *"a named SET OF MODULES, defined as a list of socket addresses"* authored in `protocols/predefined/00-zones.npps`, user-extensible and **overlapping** (the inclusive-midline rule puts every midline socket in BOTH hemisphere zones) — so a zone **can never be a hardware enable domain**, clusters partition the lattice and zones do not. The macros are structurally dead rather than miscounted, NP-HW-HUB-001 §7.2 already mandates their removal, and that deletion takes PA4's second owner with it. Cross-referenced from **NP-FMEA-001 FMEA-M08-04**, which already covers this hazard class with a design-review control that has not yet executed. Two siblings flagged for the same cleanup: `NP_NTC_CHANNEL_COUNT 6 /* 5 zones + 1 hub */` and FMEA-M04's "each PBM zone (5 zones)". **No firmware changed.** **NP-FMEA-001 Rev 3 → D in the same change set:** the retired "zone" concept was flagged across SW01-M04 and SW01-M01. §3.4 (thermal interlock) is written against the retired **5-PBM-slot** architecture — a zone is now an overlapping, user-authored set of sockets (`00-zones.npps`) and **cannot be a thermal or enable domain**; the thermal domain is the tile (~80), the aggregation boundary the cluster (18), the per-tile 62 °C throttle is on-module Class B, and module faults arrive by per-cluster wire-OR `ALERT#`. **FMEA-M04-01's mitigation is marked INVALID** (bounds a zone index 0–4, unit-tests "all 5 zone indices"); FMEA-M01-03 flagged for re-check on any enable-word re-layout (bit position ≡ charge-monitor channel index); **FMEA-M08-04 gains the PA4 double-assignment as a recorded open instance** of its own hazard class, whose design-review control has not yet executed; `NP_NTC_CHANNEL_COUNT 6 /* 5 zones + 1 hub */` flagged as the retired count. **OI-FMEA-06 raised.** **No risk scores changed and no failure modes added or removed** — re-scoring the new architecture is hazard analysis, not editorial correction, so §3.4's scores are frozen and marked not-to-be-relied-on pending OI-FMEA-06. **Corrected on inspection of the wire format (same revision):** the active heartbeat `np_safety_rx_ext_frame_t` has **zero spare bytes** — all 38 are allocated (magic 2, session_status 1, enable_lo/hi 2, channel_count 1, checksum 2, `current_ua[14]` 28, ext_checksum 2); the single reserved byte cited in an earlier draft belongs to `np_safety_rx_frame_t`, the **legacy 8-byte base retained only for the test suite**. More importantly, **enable-bit position is identical to the charge-monitor channel index** — `NP_SAFETY_MAX_CHANNELS` must match both the `s_charge_nc[]` array size and the `NP_SAFETY_EN_*` bit count, `NP_SAFETY_CH_CLIN_STIM` is defined as the bit position, and tests shift the index directly into the granted mask. **So NP-HW-HUB-001 §7.2's "bits 1–4 reserved, not reused" rule survives the SHDR rationale it was given**, and §7.2 does not record that second, Class C justification — a trap for any reader combining it with the no-records instruction, raised under OI-HEXTILE-14. Consequently only **one** enable-word change is genuinely free: collapsing `ZONE_0..4` to a single `CRANIAL` bit while leaving bits 1–4 as **holes** (nothing above bit 4 moves; `current_ua[]` and `s_charge_nc[]` untouched) — and that one does not depend on the no-SHDR-records finding at all. Compacting the word shifts every modality bit down four and changes the meaning of every `current_ua[i]` slot; widening for per-cluster policy (27 bits) requires the frame to **grow**, moving `NP_SAFETY_RX_EXT_FRAME_LEN`, both checksum spans, the size assertion and eight `offsetof` assertions, and — if the bit-to-channel identity is preserved — `current_ua[27]`, taking the 200 ms heartbeat from 38 to ~64 bytes. Recorded at §8.4.2 with the tES-only decoupling escape noted as a deliberate Class C decision rather than a side effect. **CONTIG-1** (principal direction, 2026-08-04) additionally requires each cluster's petals to form a contiguous arc — no pendant petal, because the clamp plate cannot bridge a gap (the gap socket belongs to an independently-actuated neighbouring cluster) and must instead reach on a 40 mm cantilever arm necked to <=23.09 mm; the first computed partition had four, resolved by reassignment at zero cost in cluster count. DRC gains HT-DRC-17…21. Partition diagram at `docs/diagrams/np_hextile_cluster_map.svg`. **No firmware modified; no peer controlled document corrected** — both routed as open items to their own revisions. Rev 1 content follows. — Rev 1 (2026-07-30): The electrical/FPC counterpart to NP-HEX-ZM-001's mechanical/socket/addressing model, and the hex-tile successor to the retired NP-HW-FPC-001; closes the gap that document's own supersession note declared open. **Emitter lattice:** 91-site 5-ring centered-hexagonal array at 3.80 mm pitch inside the 35 mm active field (2.5 mm bezel, 10.61 cm²) — a hexagonal array registers to a hexagonal tile with no corner waste and is exactly 3-colourable for the tri-wavelength case; independently reproduces the "~90 elements at ~3.5 mm" estimate NP-HEX-ZM-001 §3.1 carried without deriving. Site 0 reserved on **all** types (PD1 aperture on T1-A/T1-C, electrode pod axis on T1-B), leaving 90 emitter sites: **T1-A 45 × 660–670 + 45 × 808–830; T1-C 30/30/30 with 1064 nm**. One lattice for every type — a "type" is an FPC population difference only, matching the one-mould principle; T1-B is a ring-depopulation derivation (§4.5, deferred to Rev 2). **Irradiance derived, not inherited:** T1-A reaches 403 mW/cm² per channel at 150 mA — landing on the 400 mW/cm² pulsed ceiling by construction, so the array cannot be commanded past its own optical limit before firmware intervenes, with CW at ~75 mA; T1-C aggregate 566 mW/cm² vs the 600 mW/cm² three-channel ceiling (5.7 % margin, met by emitter count rather than throttling); 1064 nm at 28 mW/cm² sets a **21-minute floor** on any protocol needing the 36 J/cm² dose (OI-HEXTILE-03). **Three architecture decisions that do not scale back:** (D-3) on-module driver on **every** tile type, not only T1-C — the retired design spent 16 of 20 pins on hub-driven LED current, which at 80 sockets becomes 240 driver channels and ~1,280 power conductors routed through the Faraday envelope alongside the EEG harness; (D-4) TIA + ADC move on-module, **deleting** the ~80× DG2788A per-socket gain-switch NRE that SMART-1 opened rather than redesigning it, and removing the longest high-impedance analog path in the system (gain is fixed to the PD actually fitted, so the type-independence problem vanishes); (D-7) per-cluster I2C segments with UID-derived dynamic addressing — removes the shared-0x30 collision instead of muxing around it, reusing the UID `np_module_map` already depends on, collapsing the anticipated cascade to one mux tier. **Socket interface:** 16-position pogo, 2.00 mm pitch, springs on the socket, pads on the swappable tile (a per-tile ZIF lever would contradict the NP-HEX-ZM-001 §5.4a cluster-clamp accessibility premise). Pin count is derived as the **union of every type's needs** — the dual-rated electrode, its driven shield, and AGND are present at every socket because SMART-1 permits any type in any socket; staggered mating sequence prevents powered-floating-return and false-seated states; SEAT_N detects *partial* seating that an I2C poll would miss. **No ZONE_ID pin** — the resistor ladder is superseded by UID inventory, so NP-SW-001 §5.2.1 / RISK-18 debounce needs re-scoping (OI-HEXTILE-08). VLED 24 V (0.26 A/contact, 4× derating); safety MCU gates VLED **per cluster** (D-8, proposed as the hardware half of OI-HUB-SOCKET-01, since an STM32G071 has no 80-GPIO option); 3.3 V standby budget ≤2 mA/module is a **binding firmware requirement** (the retired 50 mA figure would cost 13 W across 80 modules). 8 decisions with rejected alternatives recorded, 12 open items (OI-HEXTILE-01..12), 16-item DRC. **Two findings routed to principal decision:** (1) driver + metering ~$11.50/tile → **~$920/headset at 80 sockets**, dominated by two InGaAs PDs per tile — quantifies what NP-HEX-ZM-001 §4a recorded as "Known cost, not yet quantified"; leading mitigation is partial socket population (OI-HEXTILE-06). (2) The T1 power envelope permits **~6 concurrent active tiles, not 80** — the lattice buys *placement freedom*, not additional deliverable dose; this also bounds NP-HEX-ZM-001 §6's aggregate-thermal concern (~6 tiles ≈ 64 cm², comparable to the retired 5-slot footprint, so the thermal risk is local not aggregate) and surfaces **OI-HEXTILE-09**, a safety-adjacent gap in the delivered Protocol v2 wire format: nothing prevents a `NP_PROTO_TARGET_SOCKET_MASK` from naming more tiles than the negotiated USB-C PD contract supports. **Values deliberately not asserted** (each an open item, not an omission): uniformity percentage, emitter part numbers and their V_f/flux, exact string-length divisibility, T1-B pod clearance, bezel width — the last being a live conflict between NP-HEX-ZM-001 §3.1 (2.5 mm, used here as the conservative choice) and NP-THERM-BEZEL-001 (1.0 mm), worth ±14.5 % on every irradiance figure (OI-HEXTILE-01). Requires **NP-HW-HUB-001 Rev 3** to adopt the interface (OI-HEXTILE-10). | — *Rev 4 (the §8.1.1 forward-voltage rule) reached `main` independently and is disjoint from this change; its entry follows.* — **Rev 4 (2026-08-16)** — **Rev 4 states §8.1's forward-voltage budget as a rule (new §8.1.1)**, separating its hard functional ceiling (`N · V_f ≤ 24 V − V_dropout`) from its thermal budget allocation (`N · V_f ≥ 22.4 V`), after the Rev 3 worked instances were cited elsewhere as a Vf constraint on the architecture (see `docs/status/pending-decisions.md` §13.2e). Raises **OI-HEXTILE-18** (the ≤7 % allocation is asserted, not derived; no minimum driver dropout specified anywhere) and **OI-HEXTILE-19** (no tolerance stated on the 24 V rail). No decision changed; no emitter selected. **Rev 3 (2026-08-11, written "Rev C")** — **Rev C re-cuts the socket interface (§7.1–7.2, §7.3, §8.1) from 16 to 19 positions**, closing `NP-DRV-SHELL-002` OI-SHELL2-09(i), which had flagged the 16-position count as **blocking socket tooling**. Two independent causes: **OI-HUB-C17c resolved against D-4** (principal, 2026-08-11 — the TIA + ADC stay on the cluster controller, so network **N3 survives** and `PD1_K`/`PD2_K`/`NTC` return to the socket), plus `SYNC` and `DGND` added for REQ-EMI-03 (deterministic phase reference — also closes the ask in OI-HUB-C05) and REQ-EMI-07 (`PGND` must be LED return only for the broadside pair to cancel). Against those five, **`VLED`/`PGND` drops 4+4 → 3+3** by principal decision under the rule *loss of any one contact must still leave ≥2× derating* (0.35 A nominal / 0.52 A degraded vs a ≥1.0 A rating). **Pad array becomes two staggered rows** (~18 mm, inside the 20.0 mm inradius) — forced, since 19 at 2.00 mm pitch spans 38 mm in one row and fits a 40 mm hex only along the vertex diagonal. **§7.2 now agrees pin for pin with `NP-DRV-SHELL-002` §5.1.4**; new **HT-DRC-22/23** keep them in step. D-4's *reasoning* is explicitly not withdrawn — it was outweighed, not refuted — and **§5.3/§6 still read as though D-4 holds (OI-HEXTILE-15, module BOM, not tooling-blocking)**; **OI-HEXTILE-16 (raised and CLOSED 2026-08-11)** settled two conductors that carried two names each: pin 18 is **`ELEC_SHLD`** (over `GUARD`, which collides with the DRL-driven *guard plane* on L1 that `NP-DRV-SHELL-002` §3.5/§4.1/REQ-EMI-02 specify) and pin 12 is **`ALERT#`** (over `/ALERT`: a leading `/` is the EDA hierarchical-path separator, so the net can be silently re-scoped on import) — propagated through HEXTILE §7.4, `NP-DRV-SHELL-002` §5.1.7 and `NP-HW-HUB-001`; **OI-HEXTILE-17 also raised and CLOSED 2026-08-11**, and the first residual proved not to be cosmetic: **`SEAT_N` → `SEAT#`**, because `_N` already means *cardinality* in this codebase (`PBM_TILE_N` / `EEG_TILE_N` are `sizeof(x)/sizeof(x[0])`), so `SEAT_N` read as "number of seats" — which settles the convention **toward** `#` and confirms `ALERT#` rather than reversing it (SMBus `SMBALERT#` precedent). **`ELEC_SIG` → `ELEC`** on ownership (the two pin tables define the interface; HUB-001 §7.4 adopts it) and accuracy (the pin carries tES **stimulation current**, so `_SIG` misdescribes it). **Rule of record: active-low interface signals take `#`, never `_N`, never a leading `/`; doc→firmware mapping is `<SIGNAL>#` → `<SIGNAL>_L`, never `_N`.** Related, NOT closed: `NP-FMEA-001` OI-FMEA-01 (firmware's ten active-LOW enables carry polarity only in comments). No naming residual remains in this document set. Nothing in §4, §8.2 or §9 is affected. **Rev B (2026-08-04) — **DESIGN STUDY, not a tooling baseline.** **Rev B is a cluster-count correction, documentation only, no firmware change.** Rev 1 §8.2 carried NP-HEX-ZM-001 §5.4a's "4–10 clusters" across to the ~80-socket lattice, but 4–10 was that document's figure for the **retired 30-socket** lattice; 4 clusters at n=80 implies 20 tiles/cluster, exceeding both CLUSTER-1's 7-tile unit and §8.2's own 10–19-modules-per-segment capacitance limit. Rev 2 derives the count from the lattice (`ROW_WIDTHS` in `scripts/sync-socket-map.ts`) under the two standing principal decisions — **CLUSTER-1** (7-hex flower) and **SYM-1** (2026-08-04, mirror-symmetric partition) — and obtains **18 clusters, provably minimal**: a cluster holding a midline socket must equal its own mirror image, so its centre is on the midline; only odd-width rows carry one (sockets {2,13,29,46,62,74}), so **6 midline clusters are forced** and absorb exactly 30 sockets, leaving two mirror-image lateral bands of 25 whose exhaustive minimum cover is 6 each → 6 + 2×6 = 18. **SYM-1 costs 6 clusters and ~$38 over the unconstrained 12**, accepted because the cluster is simultaneously the D-8 safety-cut domain and the D-7 I2C segment, so an asymmetric partition would make a safety cut asymmetric across hemispheres and split the inclusive-midline zone-membership rule across two bus segments. Propagated: **D-8 VLED high-side switches 4–10 → 18**; I2C pull-ups 20 → **36**; segments used **18 of 32** (56 %, so D-7's one-tier PCA9548A muxing over LPI2C1–4 is confirmed unaffected); cluster-tier BOM $63.40 → **$114.12**. Two new open items raised rather than absorbed: **OI-HEXTILE-13** — the safety-MCU GPIO budget does **not** demonstrably close at 18 (the STM32G071 **package is unspecified anywhere in the tree**, demand is ~40 I/O which excludes every ≤32-pin option; NP-HW-HUB-001 Rev 3 §7.2 and NP-DRV-SHELL-002 §6 specify **incompatible enable architectures** differing by 17 GPIO — one `NP_SAFETY_EN_PBM_CRANIAL` bit with an all-or-nothing cranial cut vs per-cluster `SAFE_EN_n`; and `np_safety_config.h` provisionally double-assigns PA4 to SPI1 NSS and `NP_EN_PBM_ZONE4_PIN`) — **with a proposed resolution recorded at §8.4.1 (PROPOSED, safety review arbitrates): split the enable by IEC 62304 class**, retaining per-cluster gates under Class B for availability with a single Class C `NP_SAFETY_EN_PBM_CRANIAL` in series as the hard interlock, which preserves R-11, keeps the topological socket→cluster map outside the certified boundary, and drops safety-MCU demand from ~40 to ~23 I/O — and **OI-HEXTILE-14** — peer documents still sized off 10 or 12 (NP-DRV-SHELL-002 §7.1 provisions 12 connectors/16 positions, which **18 exceeds**, and its `8 branches × ≤2 clusters = 16` tree cannot reach 18; NP-HW-HUB-001 §6.3 sizes DG2788A at 10). **Interconnect options and a recommendation are recorded at §8.2.2 (PROPOSED, not decided):** two independent limits bind — **C1** the 16 provisioned cluster-tail connector positions, and **C2** the `8 branches × <=2 clusters = 16` I2C tree. **C2 does not exist under NP-HW-HEXTILE-001's own D-7** (4 x LPI2C x PCA9548A = 32 segments, 18 used, 14 spare), so it resolves alongside OI-HUB-C17, which NP-HW-HUB-001 §7 already recommends deciding in D-7's favour; **C1 does not self-resolve and must be fixed before Rev 3 layout**. Recommended: adopt D-7's tree and provision **20** connector positions rather than 18 (adding tails is the cheap axis — SHELL-002 §7.3 notes adding a *conductor* costs 16 hub pins — and 20 absorbs a REG-1 re-cut without a second re-spin); and **if the §8.4.1 single-cranial-enable proposal survives safety review, remove `SAFE_EN_n` from the 12-conductor tail** — it is the only star component (N1 power is already a broadside tree, N2 control already a two-level tree), so a broadcast enable permits a multi-drop trunk and makes connector count insensitive to future cluster-count changes. Four further options assessed and not recommended: raise branch fan-out to 8 x 3 = 24 (no new silicon, but only relevant if the cluster-MCU tree survives); decouple electrical from mechanical clusters via partial socket population (legitimate, but trades D-7's one-physical-grouping principle for connector count); re-cut the lattice to <=16 clusters (**not available without breaking a principal decision** — the 6 midline clusters are forced by SYM-1 and 18 is provably minimal at n=80); and pairing clusters onto shared tails (breaks the single-FRU property and forces an asymmetric pairing under a symmetric partition). **Corrections applied in the same revision, after review of NP-HW-HUB-001 §7.4 and the Class C wire format:** (i) the claim that NP-HW-HUB-001 §7.2 and NP-DRV-SHELL-002 §6 specify **incompatible** enable architectures is **withdrawn** — §7.4 explicitly reconciles them as *"12–16 physical enable lines fanned out from one policy bit"*, and per-cluster physical gates exist in both designs; the genuinely open question is whether per-cluster **policy** (independently commanded cluster bits) is wanted, which §7.4 already routes to **OI-HUB-C07**, so OI-HEXTILE-13 is a cross-reference to that question rather than a new conflict. (ii) The claim that 27 enable bits (18 cluster + 9 modality) **cannot** fit the 16-bit Class C enable word is downgraded from a ceiling to a **cost**: per **standing principal instruction (2026-08-04) no SHDR fault records have been generated, and none are to be assumed until stated otherwise**, so §7.2's "bits 1–4 reserved, not reused" rule does not bind and the word can be widened to `uint32_t` pre-production with no migration and no ambiguous historical logs. Recorded as **§8.4.2**, which is explicitly **time-boxed** — the window closes at the first fleet fault record, after which bit positions become permanently load-bearing for analytics; collapsing the five zone bits to one cranial bit and re-laying out the word with no reserved holes is free **now** and worth taking regardless of how OI-HUB-C07 resolves. The §8.4.1 recommendation is unchanged but now rests on a single sufficient argument — per-cluster policy puts a **topological** socket→cluster map behind a Class C certification boundary, so a MECH-2 or REG-1 change becomes a recertification rather than a regenerated table. (iii) The **PA4** double-assignment (SPI1 NSS vs `NP_EN_PBM_ZONE4_PIN`) is **re-homed** out of OI-HEXTILE-13: `NP_SAFETY_EN_PBM_ZONE_0..4` encode the **retired 5-module-slot** meaning of "zone", whereas a zone is now *"a named SET OF MODULES, defined as a list of socket addresses"* authored in `protocols/predefined/00-zones.npps`, user-extensible and **overlapping** (the inclusive-midline rule puts every midline socket in BOTH hemisphere zones) — so a zone **can never be a hardware enable domain**, clusters partition the lattice and zones do not. The macros are structurally dead rather than miscounted, NP-HW-HUB-001 §7.2 already mandates their removal, and that deletion takes PA4's second owner with it. Cross-referenced from **NP-FMEA-001 FMEA-M08-04**, which already covers this hazard class with a design-review control that has not yet executed. Two siblings flagged for the same cleanup: `NP_NTC_CHANNEL_COUNT 6 /* 5 zones + 1 hub */` and FMEA-M04's "each PBM zone (5 zones)". **No firmware changed.** **NP-FMEA-001 Rev 3 → D in the same change set:** the retired "zone" concept was flagged across SW01-M04 and SW01-M01. §3.4 (thermal interlock) is written against the retired **5-PBM-slot** architecture — a zone is now an overlapping, user-authored set of sockets (`00-zones.npps`) and **cannot be a thermal or enable domain**; the thermal domain is the tile (~80), the aggregation boundary the cluster (18), the per-tile 62 °C throttle is on-module Class B, and module faults arrive by per-cluster wire-OR `ALERT#`. **FMEA-M04-01's mitigation is marked INVALID** (bounds a zone index 0–4, unit-tests "all 5 zone indices"); FMEA-M01-03 flagged for re-check on any enable-word re-layout (bit position ≡ charge-monitor channel index); **FMEA-M08-04 gains the PA4 double-assignment as a recorded open instance** of its own hazard class, whose design-review control has not yet executed; `NP_NTC_CHANNEL_COUNT 6 /* 5 zones + 1 hub */` flagged as the retired count. **OI-FMEA-06 raised.** **No risk scores changed and no failure modes added or removed** — re-scoring the new architecture is hazard analysis, not editorial correction, so §3.4's scores are frozen and marked not-to-be-relied-on pending OI-FMEA-06. **Corrected on inspection of the wire format (same revision):** the active heartbeat `np_safety_rx_ext_frame_t` has **zero spare bytes** — all 38 are allocated (magic 2, session_status 1, enable_lo/hi 2, channel_count 1, checksum 2, `current_ua[14]` 28, ext_checksum 2); the single reserved byte cited in an earlier draft belongs to `np_safety_rx_frame_t`, the **legacy 8-byte base retained only for the test suite**. More importantly, **enable-bit position is identical to the charge-monitor channel index** — `NP_SAFETY_MAX_CHANNELS` must match both the `s_charge_nc[]` array size and the `NP_SAFETY_EN_*` bit count, `NP_SAFETY_CH_CLIN_STIM` is defined as the bit position, and tests shift the index directly into the granted mask. **So NP-HW-HUB-001 §7.2's "bits 1–4 reserved, not reused" rule survives the SHDR rationale it was given**, and §7.2 does not record that second, Class C justification — a trap for any reader combining it with the no-records instruction, raised under OI-HEXTILE-14. Consequently only **one** enable-word change is genuinely free: collapsing `ZONE_0..4` to a single `CRANIAL` bit while leaving bits 1–4 as **holes** (nothing above bit 4 moves; `current_ua[]` and `s_charge_nc[]` untouched) — and that one does not depend on the no-SHDR-records finding at all. Compacting the word shifts every modality bit down four and changes the meaning of every `current_ua[i]` slot; widening for per-cluster policy (27 bits) requires the frame to **grow**, moving `NP_SAFETY_RX_EXT_FRAME_LEN`, both checksum spans, the size assertion and eight `offsetof` assertions, and — if the bit-to-channel identity is preserved — `current_ua[27]`, taking the 200 ms heartbeat from 38 to ~64 bytes. Recorded at §8.4.2 with the tES-only decoupling escape noted as a deliberate Class C decision rather than a side effect. **CONTIG-1** (principal direction, 2026-08-04) additionally requires each cluster's petals to form a contiguous arc — no pendant petal, because the clamp plate cannot bridge a gap (the gap socket belongs to an independently-actuated neighbouring cluster) and must instead reach on a 40 mm cantilever arm necked to <=23.09 mm; the first computed partition had four, resolved by reassignment at zero cost in cluster count. DRC gains HT-DRC-17…21. Partition diagram at `docs/diagrams/np_hextile_cluster_map.svg`. **No firmware modified; no peer controlled document corrected** — both routed as open items to their own revisions. Rev 1 content follows. — Rev 1 (2026-07-30): The electrical/FPC counterpart to NP-HEX-ZM-001's mechanical/socket/addressing model, and the hex-tile successor to the retired NP-HW-FPC-001; closes the gap that document's own supersession note declared open. **Emitter lattice:** 91-site 5-ring centered-hexagonal array at 3.80 mm pitch inside the 35 mm active field (2.5 mm bezel, 10.61 cm²) — a hexagonal array registers to a hexagonal tile with no corner waste and is exactly 3-colourable for the tri-wavelength case; independently reproduces the "~90 elements at ~3.5 mm" estimate NP-HEX-ZM-001 §3.1 carried without deriving. Site 0 reserved on **all** types (PD1 aperture on T1-A/T1-C, electrode pod axis on T1-B), leaving 90 emitter sites: **T1-A 45 × 660–670 + 45 × 808–830; T1-C 30/30/30 with 1064 nm**. One lattice for every type — a "type" is an FPC population difference only, matching the one-mould principle; T1-B is a ring-depopulation derivation (§4.5, deferred to Rev 2). **Irradiance derived, not inherited:** T1-A reaches 403 mW/cm² per channel at 150 mA — landing on the 400 mW/cm² pulsed ceiling by construction, so the array cannot be commanded past its own optical limit before firmware intervenes, with CW at ~75 mA; T1-C aggregate 566 mW/cm² vs the 600 mW/cm² three-channel ceiling (5.7 % margin, met by emitter count rather than throttling); 1064 nm at 28 mW/cm² sets a **21-minute floor** on any protocol needing the 36 J/cm² dose (OI-HEXTILE-03). **Three architecture decisions that do not scale back:** (D-3) on-module driver on **every** tile type, not only T1-C — the retired design spent 16 of 20 pins on hub-driven LED current, which at 80 sockets becomes 240 driver channels and ~1,280 power conductors routed through the Faraday envelope alongside the EEG harness; (D-4) TIA + ADC move on-module, **deleting** the ~80× DG2788A per-socket gain-switch NRE that SMART-1 opened rather than redesigning it, and removing the longest high-impedance analog path in the system (gain is fixed to the PD actually fitted, so the type-independence problem vanishes); (D-7) per-cluster I2C segments with UID-derived dynamic addressing — removes the shared-0x30 collision instead of muxing around it, reusing the UID `np_module_map` already depends on, collapsing the anticipated cascade to one mux tier. **Socket interface:** 16-position pogo, 2.00 mm pitch, springs on the socket, pads on the swappable tile (a per-tile ZIF lever would contradict the NP-HEX-ZM-001 §5.4a cluster-clamp accessibility premise). Pin count is derived as the **union of every type's needs** — the dual-rated electrode, its driven shield, and AGND are present at every socket because SMART-1 permits any type in any socket; staggered mating sequence prevents powered-floating-return and false-seated states; SEAT_N detects *partial* seating that an I2C poll would miss. **No ZONE_ID pin** — the resistor ladder is superseded by UID inventory, so NP-SW-001 §5.2.1 / RISK-18 debounce needs re-scoping (OI-HEXTILE-08). VLED 24 V (0.26 A/contact, 4× derating); safety MCU gates VLED **per cluster** (D-8, proposed as the hardware half of OI-HUB-SOCKET-01, since an STM32G071 has no 80-GPIO option); 3.3 V standby budget ≤2 mA/module is a **binding firmware requirement** (the retired 50 mA figure would cost 13 W across 80 modules). 8 decisions with rejected alternatives recorded, 12 open items (OI-HEXTILE-01..12), 16-item DRC. **Two findings routed to principal decision:** (1) driver + metering ~$11.50/tile → **~$920/headset at 80 sockets**, dominated by two InGaAs PDs per tile — quantifies what NP-HEX-ZM-001 §4a recorded as "Known cost, not yet quantified"; leading mitigation is partial socket population (OI-HEXTILE-06). (2) The T1 power envelope permits **~6 concurrent active tiles, not 80** — the lattice buys *placement freedom*, not additional deliverable dose; this also bounds NP-HEX-ZM-001 §6's aggregate-thermal concern (~6 tiles ≈ 64 cm², comparable to the retired 5-slot footprint, so the thermal risk is local not aggregate) and surfaces **OI-HEXTILE-09**, a safety-adjacent gap in the delivered Protocol v2 wire format: nothing prevents a `NP_PROTO_TARGET_SOCKET_MASK` from naming more tiles than the negotiated USB-C PD contract supports. **Values deliberately not asserted** (each an open item, not an omission): uniformity percentage, emitter part numbers and their V_f/flux, exact string-length divisibility, T1-B pod clearance, bezel width — the last being a live conflict between NP-HEX-ZM-001 §3.1 (2.5 mm, used here as the conservative choice) and NP-THERM-BEZEL-001 (1.0 mm), worth ±14.5 % on every irradiance figure (OI-HEXTILE-01). Requires **NP-HW-HUB-001 Rev 3** to adopt the interface (OI-HEXTILE-10). | **OI-HEXTILE-14 closed 2026-08-18:** every cluster-count-dependent figure re-sized off 12 (and off `ceil(n/8)` = 10) to **18**, with **20** connector positions provisioned so a REG-1 re-cut needs no PCB re-spin; `NP-HW-HUB-001` §7.4/§6.3/§5.2/§8.2/§8.5 and HUB-DRC-C02 corrected, HT-DRC-20 now passes. The 12 → 11 conductor reduction that OI-HUB-C07's closure unblocks is **not** taken — HUB-REQ-C05 leaves open how the Class B gate is commanded.
| PBM Session Power Audit — Predefined Protocol Library | `docs/np_ses_pwr_001.md` | NP-SES-PWR-001 Rev 1 (2026-08-21) — **DESIGN STUDY.** Tests `NP-HW-HEXTILE-001` §9's concurrency ceiling against the **authored** protocol library rather than a hypothetical, and finds `OI-HEXTILE-09` is an actual gap: **2 of 20 protocols with a `pbm_transcranial` block fit the 40 W emitter budget, 17 exceed it by 1.25×–40×, 1 is operator-scoped — all 20 compile clean**, since `app/web/src/lib/hubCompiler.ts` has no power or budget check. **§2.1 corrects the "~6 tiles" rule:** it assumes 6.25 W/tile and nothing runs there; measured draw spans **1.3–20.0 W** → **2–32 concurrent tiles**, confirming `NP-PWR-BUDGET-001` D-4 (governor in watts) against data. **§2.2: the binding constraint is CW, not the lattice** — pulsed protocols fail on socket count, CW ones on per-tile draw (one tile at 80 % CW is 20 W, half the budget). **§3 is the largest finding and is NOT a power finding:** protocols target **lobe-scale zones where their own cited evidence specifies electrode-scale sites** — `clinical-04` is titled *DLPFC* and irradiates **37 sockets**; no DLPFC, F3/F4 or single-site zone exists, the finest frontal targeting being a 20-socket half-lobe. Filed as a **clinical-validity** item (`OI-SESPWR-01`, gated on REG-1) so it cannot close for the wrong reason. **§4 on cascading:** it preserves dose and multiplies session length by the group count (40 m to **20 h**); it survives for dose-driven CW protocols — where **fast cascading is the same mechanism as `NP-PWR-BUDGET-001` §3.6's whole-vault mode** — and **invalidates every 40 Hz entrainment protocol, including the Grade A Alzheimer's one**, plus closed-loop and the cross-modal 40 Hz phase-lock in `15-full-t1-immersive`. A cascade primitive therefore needs a per-protocol admissibility flag defaulting to *no* (`OI-SESPWR-04`). **§5 ranks the fixes:** correct the zones first (a data edit, and a fidelity improvement), then the CW/duty semantic, then the CW ceiling, **then** the governor — a governor built against today's library would reject 17 protocols when the right response to 15 is to fix the protocol. Audit is `scripts/check-pbm-power.ts` (reports by default; `--strict` deliberately not in CI until remediation lands — D-2). 6 open items, 4 decisions, none locked. Not DHF-registered (same status as its parent). |
| Power Budget — Reverse-Engineered from Safety Ceilings | `docs/np_pwr_budget_001.md` | NP-PWR-BUDGET-001 **Rev 3 (2026-08-21)** — **A third TMS power-path option assessed (new §4.4), and a locked-section inconsistency raised.** §4.3 framed the TMS power path as a binary — shared battery-buffered rail vs. mains-tethered — and **a third option was never assessed: keep USB-C for data, programming and all T1 power, and add a second power-only inlet for high-draw T2 sessions.** It dominates both because **it scopes the Mode 3 loss to the modality that forces it** rather than to the device. **§4.4.1: the form matters more than the idea** — a second **USB-C PD sink**, not a barrel or proprietary DC inlet, and the decisive argument is **IEC 60601-1**, not cost: a PD sink stays SELV behind a certified charger, while a mains-derived inlet makes **NeurOne the isolation barrier** on a device with conductive applied parts, at the 510(k) tier, with `NP-DT-001` VE-11 still Open. It also reuses the specified USB-C Layer 5 filter, preserves §2.2's *"any PD-compliant charger must work"*, and `NP-TOOL-HUB-001` HUB-MDR-04 already specifies two hub port openings with tethered covers. **§4.4.2 raises a discrepancy in a LOCKED section without resolving it: CLAUDE.md §2.2 ships Pro Full two 65 W chargers ($26 BOM) while §4.5 negotiates a single 100 W EPR contract.** Three readings — undocumented spare, **a dual-inlet architecture assumed and never written down**, or **a separate T2 accessory with no port, enclosure or power spec anywhere** (consistent with §4's finding that TMS is absent from the electrical model). **`OI-PWR-11`, principal, resolve first — it decides whether §4.4 is a proposal or a specification.** **§4.4.3 two new engineering surfaces:** a 5 A switched inlet is a materially worse EMF aggressor than a data port, inside the occipital-arch assembly whose **measured** shielding is the primary technical claim — **bench-verify, do not argue** (`OI-PWR-12`); and dual-source OR-ing/inrush/mid-session hot-plug, where **the safety MCU has no concept of total available power at all** (`OI-PWR-13`). **§4.4.4 states what it does not do: it does not raise T1 concurrency** — §3.2's thermal ceiling (4–8 tiles) and the power-derived one (~6) are coincident, so relieving power moves the binding constraint to heat. Revisit only if `OI-PWR-01`'s CFD returns a higher thermal ceiling. **D-3's option set widens from two to three; `OI-PWR-04` restated accordingly.** CLAUDE.md §4.5 and §2.2 both still unmodified. — Rev 2 (2026-08-21) — **An efficacy FLOOR added to a methodology that had only ceilings (new §3.4); the full-population bound stated for the first time (§3.5); two findings it produced (§3.6, §3.7).** Rev 1 §1's elastic/inelastic frame classifies every modality by its *ceiling*, and **a budget bounded only from above cannot say what the power is for** — which is how §3.2 bracketed the tile count without asking what a tile delivers. `docs/pbm_neuro_protocols.md` supplies the floor: **0.02–0.3 W/cm², 10–120 J/cm², 6–30 min**, with *"under-dosing, not mechanism failure, explains most nulls."* **§3.5 answers a question the tree had never written down: a fully populated, fully driven 80-tile lattice draws ≈ 2.0 kW** (83.3 A at 24 V) — 20× the maximum negotiable USB-C PD contract, **but the electrical collision is the mild one**: at η_wp 0.30–0.45 it puts **1.1–1.4 W/cm² at the LED plane, 4–5.6× the worst-case flux modelled anywhere in the tree**, and §3.3's sealed cavity means no supply or fan reaches it. **T1-A, not T1-C, is the worst-case tile** (the 1064 nm channel's lower V_f *reduces* per-tile draw). **The distinction that makes it usable: *populated* is not *driven*** — 80 populated sockets cost BOM and 0.53 W of standby, not LED power. **§3.6: whole-vault simultaneous illumination is affordable at ~30 W** (1.5 % drive), inside the existing envelope and specified nowhere — which matters because Grade A Alzheimer's asks for *whole-head* geometry, so `NP-HW-HEXTILE-001` §9.3's *"placement options, not capability"* is too strong. Honestly bounded: 7.2 J/cm² in 20 min is **below** the threshold, so it is coverage, not dose. **§3.7: local irradiance and total optical output are different quantities** — total optical is envelope-capped at ~13–14 W *whatever the tile population*; only **coverage** scales with tiles, and with `NP-OPT-PSF-001` §3.3's 26.2 mm cortical floor at ~65 % of tile pitch, **an emitter count is not a capability claim**. **One finding runs against the product:** the **1064 nm channel is 9× below its own Grade A protocol's 0.25 W/cm²** — an emitter wall, not a budget shortfall (raised on the owning document as `OI-HEXTILE-21`). New **OI-PWR-07–10** (whole-vault mode as a product feature; §3.5's thermal extrapolation is outside the §3.2 model's validity; `pbm_neuro_protocols.md` is now load-bearing but is **not a controlled document**; the clustered-vs-distributed montage caveat still unaddressed) and **D-4/D-5**. **§3.5 and §3.6 are routed to `OI-HEXTILE-06`/`OI-HEXTILE-09` as decision inputs — not decided here.** Still does not modify CLAUDE.md §4.5. — Rev 1 (2026-08-02) — **DESIGN STUDY.** Interrogates CLAUDE.md §4.5 (T1/T2 peak power figures), which every other document treats as a given input and none derives. Frames modalities as **inelastic** (dose-capped — charge density, IEC 62471 MPE, laser class; more power/battery cannot raise the ceiling) vs. **elastic** (heat-removal-bound — PBM scalp contact, 1170nm TEC; the ceiling scales with cooling capacity, not raw wattage). **PBM finding:** NP-HW-HEXTILE-001 §9's "~6 concurrent tiles" is power-derived (budget ÷ per-tile draw), not thermally derived; a first-order aggregate estimate off the existing single-tile CFD model (NP-THERM-CFD-R1-001 §5.1, 11.3 °C nominal-ambient margin, ~90% BN-boss export efficiency, residual heat into the sealed cavity) independently brackets **4–8 tiles** — close to the existing number, but for the right reason instead of a coincidence, and with a new caveat the flat rule misses: clustered montages concentrate residual cavity heat more than spatially-distributed ones. The real lever for more concurrency is via/cavity export efficiency, not battery size (the sealed cavity cannot be ventilated without breaching the EMF shield, so extra wattage doesn't help). **TMS finding (the larger one):** zero electrical specification exists anywhere in the tree — no capacitor bank, coil current, or pulse energy; TMS is absent from the hub power architecture and from the thermal power-budget table, where T2-peak (70–74 W) is explicitly annotated as sized for the **1170nm laser zone**, not TMS. An order-of-magnitude E∝B² scaling from general clinical-device figures (~200–500 J/pulse at ~1.5–2.0 T, scaled to NeurOne's stated 0.1–0.5 T) puts **average power during a 10Hz rTMS train at ~15–400 W depending where in that range the coil actually runs** — at the top of NeurOne's own field spec, TMS alone exceeds the entire current T2-peak envelope, before any other modality. Separately flags that 0.1–0.5 T may itself be below what commercial figure-8 coils need for the 120% RMT protocols the evidence base cites (`neuromod_neuro_protocols.md`) — an EM coil-design question, not a power question. **Recommendation:** T1 figures can likely stand pending a verification-grade multi-tile CFD (OI-PWR-01, extends OI-R1-01); T2 figures cannot be revised responsibly until TMS's real field-strength requirement and coil energy are established (OI-PWR-02/03) and a power-path decision is made — shared battery rail with a purpose-built capacitor bank and likely >100W EPR PD contract, vs. external/mains-tethered TMS excluded from Mode 3 autonomous operation (OI-PWR-04, routed to principal decision). **Does not modify CLAUDE.md §4.5** — 6 open items, 3 decisions recorded (none locked). |
| PBM Optical Point-Spread Function + Spatial Resolution Floor | `docs/np_opt_psf_001.md` | NP-OPT-PSF-001 Rev 1 (2026-07-20) — Monte Carlo (MCML) photon transport at 1064nm through a 4-layer head (scalp 6 / skull 7 / CSF 2 / gray 45 mm), producing the point-spread function for one hex zone module. **Headline: the spatial resolution floor at cortical depth is an edge-spread function of ~26 mm (10–90%; 12.9 mm at 25–75%), range 24–31 mm across the CSF-thickness sweep.** Measured as a module's own edge width the figure looks sharper (22.8 mm) but that is aperture-limited, not a floor — a distinction the first revision got wrong. Contralateral cortical energy: midline module exactly 50% (symmetry self-test), `Frontal Right` 16.3%, `Frontal Right (excl. midline)` 3.6%. Confirms partial-module masking is strictly dominated as a lateralization tool (half-masked midline module still spills 22.1%, and cannot reach zero); refutes the PR #210 inference that zone narrowing would be only partially effective (it delivers a 78% reduction). Sensitivity: CSF thickness, not μs′, is the governing axis (±12% vs ±6%). **Model, not a measurement** — plane-parallel geometry and literature optical properties; tissue-phantom validation is a listed follow-up before any external claim. Re-runnable: `scripts/pbm-optical-psf.ts`. |
| RISK-03 Regulatory Opinion Scope Expansion Brief | `docs/np_reg_pbm1064_001.md` | NP-REG-PBM1064-001 Rev 1 (2026-05-13) — Issue #56. Scope expansion brief to outside regulatory counsel covering four new items beyond existing RISK-03 (Issue #5): (1) 1064nm LED array FDA general wellness pathway + IEC 62471 EH1/EH2 classification; (2) three-channel aggregate irradiance ceiling 600 mW/cm² — IEC 62471 additive assessment and FTC substantiation; (3) T2 combined 1064nm LED + 1170nm laser simultaneous session — dual-standard photobiological hazard and 510(k) coverage; (4) depth-tier penetration claim FTC substantiation adequacy. 12 questions for counsel (Q1–Q12). Marketing gate: all 1064nm irradiance, aggregate, depth-tier, and T2 combined session claims remain gated until opinion letter received. Estimated incremental cost $5,000–10,000 to existing RISK-03 engagement. |
| 1064nm Smart Zone Module Hardware (FPC Variant + Procurement) | `docs/superseded/np_hw_fpc_001.md` + `docs/np_proc_fpc_1064_001.md` | NP-HW-FPC-001 Rev 5 (FPC layout variant NP-FPC-ZM-SM-01): pins 5–8 rerouted from base LED_B to LED_C (1064nm), pins 9–12 to I2C SDA/SCL; 550 LEDs (200+200+150); InGaAs PD1/PD2 (Hamamatsu G12180-010A); rigidizer sub-board 22×14mm for ATtiny402+FETs; TIA gain switch OI-PBM-HW-01 BLOCKING for hub PCB Rev B. NP-PROC-FPC-1064-001 Rev 1: ATtiny402 ($0.38) + 3× IRLML6344TRPBF ($0.12 each) + G12180-010A InGaAs PDs + EPITEX L1064-02AU 1064nm LEDs via Marubeni; module BOM sub-total ~$18–22, delta vs base +$23–28, retail $149–199/zone or $599–699 all-5-zone kit. Closes Issue #54. **⚠ SUPERSEDED 2026-07-28 — retired 66×78mm/5-slot form factor and pricing; part selections (ATtiny402, IRLML6344, InGaAs PD, EPITEX 1064nm LED) remain relevant. See supersession notes in `docs/superseded/np_hw_fpc_001.md` and `docs/np_proc_fpc_1064_001.md`.** |
| 1064nm Smart Zone Module Tooling Variant | `docs/superseded/np_tool_zm_sm_001.md` | NP-TOOL-ZM-SM-001 Rev 1 — mold variant for smart module. Rigidizer cavity 24×16×3.5mm internal. Asymmetric mechanical key: +2.0mm lateral face width vs base module — prevents physical mis-insertion into un-upgraded hub slot (hub PCB without Vishay DG2788A analog switch would saturate TIA). 5-item DRC checklist. Cross-references NP-HW-FPC-001 Rev 5 and OI-PBM-HW-01. **⚠ SUPERSEDED 2026-07-28 — parent mould family retired by the universal hex-tile mould; the mechanical-key (F-SM-03) is CONFIRMED not needed under SMART-1 (every socket now I2C/TIA-capable, no type differentiation left to key against). See `docs/superseded/np_tool_zm_sm_001.md` and `docs/np_hex_zm_001.md` §4a.** |
| QMS Manual | `docs/np_qms_001.md` | NP-QMS-001 Rev 1 (2026-05-13) — Issue #33. QMS established at company formation. Scope: T1 + T2, all modalities, all firmware, iOS/Android app. References: 21 CFR Part 820, ISO 13485:2016, ISO 14971:2019, IEC 62304, IEC 62366-1. Quality policy. Document numbering scheme. QMS process map (design controls, risk management, software, supplier, CAPA, PMS). Management responsibility structure (Quality Lead open, interim CEO). Internal audit cycle. eQMS formalization roadmap (Month 3–24). Closes Issue #33 QMS establishment requirement. |
| Design and Development Plan | `docs/np_dp_001.md` | NP-DP-001 Rev 1 (2026-05-17) — 20-section formal D&D plan per 21 CFR §820.30(b) and ISO 13485:2016 §7.3.1. Six phases (Phase 0 complete; Phases 1–5 T1 Month 0–18; Phase 6 T2 Month 12–54). Gate schedule: G1 Month 6, G2 Month 10, G3 Month 14. Responsibilities, verification, validation, transfer, change control, risk integration, software integration, supplier management, interim doc control. docs/status/pending-decisions.md §13.4 NP-DP-001 pending decision CLOSED. |
| Design History File Index | `docs/np_dhf_001.md` | NP-DHF-001 Rev 32 (2026-08-12) — Rev 32: NP-FW-EMMC-002 Rev 2 (§H characterisation programme) indexed in §5.4/§5.13, `firmware/shdr/` added to §6, NP-PRIV-REM-001 → Rev 3, NP-PRIV-001 PDF regenerated. **Also repairs a duplicate revision number:** PRs #272 and #273 both issued a Rev 29 (#273 branched before #272 landed and merged third without re-checking), and the front-matter revision had been left at 28 while the history reached 30. #273's row is renumbered 29 → 31 — the number it would have taken against the tree it merged into — and the earlier claimant keeps 29 so pre-existing citations still resolve. The history table is now 1…32, no gaps, no duplicates. Correction recorded in §9 rather than made silently. Rev 20: NP-FEAS-FNIRS-001 Rev 1 added to §5.3 (EXPLORATORY); new FEAS document category added to §4 (feasibility/competitive analysis, not §820.30 design records) — from Neurode Labs competitive analysis. Rev 19 (2026-07-13): OI-CVNS-HUB-11 numeric hub-vs-MCU impedance cross-validation; NP-FMEA-001 → Rev 3. Rev 18: four NP-FW-EMMC-002 modules added to §6 firmware source table as DHF design outputs (§B `firmware/factory_reset/`, §C `firmware/uhdr_key/`, §D `firmware/anon/`, §E `firmware/edf/`); stale "no source directories" note corrected/reduced (§A warranty token + §F Mode F remain spec-only); §7 design-outputs row updated; privacy-protection universalization (BIPA written release + MHMD no-sale/standalone auth applied to ALL users) reflected in §5.13/§7/planned-additions rows (CLAUDE.md → Rev 30). Rev 17 (2026-07-11): FreeRTOS-Kernel V11.3.0 vendored (`firmware/vendor/freertos/`), SW-02 RTOS. Rev 16 (2026-06-15): NP-FMEA-001 Rev 1 → Rev 2 §3.7 correction for OI-SW01-M07-02 closure. Rev 15: OI-SW01-M07-02 CLOSED; `firmware/crypto/` added to §6 firmware source table; NP-SW-001 SOUP table updated (Monocypher 4.0.2); Rev 14 → Rev 15. Rev 14: OTA firmware update — iOS + firmware C module. Rev 6 updates NP-SIM-001 to v0.3.0 in §5.5; #79 (T2 TMS coil) removed from open sub-issues (CLOSED — branch claude/add-t2-tms-coil-dLbh7); CLAUDE.md updated to Rev 14. Rev 5 updates NP-SIM-001 to v0.2.0 in §5.5; #77 (WebSocket API) and #78 (probe animation) removed from open sub-issues (both CLOSED — PRs #84, #85); CLAUDE.md updated to Rev 13. Rev 4 adds NP-DP-001 Rev 1 to §5.1; design planning completeness upgraded to Good; NP-DP-001 removed from §8 planned additions; CLAUDE.md updated to Rev 12; CEO name (Steve Hickman) added to Author/Approved-by fields. Rev 3 adds NP-SIM-001 v0.1.0 (Helmet Simulator) to §5.5; new SIM category added; CLAUDE.md updated to Rev 11; open sub-issues #77–#80 and parent Issue #81 / PR #76 recorded. Rev 2: relative markdown navigation links to all file entries; NP-DB-005 (R5 design brief) added as ACTIVE; CLAUDE.md updated to Rev 10; NP-MOD-EXT-001 → SUPERSEDED; NP-FW-HUB-001 Rev 1 (hub_control) added; NPPS Language Reference added; hub_control/ added to firmware source table; NP-COORD-001 → Rev 1.8; NP-RISK-001 → Rev 2. Rev 1 (2026-05-13): Master DHF index per 21 CFR §820.30(j); all pre-formation design documents retroactively entered under change control; firmware source code directories declared DHF design output records; DHF completeness assessment (8 design phases); planned additions (NP-DP-001, NP-DT-001, NP-HFE-001, NP-PMS-001). Closes Issue #33 DHF initiation requirement. |
| Design Controls Procedure | `docs/np_qms_dc_001.md` | NP-QMS-DC-001 Rev 1 (2026-05-13) — Issue #33. Design controls per 21 CFR §820.30 and ISO 13485:2016 §7.3. Design planning (§820.30(b)); design inputs — types, sources, documentation requirements (§820.30(c)); design outputs — types, approval requirements (§820.30(d)); design review — scheduled gate reviews, record requirements, independence requirement (§820.30(e)); design changes — significant change order process and minor change notification (§820.30(i)); design verification — methods, protocol requirements, current pending FAI items (§820.30(f)); design validation — T2 HFE summative, clinical performance, software validation, biocompatibility (§820.30(g)); design transfer (§820.30(h)). Closes Issue #33 design controls process requirement. |
| ISO 14971 Risk Management Plan | `docs/np_rm_001.md` | NP-RM-001 Rev 1 (2026-05-13) — Issue #33. Risk management plan per ISO 14971:2019. Scope: full device lifecycle, T1 + T2, all accessories. Severity S1–S5 and probability P1–P5 classification tables. 5×5 risk acceptability matrix (ACCEPTABLE / ALARP / UNACCEPTABLE). Full ISO 14971 process map (planning → hazard ID → estimation → evaluation → control → overall residual risk → review → post-market). Intended use and reasonably foreseeable misuse definitions (T1 and T2). 9 hazard categories with primary interlocks. Formal QMS change-control entry of RISK-01..25 (23 MITIGATED, RISK-03 and RISK-20 OPEN, RISK-25 hardware bench pending). Risk control hierarchy (inherent safe design → protective measures → information). Software hazard and cybersecurity risk provisions. Overall residual risk evaluation requirements pre-510(k). Post-market surveillance risk feedback loops. Risk Management Report target Month 18. Closes Issue #33 ISO 14971 risk management plan requirement. |
| IEC 62304 Software Development Plan | `docs/np_sw_001.md` | NP-SW-001 **Rev B (2026-07-27)** — adds §5.2.1 ZONE_ID Detection Debounce Requirement (RISK-18): 3 reads at 100ms intervals, ≥2/3 majority, binding on SW02-M04 and SW02-M06; states the requirement at the software-development-plan level (implementations already existed in NP-FW-ZA-001 and NP-FW-PBM1064-001) and closes the corresponding pending-decisions.md item. Rev 1 (2026-05-13) — Issue #33. Software development plan per IEC 62304:2006+AMD1:2015. Three software items classified: SW-01 Safety MCU (STM32G071, bare-metal) = **Class C**; SW-02 Main processor (i.MX RT1062, FreeRTOS) = **Class B**; SW-03 iOS/Android app = **Class B**. Class C rationale: Safety MCU owns all stimulation GPIO, no independent safety backstop. Class B rationale for SW-02/SW-03: Safety MCU provides independent hardware backstop with SPI heartbeat watchdog. V-model for SW-01; iterative V-model for SW-02/SW-03. Full module inventory: 8 SW-01 Class C safety units (SW01-M01..M08); 15 SW-02 modules indexed (includes SW02-M09..M12 hub control: module registry, session runner, session log, safety SPI heartbeat); 7 SW-03 modules indexed. SOUP tracking table (FreeRTOS, LittleFS, Ed25519, CMSIS-DSP). MISRA C:2012 for SW-01 (mandatory) and SW-02 (advisory). Software requirements traceability to NP-DT-001. Safety response time requirements: heartbeat → cutoff ≤50ms; cardiac interlock ≤100ms; photoparoxysmal ≤200ms. IEC 62304 clause compliance table. OTA update process (SW-01 never automated OTA). OS compatibility 7-day SLA. Closes Issue #33 IEC 62304 software plan requirement. |
| Privacy Analysis and Repair | `docs/np_priv_001.pdf` | NP-PRIV-001 Rev 1 (2026-06-02; HIGH-01 conclusion 2026-08-12) — **HIGH-01 conclusion extended 2026-08-12** (principal decision): Option C remains the destination, was unreachable because the remediation foreclosed its own evidence, and NP-FW-EMMC-002 §H's consented time-boxed characterisation programme is what makes it reachable. PDF regenerated from `generate_privacy_report.py` — never hand-edited. 18 findings (2 Critical, 6 High, 7 Medium, 3 Low); UHDR/SHDR architecture review; consent engine; research data flows; clinical platform; what looks good; recommended next steps. PDF generated by `generate_privacy_report.py`. |
| Privacy Remediation Master Plan | `docs/np_priv_rem_001.md` | **NP-PRIV-REM-001 Rev 4 (2026-08-16)** — Rev 3 clarifies STEP-36 Step 1 only: "first screen of the consent flow" means the overall onboarding chain (age gate → BIPA disclosure → research consent), not the research-consent screens alone, so it does not conflict with the reciprocity-first screen order introduced in CLAUDE.md Rev 37 §6.2. Verified against `presentNextOnboardingStep()` in `app/ios/NeurOne/NeurOneApp.swift`. No remediation step added, removed, re-scoped or re-sequenced. Rev 3 (2026-08-12) — **STEP-10 rewritten** — Option C remains the destination but was unreachable as written; NP-FW-EMMC-002 §H is the route to it. Status OPEN → IMPLEMENTED, WINDOW OPEN; G1 (Month 6) target unchanged; closure gated on OI-EMMC2-09/10/11/12. Rev 1 (2026-06-02): 30 remediation steps (STEP-01 through STEP-30); option selections table; master calendar (Phase 0 through Phase 6); capability matrix; authority submission guide (EU-US DPF, Apple App Store, Google Play, IRB, HIPAA OCR, FTC HBNR, GDPR Art. 33, US state breach laws); direct remediations completed index. |
| Breach Response Plan | `docs/np_sec_br_001.md` | NP-SEC-BR-001 Rev 1 (2026-06-02) — escalation chain; detection signals; severity classification; containment and forensic preservation procedures; regulatory notification decision tree (HIPAA, FTC HBNR, GDPR Art. 33-34, US state laws); individual notification templates (Appendices A-C); annual tabletop exercise procedure. |
| POA Upload Procedure | `docs/np_proc_poa_001.md` | NP-PROC-POA-001 Rev 1 (2026-06-02) — accepted document types; E2E encrypted upload via signed URL; vault access controls (≤3 named reviewers, full audit log); review procedure and structured review record schema; 30-day document deletion; annual re-verification workflow; capacity restoration procedure; incident response. |
| Research anonymization Engine Firmware Specification | `docs/np_fw_anon_001.md` | NP-FW-ANON-001 Rev 1 (2026-06-03) — l-diversity (l≥3) + differential privacy (ε≤1.0, δ≤10⁻⁵) Laplace mechanism; on-device k-anonymity grouping; study descriptor schema v1 with prohibited element list; budget tracking per study; FAI-ANON-01 through FAI-ANON-09 test suite; 5 open items (OI-ANON-01 through OI-ANON-05). Addresses NP-PRIV-REM-001 STEP-11. |
| HIPAA BAA Template | `docs/np_legal_baa_001.md` | NP-LEGAL-BAA-001 Rev 1 (2026-06-03) — DRAFT TEMPLATE requiring legal counsel review before execution. Full HIPAA BAA with: required provisions (45 CFR §164.504(e)); consent revocation deletion cascade (§5.1, 30-day deletion obligation); no onward sharing (§5.2); breach notification within 5 business days (§3.3); 3-tier access table (Monitor/Assess/Full Clinical) with minimum necessary data per tier; BIPA provision for Illinois users (§5.4); Exhibit A scope template. Addresses NP-PRIV-REM-001 STEP-13. |
| Human Factors Engineering Plan | `docs/np_hfe_001.md` | NP-HFE-001 Rev 1 (2026-07-27) — IEC 62366-1 / FDA HFE Guidance 2016. Use specification (T1 general consumer, Parkinson's/post-stroke accessibility target, caregiver, T2 clinician/patient). Use-related risk analysis: 7 critical tasks (CT-01 zone module insertion/keying through CT-07 HRV coherence score misinterpretation), each cross-referenced to its existing NP-RM-001 design control. Formative evaluation plan (§7): general usability n=5–8 (Month 10–12) plus the named eject-lever accessibility study (n=5, Parkinson's Hoehn & Yahr II–III / post-stroke hemiplegia; requires NP-IRB-001; gated on FAI-A09 bench pass). Summative validation plan (§8): n=15 minimum, Month 14–18, no coaching, per FDA HFE Guidance pass/fail. Traces to DI-USE-01..07 and DI-PERF-24. 8 open items (OI-HFE-01..08). Closes NP-DT-001 OI-DT-03 and discharges DI-REG-07 (plan-authoring portion; formative/summative execution remains open per NP-DT-001 VE-12). CT-01's accessible position-ID gap closed 2026-07-31 by the child document NP-HFE-002. |
| Accessible Zone-Module Position Identification and Guided Placement | `docs/np_hfe_002.md` | NP-HFE-002 Rev 2 (2026-08-20), DRAFT — child of NP-HFE-001, closes CT-01's open accessible-position-ID gap and NP-DT-001 DI-USE-05. Supersedes RISK-15 Layers 3/4/5 (braille + raised numeral on module; N tactile dots on shell; bone-conduction insertion confirmation) rather than re-scaling them: the hex lattice tessellates, leaving only a few-mm structural web between sockets where an ISO 17049 braille cell needs ~6 × 10 mm; an 80-member tactile-symbol set is not discriminable; a universal module mould cannot carry position; and the socket count is provisional so any per-socket moulded artefact re-tools on every lattice re-cut. Design follows NP-HEX-ZM-001 §4a's identity split — tactile marking follows module *type* (closed 3–4 set), position moves to guidance. Five layers: L1 tactile landmark grid (dashed midline spine whose gaps are the six midline sockets incl. Oz; three band ridges coded 1/2/3 bumps at the spine crossing; uniform standard-electrode-site marker ×9, gated on REG-1; counting depth ≤2 rows / ≤4 sockets); L2 companion-app guided placement as primary channel (exception-structured work list, landmark grammar, corrective-movement error messages, native VoiceOver/TalkBack incl. refreshable braille, offline BLE); L3 1–4 raised bars on the module non-mating face; L4 the delivered `np_module_map_check_placement()` gate; F tactile placement card for standard maps. Rejects per-socket tactile symbols and per-socket touch-to-speak (~80 new sense channels; no pre-insertion sensing exists — `np_module_map_apply_poll()` sees a module only once electrically seated). Requirements HFE-R-01..15; 9 open items (OI-HFE2-01..09). Records the RISK-15 severity reframe (recommend S2, pending change order) and the DONT-CARE/wavelength map-authoring gap (HFE-R-15). No HFE data yet. **Rev 2 (2026-08-20), DRAFT and unapproved — §7.3's L3 encoding replaced: 1/2/3/4 raised bars (a count, bounded at ~4-5 by §2.3(a)) become a nested figure — side, corner, open U, closed square, square+diagonal, X-in-square — reaching 6 types, because a closed figure is recognised whole rather than counted, and because it turns a flat 1-of-N identification into a two-stage decision of branching <=3. Adds HFE-R-16/17, OI-HFE2-10 (validate the 3-vs-4 discrimination in OI-HFE2-03's formative) and OI-HFE2-11 (L1(d)'s premise contested in opposite directions by NP-HW-EEGNET-001). OI-HFE2-06 extended with the 2D-patch area demand. Bar row retained as explicit fallback, capping the taxonomy at four types.** |
| Post-Market Surveillance Plan | `docs/np_pms_001.md` | NP-PMS-001 **Rev B (2026-07-27)** — §3.1 Intake channels adds website contact form (distinct from in-app form), phone support line (Home Premium / T2 service contract priority-support holders only), and website chatbot (first-line capture, escalates to human agent under the same 1-business-day rule). Rev 1 (2026-07-27) — ISO 13485:2016 §8.2, 21 CFR Part 803 (T2 post-510(k)), ISO 14971:2019 §10. Complaint definition/intake (§3, 21 CFR §820.198 record fields). Post-market data sources and cadence (§4): SHDR fleet trending (ties to CLAUDE.md §5.2 predictive-maintenance phases), complaint database, service/repair data, literature scan. MDR regulatory-reporting decision tree (§5, T2 post-clearance only). CAPA/risk-management linkage (§6, feeds NP-RM-001 §12 post-market review triggers). Handoff procedure with NP-SEC-BR-001 for incidents with both safety and privacy dimensions (§7). EU MDR PMCF forward reference, not currently active (§8). Annual PMS review (§9). 4 open items (OI-PMS-01..04). Closes the "(NP-PMS-001, planned Year 2)" placeholder in NP-QMS-001's process map and the NP-RM-001 §12 post-market feedback loop reference. |
| IRB Protocol | `docs/np_irb_001.md` | **NP-IRB-001 Rev 2 (2026-08-16, DRAFT pending PI/IRB signature)** — Rev 2 adds a *layers are not screens* clarification to §6: the four a priori consent layers L1–L4 are unchanged and remain the units of consent and withdrawal, so every L3 citation in this protocol still resolves; since CLAUDE.md Rev 37 they are *presented* on two screens (S1 = L4 + L1, S2 = L2 + L3). No consent axis was merged — L2 (scope) and L3 (posture) remain independent controls and selecting all nine categories does not set L3, which is the property this protocol relies on when describing Type B consent as a standing mechanism that still admits per-study refusal. No consent mechanism, study scope or withdrawal right changed. Rev 1 (2026-07-27) — formalizes NP-PRIV-REM-001 STEP-20. Distinguishes Type A studies (device usability/accessibility, e.g. the NP-HFE-001 §7.2 eject-lever study) from Type B (clinical/observational research using NeurOne as a data platform, per NP-FW-ANON-001). Primary pathway: Rashidi-Ranjbar / Unity Health Toronto REB, with parallel 45 CFR §46.104(d)(4) exemption filing for Type B on-device-anonymized studies. Fallback: Jog/UCLA, or WCG/Advarra commercial IRB with CEO as sponsor-investigator (requires CITI certification + OHRP FWA). Consent mechanism (§6) distinguishes the app's standing a priori research consent (Type B) from a separate in-person informed-consent form (Type A). 6 open items (OI-IRB-01..06), including per-study Expert Determination certification (NP-ANON-CERT) which remains separate from this protocol's IRB approval. |
| FHIR R4 ImplementationGuide | `docs/np_int_fhir_001.md` | NP-INT-FHIR-001 Rev 1 (2026-06-03) — NeurOne T2 Clinical Profile. Permitted resource types: Patient (opaque MRN only, no name/DOB/address), Observation (EEG band power ratios, HRV RMSSD, coherence score, session dose), DiagnosticReport (weekly session summary), Procedure (Full Clinical tier only). LOINC codes for HRV; NeurOne local codes for EEG spectral features. LSL TLS encryption requirement. FHIR validator CI integration spec. 5 open items (OI-FHIR-01 through OI-FHIR-05). Addresses NP-PRIV-REM-001 STEP-14, NP-COORD-001 G3-09. |
| App Analytics and Crash Reporting Policy | `docs/np_app_telemetry_001.md` | NP-APP-TELEMETRY-001 Rev 2 (2026-06-03) — Rev 2: `session_sequence` (raw integer, health-adjacent) replaced with `engagement_tier` coarsened 3-bucket enum (new/active/established); §3.2 added with implementation requirements. Rev 1 content: analytics vendor and crash reporter requirements; 9 permitted event properties; prohibited properties; SDK initialization gate (post-consent-flow only); crash reporter configuration; SwiftLint/Android Lint linting rule; annual review cadence. |
| Core iOS App ISA | `app/ios/ISA.md` | E4 ISA for Issue #51 (2026-06-04). 164 ISCs across 17 groups: BLE GATT, session display (Mode 1), protocol upload (Mode 2), session history + EDF (Mode 4), UHDR data, SHDR upload, clinical consent, privacy compliance (age gate, BIPA, HealthKit, analytics gate), consumable tracker, OTA, setup wizard, Watch bridge, App Store readiness. Three known validator safety gaps flagged by unit tests (app-side charge density, PBM dose, zero-duration — all `XCTExpectFailure`-wrapped pending implementation). PR #106 (feature/ios-parallel-integration). |
| iOS App Development Roadmap | `docs/np_app_roadmap_001.md` | NP-APP-ROADMAP-001 Rev 2 (2026-06-03) — Rev 2 adds §9 Privacy Constraints (binding engineering constraints): §9.1 HealthKit data residency (no transmission to NeurOne servers); §9.2 minimum age gate (16+ declaration before consent flow); §9.3 biometric (BIPA-derived) written release screen — **shown to ALL users, universalized 2026-07-10** (OI-PA-03 resolved, locale gate removed); §9.4 Adaptive Adjustments card in Session History (plain-language trigger enum); §9.5 SDK init gate cross-reference. New open items OI-WA-06, OI-PA-01 through OI-PA-04. |
| Firmware Privacy Remediation Delta | `docs/np_fw_emmc_002.md` | NP-FW-EMMC-002 Rev 2 (2026-08-12) — **§H SHDR accelerometer characterisation programme (2026-08-12)**: §G.2's two thresholds are unvalidated placeholders and §G.3 forbids the data that would validate them, so §H opens a time-boxed, opt-in, warranty-owner-consented window collecting a coarsened per-gap impact histogram, purpose-bound to predictive-maintenance training, rows deleted at close. Window denominated in **records, not calendar time** (no clock survives a disconnect on this hardware); expiry fail-closed in firmware; `CHAR-01` CI gate exemption derived from its own verdict and therefore self-revoking. Implementation landed with the spec: `firmware/shdr/`, SHDR schema Rev E. OI-EMMC2-09…14 raised; **OI-EMMC2-12 BLOCKING before enrolment**, **OI-EMMC2-11 needs a principal decision**. Rev 1 (2026-06-02): §A warranty token architecture (256-bit TRNG, no-join rule, CI test); §B device factory reset procedure (12-step sequence, SANITIZE, power-loss resilience); §C two-layer UHDR key scheme (UKMD + WKMD, biometric change re-wraps 32 bytes only); §D Scratch partition encryption for research anonymization (AES-256-CTR session key, atomic pipeline, SANITIZE post-run); §E EDF+ patient header policy (opaque token, X fields, header validator in anonymization pipeline); §F Mode F spec (default-off, separate consent, triple-pulse ambient indicator, daily cumulative dose limit, regulatory gate flag). Supersedes conflicting sections of NP-FW-EMMC-001 Rev 1; to be incorporated in NP-FW-EMMC-001 Rev 2. |
| Research Anonymization — Scratch Encryption Firmware | `firmware/anon/` | IEC 62304 SW-02 Class B — implements NP-FW-EMMC-002 Rev 1 §D. **Encryption primitive** (`np_anon_scratch.c/.h`): per-run AES-256-CTR key + 16-byte nonce from TRNG, held in SRAM only (never written to eMMC); `np_anon_scratch_init/write/read/complete` lifecycle; `memset_explicit` key/staging-buffer zeroing (with non-elidable C11 fallback); eMMC SANITIZE on completion; `NP_SNVS_ANON_IN_PROGRESS` flag (SNVS_LPGPR2) set before first write / cleared last; `np_anon_scratch_resume_after_powerloss()` bootloader recovery path (§D.6). Disjoint CTR counter-window guard (`block_offset × NP_ANON_AES_BLOCKS_PER_SCRATCH_BLOCK`) prevents adjacent-block keystream reuse — a correctness fix over the §D.3 spec pseudocode. **Atomic pipeline orchestrator** (`np_anon_pipeline.c/.h`, added 2026-07-10): `np_anon_pipeline_run()` sequences the §D.5 six-step transaction (init → stage_uhdr → transform → validate → emit → complete) via an injectable step vtable; enforces the two §D.5 invariants — (A) NO PARTIAL EXTRACT: `emit` (sole output-queue writer) reached only if stage→transform→validate all pass; (B) ALWAYS TEAR DOWN: `np_anon_scratch_complete` (key-zero + SANITIZE + flag-clear) runs on both success and every abort path after init. Transforms (UHDR decrypt, k-anonymity/l-diversity/DP, EDF+ header validation, study-pubkey extract encryption) injected as callbacks — live in NP-FW-ANON-001 / uhdr_key / edf. `np_anon_step_t` reports failing stage for app/SHDR notification. **HAL stubs** (OI-ANON-AES-01..05): TRNG, AES-256-CTR (CAAM/DCP), eMMC scratch read/write, SANITIZE — real backing pending. 13 host tests (`np_anon_tests.c`): init/roundtrip/invalid-args/secret-clear/power-loss + 7 pipeline tests (success, abort-at-stage/transform/validate, emit-failure, null-args, init-TRNG-failure). Wired into `firmware/CMakeLists.txt` + `firmware-host-tests.yml`. Cross-build `-Wall -Wextra -Werror` clean. Cryptography-reviewer sign-off (OI-EMMC2-02) outstanding before pending-decisions.md §13.4 checkbox closure. |
| EDF+ Privacy Header — Writer + Validator Firmware | `firmware/edf/` | IEC 62304 SW-02 Class B — implements NP-FW-EMMC-002 Rev 1 §E (commit 81f5bf5, verified 2026-07-14). **Writer** (`np_edf_writer.c`): `np_edf_write_header()` fills a byte-exact packed 256-byte `np_edf_header_t` (compile-time size-checked) — `local_patient_id` = `"NP"` + 14 lowercase-hex chars of UHDR token bytes 0–6 (16-char opaque patient code) + ` X X NeurOne_User` (sex/birthdate/name per §E.2); `local_recording_id` = `"Startdate <DD-MMM-YYYY> NeurOne X NeurOne_v<fw>"`; `startdate`/`starttime` derived from `session_ts`. Self-contained: file-local integer `gmtime`, hex, and space-pad helpers — no libc string/time, no heap, non-NUL-terminated fixed-width fields, byte-identical on host + Cortex-M7. **Validator** (`np_edf_validator.c`): `np_edf_validate_privacy_header()` (§E.4) fail-closed — rejects non-`'X'` sex/birthdate, real patient names (`np_edf_header_contains_real_name()`), and any non-`"NP"` code prefix. **Anon-pipeline gate** (`np_edf_anon_gate.c/.h`): `np_edf_anon_gate_filter()` implements §E.4 — the per-file, exclude-not-abort staging filter. For each candidate EDF+ header it runs `np_edf_validate_privacy_header()`, sets `included[i]`, and on a violation fires an injected `np_edf_shdr_fault_cb` (code `NP_EDF_SHDR_EV_PRIVACY_HEADER_VIOLATION` 0xE1) then CONTINUES — never aborts the extract (returns `NP_EDF_OK` even with exclusions). Distinct from the anon pipeline's extract-level `validate` callback (k/l/DP, aborts); decoupled from `hub_control` via the injected callback. **11 host tests** (`np_edf_tests.c`): OI-EMMC2-05 100-headers loop + 3 validator rejection paths + prefix + 256-byte size + fw-version-in-recording-id + 4 gate tests (mixed pass/exclude with fault index+code assertions, all-pass, all-fail-does-not-abort, null/empty/NULL-cb), 0 failures. `-Wall -Wextra -Werror` clean host + cross (`arm-none-eabi-gcc` 14.2.1, Cortex-M7). Wired into root `firmware/CMakeLists.txt` (`add_subdirectory(edf)` + `add_test`) and CI `firmware-host-tests.yml`. **Residual (integration):** writer wires into the first EEG session recorder (unwritten); the study-extract runner that feeds real staged files into `np_edf_anon_gate_filter()` and maps its fault callback onto `np_log_shdr_fault()` is the remaining hub/anon seam. |
| CAPA Procedure | `docs/np_qms_capa_001.md` | NP-QMS-CAPA-001 Rev 1 (2026-05-13) — Issue #33. Corrective and Preventive Action procedure per 21 CFR §820.100 and ISO 13485:2016 §8.5.2/§8.5.3. Trigger sources (8 categories: design verification/validation failure, software problem report, internal audit, supplier non-conformance, risk escalation, post-market complaint, regulatory finding, SHDR proactive trend). Severity classification (Critical/Major/Minor) with response time requirements. 8-step CAPA process (initiation → containment → RCA → action planning → implementation → effectiveness verification → closure → management review). Root cause analysis methods per severity (fishbone/FTA for Critical; 5-Why for Major). CAPA log specification and format. CA vs PA distinction. Regulatory reporting obligations: MDR (21 CFR Part 803) within 30 days for death/serious injury; 5-day imminent hazard; Field Safety Corrective Action triggers. Record retention (5 years minimum). Closes Issue #33 CAPA process requirement. |
| iOS App Development Roadmap (Rev A) | `docs/np_app_roadmap_001.md` | NP-APP-ROADMAP-001 Rev 1 (2026-05-11) — Issue #32. iOS app development roadmap with Apple Watch sync app as Year 1 post-launch deliverable. Core iOS app scope (§3). BLE GATT custom service definition (§5). Watch app: 4-phase development plan — Phase 1 session status + HRV breathing ring (Month 1–2, App Store target Month 3); Phase 2 audio sync to AirPods via WatchConnectivity (Month 3–4); Phase 3 haptic sync 40Hz Core Haptics (Month 4–5, pending thermal characterization OI-WA-01); Phase 4 40Hz visual flicker (Month 6+, blocked on OI-WA-02 screen brightness ≥100 nits at 40Hz). Sub-50ms sync latency target. Regulatory: all Watch functions declared session monitoring / UI aids; App Store Health & Fitness category; no therapeutic claims. BOM delta $0. 5 open items (OI-WA-01–05). Addresses Issue #32 first DoD checkpoint. |
| Hub PCB — Cranial Socket Interface, I2C Fan-Out and PD Analog Front End | `docs/np_hw_hub_001.md` | **NP-HW-HUB-001 Rev 5 (2026-08-18, DRAFT — Rev 5 is a documentation-only §7.2 correction: the mask exclusion is defence against a future authoring error, not a hub that could set a retired zone bit — none exists or can. Rev 4 (2026-08-16, DRAFT — current)** — **OI-HUB-C07 DECIDED: the cranial PBM safety enable is one Class C policy bit** (§7.2.1). Per-cluster *policy* — independently commanded cluster bits — is rejected; the 18 per-cluster physical gates are **retained** and are IEC 62304 **Class B** availability gates, with the single `NP_SAFETY_EN_PBM_CRANIAL` bit in series, so **R-11 is preserved**. The falsifier `NP-HW-HEXTILE-001` §8.4.1 demanded (a hazard requiring a single-cluster cut where a whole-lattice cut is itself unacceptable) was searched for across `NP-RISK-003`, `NP-RISK-004` and `NP-FMEA-001` and **could not be produced** — and the absence is **structural, not evidential**: no hazard in the tree has an extent of one cluster, because hazard extents are physical (a tile heats, a modality accumulates charge, a rail collapses) while a cluster is a clamp-plate/FPC boundary chosen for one-handed serviceability. A supporting exhaustive case analysis shows the middle tier has no case of its own: when the Class B per-socket tier works it cuts 1 tile of the ~6 concurrently active (better than a cluster); when it is untrusted the per-cluster **gate** is right and that gate is retained at Class B; when the hub is untrusted no Class B granularity is reliable and the whole-lattice Class C cut is correct. **The availability benefit of per-cluster granularity is explicitly acknowledged as real and assigned to Class B** — the peers' shared concession that coarser is never less safe is NOT restated as a claim that finer is safer. **Enable word unchanged; no Class C behaviour change; no firmware code change** (the 2026-08-05 collapse already implemented the decided layout, and `test_enable_word_layout` was falsified against a synthetic cluster bit to confirm it bites — 3 assertions fire). New derived requirement **HUB-REQ-C05** (§7.2.2): the Class B per-cluster gate must be commanded from a tier *above* the cluster controller it gates, or a wedged controller is uncuttable at exactly the granularity the gate exists to provide. New **OI-HUB-C20**: safety-MCU STM32G071 package selection, **unblocked** (demand ~40 → ~23 I/O), now gating G1 layout, sequenced after OI-FMEA-06 re-derives `NP_NTC_CHANNEL_COUNT`. Two consequences routed, not resolved: `RISK-SHELL-03`/`OI-RISK4-01` (polarity — basis changed, **not re-scored**) and `OI-HEXTILE-14` (tail conductor/connector count — **not absorbed**). **Three rationale corrections applied at commitment-boundary review, none changing the decision:** (i) the extent claim is stated as an **empirical null over a closed enumeration** of the current hazard list, with **hazard-list completeness named as the residual assumption** and `OI-FMEA-06` flagged as the live work that could disturb it — not as an analytic impossibility; (ii) **IEC 62304 classifies software items, not gate transistors**, so §7.2.1 now carries an explicit assignment table — the 18 gates are **hardware risk controls with no 62304 class**, the hub software commanding them is Class B, the safety-MCU software stays Class C — and records that **no item is downgraded from C to B** (the gates previously had no stated class at all) and that the gates are **not load-bearing for any Class C hazard**, foreclosing a circular reading; (iii) *"no Class C behaviour change"* is split into **no runtime behaviour change** (true, verified) and **a design-control change whose verification consequence is design-review scope, not re-verification**. **New `OI-HUB-C21` raised, not resolved: a single-bit enable sits at Hamming distance 1 from the unsafe state** — pre-existing since 2026-08-05 and true of **all ten** modality bits, so orthogonal to C07; the conventional Class C form is a coded pattern plus complement. Urgent because §8.4.2's re-layout window is time-boxed to the first SHDR fault record. Deliberately deferred: it is a real Class C *behaviour* change needing its own hazard analysis and FMEA-M01-01/M01-03 re-checks. **Cato cross-vendor audit could not run — `codex` absent — so no independent-family audit is claimed.** — *Rev 3 (2026-07-29, DRAFT) detail follows:* — closes the SMART-1 residual. Finding: the binding constraint on scaling to the ~80-socket hex lattice is **interconnect, not I2C mux count** — Rev 2's star needs ~880 conductors at the parting plane, 160 TIA channels and 80 `GAIN_SEL` GPIO, none of which a deeper mux tree fixes. Replaces it with a **distributed cluster-controller tier** on the inner bowl: one board per 8 sockets (`ceil(n/8)`; 10 at n=80, 16 max = 128 = full 7-bit socket domain), each carrying an STM32G071, one PCA9548A for I2C isolation, a 16:1 PD current mux into **one shared switchable-gain TIA**, and the per-socket LED drive; hub↔inner-bowl is one differential I2C bus. Rev 2's DG2788A topology and Si-vs-InGaAs saturation analysis carried forward unchanged, relocated and shared rather than replicated 80×. **The hub PCB no longer encodes the socket count** — REG-1 no longer blocks hub tooling. Zero `GAIN_SEL` GPIO (gain set per-sample on a cluster-MCU pin from live UID inventory). PD1/PD2 ratio accuracy improves (shared TIA ⇒ gain error common-mode; Rev 2's ≤0.5% matching requirement retired). Safety enable collapses to one `NP_SAFETY_EN_PBM_CRANIAL` bit — per-cluster bits do not fit the 16-bit frame (OI-HUB-C07). Flags that calibration coefficients must be keyed to **module UID**, not socket (OI-HUB-C06). BOM $6.34/cluster → $63.40 at n=80; hub itself ≈ +$1.09. Rev 2 archived verbatim as Appendix A. OI-HUB-C01…C12; HUB-DRC-C01…C15. — *superseded Rev 2 detail follows:* NP-HW-HUB-001 Rev 2 (2026-05-13) — Issue #62. OI-PBM-HW-01 CLOSED (SPECIFIED). Five Vishay DG2788A dual-SPDT analog switches (one per zone slot) switch TIA Rf 47kΩ→22kΩ when ZONE_ID debounce confirms smart module (ADC < 1100); GPIO GAIN_SEL[0..4] on i.MX RT1062 GPIO_B0_04..08. Required to prevent InGaAs PD1 TIA saturation (0.90 A/W × 72µA × 47kΩ = 3.38V exceeds 3.3V ADC rail). NXP PCA9546A I2C bus switch (4 channels, 0x70) isolates all 5 smart module slots sharing I2C address 0x30 (OI-PBM-HW-02 CLOSED). 12× 4.7kΩ pull-ups, 10× 47kΩ and 10× 22.1kΩ feedback resistors. Total hub BOM delta $1.41–1.91. Sequencing: ZONE_ID debounce → GAIN_SEL assert (NP_TIA_GAIN_LOW) → 10µs settle → I2C mux enable → I2C probe. Boot init: GAIN_SEL[0..4] driven LOW before zone detection task. Removal: I2C mux disable → GAIN_SEL deassert (NP_TIA_GAIN_HIGH). Firmware changes in firmware/pbm_1064nm/: np_pbm1064_hal_tia_gain_set(), np_pbm1064_hal_tia_gain_boot_init(), np_tia_gain_t enum, np_sm_slot_ctx_t.tia_gain field, np_pbm1064_detect.c sequencing. Hub PCB Rev B Gerber build (OI-HUB-03) required before FAI-SM-04 and FAI-SM-06 optical bench tests. **⚠ SUPERSEDED — Rev 2 was sized for 5 zone slots; superseded 2026-07-28 by SMART-1 and replaced 2026-07-29 by Rev 3 above. Rev 2 open items OI-HUB-01…05 are all closed as moot; its Gerber is never to be released. Retained verbatim as Appendix A of `docs/np_hw_hub_001.md`.** | **OI-HEXTILE-14 closed 2026-08-18:** every cluster-count-dependent figure re-sized off 12 (and off `ceil(n/8)` = 10) to **18**, with **20** connector positions provisioned so a REG-1 re-cut needs no PCB re-spin; `NP-HW-HUB-001` §7.4/§6.3/§5.2/§8.2/§8.5 and HUB-DRC-C02 corrected, HT-DRC-20 now passes. The 12 → 11 conductor reduction that OI-HUB-C07's closure unblocks is **not** taken — HUB-REQ-C05 leaves open how the Class B gate is commanded.
| Hub Control Program | `firmware/hub_control/` | NP-FW-HUB-001 Rev 1 — FreeRTOS application for NXP i.MX RT1062 hub. Four tasks: (1) Module registry (np_hub_module_registry) — enumerates installed modules at boot and on hot-plug by UID-based auto-inventory (`np_module_map`) across the hex-socket lattice; maintains the socket state table; (2) Session runner (np_hub_session_runner) — deserialises Ed25519-signed .npps session descriptor; dispatches stimulation commands to per-modality HAL drivers; enforces session-level dose limits; (3) Session log (np_hub_session_log) — routes 27-element data classification table to UHDR/SHDR partitions per NP-FW-EMMC-001 Rev 1 §12; (4) Safety SPI (np_hub_safety_spi) — 200ms heartbeat to Safety MCU; drives 1.5s watchdog; captures fault log to SHDR. **⚠ SPEC DOES NOT EXIST.** This entry has always pointed at `docs/np_fw_hub_001.md`, which is absent from the repository — the code is the only description of this program. Tracked as **OI-DOC-01**. Two claims in the previous version of this entry were also retired design and have been corrected above: "probes all 11 zone + accessory slots" (the lattice is 80 sockets addressed by UID auto-inventory, not 11 slots) and "drives zone_announce for bone conduction" (the bone-conduction announce tone was removed — see NP-FW-ZA-001 / NP-HFE-002 Rev 1 §7.2). SW02-M09..M12 per NP-SW-001. G2-14 PARTIAL. |
| NeurOne Protocol Script (NPPS) Language Reference | `docs/np_npps_ref_001.md` | NPPS v1.0 — text-based session protocol language compiled to binary session descriptors signed by Ed25519. 15 modality blocks (eeg, bes, tdcs, pbm_transcranial, pbm_intranasal, vns_hrv, audio, visual, tms, tacs, hd_tdcs, cervical_vns, pbm_1064nm, hrv_biofeedback, vibrotactile). 19 predefined protocol templates (alpha_train, focus_prime, sleep_deep, gamma_40hz, pbm_vascular, pbm_gamma, combined_standard, combined_deep, emdr_ptsd, tms_depression_rTMS, tms_depression_TBS, ad_prevention, stroke_rehab, pd_motor, anxiety_hrv, pain_vnseeg, cognitive_hd, combined_4modal, t2_depression_full). Compiler validates all fields, dose limits, and interlock requirements before signing. Enables clinical users to script custom protocols without firmware modification. |
| Helmet Simulator | `simulator/` | NP-SIM-001 v0.3.0 (2026-05-17) — Issue #81 / PRs #76, #84, #85. Browser-based interactive 3D simulator (no build step, Three.js 0.164 via importmap CDN). Three purposes: (1) test target for macOS/iOS/Windows apps; (2) marketing and training tool embeddable in any web page; (3) clinician protocol development tool. Three.js helmet geometry based on Neuronic Light dome layout, adapted to NeurOne spec: 5 snap-in LED zone panels (ZM-01–ZM-05) with animated per-LED canvas textures; integrated goggles; over-ear audio cups with bone conduction piezo; occipital Boa arch + dial; control hub with status LEDs (power breathes, session LED pulses at freq/4 per §4.7); 8 spring-decoupled EEG pods (impedance color-coded green/amber/red); VNS auricular clip with PPG; intranasal Y-probe (animated — docked/inserting/inserted/removing; probe tip LEDs sync to active PBM session; hygiene sleeve overlay); mastoid haptic pad; **T2 TMS focal figure-8 coil** (Issue #79 — CLOSED). Wavelength false-color scheme: 660nm shown as actual red (#FF2200); 808nm → orange (#FF7200); 1064nm → amber (#FFBB00); 1170nm → gold (#FFE500) — longer actual wavelength maps to longer apparent visible wavelength. 9 NPPS protocol templates: alpha_calm, focus_prime, gamma_40hz, sleep_deep, anxiety_hrv, combined_4modal, pbm_vascular, pbm_1064_deep, tms_depression. Session engine tick-driven with ramp envelope; HRV breathing ring animates at 0.1Hz; timeline with phase segments; 4 camera presets. WebSocket device API: Node.js server (simulator/server/index.js, ws://localhost:9000); two client roles (controller = external app, display = browser tab); SESSION_START with Ed25519 test-key verification; ZONE_CONFIG; ACCESSORY_CONFIG; TELEMETRY at ≥10 Hz; FAULT; SESSION_COMPLETE; graceful standalone fallback when server absent; API badge in header goes green on connect (Issue #77 — CLOSED PR #84). ACCESSORY_CONFIG message type relays accessory state changes (intranasal probe states) from controller to display (Issue #78 — CLOSED PR #85). TMS coil (Issue #79 — CLOSED): figure-8 geometry at DLPFC_L (phi=0.38π, theta=0.25π); two Torus windings + CFRP housing + tapered handle + grip ridges + cable; CFRP non-conductive window ring at coil-shell interface; blue-white pulse animation with PointLight flash at rTMS rep rate; TMS modality pill in UI status bar; tmsCoil ACCESSORY_CONFIG key; HOWTO updated to v2.2. Open sub-issues: #80 (geometry update pending shell CAD). |
| Privacy Notice | `docs/np_priv_notice_001.md` | **NP-PRIV-NOTICE-001 Rev 4 (2026-08-16)** — Rev 4 adds §5.1 and §5.2 for the four-screens-to-two consent restructure (CLAUDE.md Rev 37 §6.2). **Published notice — external effect.** §5.1 describes what each of the two screens asks and states two things a user could otherwise get wrong: that opting into study results grants no one any access to their data, and that selecting all nine research areas is *not* the same as pre-approving studies (the app never links those settings automatically, in either direction). §5.2 tabulates the three withdrawal levels — single study, research area, pre-approval — and their differing effects, including that only pre-approval withdrawal also switches off app research analytics, and states plainly that the shorter path to consent has not made withdrawal coarser. No data processing changed and no consent option was removed. Rev 3 (2026-07-10) — Rev 3 adds §10 (biometric EEG written release + retention/destruction policy) and §11 (no-sale of all user data incl. SHDR behavioral counts), both stated UNCONDITIONALLY for all users; universalizes protections driven by BIPA (IL) and MHMD (WA); "Changes" renumbered to §12. Rev 1 (2026-06-03) — Issue #98 / NP-PRIV-REM-001 STEP-33. Full app privacy notice. §2: UHDR/SHDR architecture explained in plain English. §3: app analytics data (engagement_tier enum, SDK gate). §4: **Automated processing and adaptive stimulation** — GDPR Art. 13(2)(f) disclosure covering adaptive algorithm logic, parameters that can change (audio beat, PBM pulse, tACS, VNS phase), what is recorded (classified trigger enum + parameter delta, no raw EEG/HRV), and user's right to view adjustments in Session History. §5: research data architecture (on-device anonymization, consent withdrawal effectiveness). §6: clinician access. §7: GDPR/CCPA rights table with exercise mechanisms. §8: retention table. §9: security summary. OI-PA-04 OPEN — GDPR §4 copy requires Privacy Lead sign-off before first public beta. |
| App Privacy Audit | `docs/np_priv_audit_001.md` | NP-PRIV-AUDIT-001 Rev 1 (2026-06-03) — STEP-21 of NP-PRIV-REM-001. iOS/Android app pre-beta privacy analysis. Jurisdiction scope: US federal (FTC Act §5, FTC HBNR, HIPAA), US state (BIPA IL, MHMD WA, CCPA/CPRA CA, state omnibus), EU/EEA GDPR, UK GDPR, Canada PIPEDA. 16 findings: 0 Critical, 5 High, 7 Medium, 4 Low. Five High findings: (1) analytics/crash vendor not selected — PrivacyInfo.xcprivacy cannot be completed; (2) SDK initialization gate not CI-verified; (3) App Store privacy nutrition label and Google Play Data Safety not filed; (4) BIPA written release screen not implemented — OI-PA-03 open; (5) Washington MHMD compliance for SHDR device session counts unverified. 16-item compliance checklist (AUDIT-01 through AUDIT-16), all OPEN — BLOCKING for first external beta. Four audit open items (OI-AUDIT-01 through OI-AUDIT-04). Top recommended actions: (1) select analytics + crash vendor, execute DPA/BAA (Month 6); (2) implement + CI-verify SDK init gate before any test build; (3) BIPA + MHMD legal opinions before US activation; (4) commission research consent UX formative study; (5) author adaptive stimulation trigger enum before session runner firmware. **Updated 2026-07-10:** the two US-state HIGH findings (BIPA written release, MHMD SHDR behavioral data) are RESOLVED by universal application — the biometric written release ships to all users and the MHMD no-sale + standalone-authorization protections apply to all users; the BIPA/MHMD legal opinions now confirm scope only and do NOT gate activation (AUDIT-09/AUDIT-10 CLOSED; advisory AUDIT-09a/10a remain). |
| Consumable Tracker Privacy Analysis | `docs/np_priv_analysis_003.md` | NP-PRIV-ANALYSIS-003 Rev 1 (2026-06-08) — Consumable tracker feature audit (ISC-154). Jurisdictions: All. 0 Critical, 0 High, 0 Medium, 2 Low findings. LOW-1: notification lock-screen title reveals NeurOne device ownership and consumable type without device unlock — fix: generic title on lock screen, consumable detail in body (after unlock). LOW-2: `requestNotificationPermission()` called in `ConsumableTracker.init()` at app launch before onboarding — fix: defer to `ConsumableView.onAppear`. What looks good: UHDR/SHDR boundary structurally enforced by `ConsumableCountsProviding` protocol; consumable counts correctly classified as SHDR; `orderURL` uses static enum rawValue (not user data); no outbound network calls; notification `userInfo` contains only `consumableIndex` (Int 0–3). No blocking items for internal beta. |
| Privacy Analysis and Repair (second pass) | `docs/np_priv_analysis_002.md` | NP-PRIV-ANALYSIS-002 Rev 1 (2026-06-05, PR #112 + session 2) — Code-level privacy audit of iOS app source. Jurisdiction scope: US federal + state (BIPA IL, WA MHMD, CCPA/CPRA), EU/EEA GDPR, UK GDPR, Canada PIPEDA. 12 findings: 1 Critical, 3 High, 5 Medium, 3 Low. All 12 code-level findings resolved. PR #112: (1) UHDRKeyManager PBKDF2 hardcoded password → `#error` compile gate + `.deviceOwnerAuthentication` PIN fallback; (2) SHDRUploader `identifierForVendor` → opaque Keychain warranty token; (3) SHDRUploader staging file excluded from backup; (4) UHDRBackupScheduler backup dir excluded from iCloud/iTunes + `keyFingerprint` removed from manifest; (5) `ResearchAnalyticsGate.reset()` wired to `ConsentStore.revokeResearchAnalytics()`. Session 2: (6) MEDIUM-07 — `SessionRecord.completedAt: Date` → `sessionDay: String` + `insertionIndex: Int`; (7) MEDIUM-08 — `epoch` excluded from WC bridge payload; (8) LOW-10 — `redacted(_:)` helper in `ConsentDashboardView`; (9) LOW-11 — SPKI certificate pinning on `SHDRUploader` fleet session; (10) LOW-12 — `kSecAttrAccessible` filter in `SessionProtocolSigner` Keychain load query. OI-PA-03 (BIPA locale detection) and OI-PA-04 (Adaptive Adjustments Privacy Lead copy sign-off) remain as process open items. |
| SW-01 Safety MCU Unit-Level FMEA | `docs/np_fmea_001.md` | NP-FMEA-001 Rev 2 (2026-06-15) — IEC 62304 §7.1 Class C FMEA for SW01-M01..M08. 43 failure modes across: GPIO management (M01), SPI heartbeat watchdog (M02), charge density monitor (M03), thermal interlock (M04), cervical VNS cardiac interlock (M05), impedance check (M06), session signature verification (M07), fault latch (M08). ISO 14971 S1–S5 / P1–P5 scoring. Two initial UNACCEPTABLE risks (S×P=10): FMEA-M03-01 (charge accumulator overflow, mitigated by 64-bit uint64_t) and FMEA-M05-02 (HR delta integer underflow, mitigated by int16_t signed arithmetic + MISRA C:2012 Rule 10.1); both residual risk ≤3 (ACCEPTABLE). 5 open items: OI-FMEA-01 (watchdog GPIO cutoff bench ≤50ms), OI-FMEA-02 (cardiac interlock bench <5.1ms), OI-FMEA-03 (Ed25519 key flash CRC at startup), OI-FMEA-04 (MISRA scan), OI-FMEA-05 (update after FAI-CV02 hardware bench). Rev 2 (OI-SW01-M07-02 CLOSED, PR #132): §3.7 description corrected to state Ed25519 is backed by np_crypto/Monocypher 4.0.2; FMEA-M07-05 mitigation updated to reference Monocypher ct_memcmp. |
| T2 Clinical Scripting API Specification | `docs/np_api_001.md` | NP-API-001 Rev 1 (2026-06-07, PR #121) — T2 clinical API specification (BAA required; must exist before any T2 API code is written). §2: auth/tiers (Monitor $49/mo, Assess $149/mo, Full Clinical $299/mo, Research $599/mo), UHDR three-condition access gate (ALL THREE: explicit consent + API toggle + use-case scope). §3: 8 REST endpoints — POST /protocols (NPPS compile + Ed25519 sign), POST /devices/{id}/sessions, GET /devices/{id}/status (SHDR-only), GET+POST /patients/{token}/fhir/Observation/DiagnosticReport/Procedure, GET /patients/{token}/export (bulk, separate credential), GET /audit. §4: WebSocket API (controller/display roles; SESSION_START/PAUSE/STOP/ZONE_CONFIG/ACCESSORY_CONFIG/TELEMETRY/FAULT/SESSION_COMPLETE/ERROR); UHDR telemetry fields gated at session-start time. §5: FHIR data model (NP-INT-FHIR-001). §6: privacy governance — SHDR-only default; consent withdrawal → 403 UHDR_ACCESS_DENIED for all data periods; research tier k≥10 l-diversity l≥3 DP ε≤1.0. §7: rate limits (1,000/hr, 10,000/day). §8: error codes (17). §9: TLS 1.3 mandatory; certificate pinning; key vault required; CI boundary tests; independent security audit (NP-SEC-PENTEST-002) before any external clinical key. 7 open items (OI-API-01..07). G1 gate item (NP-PRIV-REM-001 STEP-15). |
| Design Input/Output Traceability Matrix | `docs/np_dt_001.md` | NP-DT-001 Rev 1 (2026-06-07, PR #122) — Design I/O traceability per 21 CFR §820.30(c)/(d) and ISO 13485:2016 §7.3. §3: 57 design inputs — DI-PERF-01..25 (performance: EEG, PBM, stimulation modalities, UHDR/SHDR, OTA, EMF, fit, zone modules, HRV), DI-SAFE-01..12 (safety: charge density HW limit, SPI watchdog, CVNS cardiac interlock, photoparoxysmal, IEC 62471 MPE, Ed25519 signature, tDCS ramp, thermal cutoff, fault latch, TMS EMF gating, Mode F, impedance check), DI-USE-01..07 (usability), DI-REG-01..12 (regulatory: IEC 60601-1/2-10/62471/62133, FCC Part 15, IEC 62304, ISO 14971, 21 CFR 820, GDPR), DI-INT-01..05 (interface). §4: 35 design outputs (DO-HW-*, DO-FW-*, DO-SW-*, DO-API-*, DO-FHIR-*, DO-RISK-*, DO-REG-*, DO-TEST-*). §5: full DI→DO→VE traceability matrix with status (Traced/Partial/Open). §6: 13 verification evidence entries (VE-01..13). §7: gap analysis — safety-critical blocking gaps identified (hardware bench items, standards lab testing). §8: DHF completeness upgraded from Partial to Good. G2 exit criterion per NP-DP-001 §6.4. 5 open items (OI-DT-01..05). |
| SW-01 Safety MCU Firmware Source (all 8 modules) | `firmware/safety_mcu/` | IEC 62304 Class C — STM32G071 (Cortex-M0+, 64 MHz, bare-metal, no RTOS). Build: `firmware/cmake/stm32g071.cmake` toolchain + `firmware/safety_mcu/CMakeLists.txt` (produces `.elf/.bin/.hex`). Linker script (`startup/stm32g071_flash.ld`): 128 KB Flash at 0x08000000, 36 KB SRAM at 0x20000000, `.fault_latch` NOLOAD **pinned** to top 64 bytes of SRAM via `. = _fault_latch_start;` (not zeroed on warm reset — placement bug fixed 2026-06-08). Startup (`startup_stm32g071xx.s`): copies .data, zeros .bss skipping .fault_latch, calls main. Config (`include/np_safety_config.h`): all GPIO pin assignments, timing constants, limits. Protocol (`include/np_safety_protocol.h`): 8-byte SPI frame types, `NP_SAFETY_STATUS_*` bits, `NP_SESSION_STATUS_*` bits (bit 0 = ACTIVE, bit 1 = CVNS_REENABLE), `np_safety_state_t`. SW01-M01 `np_gpio_mgr`: owns 14 stimulation GPIOs active-LOW (LOW = enabled, HIGH = disabled — startup drives HIGH). SW01-M02 `np_spi_watchdog`: tick on valid frame; fault flags extracted to local variable (ISR race fix); timeout check **every loop iteration** via `np_spi_watchdog_check()` (1500ms cutoff); CUTOFF bit cleared only when no other interlock active. SW01-M03 `np_charge_monitor`: uint64_t per-electrode accumulator (`s_charge_nc[NP_SAFETY_MAX_CHANNELS]`); per-channel limits (`s_limit_nc[]`, default 1000µC, override via `np_charge_monitor_set_channel_limit()`); accumulate() called every heartbeat for granted channels from `current_ua[]` in extended 38-byte frame; OI-CHARGE-01 CLOSED (2026-06-16). `np_charge_monitor_set_channel_area_mcm2()` derives a per-channel limit from electrode AREA (`limit_nc = NP_CHARGE_LIMIT_UC_CM2 × area_mcm2`); called from the main loop when the hub delivers a 34-byte `np_safety_chan_limit_cmd_t` — OI-CHARGE-02 CLOSED (2026-07-07): HD-tDCS CLIN_STIM (ch 13) limited to 3.84µC. Density constant stays on the MCU (hub sends area only). OI-CHARGE-03 CLOSED (2026-07-07): `np_charge_monitor_geom_gate()` blocks CLIN_STIM from granted_mask when the hub asserts `NP_SESSION_STATUS_GEOM_REQUIRED` (heartbeat bit 2) but no valid area has been applied (`geom_applied` latch, reset per session) — fail-closed against a lost area command. Runs before the accumulate loop; scoped to CLIN_STIM (clinical tACS unaffected). SW01-M04 `np_thermal_interlock`: NTC ADC lookup (0–110°C, 1°C resolution), 10ms poll, 62°C cutoff. SW01-M05 `np_cardiac_interlock`: RR buffer (8 intervals); `s_first_beat_seen` guard prevents phantom first interval; `rr_to_bpm()` saturates at INT16_MAX (overflow fix); `current_hr_bpm()` called once per tick (ISR race fix); baseline refresh gated on `!s_cutoff_active` (desensitization fix); rolling baseline refreshed every 5s (NP_CARDIAC_OBS_MS); lockout via elapsed subtraction (wrap-safe at ~49 days); `s_cutoff_active` reset when lockout expires; `np_cardiac_interlock_reenable()` clears CARDIAC status + resets baseline on hub CVNS_REENABLE signal. SW01-M06 `np_impedance_check`: pre-session and post-cutoff impedance check for VNS/tDCS/BES/CVNS; polled every loop iteration; IMPEDANCE status bit cleared on successful recheck (permanent-lock bug fixed); OI-CVNS-HUB-11 per-electrode CVNS impedance read + 8-byte report packing (additive to enable gate). SW01-M07 `np_session_sig`: Ed25519 OTP pubkey verification via `np_crypto` (Monocypher 4.0.2, RFC 8032, SHA-512); `np_session_sig_reset(state*)` sets `NP_SAFETY_STATUS_SIG_PENDING` on session 0→1 transition; `np_session_sig_verify()` clears SIG_PENDING on valid sig; SIG_PENDING in `active_faults` mask blocks `granted_mask` until hub delivers 102-byte `np_safety_sig_cmd_t` frame via `np_safety_spi_send_session_sig()`. OI-SW01-M07-02 CLOSED (2026-06-11). OI-SW01-M07-01 CLOSED (2026-06-16). SW01-M08 `np_fault_latch`: `.fault_latch` section persists through warm reset; magic sentinel 0xDEADBEEF; commit change-gated (count tracks distinct fault transitions, not loop iterations); first-loop overwrite of warm-reset latch prevented by `s_prior_latch_reported` flag in main; SHDR marshalling via the single fixed-shape `np_fault_latch_build_report()` → `np_fault_latch_report_t {status, slot, count}` — every member reported for every fault type, no branch on the status word; `tick_ms` is NOT reportable (fault event timing is UHDR) and stays device-internal. **Changed 2026-08-12:** replaces the retired independent accessors `np_fault_latch_report_tick_ms()`/`_report_count()`, whose CARDIAC→tick_ms=0 suppression made `count > 0 && tick_ms == 0` a one-bit cardiac oracle; 23 host assertions incl. a falsified cardiac/non-cardiac indistinguishability guard. Second-pass review: 9 additional bugs corrected 2026-06-08. |
| Firmware Build Super-Project + CI Workflows | `firmware/CMakeLists.txt`, `.github/workflows/` | `firmware/CMakeLists.txt`: dual-mode CMake super-project — `-DNP_BUILD_TESTS=ON` runs host-native tests + default cross-compiles all modules for i.MX RT1062. Three CI workflows: `firmware-host-tests.yml` (Ubuntu, cmake + ctest); `web-ci.yml` (Ubuntu, Bun install → tsc → vitest → vite build); `ios-ci.yml` (macOS-14, xcodebuild test with iPhone 15 simulator). `.gitignore` rewritten with project-appropriate rules. Host test suite grows as modules add `add_test` entries (safety_mcu SPI-proto, charge monitor, cvns_reenable, log_backend, transport, anon, edf, factory_reset, uhdr_key, ota, crypto, freertos smoke, fault-latch privacy, impedance report). CI trigger paths extended per module directory. |
| Hub Enclosure Tooling Specification | `docs/np_tool_hub_001.md` | NP-TOOL-HUB-001 Rev 1 (2026-07-27) — closes the `docs/status/pending-decisions.md` §13.4 "Hub tooling" item. Four molded features on the single hub housing tool (no separate mold, unlike the smart zone module variant): F-01 intranasal probe dock (Y-junction saddle sized to junction body OD + two silicone-padded bilateral probe-tip receptacles, ≤2N retention — prevents Y-junction fracture in storage, CLAUDE.md §3); F-02 two tethered anchor posts for the hub's own USB-C and DFU/service port covers (≤20mm tether, bounded so a detached cover cannot reach the F-04 fan intake); F-03 large-radius Boa cable channel continuing the shell's PTFE-lined, 50,000-cycle channel through the hub housing at ≥12×-lace-OD minimum bend radius (exact radius pending supplier lace datasheet, OI-HTOOL-02); F-04 tool-free fan/heatsink service door — two diagonally-opposed quarter-turn captive fasteners, ≤6mm louvre slot width for finger-probe safety, supporting the SR-FAN-05 predictive-maintenance alert (RISK-26 / FMEA-G07-01) with a user-clearable cleaning path. Hub placement (occipital arch, external to the 5-layer shielded shell) is inferred, not newly asserted, from three already-locked/adopted facts: the EEG harness routing origin at "Hub PCB EEG connector" (NP-DRV-SHELL-001 Rev 2), hub-resident antennas (CLAUDE.md §4.1), and the external fan-cooled heatsink terminating the BN-boss thermal export (NP-THERM-CFD-R1-001 §5) — the exact hub-to-shell mechanical interface remains open (OI-HTOOL-01). 11-item mold design review checklist (HUB-MDR-01–11), 4 FAI items (FAI-HTOOL-01–04, one BLOCKING), 6 open items (OI-HTOOL-01–06). |
| BLE GATT Layer (iOS) | `app/ios/NeurOne/BLE/` | SW-03 iOS App — BLE GATT implementation for NeurOne hub connectivity (2026-06-08). Three files authored/rewritten: `BLECentral.swift` (`BLECentralManager` protocol wrapping `CBCentralManager`, zero-cost extension conformance, testable without BLE hardware); `GATTCharacteristics.swift` (`warrantyToken` UUID `4E455550-0010-…` added, READ 32B SHDR-linked TRNG token per NP-FW-EMMC-002 §A, excluded from `NPUUID.all`, count 14 = 9 NOTIFY + 5 WRITE); `NeurOneGATTManager.swift` (rewrite: `@Published` state, internal apply methods `applyStateUpdate`/`applyCharacteristicAssignment`/`applyDisconnection`/`applyWarrantyToken`, SHDR early-return guards, explicit `.littleEndian` EDF request serialization). 25 unit tests (25/25 pass). Code + privacy review; 5 findings applied. OI-BLE-01 open (SHDRUploader hub-token upgrade path — app side since implemented 2026-07-14, residual hub firmware OI-WA-03). |
| Firmware Cross-Compile CI Plan | `docs/np_sw_ci_001.md` | NP-SW-CI-001 Rev 1 (2026-08-06) — **DRAFT**. Records that no CI verifies the firmware cross-compiles for its target: `firmware-host-tests.yml` configures `-DNP_BUILD_TESTS=ON`, and that branch of `firmware/CMakeLists.txt` (lines 122–242) ends in `return()`, so the entire cross-compiled tree below line 243 is unreachable in CI. Consequences: the STM32G071 safety MCU (SW-01 **Class C**, separate CMake project + separate toolchain file) is built by no workflow; `firmware/bootloader` and `firmware/hrv_biofeedback` are reachable only via the cross-compiled path and so have no CI at all — they are correctly absent from the host-test `paths:` filter, which could not build them if triggered, so the gap is the missing workflow rather than filter drift. Three blocking defects measured by running the cross-compile locally (ARM GNU 14.2.1) — all three targets configure `rc=0` and fail to build `rc=2`, host-test control builds clean: **Defect A** safety MCU `GPIOA`/`GPIOB` undeclared (STM32G0 CMSIS/HAL headers not vendored); **Defect B** bootloader 19 unresolved `memcpy` + 7 `memset` under its intentional `-nostdlib -nodefaultlibs`; **Defect C** bootloader OCRAM 520 KiB used of 512 KiB (101.56%) — not code size (objects total 26.1 KB) but linker-script arithmetic, `.app_staging` ALIGN(65536)+448K consuming the region to its last byte before `.stack` adds 8 KiB. Defect C needs a product decision on maximum OTA image size (OI-SWCI-02), not a build fix. Specifies `.github/workflows/firmware-cross-build.yml` as a 3-leg matrix (safety MCU / bootloader / main firmware) landing with `continue-on-error: true` and promoted per leg across a 6-phase rollout; size gating delegated to the linker's own `--print-memory-usage` + `ASSERT` rather than a second parser. **Rev B (2026-08-08)** adds §9: **all SDKs are vendored in-tree, no build-time fetching** — closes OI-SWCI-01 and unblocks phases 4–5, following the FreeRTOS (`firmware/vendor/freertos/`, byte-exact subset + explicit not-vendored list) and Monocypher (`VERSION` per IEC 62304 §8.1.2) precedents. Flags a consequence: the STM32G0 CMSIS/HAL feeds the SW-01 **Class C** safety MCU, so it is Class C SOUP and carries the §7.1.2 anomaly-list review obligation — a higher bar than the existing Class B FreeRTOS case; vendor the narrowest subset accordingly. Raises OI-SWCI-06 (CMSIS device headers only vs full ST HAL — different Class C consequences). 6 open items (OI-SWCI-01 CLOSED, 02–06 open). Found during the PR #250 workflow least-privilege pass. |
| **Configuration Cost Model — Hex-Tile Re-derivation** | `docs/np_cost_001.md` | **NP-COST-001 Rev 2 (2026-08-16)** — **ACTIVE.** Re-derives the CLAUDE.md §2.1 BOM / COGS / GM% columns against the hex-tile architecture and replaces the stopgap caveat in `NP-DB-005` §4. **Retail is locked and untouched; GM% is the output, not an input.** **Result: all four T1 configurations are gross-margin negative at the floor** — Home Standard $405 → $897–959 BOM and +36% → −41% to −51% GM, Home Lite worst at −50% to −64%, Home Premium −7% to −14%, Core −23% to −45%; **Pro is unaffected** (+50% / +73%) because its retail is 3–6× its cost. §3 corrects the commissioning framing: the cluster tier ($114.12), the L1 carrier and the socket arrays ($32–64) are **not additive** — `NP-DRV-SHELL-002` **Rev 2** §10.1's "$175–225, all lines" already contains all three and supersedes Rev 1's $125–216. §4 finds the dominant term is not the cluster tier at all but `NP-HW-HEXTILE-001` §6.4's **$11.53/tile driver + metering**, ~$10 of which is two InGaAs photodiodes — $346 at 30 tiles, 63–70% of the whole increase. §6 runs `OI-HEXTILE-06`'s three options and finds **none restores a positive T1 margin** (−33% / −8% / −4% combined); the residual is structural because the L1 tier alone is 36–51% of the entire old BOM. **§5 establishes that `OI-HUB-C08` cannot be closed from the record**: `OI-HEXTILE-02` has selected no 660/808 nm emitter, so per `NP-ART-001` §5 the dominant BOM line is unpriced on **both** sides of the subtraction — and OI-HUB-C08 is under-scoped, covering only drive electronics while the larger emitter-count delta (600 → up to 2,814) was owned by nothing (**OI-COST-07**). Every figure is therefore a **floor** excluding term **U**, which is very likely positive and large. §2 states the three inputs that **do not exist anywhere in the document set** as assumptions rather than burying them — per-configuration tile population (**OI-COST-01**), full-L1-per-configuration (**OI-COST-04**), and a per-configuration BOM→COGS multiplier (**OI-COST-05**), the last because **no single BOM→COGS rule is recoverable** from the six published pairs: the multiplier is neither constant nor monotonic (1.537 → 1.333 → 1.745) and no fixed markup, adder or linear fit reproduces them. §7 confirms CLAUDE.md §2.2 charger policy unaffected. Socket count ~80 remains PROVISIONAL pending REG-1/ACT-1 and every derived figure inherits that. Raises OI-COST-01…10. **Rev 2 (2026-08-16) — retail pricing UNLOCKED by principal direction; §8 added.** Rev 1 §1–§7 preserved verbatim as derived *under the lock*, since the lock is what made margin an output. §8 publishes the implied ladder — retail = COGS ÷ (1 − original GM target) — giving **break-even Home Standard $1,196–1,278 and target margin $1,869–1,997, a 2.20–2.35× increase on $849**; Core $955–1,121; Home Lite $1,445–1,587; Home Premium $2,475–2,637. **These are implied, not set**: unlocking a constraint is not choosing a price, and all six configurations keep the prices in force pending a separate commercial decision. **Break-even binds before margin does** — Home Standard cannot be sold below ~$1,196 at any margin. §8.3 records three consequences the lock concealed: the T1 ladder **collides with the T2 ladder** (**OI-COST-08**); **Pro is where the target, not the cost, should be questioned** — both Pro rows profitable today, so the unlock is not a mandate to raise T2 prices; and **every competitive price claim is live again** because Rev 1 §7's "safe to use" finding rested entirely on the lock (**OI-COST-09**). §8.4 fixes the **binding sequence — decide `OI-HEXTILE-06` before setting a price** (**OI-COST-10**), since silicon PD plus a 20-tile build moves the implied Home Standard price $1,997 → ~$1,383. All §8 figures inherit §5 floor status. |
| **Manufactured Artifact Register and Documentation Readiness** | `docs/np_art_001.md` | **NP-ART-001 Rev 1 (2026-08-11)** — **ACTIVE.** Enumerates the fifteen manufactured artifacts (A1–A15) and records, per artifact, whether its tooling specification, risk register and FAI checklist exist or are blocked — **with every blocker named by open-item ID rather than described in prose**, which is what makes it a work list. Written to answer the replacement question for the retired `NP-FAI-ZM-001`. §3.1 delivers one checklist; §3.2 names the eleven that cannot yet be written and exactly what must close first (GATE-1/GATE-2 and OI-HEXTILE-01 for the tile; OI-HEXTILE-11 and SH2-DRC-05a for the socket; OI-SHELL2-04/05/11 for the cluster board; MECH-1 for L1; MECH-1/EMF-1/EMF-3/RISK-20 for the shell; MECH-2 and OI-SHELL2-03(b) for the clamp; OI-HUB-C01…C19 for the Hub PCB). **Principal finding (§4): nine of fifteen artifacts have no owning specification document, four of them shipping T1 modalities** — the intranasal Y-probe, audio cup assembly, auricular VNS/HRV clip and TMS coil exist only as `CLAUDE.md` prose. The gap was invisible while the document set was six-deep on the zone module and zero-deep on everything else. §5 records the three architecture-coupled documents deliberately **retained** rather than retired (`NP-TOOL-SHELL-001`, `NP-COORD-001`, `NP-PROC-FPC-001`), because retiring each would leave a live artifact with no governing document at all. Raises OI-ART-01…06. |
| **First Article Inspection Programme** | `docs/np_fai_001.md` | **NP-FAI-001 Rev 1 (2026-08-11)** — **ACTIVE.** Separates the FAI *method* from the per-artifact *checklists* and from the *artifact register*, which `NP-FAI-ZM-001` conflated. **§2 states the four conditions (F1–F4) under which a checklist may be issued at all** — governing spec BASELINED/ACTIVE, every dimension traceable or explicitly gated, a supplier category for the process, and failure modes in a risk register — the rule that produced `NP-ART-001` §3.2's blocked list. §3 keeps the nine-section pattern and adds the `[GATED: OI-xxx]` marker so an item can exist without an invented accept criterion. §4–§5 carry forward the two qualifications that are properties of **materials and process**, not of the retired geometry: PDMS–PI bond (FAI-M01…M03, FAI-TC01…TC06) and post-service ingress (FAI-IPX-01…05). **Two criteria tightened rather than relaxed by the architecture change**: FAI-IPX-02 grows from 50 swaps over 5 seals to ~300 over ~30, and FAI-TC04a adds a 1064 nm transmittance check the original predates. **FAI-TC02 remains BLOCKING and has never been run** — the programme's oldest open production gate, unaffected by the hex-tile decision. Raises OI-FAI-01…04. |
| **Hub Enclosure FAI Checklist** | `docs/np_fai_hub_001.md` | **NP-FAI-HUB-001 Rev 1 (2026-08-11)** — **DRAFT.** 26 items across the nine-section pattern, expanding `NP-TOOL-HUB-001` §5's four-line stub (FAI-HTOOL-01…04 preserved by number). **The only NeurOne artifact FAI meeting `NP-FAI-001` §2's F1 condition**, because `NP-TOOL-HUB-001` is the sole artifact specification in the set with status BASELINED. Two items issued `[GATED]` rather than given fabricated criteria: FAI-HUB-11 (Boa channel bend radius — the spec states ≥12× lace OD and the OD is not on file, `OI-HTOOL-02`) and FAI-HUB-16 (**the hub enclosure has no environmental rating in any NeurOne document**, `OI-HTOOL-03`). Two `[BLOCKING]` items cover hazards that mitigations introduced: FAI-HUB-23 (a loss-prevention tether reaching the fan intake) and FAI-HUB-24 (finger access through the tool-free service door). §9 records RISK-HUB-01…03. |
| **Risk File Re-Baseline and NP-RISK-001 Disposition** | `docs/np_risk_002.md` | **NP-RISK-002 Rev 1 (2026-08-11)** — **ACTIVE. The ISO 14971 risk file of record**, with `NP-RISK-003` and `NP-RISK-004`. §3 dispositions **all twenty-six RISK IDs individually**: 5 RETIRED (mechanism no longer exists — RISK-01/07/09 connector-family hazards with no connector left, RISK-15 keying with no wrong socket, and **RISK-11**, whose dynamic-flex fatigue set `NP-DRV-SHELL-002` §8.2 makes *empty* — the largest reliability gain the architecture change bought), 20 CARRIED with original IDs and meanings (10 → NP-RISK-003, 9 → NP-RISK-004, RISK-25 held in §4), and **RISK-24 CLOSED-CONFIRMED** — the risk was that the ≥15 mm PBM-to-EEG separation might be unachievable; it was investigated, found impossible at any geometry, and the requirement was withdrawn in favour of the <5 µVpp threshold it had been a proxy for. **RISK-16 is the one risk the change made worse** (5 → ~30 series perimeter seals). Nothing is re-scored — deliberately; disposition and re-rating are separate acts under `NP-RM-001` §8.2, and OI-RISK2-01 holds the re-scoring. §4 surfaces that **three shipping T1 modalities have never had a hazard analysis** (OI-RISK2-02). §5 records five new hazards under prefixed IDs so the `RISK-NN` sequence stays closed at 26 and append-only. §6 amends `NP-RM-001` §8.1. |
| **Hex-Tile Module Risk Register and Problem Analysis** | `docs/np_risk_003.md` | **NP-RISK-003 Rev 1 (2026-08-11)** — **ACTIVE.** Artifacts A1/A2. Carries RISK-02, -03, -04, -05, -06, -08, -14, -16, -19, -23 plus RISK-HEX-01…03. **Problem analysis states the governing trade — one part repeated ~30 times instead of five unique parts — and the two rows where it costs**: perimeter seals rise 5 → ~30 for a *series* IPX4 claim with no seam-length budget set, and the emitting face shrinks ~3× while carrying the same optical furniture (PD1 baffle, PD2 aperture, NTC, gasket groove). RISK-16 carried at raised severity on that basis. Records that the **unpropagated 1.0 mm bezel decision (OI-HEXTILE-01) is the highest-leverage open item on the artifact and is an unpropagated *answer*, not an open question** — a 14.5 % swing in active field area that moves every irradiance figure and every future FAI criterion. Raises OI-RISK3-01…04. |
| **Shell, Socket, Interconnect and Hub Risk Register and Problem Analysis** | `docs/np_risk_004.md` | **NP-RISK-004 Rev 2 (2026-08-16)** — **`RISK-SHELL-03` basis changed by the OI-HUB-C07 decision; no score re-assessed, no hazard added or removed.** New **§2.1** records that the conflicted `SAFE_EN[n]` line is no longer a Class C stimulation enable but a Class B availability gate with a Class C broadcast line in series — which changes what the entry's consequence column describes but **does not close it**, because a Class B gate defaulting to enabled at reset still energises a rail and the safety claim now rests on the Class C line's own reset polarity, the same unresolved convention question one level up. **CRITICAL stands as the score**; re-scoring is hazard analysis rather than editorial correction, per the `NP-FMEA-001` Rev 4 precedent, and the note explicitly warns against reading it as a downgrade. `OI-RISK4-01` no longer waits on OI-HUB-C07 and gains two inputs: the Class C line's reset polarity, and **`HUB-REQ-C05`** (commanding authority for the Class B gate) — polarity and commanding authority being the same question asked twice. Verification map updated to state the item still has no verification defined, and why. — Rev 1 (2026-08-11) — **ACTIVE.** Artifacts A3–A9. Carries RISK-10, -12, -13, -17, -18, -20, -21, -22, -26 plus RISK-SHELL-01…04 and RISK-HUB-01…03. **`RISK-SHELL-03` is the only CRITICAL entry**: `SAFE_EN[n]` polarity is inverted between `NP-DRV-SHELL-002` §6 (LOW = disabled) and the safety MCU (`np_safety_config.h:7-8`, LOW = enabled) — both internally coherent and fail-safe on their own terms, together meaning a power-on reset that is the *safe* state under one convention is **stimulation enabled across the cranial lattice** under the other. It was found by applying a naming convention mechanically, not by review. §1.1 records that the interconnect stopped being a cable and became a distributed system (18 continuously dissipating controllers behind ~59 % of the outward thermal resistance, RISK-SHELL-02). §1.2 records the pattern by which the ≥15 mm separation requirement was correctly withdrawn: keep the threshold, replace the mechanism. §1.4 names two hub hazards created by their own mitigations. Raises OI-RISK4-01…05. |
| **Shell Interconnect Design Review Record** | `docs/np_rev_shell_001.md` | **NP-REV-SHELL-001 Rev 1 (2026-08-11)** — **DRAFT, open review.** Restores the design-review *record* instrument lost when `NP-DRV-SHELL-001` Rev 2 was retired — its §5 checklist was replaced by `NP-DRV-SHELL-002` §11, but its §8 eight-signatory sign-off block, the artifact that actually gated shell tooling first cut, had no successor. **§0 answers the routing question directly: the requirements exist and were restated rather than lost** (REQ-BR2-01…05 bend radius, REQ-SKT-01 socket array, REQ-EMI-03…11), and the 33-item checklist exists; only the record was missing. **Holds no criteria of its own** — reviewer, date, verdict and evidence per item only, pointing at `NP-DRV-SHELL-002` §11 for what is judged, so the two cannot drift. Three items open **BLOCKED** rather than unreviewed, each naming its missing input: SH2-DRC-13 (`OI-CONV-01` — reviewing it either way would record a pass for a state unsafe under the competing convention), SH2-DRC-17 (`OI-SHELL2-07`), SH2-DRC-20 (`OI-HUB-C07`). §3 marks the three items that must be verified by mechanical diff, and requires the diff be **falsified before it is trusted**. Raises OI-REVSH-01…04. |
| **Universal Hex-Tile Module Shell Tooling Specification** | `docs/np_tool_hextile_001.md` | **NP-TOOL-HEXTILE-001 Rev 1 (2026-08-11)** — **DRAFT, Pre-Tooling.** Replaces `NP-TOOL-ZM-001` Rev 1 and `NP-TOOL-ZM-SM-001` Rev 1: **six position-unique tools become one universal mould**, the tooling win `NP-HEX-ZM-001` §6 took the hex decision for. §1.2 dispositions all eight retired features — **F-01 key, F-02 raised numeral and F-03 braille all retire for one reason: identity moved from the module to the socket, so a module can no longer name its own position**; F-08's eject lever moves to a different part (the cluster clamp) and **its ≤1 N accessibility figure has no equivalent in the replacement's 34.2–57.0 N plate load** (RISK-22); four features carry forward. Records that **`NP-TOOL-ZM-001` F-04's *POSITION LOCKED* PD2 coordinates are void** — they were the centre of a 66 × 78 mm array, the clearest instance in the set of a locked number surviving an architecture change unnoticed. Adds F-TH-01 orientation-only key, F-TH-02 19-contact staggered pad pocket, F-TH-03 spring-plunger landing, F-TH-08 **two** NTC locations (junction-side and scalp-facing — FMEA-G07-01 is the record of why one was insufficient). §1 and §3 prohibit any type-specific variant or per-type colour, which is what protects the one-mould return. Steel cut gated on GATE-1 + GATE-2. Raises OI-THEX-01…06. |
| **Superseded Document Index** | `docs/superseded/README.md` | Index of every retired document with its successor named, and the rules that apply inside that directory: revision labels are **not** converted (`NP-CONV-001` §1.1 — rename forward, never backward, so the mapping table resolves old citations), filenames are not normalised, and nothing is edited except to add a supersession banner. Distinguishes wholly-retired documents from partly-retired ones, and records the three architecture-coupled documents deliberately **not** moved. |
