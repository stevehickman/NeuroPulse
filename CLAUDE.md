# CLAUDE.md — NeurOne Design program
**Project:** NeurOne — closed-loop multi-modal neuromodulation wearable platform  
**Revision:** 28 (current)  
**Status:** Pre-tooling design phase. No hardware committed yet. All decisions below are locked unless explicitly noted as pending.

---

## 1. PRODUCT OVERVIEW

Two-tier platform sharing a single chassis, processor stack, app, and USB-C connectivity.

| Tier | Name | Regulatory | Modalities | Price range | Timeline |
|------|------|-----------|------------|-------------|----------|
| T1 | NeurOne Home | FDA-exempt wellness | 8 | $449–$1,199 | 12–18 months |
| T2 | NeurOne Pro | FDA 510(k) target | 11 | $4,999–$13,999 + $1,800/yr | 18–36 months post T1 |

**Founding design principles:**
- Shared platform (one production line, two markets)
- Wired-first USB-C default (zero RF at scalp)
- Autonomous closed-loop EEG-adaptive stimulation without phone (primary competitive moat)
- 5-layer EMF shielding + active Helmholtz cancellation (only consumer brain wearable with measured shielding)
- Modular field-upgradeability via snap-in zone modules
- No mandatory subscription — all core functions offline-capable permanently
- UHDR/SHDR data separation (user health data never accessed by NeurOne)

---

## 2. CONFIGURATIONS + PRICING (all locked)

### 2.1 Integrated system configurations

| Config | BOM | COGS | Retail | GM% | Modalities included |
|--------|-----|------|--------|-----|---------------------|
| Core — EEG only | $168–169 | $258–260 | $449 | 42% | 4-ch EEG · all connectivity · EMF shielding · processor stack · 8GB eMMC |
| Home Lite | $265–266 | $370–372 | $599 | 38% | Core + PBM 5 zones (660+810nm, 600 LEDs) · 8-ch EEG · VNS+HRV clip |
| Home Standard ★ (flagship) | $405 | $540 | $849 | 36% | All T1 modalities (see §3) |
| Home Premium | $460 | $622 | $1,199 | 48% | All T1 + EC lens (+$89 value) · 2yr warranty · priority support |
| Pro Entry | $833 | $1,365 | $4,999 | 73% | All T1 + 21-ch qEEG · 1170nm deep PBM · clinical tACS · HIPAA cloud · sLORETA |
| Pro Full | $1,506 | $2,628 | $13,999 | 81% | All T2 + TMS hub · multi-patient dashboard · scripting API · FHIR R4 · $1,800/yr service |

**★ Home Standard box contents:** All T1 modules · hard clamshell case · braided aramid USB-C cable (spare in box) · **45W NeurOne branded GaN charger** · S1 opaque shade · interface covers (installed + spare set each type) · mesh cleaning brush · Boa replacement cable + hook tool · moisture-barrier electrode tip hydration caps · humidity indicator card · pre-impregnated cleaning cloth packets

### 2.2 Charger policy (locked)

Charger scaled to peak draw of configuration. Auto-included at every upgrade by serial number tracking. Upfront 65W upgrade option ($19 at-cost) offered at checkout as intent signal.

| Config | Charger included | BOM |
|--------|-----------------|-----|
| Core | 15W USB-C (unbranded) | $3–4 |
| Home Lite | 30W GaN (unbranded) | $5–6 |
| Home Standard ★ | 45W NeurOne GaN (branded) | $10 |
| Home Premium | 45W NeurOne GaN (branded) | $10 |
| Pro Entry | 65W NeurOne GaN (branded) | $13 |
| Pro Full | 65W NeurOne GaN (branded) × 2 | $26 |

**Charger upgrade intent signals:**
- Core buyer selects 30W upfront → PBM intent → 14-day follow-up
- Core buyer selects 45W upfront → Full T1 intent → 7-day completion bundle offer
- Any buyer selects 65W upfront → T2 intent → human clinical sales call within 48 hours

**EU note:** Chargers are branded recommendations, not proprietary requirements. Any PD-compliant charger must work. App displays "power level: reduced" informatively, never blocks.

### 2.3 Consumables + recurring revenue

| Item | Price | Interval | GM% | Notes |
|------|-------|----------|-----|-------|
| Intranasal sleeves (30-pack) | $19/pack or $19/mo sub | Single use | 68–79% | Only authenticated consumable. COGS $4–6. Primary MRR driver. |
| Electrode hydrogel tips (8-pack) | $12–16 or $9.99/mo sub | 30–60 sessions | 60–72% | App impedance trend prompts. Bayonet snap, zero training. |
| VNS clip pads (2-pack) | $8/pack | 20–40 sessions | 65% | Electrochemical degradation from VNS current. |
| Audio cup foam (set) | $24/set | 6–12 months | 58% | Calendar reminder. |
| Audio cup mesh frame (pair) | $9.99/pair | Annual | 62% | App driver impedance flags fouling. Snap-in, user-replaceable. |
| Interface protection covers (complete kit) | $22.99 or $19.99/yr bundle | Annual / as lost | 70% | All tethered — loss prevention by design. |
| S3 prescription Rx insert | $49–139 | 12–24 months | Variable | Optician partner network. Zero marginal marketing cost per renewal. |
| T2 service contract | $1,800/yr | Annual | ~75% | Same-day loaner, priority support, annual calibration. |

---

## 3. MODALITY STACK (all locked)

### T1 — 8 modalities

**1. PBM Transcranial**
- 660–670nm + 808–830nm LEDs (base module)
- **1064nm snap-in smart zone module upgrade (accessory):** Path B smart module architecture — on-module Microchip ATtiny402 I2C slave + 3× Infineon IRLML6344 N-FETs drives 660nm (CH_A), 808nm (CH_B), and 1064nm (CH_C) independently from existing 20-pin FPC connector. 550 LEDs per module (200+200+150). InGaAs PDs (Hamamatsu G12180-010A) for dose metering. Smart module detected via 3.3kΩ ZONE_ID resistor (pin 18, ADC < 1100 counts). Hub enables dedicated LPI2C3 bus (400kHz fast mode) per slot on detection. Hub PCB Rev B adds Vishay DG2788A TIA gain switch per slot (OI-PBM-HW-01, BLOCKING). Base modules unchanged. BOM delta +$23–28, retail $149–199/zone. See NP-FW-PBM1064-001 Rev A, NP-HW-FPC-001 Rev E.
- **T2 combined 1064nm + 1170nm session:** 1064nm smart zone modules (cortical depth) + existing 1170nm laser system (subcortical depth) coordinated by session orchestrator. Three-tier penetration stack: 660nm surface → 1064nm cortical → 1170nm deep. Thermal throttle priority: 1170nm throttled first, then 1064nm CH_C, then CH_B. See NP-FW-PBM1064-001 §8 and NP-SES-1064-001 §6.
- 5 independently addressable zones
- **600 total LEDs: 300×660nm + 300×808–830nm** on FPC strips (launches at 600 from day one — no Rev A/Rev B)
- 6mm inter-LED pitch → ±15–25% irradiance variation (near-uniform field)
- 120–180mA per LED → L70 80,000–100,000 hours
- **400 mW/cm² peak pulsed** (≤25% duty cycle, firmware-enforced) / 200 mW/cm² CW max
- **Dual photodiode dose-metering (RISK-14 Option B):** PD1 behind PDMS window (measures forward emission) + PD2 on scalp-facing surface (measures backscattered tissue power). PD1/PD2 ratio separates PDMS fouling from LED aging in firmware. Pin 19 (PD2_CATHODE). BOM +$0.75–1.50/headset. T1 and T2 use identical zone module mold.
- Plasma-activated anti-fouling PDMS optical windows. **PDMS–PI bond uses 75 nm SiO₂ interlayer (RF magnetron sputter) + O₂ plasma activation — achieves 174–860 N/m peel force.** 200-cycle IEC 60068-2-14 thermal cycling qualification required before production (BLOCKING).
- Real-time J/cm² dose metering — primary differentiator over Vielight
- 7 frequency presets: Gamma clarity (40Hz), Alpha calm (10Hz), Theta memory (6Hz), Sleep deep (2Hz), Gamma+theta coupled (40+6Hz split-zone), Focus prime (20Hz), Vascular baseline (CW)

**2. PBM Intranasal**
- Bilateral Y-probe · 660nm + 808–830nm per probe
- 15/20/25mm silicone over-molded depth stop rings (wear-resistant)
- Photodiode contact/dose sensing + reference LED at probe base
- Optical code + pogo pin resistive sleeve authentication (no NFC, no EMF)
- Hub dock storage (in hub tooling from day one — prevents Y-junction fracture)
- Hygiene sleeve consumable: 30-pack $19/pack or $19/mo subscription
- Silicone over-mold at Y-junction for impact protection

**3. EEG Neurofeedback**
- 8-ch semi-dry hydrogel · Fp1/2, F3/4, C3/4, P3/4
- 500Hz · 24-bit ADC (ADS1299)
- **ADS1299 internal reference self-calibration at every session start** (eliminates gain/offset drift)
- Spring-decoupled electrode pods: 80–120g contact force, ±12mm travel, independent of dial tension
- Replaceable hydrogel tips: snap-off bayonet, 30–60 sessions, $12–16/8-pack
- Moisture-barrier silicone hydration caps (WVTR <0.5 g/m²/day) — extend storage life to 24+ months

**4. BES / tACS (consumer name: Brainwave Entrainment Stimulation)**
- 0.5–40Hz · ≤1mA · charge-balanced biphasic
- Per-electrode impedance monitoring
- Adaptive EMF notch firmware prevents Helmholtz cancellation of therapeutic signal
- Regulatory naming avoids FDA medical device classification trigger

**5. tDCS (consumer name: Cortical Priming Stimulation)**
- 0.1–2mA DC · 40µC/cm² hardware limit (safety MCU enforced, app cannot override)
- 30s ramp up/down (hardware-enforced)
- ≤3 electrode pairs

**6. VNS + HRV + HRV Biofeedback**
- Auricular clip · auricular branch CN X
- 1–25Hz · ≤2mA · biphasic charge-balanced
- PPG HRV (808–830nm) in same clip
- **A1/A2 EEG references on clip contact pads** (2 spare conductors in existing 6-pin cable, +$15 BOM)
- PDMS hydrogel pads: 20–40 sessions, $8/2-pack
- Force contact confirmation
- **HRV Biofeedback Protocol (software only, no additional hardware):**
  - Resonance frequency breathing pacer: default 6 breaths/min (0.1 Hz); personalized to user's peak HRV frequency during first-session sweep (4–7 breaths/min range)
  - Breathing cue delivery: visual ring expanding/contracting in app + optional bone conduction audio cue (uses existing audio hardware)
  - Real-time coherence score: LF peak power / (LF + HF total power), displayed 0–10 color-coded
  - RMSSD displayed per session; session trend graph over 30 sessions
  - **Four protocols:**
    - Standalone coherence training (5–20 min, breathing pacer + coherence display)
    - HRV + taVNS synchronized: stimulation pulses timed to inspiration phase (PPG R-R interval detects respiratory cycle); optimises noradrenergic modulation window
    - HRV + EEG dual biofeedback: coherence score + EEG band power displayed simultaneously; closed-loop EEG-adaptive frequency adjusts to both signals
    - HRV + PBM: PBM running during HRV coherence training (replicates 2025 multi-modal RCT protocol: PBM + qEEG NF + HRV biofeedback)
  - UHDR: HRV time series, coherence scores, session logs
  - SHDR: coherence trend slope (no user biology)
  - Evidence: meta-analysis 24 RCTs (d=0.83 anxiety reduction, d=0.65 depression); 2025 multi-modal RCT (PBM + qEEG NF + HRV combined, nationally conducted RCT)

**7. Neural Audio Entrainment**
- Over-ear planar magnetic 40mm + bone conduction at mastoid
- Binaural beats + isochronic tones + pink/brown noise
- EEG-adaptive frequency (closed-loop)
- **User-replaceable snap-in mesh frame** (silver-coated nylon, 40dB RF, $9.99/pair annual)
- Aluminium bayonet mount (replaces plastic — wear-resistant)
- Driver impedance monitors mesh fouling (detects both acoustic degradation AND RF shielding loss simultaneously)
- Mesh cleaning brush in box
- Silicone isolator for bone conduction piezoelectric element

**8. Visual Stimulation**
- 108 micro-LEDs per lens (660nm + 808–830nm) · 6 zones per eye
- Inner PDMS diffuser film (plasma-activated anti-fouling)
- **AgNW (silver nanowire) outer conductive coating** — replaces ITO (ITO has 0.5% strain-to-failure; AgNW tolerates 5–10% flex)
- IR proximity sensors (940nm) — eye-open detection
- Hall sensor: goggle lift = instant LED cutoff
- IEC 62471 hardware MPE limit (50% of exempt group threshold)
- Photoparoxysmal EEG detection at Oz → goggle halt <200ms
- **Mode F (invisible NIR retinal walk):** 808–830nm daily retinal PBM during normal-looking wear
- EMDR L/R alternation · photic driving 0.5–100Hz

**Snap-on shade system:**
- S1 opaque (<0.5% VLT, included in box, instant cutoff for immersive sessions)
- S2 polarising (~12% VLT, $24, standard lens only)
- S3 prescription clip ($49 carrier + $49–139 Rx insert, compatible with both standard and EC lenses, 12–24 month renewal)
- 6× N42 neodymium magnets in lens rim (N42 not N52 — better impact tolerance, −$0.80 BOM)
- Sliding rail lens mount (user self-install, eliminates alignment jig requirement, +$1.20 BOM)

**EC lens (premium, +$89 upgrade / $129 standalone):**
- Bistable electrochromic 5–75% VLT · 2s transition · ~15mW hold
- Clears to 75% on power restore (safety failsafe)
- 3–5µm hard coat over EC film (scratch protection, standard in automotive EC mirrors)
- EC driver monitors transition time as contact resistance proxy (detects rim contact corrosion)

### T2 additions

- **21-ch qEEG wet gel:** Full 10-20 + FC3/FC4 (M1 TMS targeting) + Oz (photoparoxysmal detection) + A1/A2 (linked-ear normative reference, on VNS clips)
- **TMS focal figure-8 coil:** 0.1–0.5T · rTMS + TBS · non-conductive CFRP window at coil site · TMS-gated EMF cancellation (safety MCU gates Helmholtz off 5ms pre-pulse, 50ms post-pulse hold)
- **1170nm deep PBM:** Laser diodes · 35–40mm subcortical depth · TEC stabilisation · ≤1,000 mW/cm²
- **Clinical tACS:** ≤4mA · 16-ch arbitrary waveform
- **sLORETA-guided HD-tDCS:**
  - 4×1 ring montage: center anode + 4 return cathodes positioned by sLORETA source map — provides ~3–5× spatial focality vs standard 2-electrode tDCS
  - Electrode: Ag/AgCl sintered 3.5mm diameter, dual-rated for EEG recording AND stimulation current (simultaneous or sequential); part of T2 qEEG wet-gel cap
  - Current sourcing: 16-ch tACS driver (already in T2) provides independently controlled channels — no additional stimulation hardware
  - Workflow: (1) T2 21-ch qEEG resting-state session → (2) sLORETA computes cortical source map (real-time or post-session) → (3) app identifies target region (e.g., DLPFC hypoactivity, anterior cingulate hyperactivation) → (4) firmware maps MNI target to nearest 10-20 electrode positions → (5) configures 4×1 current distribution automatically → (6) delivers personalized tDCS session
  - Montage options: 4×1 ring (most focal, ~1.5cm FWHM), bilateral 4×1 (dual hemisphere), standard 2-electrode (T1-compatible fallback)
  - Safety: 40µC/cm² charge density limit enforced by safety MCU; ≤2mA per electrode; focal electrode density ≤6 A/m² (within Bikson lab safety limits for 3.5mm electrode geometry)
  - Clinical evidence: Jog/UCLA 2025 (n=71, personalized MRI-guided HD-tDCS, significant depression improvement + gray matter changes); BRIGhTMIND 2024 (n=255, connectivity-guided iTBS shows personalized targeting outperforms fixed F3)
  - BOM delta: Ag/AgCl dual-rated electrodes in T2 cap specification; no additional driver hardware; +$0 software
- **Cervical VNS (tcVNS) — T2 accessory:**
  - Neck-worn accessory stimulating cervical vagus trunk (higher activation than auricular branch CN X)
  - Gel electrodes applied to skin overlying carotid sheath; bilateral or unilateral
  - Indication: cluster headache + migraine (FDA-cleared precedent: electroCore gammaCore K163334, K173323); extending to depression, PTSD, post-stroke rehabilitation
  - Safety MCU ownership: current path near carotid → safety MCU reads impedance + cardiac rhythm monitor before enable; automatic cutoff if HR changes >15 BPM within 5s of stimulation
  - Regulatory: 510(k) predicate = electroCore gammaCore (K163334 cluster headache, K173323 migraine); separate 510(k) required for T2 product launch; T1 uses auricular-only (no carotid proximity)
  - Connects via existing hub accessory port; separate cable + electrode assembly; gel pad consumable (5-pack)
  - BOM delta: +$35–55 for cervical tcVNS accessory module
- **HIPAA cloud + EHR:** FHIR R4 · multi-patient dashboard · sLORETA source imaging (also drives HD-tDCS targeting) · LSL streaming · scripting API
- **anonymized session tag:** Random session identifier for clinical multi-patient environments — clinic holds patient-to-tag mapping, NeurOne cannot cross-reference

---

## 3b. OPTIONAL ACCESSORIES + COMPANION SOFTWARE (provisional specs)

### 40Hz Vibrotactile — Mastoid LRA Pad (provisional)

Purpose-built accessory delivering precisely characterized 40Hz somatosensory stimulation, aligned to Tsai lab (MIT) GENUS multi-sensory protocol. **Provisional — release contingent on HOPE Phase 3 results (mid-2026). Hardware provision in first revision at zero tooling cost.**

- **Actuator:** Linear resonant actuator (LRA), 8–10mm diameter, e.g. Jinlong JMC0834 or equivalent. LRA preferred over ERM: precise frequency control, low distortion, flat resonance profile at 40Hz drive.
- **Driver IC:** Texas Instruments DRV2605L (I2C, open-loop mode). Open-loop drive at 40Hz ± 0.5Hz with amplitude control via gain register. No resonant frequency tuning required (drive frequency set in firmware, not actuator resonance).
- **Placement:** Posterior temporal / mastoid process. Rationale: (1) direct bone coupling to skull — better transmission to somatosensory cortex than wrist; (2) adjacent to temporal lobe somatosensory representation; (3) compatible with existing temporal stability wing anchor — no new shell tooling required if anchor boss provisioned at first cut.
- **Output spec:** 0.6–1.2G peak acceleration at skin surface (DRV2605L gain register configurable); 40Hz ± 0.5Hz; duty cycle 100% continuous (20-minute session target).
- **Form factor:** 30mm diameter silicone overmoulded pad; clip attachment to temporal wing; 3-pin pogo or JST connector to hub accessory port. Shore 30A silicone contact face for comfort.
- **Power draw:** ~80–120mW continuous. Supplied from hub accessory port (existing 500mA capability).
- **BOM estimate:** LRA $1.50–2.50 + DRV2605L $1.20–1.80 + PCB/passives $0.50 + silicone housing $0.80–1.20 = **$4–7 total per pad**. Pair (bilateral option): $8–14.
- **Retail accessory price (projected):** $49–79 per pad / $79–119 bilateral.
- **Firmware:** 40Hz square-wave drive pattern; session start/stop synchronized with audio/visual 40Hz channels via hub; amplitude ramp 2s up/down (comfort).
- **Marketing note:** Apple Watch sync app is available as a free companion — but the mastoid pad delivers results the Watch cannot. Key differences: mastoid placement couples directly to skull (vs wrist-to-brain soft tissue attenuation); 40Hz ± 0.5Hz locked precision (vs uncharacterised Taptic Engine output); hub-powered (vs Watch battery drain from 20 min continuous haptics). Message: "Use the Watch app as an extra layer — for the full experience, the pad is what delivers it." See §15 Marketing Notes for full draft copy.
- **Status:** PROVISIONAL — await HOPE Phase 3 (Cognito Therapeutics, n=670, mid-2026). If positive: release mastoid pad within 6 months. Hardware anchor boss in temporal wing tooling at first cut (zero incremental tooling cost).

### Apple Watch Sync App (provisional)

Companion watchOS app extending NeurOne session experience to Apple Watch. Three sync channels. Does **not** replace purpose-built NeurOne hardware for any therapeutic function — supplements it.

- **Communication:** BT 5.3 LE (hub already has BT radio, antennas in hub); WatchConnectivity framework via paired iPhone app; session sync protocol over BLE GATT custom service.
- **Channel 1 — Haptic sync:** watchOS Core Haptics delivers 40Hz pattern in synchronization with NeurOne hub session clock. Adds wrist somatosensory channel on top of mastoid LRA pad. Not a standalone therapeutic — supplement only. Caveat in app: "Works best with NeurOne mastoid vibrotactile accessory."
- **Channel 2 — Audio sync:** Watch app plays binaural beats / isochronic tones / breathing pacer audio through AirPods or earphones paired to Watch, synchronized to hub session. Useful when user wants bone conduction reserved for breathing cue while earphones handle binaural beats, or for sessions away from the hub speaker range.
- **Channel 3 — Visual sync:** Watch display shows 40Hz visual flicker (reduced brightness, GENUS-compatible) or EMDR left/right indicator arrow synchronized to goggle session. Also: session status, coherence score live feed, HRV biofeedback breathing ring (complementary to app display for wrist-glance UX).
- **Additional Watch functions:** Session timer + haptic end-of-session alert; protocol selector (basic, without phone); quick impedance check result notification; consumable low reminders.
- **Regulatory note:** All Watch-delivered functions are declared as session monitoring / user interface aids, not therapeutic delivery. Therapeutic claims attach to NeurOne hardware only.
- **BOM delta:** $0 hardware. Software development cost only.
- **Status:** PROVISIONAL. Added to Year 1 iOS app development roadmap (NP-APP-ROADMAP-001 Rev A — Issue #32). Develops in 4 phases post-core-app-launch: Phase 1 session status + HRV breathing ring (Month 1–2), Phase 2 audio sync (Month 3–4), Phase 3 haptic sync 40Hz (Month 4–5), Phase 4 40Hz visual flicker (Month 6+, blocked on OI-WA-02 screen brightness characterization ≥100 nits at 40Hz). See `docs/np_app_roadmap_001.md`.

---

## 4. HARDWARE SPECIFICATIONS (all locked)

### 4.1 Processor stack
- **Main:** NXP i.MX RT1062 · Cortex-M7 · 600MHz · FPU+DSP+SIMD · 1MB on-chip SRAM + 32MB LPSDR4 · USB-HS OTG · FreeRTOS · ~1.1% CPU at full load (98.9% headroom for future ML)
- **Safety MCU:** STM32G071 (NOT G031 — G031 has only 8KB SRAM, insufficient for EMF firmware) · Cortex-M0+ · 64MHz · 36KB SRAM · 128KB flash · bare-metal · owns all stimulation GPIO enable lines · +$0.45 BOM
- **Storage:** 8GB industrial eMMC (SLC cache, 30,000+ P/E cycles) · LittleFS filesystem · firmware partition write-protected · separate UHDR/SHDR partitions from first firmware line
- **Connectivity:** USB-C 3.2 Gen1 (default, zero RF, <1ms) · BT 5.3 LE Audio · Wi-Fi 6 · antennas in control hub NOT headset · single rear toggle

### 4.2 Safety architecture
- Safety MCU physically owns all stimulation enable GPIO — app crash cannot cause unsafe stimulation
- SPI heartbeat from main processor every 200ms; 1.5s watchdog → all-stimulation cutoff <50ms
- Dual-processor isolation: IEC 62304 Class C (safety MCU, ~500 lines bare-metal) + Class B (main processor) separately certified
- Session protocol cryptographically signed by app — headset rejects unsigned or corrupted protocols

**Modality-specific interlocks:**
| Modality | Interlock | Implementation |
|----------|-----------|----------------|
| EEG + Visual | Photoparoxysmal detection → goggle halt | Oz electrode, <200ms, clinician-unlock for 3–30Hz |
| BES / tDCS | 40µC/cm² charge density limit | Safety MCU hardware — app cannot override |
| Visual / retinal | IEC 62471 MPE ceiling | IR proximity + Hall sensor + hardware current limit (3 independent layers) |
| PBM scalp | IEC 60601 42°C limit | NTC per zone → hardware current throttle at 62°C junction |
| TMS | Coil protection | EMF cancellation gated off 5ms before pulse, 50ms hold |
| VNS | Contact confirmation | Safety MCU reads impedance; holds if contacts not confirmed |
| Cervical VNS (T2) | Cardiac rhythm interlock | Safety MCU owns enable GPIO; monitors R-peak GPIO; HR change >15 BPM within 5s → GPIO cutoff <100ms; 30s re-enable lockout + app confirm + repeat impedance |
| All | Firmware anti-fragility | CSPRNG session protocol signing |

### 4.3 EMF shielding (5-layer passive + active)
- Layer 1: CFRP outer (30–50dB RF)
- Layer 2: 0.2mm mu-metal liner (15–25dB ELF magnetic) — PETG laminate encapsulation, silicone RTV sealant at all cutout edges
- Layer 3: **Palladium-coated polyester** inner liner (replaces silver — tarnish-immune for device lifetime, 40–60dB RF) — permanent shielding claim, verified by fleet SHDR attenuation monitoring
- Layer 4: Carbon-loaded EMI absorber foam (cavity resonance suppression)
- Layer 5: USB-C + accessory port filters (30–50dB)
- **Active:** 3-axis fluxgate magnetometers + Helmholtz coil pairs · Combined: 35–45dB ELF magnetic, 40–60dB RF
- Shell bonded to EEG DRL output (active EEG shield)
- Non-conductive CFRP window at TMS coil site (prevents eddy current field loss)
- Three firmware additions: TMS-gated cancellation · adaptive notch at BES/tACS stimulus frequency · synchronous Helmholtz subtraction from EEG

### 4.4 Fit system
- Boa-style occipital dial · 10cm range · 0.5mm/click · 50,000-cycle rated · enclosed PTFE-lined cable channel (prevents hair entanglement) · Boa replacement cable + tool in box · regrease kit available ($4.99 accessory)
- 5-position forehead bridge (5mm steps)
- Spring-decoupled electrode pods (80–120g, ±12mm, Shore 30A silicone)
- Temporal stability wings (snap-on, stored in hub dock)
- 1 adult SKU covers 52–62cm heads

### 4.5 Power
| Mode | Draw | Min USB-C PD | Power bank runtime (10,000mAh) |
|------|------|-------------|-------------------------------|
| Standby | 1W | 5V/0.5A | ~330 hours |
| EEG only | 2.5W | 5V/1A | ~130 hours |
| Standard T1 ★ | ~17–20W | 15V/2A (45W) | ~95–110 min |
| T1 peak | ~45–50W | 20V/3A (65W) | ~38–42 min |
| T2 standard | ~44–46W | 20V/3A (65W) | ~41–43 min |
| T2 peak | ~70–74W | 20V/5A (100W EPR) | ~24–27 min |

- 22F supercapacitor in control hub (absorbs LED duty-cycle transients, allows 50% aging over 5 years)
- Hub NTC thermistor for supercapacitor aging estimation (logged in SHDR)

### 4.6 Operating modes
- **Mode 1 Connected:** Real-time streaming <1ms
- **Mode 2 Programming:** App uploads session protocol <5s
- **Mode 3 Autonomous:** Pre-programmed, runs from any USB-C PD power bank, full closed-loop EEG-adaptive operation without phone or app
- **Mode 4 Download:** USB-C reconnect → EDF+ + parameter logs to app

### 4.7 Status indicators
- Left temple: green power LED (breathes at idle)
- Right temple: amber in-use LED (pulse rate mirrors session frequency — caregiver can confirm correct protocol across room)
- Fault: power LED red blink
- Stealth mode: app-controlled suppress (safety faults always fire)

---

## 5. DATA ARCHITECTURE — UHDR / SHDR (all locked)

### 5.1 Definitions

**UHDR — User Health Data Record**
- Owner: user unconditionally
- NeurOne access: **NEVER** — not for support, engineering, research, or regulatory submission
- Clinician access: per-element, per-use-case, time-limited, audited, revocable
- Researcher access: anonymized aggregate only, separate IRB + explicit research consent
- Defining test: does this record tell us something about the **person**? If yes → UHDR
- Contents: EEG waveforms (all channels) · HRV time series · PPG optical signal · neurofeedback performance scores · session timestamps and duration · protocol parameters used · closed-loop adaptation events · PBM dose (J/cm²) per zone · user-entered symptom/outcome logs · eye-open/closed state during sessions
- Storage: on-device eMMC UHDR partition, AES-256 encrypted with user biometric-derived key (NeurOne does not hold decryption key)
- Backup: automated nightly incremental backup to USB-C local or E2E encrypted cloud (user-held key) when on USB-C power

**SHDR — System Health Data Record**
- Owner: NeurOne
- Linked to: device ID + opaque TRNG warranty token **only** — never to user identity
- **Consent subject: warranty owner** (the entity who registered warranty — may be a clinic, institution, or individual purchaser; is NOT assumed to be the person wearing the device). Warranty consent is entirely separate from user research consent. A clinic staff member activating warranty is not consenting on behalf of any patient.
- Defining test: does this tell us about the **device's condition**, with nothing that reveals user biology? If yes → SHDR
- Contents: LED output ratio per zone · NTC temperature profiles · EMF shielding attenuation ratio · device session count (unsigned integer, no timestamps) · consumable session counts · USB-C insertion counter · PD negotiation log · impact events (g-force, orientation — between sessions only) · fan RPM · supercapacitor cycles · firmware version history · OTA log · accessory authentication pass/fail · calibration coefficient history
- Storage: on-device eMMC SHDR partition, separate encryption from UHDR
- Upload: to NeurOne fleet database on USB-C connect (warranty owner consent required at device registration; unrelated to user research consent)

**Boundary case resolution rule:** When in doubt → UHDR. Reclassification requires positive demonstration of no user biology content.

Specific boundary resolutions:
- Raw EEG impedance → UHDR; derived trend slope → SHDR
- Accelerometer during active sessions → UHDR; impact events between sessions → SHDR
- Raw ambient light → UHDR; cumulative UV exposure index → SHDR
- Raw VNS impedance → UHDR; contact resistance trend → SHDR
- IR eye state during sessions → UHDR; safety interlock log → SHDR
- Device session count (unsigned integer) → SHDR; session timestamps → UHDR

### 5.2 Predictive maintenance system (SHDR-based)

Three phases:
- **Phase 1** (0–1,000 devices, Year 1): Population-average survival analysis on time-to-failure data
- **Phase 2** (1,000–10,000 devices, Year 2): Fleet-trained LSTM on HDR sensor trajectories
- **Phase 3** (10,000+ devices, Year 3+): Bayesian personalization — continuously revised RUL predictions

All models version-stamped by hardware revision. New revision falls back to Phase 1 until fleet data accumulates. Models deployed back to devices via OTA — competitive moat grows automatically with fleet size.

**Reminder engine rules:**
- Safety-critical: cannot be dismissed — blocks session start
- Performance-critical: snooze max 3×
- Comfort/longevity: snooze max 5×
- All reminders measurement-triggered, not calendar-triggered
- Every reminder includes measured data that triggered it + one-tap order link

### 5.3 Research data anonymization architecture (locked)

All anonymization of UHDR data for research purposes must occur **on-device**, within the NeurOne app, before any data leaves the device. NeurOne cannot access raw UHDR at any point — including for research purposes — because the biometric-derived AES-256 key is never held by NeurOne infrastructure.

**Data flow per approved study:**
1. NeurOne server sends device a signed study descriptor (study ID, approved UHDR element list, anonymization parameters: k≥10, suppression rules, date-rounding ≥1-week interval). Descriptor is cryptographically signed.
2. App reads encrypted UHDR partition in-app, applies on-device anonymization transformations: k-anonymity grouping, date/time rounding, direct identifier removal, quasi-identifier suppression per study descriptor.
3. Only the pre-anonymized, signed extract is transmitted to NeurOne research infrastructure. Raw UHDR never leaves the device.
4. NeurOne servers store extract keyed to study ID and device ID only. No persistent per-user anonymized data store. No linkage table exists that could re-identify users.
5. Researchers access aggregated study datasets with no device ID fields.

**Consent withdrawal effect:** Because each study extract is generated on-device on-demand, withdrawing consent permanently blocks the device from processing future study descriptors. No further extracts are generated or transmitted — **for any data period, including sessions predating withdrawal**. Already-published extracts cannot be individually removed from datasets (irreversibility notice given at consent time); no new data flows ever.

**Audit trail (SHDR):** Study ID, study descriptor hash, extract transmission timestamp, and extract byte count are logged in SHDR. User can inspect all studies their device has contributed to via the app. This log is never shared with researchers.

---

## 6. CLINICAL CONSENT ENGINE (all locked)

### 6.0 Two consent subjects (locked)

NeurOne has **two distinct consent subjects** that must never be conflated:

| Subject | Who | Data | Consent granted at | Managed by |
|---------|-----|------|--------------------|------------|
| **Warranty owner** | Entity that purchased/registered the device — may be a clinic, institution, or individual; is **NOT assumed to be the device user** | SHDR fleet telemetry only | Device warranty registration | `SHDRUploader` (device-linked opaque token, no user identity) |
| **User** | Person wearing the device during sessions | UHDR (EEG, HRV, PBM dose, adaptation events — user biology) | Per-user research consent flow (L1–L4 below) | `ConsentStore` (per user, on-device) |

**Invariants:**
- A clinic that registers a device warranty has NOT consented on behalf of any patient.
- A patient using a clinic-owned device has their own independent `ConsentStore` state.
- SHDR and UHDR consent gates are code-structurally independent — `SHDRUploader` has no reference to `ConsentStore`.
- Revoking user research consent has no effect on SHDR uploads; revoking warranty consent has no effect on user research participation.

**Research consent withdrawal scoping:**
- Withdraw from specific study → stops data flows for that study only; app analytics unaffected.
- Withdraw from specific category → stops data flows for that category; app analytics unaffected.
- Withdraw blanket research consent (L3) → stops ALL research data flows AND tears down research analytics (`ConsentStore.withdrawBlanketResearchConsent()` calls `revokeResearchAnalytics()`), because blanket withdrawal signals the user does not want any data collection beyond basic device function.

### 6.1 Use case subscription tiers

| Tier | Price | Use cases | UHDR elements | Target clinician |
|------|-------|-----------|---------------|-----------------|
| Monitor | $49/mo/patient | Adherence monitoring, protocol compliance | Session timestamps, duration, protocol parameters | Primary care, wellness, coordinators |
| Assess | $149/mo/patient | All Monitor + EEG review, neurofeedback, efficacy | Adds EEG waveforms, neurofeedback scores, dose logs | Neurologists, psychiatrists |
| Full Clinical | $299/mo/patient | All Assess + HRV, closed-loop events, outcomes | Adds HRV, PPG, adaptation events, outcome logs | TMS clinics, neuromodulation programmes |
| Research | $599/mo/study | IRB-defined custom (NeurOne review required) | IRB-approved minimum, k≥10 anonymization, no IDs | Academic trials, observational studies |

**Key principle:** Clinicians select **use cases** (not data elements). System determines minimum necessary UHDR elements. Users receive plain-language decision support document listing what clinician CAN learn, CANNOT learn, and privacy implications per element.

**Expansion workflow:** Differential consent document → persistent user notification → user approves/denies/asks questions → retroactive access is a separate decision. Retroactive and prospective access presented as separate consent decisions even if made simultaneously.

### 6.2 A priori research consent (4 onboarding screens)

| Layer | Question | If yes | If no | Brand ambassador mechanism |
|-------|----------|--------|-------|--------------------------|
| L1 — Contact consent | Can we reach you about future research opportunities? | Provide contact method + frequency limit. POA holders upload POA (human review, 3 business days, jurisdiction-flagged, annual re-verification) | No contact. All features unchanged. | Being asked creates perceived agency → trust baseline |
| L2 — Category consent | Which research areas? (9 categories: AD/dementia, Depression, PTSD, TBI, Sleep, Attention, Parkinson's, Healthy ageing, Visual health) | Per-project contact for selected categories only. Each project is a fresh decision. | Not contacted for that category. | Personal category choice deepens engagement |
| L3 — Blanket consent | Pre-approve all NeurOne-reviewed research? | Data included in all studies. **Still receives per-study engagement notifications** (not consent requests — maintains engagement, can opt out per-study). anonymization: k≥10, no IDs, no sub-weekly timestamps. **Irreversibility notice displayed at this screen:** "Once your anonymized data has been included in a published study, it cannot be individually withdrawn from that dataset. However, because NeurOne anonymises your data fresh from your device for each study, withdrawing consent immediately and permanently stops any further data flowing to any future dataset — including data from sessions that occurred before your withdrawal." | Per-category and per-project process applies. | Blanket patients kept engaged — not taken for granted |
| L4 — Results + community | Hear study results? Join suggestion portal? | Plain-language results notification per study (including null results) + paper link + "suggest next steps" link. Access to suggestion/voting/pledge portal. | No results contact, no portal. | Results notification is the highest-value brand moment |

**POA workflow:** POA holder uploads executed healthcare POA → human review 3 business days → jurisdiction flagging → scope limitation noted → annual re-verification. If patient regains capacity, all proxy consent decisions presented for ratification or revocation. Research contact goes to POA holder only.

**Vulnerable population disclosure:** At per-project consent time, explicitly state: "Once your anonymized data is included in a study, individual withdrawal is not possible from that dataset — this is a fundamental property of k-anonymized aggregate data and is required by Common Rule (45 CFR 46). However, because NeurOne anonymises your data fresh from your device for each new study, withdrawing consent immediately and permanently prevents any further data from flowing to any future dataset — including data from sessions that occurred before your withdrawal. Your historical sessions remain on your device under your sole control."

### 6.3 Research suggestion portal (three functions)

1. **Patient research agenda:** Patients submit study ideas in plain language, community votes ("interested"), comments, expresses participation intent. Top suggestions visible to researcher community.

2. **Pre-identified subject pool:** "Would participate" intent flag creates pre-screened, device-familiar, motivated cohort. Researcher portal shows willing participant count, geographic distribution, anonymized device usage profiles per suggestion. Solves researchers' hardest problem (recruitment = 40–60% of trial cost) before grant is written.

3. **Crowdfunding catalyst:** Pledges ($10–$100+) are intent, not charges. When researcher confirms pilot feasibility, formal campaign activates. Escrow held until target met; refunded if not. Released to institution research account. NeurOne contribution matching for strategic studies. Pilot data (even n=20–30) supports NIH SBIR/R21 application. Funders receive results notification + paper acknowledgement as "NeurOne Patient Research Fund contributors."

**Per-project contact workflow:**
1. NeurOne reviews study (use case library, minimum necessary data, IRB verification)
2. Eligible patient list generated by device ID + contact prefs only (no UHDR)
3. personalized invitation from NeurOne (not researcher) — personal tone, specific about study, explicit about what researchers CAN and CANNOT see
4. Patient decision: Yes / No / Ask a question (secure message to NeurOne liaison, 2 business day response). Invitation includes irreversibility notice: data already included in published studies cannot be individually removed; consent withdrawal blocks all future data flows from any time period.
5. Results notification closes loop for all who opted in (including null results). Users who later withdrew consent still receive results for studies they previously participated in — notification only, no new data.
6. Consent withdrawal effect: device immediately stops processing study descriptors; no further extracts generated or transmitted, for any data period including historical sessions.

---

## 7. DURABILITY + MAINTENANCE (all locked)

### 7.1 Critical design changes (must be in tooling specifications before first cut)

| Change | BOM delta | Why critical |
|--------|-----------|-------------|
| ITO → AgNW conductive lens coating | +$8–12/lens | ITO: 0.5% strain-to-failure, cracks on flex or point impact. AgNW: 5–10% flex, compatible with hard coat, maintains 85–90% transmission |
| Hard clamshell case (replaces soft pouch) | +$8–14 | Lens scratching certain within first month without case. Includes probe dock. Doubles as shipping container. |
| Intranasal probe hub dock (molded) | Hub retool | Y-probe dropped probe-first fractures junction. Cannot be retrofitted. |
| Reference photodiode per zone (behind PDMS window) | +$2 total | Detects LED aging AND PDMS window fouling simultaneously. Eliminates 3-year service calibration visit. Protects J/cm² dose metering claim. |
| **Second scalp-side photodiode PD2 per zone (RISK-14 Option B)** | +$0.75–1.50/headset total | On scalp-facing PDMS surface, pin 19. PD1/PD2 ratio separates fouling (PD1↓ PD2 stable) from LED aging (both↓). T1 and T2 share identical zone module mold — firmware flag only. |
| Zone module connectors: 1,000-cycle rated (Hirose FH34S-20S-0.5SH or JAE FF03 — 0.5 mm pitch, back-flip lever ZIF, ≥1,000 insertion cycles) | +$2.00 | Molex SlimStack is a board-to-board connector — not an FPC family. Standard Molex ZIF FPC connectors rated only 20–30 cycles. Hirose FH34S confirmed as correct family. Confirm ≥1,000-cycle rating from full datasheet before BOM lock. 0.35 mm pitch insufficient — current per pin too high. Must specify before PCB layout. See NP-HW-FPC-001. |
| Lever-actuated ZIF for zone modules | Included above | Back-flip lever ZIF (Hirose FH34S mechanism) — zero insertion force, tool-free extraction. Enables user self-service zone module swaps. |
| **Zone module sliding eject lever (RISK-22 Option A)** | +$0.40/module | 10–12mm lever arm, 3:1 mechanical advantage, ≤1N extraction force. Recessed flush when closed; snap-fit detent prevents accidental ejection. 316SS hinge pin. Required for users with Parkinson's/post-stroke hand weakness. |
| **Self-sealing co-molded silicone gasket per zone module (RISK-16 Option A)** | +$0.30–0.60/module | Shore 40–50A medical silicone, D-section 2.5×2.0mm, 20% compression when seated. No user RTV required. IPX4 compliant after 10 field swap cycles (FAI-IPX-02 BLOCKING test). Gasket retention groove + silicone primer prevent delamination. |
| **Five-layer zone module keying (RISK-15)** | Included in tooling | Layer 1: asymmetric mechanical key (unique per zone, prevents physical mis-insertion). Layer 2: ZONE_ID resistor (ZM-01=10kΩ through ZM-05=220kΩ, 1%, pin 18; firmware debounce 3×ADC at 100ms). Layer 3: ISO 17049 braille + raised numeral. Layer 4: N tactile dots on shell at each slot. Layer 5: bone conduction audio ("Frontal Left connected"). Covers color-blind AND blind users. |
| **EEG cable routing channel in shell (RISK-21)** | $0 (tooling) | Dedicated 8×5mm molded channel on outer CFRP surface (opposite side from FPC bundle). ≥15mm separation from zone module FPCs required (DRC-18). Must be in shell tooling spec before first cut. |
| **PDMS SiO₂ interlayer bonding process** | Process cost | 75 nm RF magnetron sputtered SiO₂ on PI surface before O₂ plasma activation. Achieves 174–860 N/m peel force. 200-cycle IEC 60068-2-14 qualification required before production. See NP-FAI-ZM-001 §3e. |
| Interface protection covers (all tethered) | +$8–9 total | Anchor posts molded into shell at zero cost if specified before first cut. |
| Sliding rail lens mount | +$1.20 | Eliminates alignment jig. User self-install. |
| Dual-bank OTA firmware + USB-C DFU recovery | $0 (software) | Must be in bootloader from first firmware line. Cannot be added later. |
| Separate UHDR/SHDR eMMC partitions | $0 (firmware) | Must be in firmware specification before any storage architecture is written. |

### 7.2 Other locked design changes

| Change | BOM delta | Rationale |
|--------|-----------|-----------|
| Palladium-coated EMF shielding fabric | +$6/headset | Silver tarnishes 12–18 months. Palladium tarnish-immune for device lifetime. Fleet SHDR verifies stable attenuation — marketable, measurable claim. |
| N52 → N42 magnets in lens rim | −$0.80 | N52 brittle under corner drop. N42 more impact-tolerant, ≥1mm polymer wall required on all faces. |
| Braided aramid USB-C cable + dual silicone strain relief | +$3–4/cable | Commodity cables fail at strain relief within 6–18 months. 50,000+ flex cycle rating. Spare in box. |
| MagSafe hard gold contacts (>0.5µm cobalt-alloyed) | +$1.20 | Oxidised contacts → power throttling. Contact resistance monitored in SHDR. |
| 22F supercapacitor (from 10F) | +$1.80 | Allows 50% degradation over 5 years while maintaining transient absorption. |
| Industrial eMMC + LittleFS | +$2.40 | 30,000+ P/E cycles. Write endurance monitored in SHDR. |
| Bone conduction driver silicone isolator | +$1.80 | Piezoelectric element brittle — Shore 20–30A silicone mount absorbs impact. |
| Silicone over-mold at Y-probe junction | +$1.20 | Flex without fracture. Minimum 20mm bend radius marked on probe shaft. |
| Silicone potting for micro-LED array | +$2.40/lens | 1,800+ thermal cycles over device lifetime without delamination. |
| Hard coat on EC lens (3–5µm silicone) | +$4.00/lens | Standard in automotive EC mirrors. Prevents scratch damage to active EC layer. |
| Aluminium bayonet for audio cups | +$1.60 | Plastic detent flattens at 500–1,000 cycles. Metal-to-metal rated for device lifetime. |
| Moisture-barrier electrode tip hydration caps (WVTR <0.5) | +$0.80 | Extends factory-sealed electrode storage life to 24+ months. |
| Audio cup user-replaceable snap-in mesh frame | +$0.60 | Snap-in frame; user pops out, rinses, reinserts or replaces ($9.99/pair). |
| Mesh cleaning brush in box | Minimal | Standard maintenance tool for mesh surfaces. |
| Hub air filter (30-micron foam, snap-out) | +$0.45 | Captures carpet fibres in clinical environments. Hub NTC temperature trend flags cleaning need. |
| Automated nightly UHDR backup (incremental) | $0 | When on USB-C power. Failure becomes hardware swap not data loss. |
| ADS1299 self-calibration at session start | $0 | Internal reference routed to all channels. Eliminates EEG amplitude drift. Coefficients in SHDR. |
| Plasma-activated PDMS for optical windows | Minimal | Hydrophilic surface repels sebum. Anti-fouling without additional parts. |
| EC rim contact hard gold plating | Included in contact spec | EC driver monitors transition time as contact resistance proxy. Cleaning prompt when >3s transition. |
| Hub NTC thermistor | +$0.15 | Cross-calibrates headset NTCs, monitors supercapacitor aging, enables hub temperature-based SHDR alerts. |
| Enclosed PTFE-lined Boa cable channel | $0 (tooling) | Prevents hair entanglement — must be in occipital arch tooling. |
| Tool-free hub fan (quarter-turn captive fastener) | +$0.80 | No screwdriver required for fan replacement. |
| Humidity indicator card in box | +$0.30 | Visual confirmation package integrity during shipping and storage. |
| IPX4 rating target for headset | Testing cost | Splash-proof from all directions. Enables "workout-safe" marketing claim. |

### 7.3 Calibration self-maintenance

| Sensor | Self-calibration method | Residual service requirement |
|--------|------------------------|------------------------------|
| PBM photodiode | Dual-PD: PD1 (behind PDMS, forward emission) + PD2 (scalp-facing, backscatter). PD1/PD2 ratio separates fouling from LED aging. PD1↓ PD2 stable → fouling prompt. Both↓ proportional → LED aging correction. | None — dual-PD eliminates ambiguity that single PD could not resolve |
| EEG amplifier (ADS1299) | Internal reference routed to all channels at session start — gain/offset correction applied | None — fully self-calibrating |
| NTC thermistors | Hub NTC cross-calibration: compare headset NTCs vs hub reference at ambient equilibrium (>10 min since last session) | None — flag at ±1.5°C offset |
| Fluxgate magnetometers | Zero-field nulling at session start + geomagnetic field magnitude comparison via phone GPS | **3–5 year Tier B service visit** (scale factor drift requires Helmholtz test coil) |
| EC lens contacts | EC driver monitors transition time — flag when >3 seconds (vs 2s spec) | Cleaning prompt; no service visit |
| Audio cup mesh | Driver impedance monitoring — detects fouling pattern | User-replaceable snap-in frame — no service visit |

---

## 8. SERVICE NETWORK (all locked)

### 8.1 Partner tiers

| Tier | Examples | Service tasks | Certification | Equipment | Revenue/yr at scale | Launch timing |
|------|----------|---------------|---------------|-----------|---------------------|---------------|
| A — Optical centers | LensCrafters, Pearle Vision, independent opticians | S3 Rx clip manufacture + fitting (primary) · Lens replacement (standard + EC) · Calibration (secondary) | 4-hr initial · 1-hr annual online | $400–600 calibration reference (loaned) + $80–120 jig (optional with sliding rail) | $8K–35K/yr | Year 1 — already engaged via S3 program |
| B — Electronics repair | uBreakiFix/Asurion · iFixit partners | Zone module FPC swap · DFU recovery · eMMC data recovery · Impact inspection · Fluxgate calibration | 6-hr initial · 2-hr annual practical | ESD workstation (existing) + eMMC adapter ($150–200) + DFU software (downloaded) | $4K–18K/yr | Year 2 — major metros first |
| C — Retail triage | Best Buy Geek Squad · Apple Authorized Service | Warranty intake + triage · Routing to Tier A/B · Consumable sales | 2-hr initial · 30-min annual online | None — partner app access only | $1K–7K/yr | Year 1–2 broadly — legitimacy signal |
| Depot — NeurOne mail-in | Backstop | All tasks · T2 same-day loaner · Precision fluxgate calibration | Full internal training | All in-house | Highest margin per task — backstop not primary | Day 1 |

### 8.2 Design changes that reduce service dependency

- Reference photodiode → eliminates 3-year PBM calibration service visit
- Sliding rail lens mount → user self-install, eliminates Tier A lens installation visit
- Lever ZIF connectors → user zone module swap, eliminates Tier B visit for upgrades
- Tool-free hub fan → user self-service
- Automated nightly UHDR backup → eliminates most data recovery emergencies

**Residual mandatory service per T1 user over 5 years:** 2–5 optician visits (Rx clip, already part of their workflow) + 0–1 fluxgate calibration + 0–2 damage-driven events.

### 8.3 Interface protection covers

Three cover types, all tethered to headset:

| Cover type | Material | Retention | Count in box | Replacement |
|-----------|----------|-----------|-------------|-------------|
| Zone slot plugs (5 per headset) | Shore 30A medical silicone, 5 colours (position-coded) | Friction/compression in slot, IP54 | 5 installed + 5 spare | 5-pack $9.99 |
| Accessory port covers (3 per headset) | Shore 40A TPE + encapsulated steel disc + Shore 20A silicone fins | N42 magnetic attraction via steel disc, ~400g pull | 3 installed + 2 spare | 3-pack $7.99 |
| Lens rim guards (2 per headset) | Shore 85A UV-stable TPU, clear | Mechanical snap-fit over rim profile | 2 installed + 1 spare pair | Pair $6.99 |

**Anchor posts:** molded into headset shell at zero incremental tooling cost if specified before first cut. All tethered — cannot be permanently lost without deliberate cutting.

---

## 9. COMPETITIVE POSITION

| Feature | NeurOne Home | NeurOne Pro | Vielight Neuro Pro 2 (~$5K) | Neuronic 1070 ($3K–5K) | Sens.ai (~$1.5–2K + sub) |
|---------|----------------|----------------|------------------------------|------------------------|--------------------------|
| PBM wavelengths | 660+810nm base / 660+810+1064nm with smart module upgrade (3λ) | 660+810+1064nm+1170nm (4λ) | 810nm (1λ) | 1070nm (1λ) | ~810nm (1λ) |
| Total LED count | 600 (300/wavelength) | 600 + 1170nm LDs | ~12 transcranial | 256–300 (1 wavelength) | ~7 midline |
| Peak irradiance | 400 mW/cm² pulsed* | 400 + 1,000 mW/cm² | 400 mW/cm² | Not specified | Not specified |
| Real-time dose (J/cm²) | Yes — per zone | Yes | No | No | No |
| EEG channels | 8 semi-dry 24-bit | 21 wet gel 24-bit | None | None | 3 dry midline |
| Closed-loop EEG | Yes — autonomous | Yes — all modalities | No | No | EEG→PBM only |
| BES/tACS | Yes | Yes clinical | No | No | No |
| tDCS | Yes | Yes + HD-tDCS | No | No | No |
| TMS | No | Yes focal | No | No | No |
| VNS + HRV | Auricular electrical + PPG | + cervical option | Optical VNS (separate) | No | HRV only |
| Audio entrainment | Binaural + bone conduction | + clinical EMDR | No | No | No |
| Visual stimulation | 108 LEDs/lens + EMDR + retinal PBM + Mode F | + EEG-adaptive, seizure detection | No | No | No |
| EMF shielding | 5-layer palladium + active | Same | None | None | None |
| Autonomous mobile | Yes — power bank | Yes | BT only | BT only | BT only |
| No mandatory subscription | Yes | Yes | Yes | Paywall on PLUS | Required $99–199/yr |
| Published clinical trials | None (new product)* | None (new product)* | 35+ RCTs | Limited | Ongoing |

*See §10 for pending actions on irradiance claim and evidence gap.

**Key competitive claims:**
- "50× more transcranial LEDs than Vielight at 17% of the price"
- "300 LEDs per wavelength — matching Neuronic's total LED count at each of the two CCO absorption peaks they don't cover"
- "Real-time J/cm² dose metering — the only device that shows you the exact dose your brain received"
- "Only consumer brain device with palladium-fabric EMF shielding verified by continuous fleet monitoring"
- "Autonomous closed-loop operation from any power bank — no phone required"

---

## 10. REGULATORY STRATEGY

### T1 — FDA-exempt wellness pathway
- General wellness device (same category as Muse, sens.ai, Apollo Neuro)
- Consumer naming: "Brainwave entrainment stimulation" (not tACS), "Cortical priming stimulation" (not tDCS)
- Required standards: IEC 60601-1, IEC 60601-2-10, IEC 62471, IEC 62133, FCC Part 15
- Cybersecurity: SBOM, vulnerability disclosure policy, documented OTA update approach
- FTC claims substantiation: 33-entry bibliography maps each marketing claim to supporting citations

### T2 — FDA 510(k)
- Modular predicate: TMS (NeuroStar K083538, BrainsWay K122288) + tACS (Soterix K142485, Neuroelectrics K173185) + taVNS (electroCore K163334, K173323)
- Timeline: 18–36 months from T1 launch, $2–5M budget
- **QMS (21 CFR Part 820 / ISO 13485:2016): ESTABLISHED at company formation 2026-05-13** — NP-QMS-001 Rev A (manual), NP-DHF-001 Rev A (DHF index), NP-QMS-DC-001 Rev A (design controls), NP-RM-001 Rev A (risk management), NP-SW-001 Rev A (IEC 62304 SW plan), NP-QMS-CAPA-001 Rev A (CAPA procedure). All pre-formation design documents retroactively entered under change control. See Issue #33.
- Pre-Submission (Q-Sub) meeting with FDA at ~Month 20: free, prevents filing on avoidable grounds
- IEC 62304 software classification: Safety MCU → **Class C** · Main processor → **Class B** · App → **Class B** — formalized in NP-SW-001 Rev A
- Clinical data: required for TMS modality; seeded T2 units into research institutions (Years 2–3) generate this data
- Human factors engineering (FDA 2016 HFE Guidance): URRA + formative + summative testing — governed by NP-HFE-001 (planned Month 9)

---

## 11. CLINICAL TRIAL RESEARCHER CANDIDATES

Priority first contacts (in order):

1. **Neda Rashidi-Ranjbar** (neda.rashidi-ranjbar@unityhealth.to) — St. Michael's Hospital Toronto · 2025 MCI PBM RCT PI · early-career, device-ready, motivated collaborator · CIHR funding pathway · most direct PBM upgrade path
2. **Mayank Jog** (mjog@mednet.ucla.edu) — UCLA Brain Mapping Center · active K99/R00 NIH (MH128572) aligned to HD-tDCS protocols · JAMA Network Open 2025 RCT PI
3. **Mark George** (georgem@musc.edu, 843-876-5142) — MUSC Brain Stimulation Lab · highest-credibility TMS infrastructure · 55 active studies · 15 TMS machines on-site
4. **Margaret Naeser** (mnaeser@bu.edu) — VA Boston · foundational TBI/PTSD PBM cohort · 47 years VA-funded · existing veteran patient cohort

**SAB priority:**
- **Li-Huei Tsai** (617-324-0305, MIT Picower) — SAB role ONLY (Cognito Therapeutics conflict prevents PI role) · founded GENUS field · Nature 2016 paper is scientific basis for 40Hz visual protocol
- **Glen Jeffery** (g.jeffery@ucl.ac.uk, UCL Institute of Ophthalmology) — world's leading retinal PBM researcher · foundational 670nm human studies

Full researcher candidate list (12 researchers, 7 modalities, contact info, cost estimates, funding sources): `neurone_researchers.docx`

---

## 12. CLINICAL EVIDENCE BIBLIOGRAPHY

33-entry bibliography across 11 modality sections available: `neurone_bibliography.docx`

**Evidence summary:**
- TMS for depression: strongest — FDA pivotal trial (n=301), CPT reimbursement codes, 50–55% response rate
- EMDR for PTSD: strong — WHO recommendation, multiple meta-analyses
- EEG neurofeedback for ADHD: strong — 21 RCTs, n=1,261
- Transcranial PBM: good but concentrated in Vielight/BU/Toronto group — Rashidi-Ranjbar 2025 RCT (n=20, multimodal neuroimaging) is strongest
- taVNS: good — scoping review 109 studies (n=3,231), largest epilepsy RCT (n=150)
- 40Hz gamma: foundational animal study (Nature 2016) + Phase 2A human RCT (n=15 AD)
- **Critical gap:** No RCT of combined multi-modal protocol (PBM + BES + VNS + audio + visual simultaneously) exists anywhere

---

## 13. REMAINING WEAKNESSES + OPEN ITEMS

### 13.1 Critical — action required immediately

| Issue | Action | Cost/Timeline |
|-------|--------|---------------|
| **Zero published clinical trials** — 35-trial gap vs Vielight | Commission SBIR Phase I at company formation. First contacts: Rashidi-Ranjbar → Jog → Naeser | 2–3 years to published data |
| **400 mW/cm² regulatory opinion not obtained (RISK-03)** — cannot appear in ANY public material until cleared | Commission outside regulatory counsel (PBM/digital health specialist). Also assess Vielight comparison claim under FTC implied claim doctrine | $8,000–15,000 · 3–5 weeks |
| **"NeurOne" is an uncleared placeholder** — trademark not searched | Trademark search and clearance: US, EU, Canada, Australia. Required before ANY external conversation. | $15,000–25,000 |
| **CFRP shell slot rim Ra ≤ 1.6 µm unconfirmed (RISK-20)** — BLOCKING for tooling release | Obtain written confirmation from CFRP shell tooling supplier (letter + Ra measurement data from representative coupon). Supplier qualification item SUP-M-07 in NP-PROC-SUP-001. If Ra > 1.6 µm unavoidable: escalate to ME + EE; gasket or shell geometry must be revised. | $0 — supplier engagement required |
| **PDMS CAT-C supplier not selected (RISK-04)** — BLOCKING for production start | Select PDMS bonding supplier per NP-PROC-SUP-001 CAT-C criteria. Supplier must confirm IEC 60068-2-14 thermal cycling qualification capability. 200-cycle qualification (FAI-TC02) must pass before any production FPCs are built. | Supplier lead time 8 weeks from selection |

### 13.2 Moderate

| Issue | Status |
|-------|--------|
| SAB not formed — no scientific credentialing | Tsai outreach at 617-324-0305 (SAB role only). Jeffery and Naeser natural SAB candidates. Budget $50,000–80,000/yr for 5-person SAB. |
| Vulnerable population withdrawal edge case in research consent | **Resolved and locked** — irreversibility notice added at L3 blanket consent screen AND at per-project invitation (step 4). Forward-effectiveness guarantee added: on-device fresh-per-study anonymization makes consent withdrawal fully effective for all data periods. |
| 45W charger in box | **Decided and locked** — included in BOM across all configurations at appropriate wattage. Weakness resolved. |
| Zone module mold complexity (RISK-23) | NP-TOOL-ZM-001 created consolidating all 8 molded features (F-01 through F-08). 12-item mold design review checklist must be completed before steel is cut (NP-COORD-001 G1-05). |

### 13.2b tFUS / LIFU — modality watch (elevated to MEDIUM 2026-05-13)

Science basis confirmed: PIEZO1 mechanosensitive ion channel mechanism (PNAS 2023); strong safety record (n > 400 human subjects, no serious AEs at MI < 1.9); first wearable coupling breakthrough (MiniUlTra bioadhesive hydrogel, Nature Communications 2025). Cannot enter T1 (no wellness regulatory pathway for brain-targeted therapeutic ultrasound). T2 add-on requires 510(k) predicate, which does not yet exist.

**Re-evaluation trigger:** SPIRE DIADEM FDA clearance (pivotal trial submission anticipated 2026, decision ~2027–2028). On clearance: evaluate T2 add-on 510(k) for DLPFC-targeting LIFU pod at 500–650 kHz, targeting frontal cortex (~15–20 mm depth, no skull correction required). Natural synergy with existing tDCS/tACS/sLORETA-HD-tDCS stack and EEG-guided closed-loop architecture.

**Active watch items:**

| Item | Why it matters | Expected timing |
|------|---------------|----------------|
| SPIRE DIADEM FDA pivotal trial + clearance | First US 510(k) acoustic neuromodulation predicate | Submission 2026, decision 2027–2028 |
| Sonomind CE mark (EU, depression, acoustic metalens) | First EU CE mark for focused tFUS neuromodulation; EU regulatory precedent | 2027 |
| Hair-compatible hydrogel coupling | MiniUlTra demonstrated 35-day adhesion on shaved scalp — hair-gap solution is the remaining consumer engineering blocker | Watch UT Austin + Stanford Wearable Electronics Initiative |
| Li-Huei Tsai lab (SAB candidate) on 40 Hz ultrasound | If GENUS multi-sensory protocol extends to ultrasound as 40 Hz entrainment channel, adds strategic synergy to existing mastoid pad program | Ongoing; flag at SAB formation outreach |
| ITRUSST standards consolidation | Standardised reporting enables cross-study comparison; affects which clinical evidence is citable | Published 2024–2025; monitor updates |

**Not feasible under any current regulatory framework for T1.** Do not include in T1 product claims, BOM, or marketing.

### 13.3 Structural (accepted, managed)

| Issue | Mitigation |
|-------|-----------|
| Fluxgate calibration requires Tier B service visit (every 3–5 years) | Geomagnetic comparison detects severe drift. Fleet SHDR predicts need with months of lead time. 3–5 year interval means Tier B network established before first visits needed. |
| iOS/Android OS update dependency | Apple/Google developer beta participation. 7-day OS compatibility SLA. Autonomous Mode 3 as structural fallback. |

### 13.4 Pending decisions — must resolve before tooling is cut

- [ ] Product name trademark clearance
- [ ] 400 mW/cm² regulatory opinion letter (RISK-03 — BLOCKING for all public material)
- [ ] LED emitter pulse current rating verification (660nm + 808–830nm FPC candidates at 120–180mA)
- [x] Zone module FPC layout specification — freeze layout so PD2 aperture position (F-04 in NP-TOOL-ZM-001) can be specified in mold design
- [ ] Zone module mold design review — NP-TOOL-ZM-001 §5 checklist (all 8 features F-01 through F-08) signed off before steel cut
- [ ] **CFRP shell slot rim Ra ≤ 1.6 µm — written supplier confirmation (RISK-20 BLOCKING)**
- [ ] **PDMS CAT-C supplier selection + 200-cycle IEC 60068-2-14 thermal cycling qualification (RISK-04 BLOCKING)**
- [ ] ZONE_ID firmware debounce spec written into firmware requirements document (3×ADC at 100ms, ≥2/3 pass)
- [ ] HFE formative study for sliding eject lever — 5 subjects with Parkinson's/post-stroke (NP-TOOL-ZM-001 OI-4, NP-FAI-ZM-001 FAI-A15)
- [x] EEG cable routing path in shell CAD model — OI-09 in NP-DRV-SHELL-001 (required for DRC-18 verification, RISK-21)
- [ ] Hub tooling: probe dock + anchor posts + large-radius Boa cable channel + tool-free fan (quarter-turn captive fastener)
- [x] Shell tooling: anchor posts for all interface covers (5 zone + 3 port positions, color-coded for zones) + EEG cable channel (§2.4) — **NP-TOOL-SHELL-001 Rev A written 2026-05-10; CAD verification (OI-01–OI-07) and temporal wing boss (F-04, PROVISIONAL) specified; see `docs/neurone_tool_shell_001.docx`**
- [x] Lens tooling: sliding rail + N42 magnet positions (≥1mm polymer wall all faces) + AgNW spec + hard coat + EC driver contacts — **NP-TOOL-LENS-001 Rev B (2026-05-10)**
- [x] Goggle arm tooling: anchor hook for lens rim guard tether — **NP-TOOL-LENS-001 Rev B F-07: hook at superior-posterior corner, 4mm ID, shade clearance ≥4mm (MR-19), tether ≤45mm posterior routing no FOV crossing (MR-20), OI-10 tracks CAD confirmation. Addresses Issue #18.**
- [x] **1064nm smart module hardware — FPC layout variant (NP-HW-FPC-001 Rev E, NP-FPC-ZM-SM-01), mold variant (NP-TOOL-ZM-SM-001 Rev A), InGaAs PD (Hamamatsu G12180-010A), 1064nm LED emitters (EPITEX via Marubeni), on-module driver (ATtiny402 + 3× IRLML6344 N-FETs, BOM $1.00), 550 LEDs (200×660nm + 200×808nm + 150×1064nm) — Issue #54 CLOSED (PR #58)**
- [x] **1064nm smart module — on-module I2C LED driver IC selected: Microchip ATtiny402 (I2C slave, SOT-23-8) + 3× Infineon IRLML6344TRPBF N-channel MOSFETs (SOT-23); total driver BOM $1.00 — hardware decision LOCKED (Issue #53, PR #57)**
- [x] **OI-PBM-HW-01 (SPECIFIED — Issue #62): Hub PCB Rev B adds Vishay DG2788A per-slot analog switch (×5, $0.75–1.25 BOM) to reduce TIA Rf 47kΩ→22kΩ for InGaAs PD slots (2× higher responsivity saturates TIA at current gain). ZONE_ID debounce triggers gain switch (GAIN_SEL GPIO HIGH) BEFORE I2C mux enable. NXP PCA9546A I2C isolation switch added (OI-PBM-HW-02 also closed). Hub PCB Rev B Gerber build required before FAI-SM-04/06 optical bench tests. See NP-HW-HUB-001 Rev B, firmware/pbm_1064nm/ (np_pbm1064_hal_tia_gain_set, np_pbm1064_hal_tia_gain_boot_init, np_pbm1064_detect.c).**
- [x] **RISK-03 regulatory opinion scope expansion: must include 1064nm irradiance, three-channel aggregate irradiance (600 mW/cm² limit OI-PBM-05), and any T2 combined 1064+1170nm claim (Issue #56)** — **NP-REG-PBM1064-001 Rev A (2026-05-13): scope expansion brief delivered to counsel; 12 questions covering 1064nm FDA pathway (Q1–Q3), aggregate irradiance IEC 62471 + FTC (Q4–Q6), T2 combined 1064+1170nm (Q7–Q9), depth-tier penetration claim FTC substantiation (Q10–Q12). Marketing copy gate maintained until opinion letter received. OI-PBM-05 resolution pending counsel Q5/Q6. See `docs/np_reg_pbm1064_001.md`.**
- [ ] **Factory calibration procedure for InGaAs PD coefficients (K_PD1_1064, K_PD2_1064): integrating sphere reference at 1064nm; write to Config partition at manufacture (OI-PBM-04 in NP-FW-PBM1064-001)**
- [x] **1064nm FTC bibliography extension: add Yao et al. Science Advances 2022 + UT Dallas follow-ons to neurone_bibliography.docx (Issue #56)** — **NP-BIB-1064-001 Rev A (2026-05-13): six entries added — Yao et al. (2022) Science Advances DOI:10.1126/sciadv.abj7390 (primary working memory evidence), UT Dallas follow-on search guidance, tissue optics penetration depth refs (Bashkatov 2005, Yaroslavsky 2002, Strangman 2002), CCO mechanism at 1064nm (Rojas & Gonzalez-Lima 2013), safety literature (Hamblin 2016), multi-modal combination evidence gap. Marketing copy gate remains until RISK-03 opinion letter also received. See `docs/np_bib_1064_001.md`.**
- [ ] **QMS — hire Quality Lead (VP Quality)** — Month 6 target; interim authority CEO. First action: gap assessment against 21 CFR Part 820 + ISO 13485:2016 with external QMS consultant (Month 3).
- [x] **QMS — author Design and Development Plan (NP-DP-001)** — **NP-DP-001 Rev A (2026-05-17):** 20-section plan per 21 CFR §820.30(b) and ISO 13485:2016 §7.3.1. Six design phases (Phase 0 complete; Phase 1–5 T1; Phase 6 T2). Relative-month milestone schedule. Responsibilities (Steve Hickman CEO/Design Authority; all other leads TBD). Gate reviews (G1 Month 6, G2 Month 10, G3 Month 14, T1 launch Month 18). Verification, validation, transfer, change control, risk integration, software integration, supplier management, interim doc control (GitHub + CLAUDE.md → eQMS Month 9). DHF index updated to NP-DHF-001 Rev D. See `docs/np_dp_001.md`.
- [x] **QMS — author Design Input/Output Traceability Matrix (NP-DT-001 Rev A, 2026-06-07, PR #122)** — 57 design inputs (DI-PERF/SAFE/USE/REG/INT), 35 design outputs, 13 verification evidence entries; full DI→DO→VE traceability matrix; DHF completeness upgraded from Partial to Good. See `docs/np_dt_001.md`.
- [ ] **QMS — author Human Factors Engineering Plan (NP-HFE-001)** — Month 9 target. IEC 62366-1 / FDA HFE Guidance 2016; URRA + formative + summative testing plan.
- [ ] **QMS — author Post-Market Surveillance Plan (NP-PMS-001)** — Month 12 target (before T1 launch). Required for 510(k) submission and ongoing EU MDR compliance.
- [ ] **SHDR warranty token architecture** — opaque 256-bit TRNG token replaces warranty owner ID as device-to-person linkage in SHDR. Spec in NP-FW-EMMC-002 Rev A §A. BLOCKING for warranty registration system build and any SHDR fleet DB schema freeze. No-join CI test required (OI-EMMC2-06). **App-side PARTIAL (2026-06-05, PR #112):** `SHDRUploader` now uses a Keychain-stored 32-byte random token instead of `identifierForVendor`; upgrade to hub-provisioned TRNG token pending GATT warranty characteristic (hub firmware work remaining).
- [ ] **Device factory reset procedure** — full SANITIZE + SHDR wipe + warranty token clear + new TRNG salt + new warranty token. Spec in NP-FW-EMMC-002 Rev A §B. Must be in firmware spec before any device is shipped outside the company.
- [ ] **UHDR two-layer key scheme** — master key wrapper (UKMD + WKMD). Spec in NP-FW-EMMC-002 Rev A §C. BLOCKING for UHDR firmware implementation; supersedes single-layer Argon2id spec in NP-FW-EMMC-001 Rev A §6. Cryptography reviewer sign-off required (OI-EMMC2-02). **Firmware IMPLEMENTED + host-tested (2026-07-08):** `firmware/uhdr_key/` — `np_ukmd_record_t` (§C.3), `np_uhdr_key_provision` / `_unlock` (§C.4) / `_background` (§C.4 step 7) / `_change_credential` (§C.5 — re-wraps the 32-byte UKMD only, UHDR ciphertext untouched, no remount). WKMD is a function-local, never in the context struct or any header, `memset_explicit`-zeroed before every return; only the GCM ciphertext+tag reach Config. HAL boundary (OI-UHDRK-01..07): TRNG, Argon2id, AES-256-GCM wrap/unwrap, Config read/atomic-write, UHDR XTS mount — real CAAM/DCP + Argon2id + RNGB backing pending. Host stubs model Argon2id/hw_bind/GCM with `np_crypto` SHA-256 so wrong-credential rejection (`NP_UHDR_ERR_AUTH`) is a genuine probe. The Argon2 `version` is plumbed through the KDF HAL (`np_uhdr_hal_argon2id`) and the host TRNG is counter-seeded so nonce-uniqueness is exercised (code-review CR-2/CR-3). 13 host test functions, `ctest` 9/9 green; wired into `firmware/CMakeLists.txt` (host + cross) and `firmware-host-tests.yml`. **Privacy-review hardening (2026-07-08):** (F1) **hardware binding** — `WKMD = hw_bind(Argon2id(credential, salt), device-fused HUK)` via new HAL **OI-UHDRK-09** (CAAM/OTPMK on target), so an eMMC image can't be brute-forced offline (defeats the §C.6 PIN-fallback offline vector); (F2) the salt + KDF params are **authenticated as GCM AAD** and stored params are **validated against a floor** (`NP_UHDR_ARGON2ID_*_MIN`) → `NP_UHDR_ERR_PARAM` on rollback; (F3) **every unlock failure path scrubs the UKMD**. (F4) UKMD key-material (incl. per-device salt) added to the SHDR-schema CI prohibition (`ci/test_shdr_schema.py` KEYMAT-01, 7 checks green) and a fleet-export prohibition documented in `np_uhdr_key_types.h`. **OI-UHDRK-08:** §C.3 struct is self-inconsistent — fields sum to 152 B but the comment says "Pad to 192 bytes"; resolved to the stated 192 B (`reserved[84]`), pending spec-author confirmation. Remaining before checkbox close: real AES-GCM/XTS/Argon2id + CAAM-HUK HAL implementations, and **cryptography-reviewer sign-off (OI-EMMC2-02) — scope MUST cover F1 (hardware binding / offline-attack resistance) and F2 (key-wrap construction; consider AES-KW / AES-GCM-SIV vs the current AES-GCM+AAD)**.
- [ ] **Scratch partition encryption for research anonymization** — per-task AES-256-CTR key, SRAM-only, memset_explicit on completion, eMMC SANITIZE post-run. Spec in NP-FW-EMMC-002 Rev A §D. BLOCKING for research anonymization firmware implementation.
- [ ] **EDF+ patient header policy** — patient code = 16-char UHDR token; sex/birthdate/name = 'X'. Header validator in research anonymization pipeline. Spec in NP-FW-EMMC-002 Rev A §E. BLOCKING for EDF+ writer implementation.
- [ ] **SHDR accelerometer data reclassification** — raw accelerometer series never written to any partition; only `drop_detected: bool` and `maintenance_alert: bool` written to SHDR. Spec NP-FW-EMMC-002 Rev A §G (to be authored). BLOCKING for SHDR fleet DB schema freeze.
- [ ] **NP-SEC-BR-001 annual tabletop exercise** — before T1 launch and annually thereafter. See `docs/np_sec_br_001.md` §9.
- [ ] **EU-US Data Privacy Framework self-certification** — before any EU resident's personal data is transmitted to US infrastructure. Month 3 target. See NP-PRIV-REM-001 STEP-12 and §4.1.
- [ ] **Clinician BAA template (NP-LEGAL-BAA-001)** — must include consent revocation deletion cascade clause (30-day deletion obligation). Before any clinician subscription is sold. See NP-PRIV-REM-001 STEP-13.
- [ ] **Research anonymization upgrade (NP-FW-ANON-001)** — l-diversity l≥3 + differential privacy ε≤1.0. Before first study descriptor deployment. External DP reviewer sign-off required. See NP-PRIV-REM-001 STEP-11.
- [ ] **Mode F regulatory opinion (Q-13)** — add Q-13 to NP-REG-PBM1064-001 Rev B covering Mode F cumulative retinal dose under RISK-03 existing counsel engagement. See NP-FW-EMMC-002 Rev A §F and NP-PRIV-REM-001 STEP-18. **Firmware gate IMPLEMENTED (2026-06-17):** `NP_MODE_F_REGULATORY_CLEARED = 0` compile-time guard in `firmware/hub_control/include/np_hub_config.h` (line 141) + `#if NP_MODE_F_REGULATORY_CLEARED` blocks in `firmware/hub_control/modules/np_mod_visual.c`. Mode F code path is compiled-in but cannot execute until opinion letter received and flag set to 1 in np_hub_config.h.
- [ ] **IRB protocol (NP-IRB-001)** — before first study descriptor is sent to any device. Month 9 target. See NP-PRIV-REM-001 STEP-20 and §4.4.
- [x] **App privacy audit (NP-PRIV-AUDIT-001 Rev A, 2026-06-03)** — 16 findings (0 Critical, 5 High, 7 Medium, 4 Low); 16-item compliance checklist AUDIT-01 through AUDIT-16 (all OPEN — blocking external beta). See NP-PRIV-REM-001 STEP-21. **Follow-on analysis NP-PRIV-ANALYSIS-002 (2026-06-05, PR #112):** second-pass code review surfaced 12 additional findings (1 Critical, 3 High, 5 Medium, 3 Low); 6 repaired on PR #112; remaining 6 repaired in session 2026-06-05: MEDIUM-07 (sessionDay coarsening), MEDIUM-08 (WC bridge epoch exclusion), LOW-10 (contact redaction), LOW-11 (SPKI cert pinning), LOW-12 (Keychain load query filter). OI-PA-03 (BIPA locale detection) resolved: locale gate removed, biometric consent shown to all users. OI-PA-04 (Adaptive Adjustments copy sign-off) remains open. All code-level findings in NP-PRIV-ANALYSIS-002 now resolved. **Consumable tracker analysis NP-PRIV-ANALYSIS-003 Rev A (2026-06-08):** consumable tracker feature audited. 0 Critical, 0 High, 0 Medium, 2 Low findings. LOW-1: notification lock-screen title exposes device ownership (recommended: generic title "NeurOne" on lock screen, consumable detail revealed after unlock). LOW-2: `requestNotificationPermission()` called at app init before onboarding (recommended: defer to `ConsumableView.onAppear` for in-context permission request). See `docs/np_priv_analysis_003.md`.
- [ ] **FHIR ImplementationGuide (NP-INT-FHIR-001)** — before any T2 EHR integration code is written. G1 gate item NP-COORD-001 G3-09. See NP-PRIV-REM-001 STEP-14.
- [x] **T2 scripting API spec (NP-API-001 Rev A, 2026-06-07, PR #121)** — REST + WebSocket API specification; UHDR three-condition gate; SHDR-only default; 8 REST endpoints; rate limits (1,000/hr, 10,000/day); 256-bit key / 90-day expiry; audit log; 7 open items (OI-API-01..07). G1 gate item. See `docs/np_api_001.md`.
- [ ] **App Store privacy nutrition label and Google Play Data Safety** — before first TestFlight / Play internal test. Requires NP-APP-TELEMETRY-001 complete. See NP-PRIV-REM-001 STEP-16, STEP-17 and §4.2, §4.3.
- [ ] **EU data residency for T2 clinical cloud** — when T2 cloud infrastructure vendor is selected. Month 9 target. See NP-PRIV-REM-001 STEP-19.
- [ ] **T2 scripting API independent security audit (NP-SEC-PENTEST-002)** — before any clinical API key is issued. See NP-PRIV-REM-001 STEP-26.
- [ ] **BIPA legal opinion (Illinois biometric data)** — obtain before any Illinois resident activates a NeurOne device. EEG patterns qualify as biometric information under BIPA 740 ILCS 14. Required elements: BIPA-specific written release in consent flow; publicly available biometric retention/destruction policy on website; legal opinion on whether UHDR encryption creates a BIPA "possession" defense. See NP-PRIV-001 Rev B HIGH-01.
- [ ] **Washington MHMD regulatory analysis** — obtain before any Washington state device activation. MHMD RCW 70.372 covers SHDR behavioral patterns (consumable counts, session counts) as consumer health data. Requires standalone authorization distinct from HIPAA consent, and prohibits sale. Engage WA-qualified privacy counsel. See NP-PRIV-001 Rev B HIGH-02.
- [x] **Minimum age declaration in app consent flow** — `AgeGateView.swift` implemented (Issue #51, PR #106, 2026-06-04). "I confirm I am 16 years of age or older." unchecked checkbox; Continue disabled until checked; persisted to `np.onboarding.age-confirmed` in UserDefaults; presented before all consent layers; `Under16View.swift` explains feature availability for under-16 users. OI-PA-01 (legal counsel 16-year threshold confirmation) still open. See NP-PRIV-001 Rev B MEDIUM-03.
- [ ] **HIPAA Expert Determination certifier engagement (STEP-31)** — before NP-FW-ANON-001 is finalised; before any study descriptor is authored. Named certifier (individual or firm) under standing engagement. NP-ANON-CERT document template required. See NP-PRIV-REM-001 STEP-31 and 45 CFR §164.514(b)(1).
- [ ] **NP-ANON-CERT-[study_id] per study (STEP-32)** — signed Expert Determination certification required before each study descriptor is cryptographically signed and deployed. BLOCKING gate enforced in deployment pipeline. See NP-PRIV-REM-001 STEP-32.
- [x] **Adaptive stimulation transparency (STEP-33, Issue #98, 2026-06-03):** np_adapt_trigger_t (17 values) + np_adaptation_event_t + API in firmware/hub_control/include/np_adaptation_log.h; NP_LOG_TAG_UHDR_ADAPT_EVENT 0x18 + np_log_adapt_event() in np_session_log; AdaptationEvent.swift + AdaptTrigger enum (plainLanguageDescription) + CompletedSessionSummary; AdaptiveAdjustmentsCard.swift (≤5 inline + "View all"); SessionHistoryView.swift; GDPR Art. 13(2)(f) §4 in docs/np_priv_notice_001.md. T2 NP-API-001 clinical report deferred to G3. OI-PA-04 OPEN — card must not ship without Privacy Lead copy sign-off.
- [ ] **SHDR fleet DB schema freeze gate (OI-EMMC2-07)** — CI schema test must pass before SHDR schema is frozen. Test confirms no raw accelerometer columns (g-force, orientation, drop count, timestamps) exist. Spec in NP-FW-EMMC-002 §G. BLOCKING for SHDR fleet DB schema freeze.
- [ ] **QMS — select and implement eQMS platform** — Month 9 target. Candidates: MasterControl, Greenlight Guru, SimplerQMS, Veeva Vault QMS. Must be 21 CFR Part 11 compliant with DHF and IEC 62304 modules.
- [x] **SW-01 Safety MCU unit-level FMEA (NP-FMEA-001 Rev B, 2026-06-15)** — 43 failure modes across SW01-M01..M08; ISO 14971 S×P scoring; two initial UNACCEPTABLE risks (FMEA-M03-01 charge accumulator overflow, FMEA-M05-02 HR delta underflow) mitigated to ACCEPTABLE by architectural controls (64-bit accumulator, int16_t signed arithmetic, MISRA C:2012 enforcement); 5 open items (hardware bench OI-FMEA-01/02 pending G2 prototype). Rev B (OI-SW01-M07-02 CLOSED, PR #132): §3.7 description updated to reflect np_crypto/Monocypher 4.0.2 backing; FMEA-M07-05 mitigation updated to reference Monocypher ct_memcmp. See `docs/np_fmea_001.md`.
- [x] **Build infrastructure established (2026-06-08):** `firmware/CMakeLists.txt` super-project (host-test mode `-DNP_BUILD_TESTS=ON` + ARM cross-compile mode); `firmware/cmake/stm32g071.cmake` Cortex-M0+ toolchain; `firmware/safety_mcu/` full build system (STM32G071, produces `.elf/.bin/.hex`, linker script with `.fault_latch` NOLOAD section, startup assembly for 36 KB SRAM); `.github/workflows/firmware-host-tests.yml` (CMake host tests, Ubuntu); `.github/workflows/web-ci.yml` (Bun + tsc + Vitest + Vite, Ubuntu); `.github/workflows/ios-ci.yml` (xcodebuild test, macOS-14); `.gitignore` (project-appropriate rules replacing Java boilerplate).
- [x] **SW-01 Safety MCU firmware source authored + 10 critical bugs corrected (2026-06-08):** All 8 IEC 62304 Class C modules implemented: np_gpio_mgr (M01), np_spi_watchdog (M02), np_charge_monitor (M03), np_thermal_interlock (M04), np_cardiac_interlock (M05), np_impedance_check (M06), np_session_sig (M07), np_fault_latch (M08). 7-angle code review surfaced 10 confirmed bugs; all fixed: **(1) CRITICAL — watchdog dead path:** `np_spi_watchdog_check()` was never called; added extern + call every main-loop iteration. **(2) Session transition wiring:** `np_session_sig_reset()`, `np_charge_monitor_reset_session()`, `np_impedance_check_request()` now called on `session_active` 0→1 transition. **(3) Impedance poll wired:** `np_impedance_check_poll()` now called every main-loop iteration. **(4) CUTOFF bit cleared unconditionally:** `np_spi_watchdog_tick()` cleared `NP_SAFETY_STATUS_CUTOFF` on any valid heartbeat, masking active thermal/charge/cardiac faults from main processor — now only clears when no other interlock active. **(5) `s_cutoff_active` never reset:** After 30s lockout expiry, cardiac interlock was permanently disabled for the session — now reset when lockout expires. **(6) Lockout wrap at ~49 days:** Absolute-timestamp `>=` comparison wraps at `UINT32_MAX` — replaced with elapsed subtraction `(now - start) >= period`. **(7) First RR phantom interval:** `s_last_capture=0` produced a phantom RR on first beat — guarded with `s_first_beat_seen` flag. **(8) 5s observation window not implemented:** `NP_CARDIAC_OBS_MS` defined but never used — rolling baseline now refreshed every observation window. **(9) `NP_SAFETY_STATUS_CARDIAC` sticky:** Never cleared, blocking CVNS re-enable permanently — cleared in new `np_cardiac_interlock_reenable()` called by main when hub asserts `NP_SESSION_STATUS_CVNS_REENABLE`. **(10) CVNS re-enable protocol:** New `NP_SESSION_STATUS_CVNS_REENABLE` bit (bit 1 of `session_status` frame byte) allows hub to signal explicit re-enable after cardiac cutoff; triggers reenable + fresh impedance check. See `firmware/safety_mcu/`.
- [x] **SW-01 Safety MCU second-pass code review — 9 additional bugs corrected (2026-06-08):** 7-angle second-pass review of all 8 Class C modules surfaced 9 additional bugs (8 fixed in code; 3 documented as open items requiring design work — see OI-CHARGE-01, OI-CHARGE-02 below (OI-SW01-M07-02 subsequently CLOSED 2026-06-11)): **(1) `.fault_latch` linker placement bug:** `.fault_latch` section floated immediately after `.bss` mid-SRAM rather than at the top-of-SRAM address where `_fault_latch_start` symbol pointed — warm-reset persistence silently broken; fixed by adding `. = _fault_latch_start;` inside the section body (`startup/stm32g071_flash.ld`). **(2) `rr_to_bpm()` int16_t overflow:** RR intervals < 1831µs (noise spikes, contact bounce, > 32767 BPM) silently overflow int16_t, producing phantom cardiac cutoffs or missed cutoffs; now saturates to INT16_MAX (`np_cardiac_interlock.c`). **(3) `current_hr_bpm()` double-call ISR race:** function called twice per main-loop tick with no atomic guard — an R-peak ISR arriving between calls produces an inconsistent HR delta that could both miss a true event and false-trigger; now called once into a local variable and reused. **(4) Baseline refresh desensitization after cutoff:** `s_baseline_bpm` refreshed unconditionally while `s_cutoff_active`, adopting elevated post-event HR as the new baseline and desensitizing cardiac detection for any subsequent event in the same session; refresh now gated on `!s_cutoff_active`. **(5) `NP_SAFETY_STATUS_IMPEDANCE` permanent lock:** IMPEDANCE status bit set on a failed check but never cleared on successful recheck — any single impedance measurement failure would permanently disable the affected channel for the device's operational lifetime; fixed: bit now cleared when re-check passes (`np_impedance_check.c`). **(6) Fault latch overwrite on first loop after warm reset:** `np_fault_latch_commit()` executing on the first main-loop iteration after a warm reset overwrote the preserved latch data (specific fault slot + detailed status bits) with the generic `FAULT=0x01 / slot=0xFF` from freshly-zeroed `s_state` — destroying the diagnostic forensic record; guarded by new `s_prior_latch_reported` flag (`np_safety_main.c`). **(7) GPIO startup comment inversion:** comment stated "All stimulation GPIO is driven LOW (cutoff) at startup" — incorrect for active-LOW system (LOW = enabled, HIGH = disabled) — would mislead HAL implementer and cause bring-up error; corrected to "HIGH (disabled) at startup; LOW = enabled". **(8) Double-evaluated fault flags in `np_spi_watchdog_tick()`:** four-flag OR expression evaluated twice with no critical section — if any interlock migrates to ISR, a fault asserting between the two evaluations clears CUTOFF while simultaneously granting the full requested mask (unsafe state); extracted to single local `uint8_t active_faults`. **(9) Fault latch count saturation:** `np_fault_latch_commit()` incremented `s_latch.count` every main-loop iteration while FAULT bit set — UINT16_MAX (65535) reached in ~65 seconds, freezing the event counter and hiding all subsequent distinct fault events in SHDR audit trail; now change-gated: count increments only when status or fault_slot actually changes (`np_fault_latch.c`). See `firmware/safety_mcu/`.
- [x] **OI-SW01-M07-01 CLOSED: Session signature delivery mechanism — 102-byte SPI command frame (2026-06-16)** — Two-frame SPI protocol designed and wired. New `np_safety_sig_cmd_t` (102 bytes, magic `0xC0 0xDE`): 2-byte magic + 1-byte cmd type + 1 reserved + 32-byte SHA-256 hash + 64-byte Ed25519 sig + 2-byte additive checksum. HAL distinguishes from 8-byte heartbeat by NSS-delineated transfer length. `NP_SAFETY_STATUS_SIG_PENDING` (bit 7) added to `np_safety_status_t`: set by `np_session_sig_reset(state*)` on session 0→1 transition; cleared by `np_session_sig_verify()` on valid sig; persists (+ FAULT + CUTOFF, fault_slot=0xFD) on bad sig. `NP_SAFETY_STATUS_SIG_PENDING` added to `active_faults` mask in `np_spi_watchdog_tick()` — blocks `granted_mask` until hub delivers signature. Hub-side `np_safety_spi_send_session_sig(hash, sig)` builds and sends the 102-byte frame; must be called after first heartbeat with `session_active=1` and before first non-zero enable request. Sequencing: heartbeat(session_active=1, mask=0) → MCU sets SIG_PENDING → hub sends 102-byte cmd → MCU verifies → hub checks SIG_PENDING in next heartbeat reply → if cleared, sends heartbeat(mask=channels). Files modified: `np_safety_protocol.h`, `np_session_sig.c`, `np_spi_watchdog.c`, `np_safety_main.c`, `np_safety_spi.h/.c`, `np_hub_config.h`, `np_hub_types.h`. **38 host tests added** (`firmware/safety_mcu/tests/np_safety_spi_proto_tests.c`): frame sizes + field offsets, magic distinctness, checksum round-trip + corruption detection, SIG_PENDING lifecycle, watchdog grant gating, constant values, heartbeat unaffected. All 38 pass. `np_safety_spi_proto_tests` added to root `firmware/CMakeLists.txt` host test suite (now 6 test binaries). Session sig verification now has full safety effect on `granted_mask`.
- [x] **OI-CVNS-HUB-01 CLOSED: hub-side `NP_SESSION_STATUS_CVNS_REENABLE` logic (2026-07-08)** — New pure-C, host-tested three-gate state machine `firmware/hub_control/src/np_cvns_reenable.c` (+ `include/np_cvns_reenable.h`) asserts the re-enable bit ONLY when ALL three conditions hold: (1) `NP_CVNS_REENABLE_LOCKOUT_MS`=30000 elapsed since the hub OBSERVED `NP_SAFETY_STATUS_CARDIAC` in a heartbeat reply (the hub window ⊇ the MCU's own 30s lockout because hub observation lags MCU cutoff by ≤1 beat); (2) explicit app confirmation via `np_cvns_reenable_confirm()`; (3) a fresh hub-side impedance check (HAL `np_cvns_hal_impedance_start`/`_poll`, OI-CVNS-HUB-02) PASSED. States IDLE→LOCKOUT→AWAIT_CONFIRM→IMPEDANCE→ASSERTING; only ASSERTING emits the bit; impedance fail/timeout and a 2s assert-timeout all fail-closed to AWAIT_CONFIRM; any session-boundary or external CARDIAC clear resets to IDLE. **Single-writer concurrency design (Forge audit findings 1+2):** `confirm()` (transport task) only posts a one-shot intent flag; the higher-priority heartbeat task is the SOLE state mutator and consumes/re-validates the flag from AWAIT_CONFIRM — race-free without a lock, and a stale confirmation is discarded on every reset and new-cutoff rising edge. Wired into `task_safety_heartbeat` (`np_hub_control_main.c`): drives the machine and sets the bit BEFORE building each TX frame (Forge finding 3 — guarantees CVNS_REENABLE and ACTIVE are consistent in the same frame, never REENABLE=1 with ACTIVE=0 during teardown). Recoverable-cutoff policy: a cardiac-only fault status (CARDIAC set, none of `NP_CVNS_NONRECOVERABLE_FAULTS`) no longer aborts the session — the hub holds it so re-enable is possible. **Two blocking SPI bugs fixed:** (a) `np_safety_spi_heartbeat()` was casting the `np_session_state_t` enum directly into `session_status` (RUNNING=3 spuriously set CVNS_REENABLE, PAUSED=4 set GEOM_REQUIRED) — replaced with pure host-tested helper `np_safety_session_status_bits()` (ACTIVE for RUNNING/PAUSED/STOPPING); (b) `task_safety_heartbeat` used a stale 2-arg signature and echoed the granted mask — now calls the 4-arg form and transmits the REQUESTED mask via new `np_safety_spi_get_requested_mask()`. Defense-in-depth: safety MCU `np_safety_main.c` re-enable now additionally gates on `s_state.session_active`. Session runner resets the manager at session start. SHDR lifecycle logging via `np_log_shdr_fault()` with `NP_CVNS_SHDR_EV_*` flag codes and suppressed timestamp (no HR/RR biology). **76 host tests** (`firmware/hub_control/tests/np_cvns_reenable_tests.c`) wired into `firmware/CMakeLists.txt` + `hub_control/CMakeLists.txt` NP_BUILD_TESTS path; full host suite **8/8 binaries, 244 checks, 0 failures**. CI `firmware-host-tests.yml` trigger paths extended to `firmware/hub_control/**`, `firmware/safety_mcu/**`, `firmware/common/**`. **RESIDUAL — OI-CVNS-HUB-02** (hub-side impedance HAL driver) and **OI-CVNS-HUB-03** (app-transport wiring of `np_hub_cvns_reenable_confirm()`) are stubbed integration points. FAI-CV02 hardware bench timing still required at T2 prototype. OI-CVNS-HUB-01 in NP-FW-HUB-001.
- [x] **OI-SW01-M07-02 CLOSED: `np_ed25519_verify` shared crypto library — ARM link error resolved (2026-06-11)** — Created `firmware/crypto/` static library (`np_crypto`) backed by **Monocypher 4.0.2** (RFC 8032, SHA-512, vendored in `firmware/crypto/vendor/monocypher/`). `np_session_sig.c` `extern` replaced with `#include "np_crypto.h"`; `firmware/safety_mcu/CMakeLists.txt` links `np_crypto`. All 11 host tests pass (`np_crypto_tests`: 5 SHA/CRC + 6 Ed25519 including TV1/TV2 RFC 8032 vectors + all-zero pubkey guard). Bootloader intentionally retains self-contained `np_signature.c` (uses `-nostdlib/-nodefaultlibs`; Monocypher requires libc symbols unavailable in that context). SOUP record: `firmware/crypto/vendor/monocypher/VERSION` (version 4.0.2, BSD-2-Clause OR CC0-1.0, IEC 62304 §8.1.2). Build system: `C_EXTENSIONS OFF` via CMake property (prevents GNU extensions, MISRA C:2012 compliant); Monocypher SOUP files compiled with `-w` to isolate from first-party `-Wall -Wextra`. Code review: 3 findings resolved. Privacy analysis: clean — Ed25519 verify operates on manufacturing root key + session descriptor hash (SHDR-class, no UHDR). OI-SW01-M07-01 (SPI delivery frame for 96-byte hash+sig payload) CLOSED 2026-06-16. See `firmware/crypto/`, `firmware/safety_mcu/`.
- [x] **OI-CHARGE-01 CLOSED: `np_charge_monitor_accumulate()` wired — 40µC/cm² charge density limit now enforced (2026-06-16)** — Extended heartbeat frame `np_safety_rx_ext_frame_t` (38 bytes) introduced in `firmware/common/include/np_spi_wire_types.h`: 8-byte base (magic + session_status + enables + channel_count + base_checksum) + 28-byte `current_ua[14]` (NP_SAFETY_MAX_CHANNELS × uint16_t, commanded µA per channel) + 2-byte `ext_checksum`. Privacy gate satisfied: `current_ua[]` is SHDR "commanded current from session descriptor" — explicitly NOT ADC-measured actual current (which would be UHDR) — classification comment in all 5 affected production files. Accumulate loop wired in `np_safety_main.c` after all interlocks reduce `granted_mask` (thermal → impedance → cardiac → accumulate → charge_tick — order critical). Per-channel `s_limit_nc[14]` replaces global constant in `tick()`. `np_charge_monitor_set_channel_limit()` API hook added for geometry override (OI-CHARGE-02). Hub-side `np_safety_spi_heartbeat()` extended with `current_ua` + `channel_count` params; builds 38-byte ext frame; receives MCU reply as first 8 bytes of 38-byte full-duplex transfer. Array bounds use `NP_SAFETY_MAX_CHANNELS` throughout (code-review fix). **26 np_charge_monitor_tests + 12 new np_safety_spi_proto_tests pass (76 total / 0 failures).** FAI bench test with calibrated current source still required at G2 prototype. OI-CHARGE-01 in NP-SW-001.
- [x] **OI-CHARGE-02 CLOSED: HD-tDCS electrode geometry wired from hub session runner to safety MCU charge monitor (2026-07-07)** — The hub session runner (`np_session_runner.c`) scans the verified session descriptor at session start; for any `NP_MOD_HD_TDCS` command with a `RING_4X1`/`BILATERAL_4X1` montage (3.5mm Ag/AgCl electrodes) it sends the electrode AREA (96 milli-cm² = floor(0.0962cm²×1000), `NP_HD_SMALL_ELECTRODE_AREA_MCM2`) for the CLIN_STIM channel (index 13, `NP_SAFETY_CH_CLIN_STIM`) to the safety MCU via a new 34-byte command frame `np_safety_chan_limit_cmd_t` (magic 0xC0/0xDE, `cmd_type=NP_SAFETY_CMD_CHAN_LIMIT` 0x02; `firmware/common/include/np_spi_wire_types.h`). Delivered by `np_safety_spi_send_channel_limits()` (`hub_control/src/np_safety_spi.c`). The safety MCU main loop reads it AFTER the session-start reset block (`np_safety_main.c`) and calls the new `np_charge_monitor_set_channel_area_mcm2()`, which derives `limit_nc = NP_CHARGE_LIMIT_UC_CM2 (40) × area_mcm2` → 3840nC = **3.84µC** on CLIN_STIM (vs the 1000µC pad default). **Design invariant:** the hub transmits electrode AREA only — the 40µC/cm² density constant stays resident on the Class C safety MCU. Area is floored so the derived limit never exceeds the true 40µC/cm² ceiling. **Tests:** +3 charge-monitor cases (HD-tDCS trip at 3.84µC, 25cm²-via-area = 1000µC, other-channel isolation) and +3 SPI-proto cases (34-byte frame size/offsets, checksum round-trip + corruption); host suite 7/7 green. **RESIDUAL — OI-CHARGE-03 CLOSED (2026-07-07):** the fail-open hole is now closed by a fail-safe geometry gate (see next bullet). FAI bench with a calibrated current source still required at G2 prototype. OI-CHARGE-02 in NP-SW-001.
- [x] **OI-CHARGE-03 CLOSED: fail-safe electrode-geometry gate blocks CLIN_STIM until HD-tDCS area applied (2026-07-07)** — Closes the OI-CHARGE-02 fail-open residual: if the electrode-area command is lost/corrupt, CLIN_STIM would keep the permissive 1000µC pad default. New shared heartbeat bit `NP_SESSION_STATUS_GEOM_REQUIRED` (bit 2, `firmware/common/include/np_spi_wire_types.h`) is asserted by the hub every 200ms while an HD-tDCS ring/bilateral montage is in the session (`np_safety_spi_set_geom_required()` in `hub_control/src/np_safety_spi.c`; armed by the session runner in `np_session_runner.c` before any CLIN_STIM enable; cleared on abort/end). The safety MCU reads it into `np_safety_state_t.geom_required` and `np_charge_monitor_geom_gate()` clears `NP_SAFETY_EN_CLIN_STIM` from `granted_mask` whenever `geom_required && !geom_applied` — where `geom_applied` latches true only when a valid area command sets CLIN_STIM's limit (`np_charge_monitor_set_channel_area_mcm2()`), and resets each session. The gate runs among the interlocks **before** the charge-accumulate loop (a gated channel accrues no charge). **Fail-CLOSED:** a dropped area command keeps HD-tDCS disabled until a retry lands, rather than running at the wrong limit. **Scoped to CLIN_STIM only** — clinical tACS shares enable bit 13 but never sets GEOM_REQUIRED, so it is never blocked. **No new MCU status bit** (the 8-bit status byte is full); the block is observable via CLIN_STIM absent from granted_mask, and the hub needs no prompt since it already sends the area command. **Irreducible limit (documented):** a hub that never declares GEOM_REQUIRED for an HD-tDCS session still runs at the default — HD-tDCS and clinical tACS share enable bit 13, so full closure would need per-electrode enable lines (hardware). **Tests:** +5 charge-monitor gate cases (blocked-until-applied, granted-after-applied, tACS-unaffected, CLIN_STIM-only, re-arm-per-session) and +4 SPI-proto session-status-bit assertions; host suite 7/7 green. OI-CHARGE-03 in NP-SW-001.
- [ ] **Fault latch extended SPI command privacy gate** — the 8-byte `np_safety_tx_frame_t` does not transmit the fault latch `tick_ms` (event timestamp) or `count` fields from `np_fault_latch_t`. When a future dedicated SPI read command is designed to let the hub retrieve the full fault latch, the `tick_ms` routing must be specified: device fault timestamps in general → SHDR (consistent with "safety interlock log → SHDR"). However, when the fault type is `NP_SAFETY_STATUS_CARDIAC` (cardiac cutoff during CVNS), the `tick_ms` co-locates a specific event time with a UHDR-class health event and could be linkable to a session record. **Privacy gate:** the extended SPI command spec must explicitly assign tick_ms to SHDR (or suppress it for CARDIAC faults) and add this data element to the 27-element session data classification table in NP-FW-EMMC-001 Rev B. No change required to current firmware — the tick_ms never leaves SRAM in the current 8-byte frame. See `firmware/safety_mcu/src/np_fault_latch.c`.
- [ ] **SBOM (Software Bill of Materials)** — NP-SBOM-001 planned Year 2; required for 510(k) cybersecurity submission.
- [x] **SessionHistoryStore session timestamp migration (NP-PRIV-ANALYSIS-002 MEDIUM-07, 2026-06-05)** — `SessionRecord.completedAt: Date` replaced with `sessionDay: String` ("yyyy-MM-dd" local calendar day) + `insertionIndex: Int` (same-day ordering tiebreaker). Exact timestamps no longer written to UserDefaults or iCloud/iTunes backups. No v1 migration — this is pre-production with no deployed user data. Synthesized `Codable` (no custom init/encode). One new test: `testPersistedBlobContainsNoCompletedAtKey` (privacy invariant). Display formatters updated to omit time. `CompletedSessionSummary(record:)` reconstructs start-of-day Date from sessionDay for display. Resolved before US beta.
- [x] **Watch bridge payload review — `SessionState.toWCMessage()` (NP-PRIV-ANALYSIS-002 MEDIUM-08, 2026-06-05)** — Payload reviewed. No raw EEG or HRV time-series values present. `coherenceX100` and `rmssd` are derived display metrics (same values in `SessionRecord`). `epoch` (Unix ms timestamp, UHDR-class) removed from payload — Watch elapsed timer is driven by a local `Timer`, not `epoch`. `from(wcMessage:)` defaults `epoch: 0` on reconstruction. `WatchSessionManager` is entirely in-memory; no Watch-side UserDefaults persistence. Resolved before Watch beta.
- [x] **Certificate pinning for SHDR fleet endpoint (NP-PRIV-ANALYSIS-002 LOW-11, 2026-06-05)** — `SHDRUploader` now uses a dedicated private `URLSession` with `SHDRFleetPinningDelegate: URLSessionDelegate` implementing SPKI SHA-256 pinning. Algorithm: OS trust store evaluation first, then leaf-certificate SPKI hash compared against `pinnedHashes: Set<Data>`. Covers RSA-2048/4096 and EC P-256/P-384 via TrustKit-compatible DER SPKI headers. Session uses `.ephemeral` configuration. Two placeholder hashes installed — replace with actual derived hashes before production deployment. `CryptoKit.SHA256` used for hash computation. `SecTrustCopyCertificateChain` used (iOS 15+).
- [x] **SessionProtocolSigner Keychain load query — add `kSecAttrAccessible` filter (NP-PRIV-ANALYSIS-002 LOW-12, 2026-06-05)** — `kSecAttrAccessible: kSecAttrAccessibleWhenUnlockedThisDeviceOnly` added to the `SecItemCopyMatching` read query in `loadOrCreateSigningKey()`. Prevents an attacker-written key with a different accessibility class but matching `kSecAttrApplicationTag` from being returned by the query. Write/add query already had the filter; now read and write are consistent.
- [x] **ConsentDashboardView contact email redaction (NP-PRIV-ANALYSIS-002 LOW-10, 2026-06-05)** — `redacted(_:)` helper added to `ConsentDashboardView`. Email → `•••@domain.com`; phone → `••• ••• ••XX`. Summary row uses redacted form; full value remains in Research Preferences sheet.
- [ ] Partner optician network contract (S3 Rx program)
- [x] eMMC partition architecture + separate UHDR/SHDR encryption in firmware specification (before any storage code is written) — **NP-FW-EMMC-001 Rev A (2026-05-11); see `docs/neurone_fw_emmc_001.docx`**
- [x] Dual-bank OTA bootloader (must be from first firmware line) — **NP-FW-EMMC-001 Rev A §8.1–§8.4; firmware/bootloader/ (Issue #20)**
- [x] Cervical VNS safety interlock specification + safety MCU cardiac interlock (NP-FW-CVNS-001 Rev A — Issue #24, G3-08 SOFTWARE BASELINED)
- [x] Cervical VNS 510(k) Q-Sub substantial equivalence argument (NP-REG-CVNS-001 Rev A)
- [x] 1064nm smart zone module firmware specification (NP-FW-PBM1064-001 Rev A — Issue #53, firmware/pbm_1064nm/)
- [x] 1064nm multi-wavelength session protocol (NP-SES-1064-001 Rev A — Issue #53/#55, docs/np_ses_1064_001.md)
- [ ] FAI-CV01 electrode placement bench (anatomical phantom — T2 prototype required)
- [ ] FAI-CV02 cardiac interlock timing bench (oscilloscope, R-peak signal generator — T2 prototype required)
- [ ] FAI-CV03 tolerability (3 healthy adults — IRB approval required)
- [ ] **OI-BLE-01: SHDRUploader upgrade path to hub-provisioned TRNG warranty token** — `SHDRUploader` currently uses a Keychain-generated 32-byte random token as device identity in SHDR uploads. When hub firmware ships the `warrantyToken` GATT characteristic (OI-WA-03, UUID `4E455550-0010-…`), `SHDRUploader` must subscribe to `NeurOneGATTManager.$warrantyToken` via Combine and call an `upgradeDeviceToken(_:)` method to replace the Keychain token with the hub-provisioned TRNG value. This upgrade path is BLOCKING for the full SHDR fleet DB no-join architecture (NP-FW-EMMC-002 Rev A §A). `NeurOneGATTManager.warrantyToken` is `@Published private(set) var warrantyToken: Data?` — the Combine subscriber is ready to wire. Tracked in app/ios/ISA.md Decisions as OI-BLE-01.
- [ ] SBIR Phase I application
- [ ] First researcher contacts
- [ ] SAB formation (Tsai outreach first)

### 13.5 Completed and locked decisions

- Two-tier product strategy with shared platform
- 600-LED FPC zone modules from launch (no Rev A/Rev B)
- 400 mW/cm² peak pulsed via firmware (Path A, $0 BOM) — pending regulatory opinion
- 21-channel EEG montage with FC3/FC4/Oz/A1/A2
- STM32G071 safety MCU (not G031)
- No NFC anywhere — optical code + resistive pogo authentication
- USB-C wired-first, BT/Wi-Fi toggle, antennas in hub not headset
- Boa dial fit system with spring-decoupled pods
- 5-layer passive + active Helmholtz EMF shielding with palladium fabric
- UHDR/SHDR terminology framework — complete separation, never linked by design
- Clinical consent engine: use case library → minimum necessary data mapper → plain-language consent document
- Research partnership: a priori consent (4 layers), results opt-in, suggestion portal, crowdfunding
- POA workflow with human document review
- Service network: Tier A optical + Tier B electronics + Tier C retail + depot
- All interface protection covers tethered to headset
- Predictive maintenance system via SHDR fleet telemetry
- Charger policy: auto-include correct charger at every upgrade + $19 upfront 65W option at checkout
- All durability changes listed in §7
- **FPC 20-pin pinout locked:** Pin 18 = ZONE_ID (resistor per zone, 1% 0402: ZM-01=10kΩ, ZM-02=22kΩ, ZM-03=47kΩ, ZM-04=100kΩ, ZM-05=220kΩ); Pin 19 = PD2_CATHODE (scalp-side reference photodiode, RISK-14 Option B)
- **Zone module dual photodiode (RISK-14 Option B):** PD2 on scalp-facing surface, pin 19; PD1/PD2 ratio separates fouling from LED aging; T1/T2 identical mold
- **Five-layer zone module keying (RISK-15):** mechanical key + ZONE_ID resistor + braille/numeral + shell tactile dots + bone conduction audio; works for color-blind and blind users
- **Self-sealing gasket (RISK-16 Option A):** co-molded Shore 40–50A silicone, no user RTV, IPX4 rated after 10 swap cycles
- **Sliding eject lever (RISK-22 Option A):** 10–12mm, 3:1 mechanical advantage, ≤1N extraction force; accessibility target: Parkinson's Hoehn & Yahr II–III
- **ZONE_ID firmware debounce:** 3× ADC reads at 100ms intervals before FAULT; ≥2/3 must pass (RISK-18)
- **PDMS bonding process:** SiO₂ 75nm interlayer + O₂ plasma, ≥150 N/m peel strength; 200-cycle IEC 60068-2-14 qualification BLOCKING before production (RISK-04)
- **EEG cable routing (RISK-21, OI-09 CLOSED — NP-DRV-SHELL-001 Rev B, 2026-05-10):** Dedicated 8×5mm main trunk + 6×4mm lateral branch channels on outer CFRP inner surface (opposite side from FPC bundle, scalp-side). Routing: Hub PCB EEG connector → occipital arch main trunk (~120mm) → crown junction → left branch (P3/C3/F3/Fp1) and right branch (P4/C4/F4/Fp2), each ~90–200mm arc. Shell wall routing achieves 18–22mm point-to-point FPC-to-EEG separation — DRC-18 PASSES without barrier. Snap-in retention clips at 80mm intervals. Channel spec handed to shell tooling supplier. DRC-22 CLOSED. DRC-23 CLOSED. G2-11 CLOSED in NP-COORD-001.
- **Multi-FPC bundle management (RISK-17):** ≥2mm inter-FPC separation; ≥15mm FPC-to-EEG (or grounded Al foil barrier); 3 anchor bosses per FPC; all 5 Hub ZIF connectors on same PCB edge
- **Risk register documented:** 25 risks total (RISK-01 through RISK-25); 23 MITIGATED; 2 OPEN: RISK-03 (regulatory opinion, external) and RISK-20 (CFRP Ra confirmation, external); RISK-25 (cervical VNS cardiac reflex) MITIGATED pending FAI-CV02 hardware bench
- **HRV biofeedback protocol (software only):** Resonance frequency breathing pacer (6 breaths/min default, personalized sweep); real-time coherence score; four protocols (standalone, HRV+taVNS synchronized, HRV+EEG dual biofeedback, HRV+PBM); no additional hardware; bone conduction delivers breathing cue; uses existing VNS clip PPG. Locks the VNS+HRV modality as the only NeurOne modality with multi-modal trial evidence (2025 RCT: PBM + qEEG NF + HRV biofeedback simultaneously).
- **sLORETA-guided HD-tDCS (T2, NP-FW-HD-001 Rev A, 2026-05-11):** Full firmware specification baselined. Scalar sLORETA weight matrix W (2447 voxels × 21 channels, 7mm MNI grid, ≈205KB) precomputed offline; loaded into LPSDR4 at session start. On-device runtime: 64-epoch covariance accumulation (C = 21×21 symmetric, 1.8KB SRAM) → voxel source power map (P[v] = W_v^T C W_v, 2447 floats, 9.6KB LPSDR4) → peak detection → MNI→10-20 electrode mapping. Electrode MNI lookup table from Jurcak et al. (2007). 4×1 ring selection: E_center = nearest electrode to MNI target; 4 cathodes = nearest neighbours with ≥2 quadrant angular spread constraint. Current distribution: anode I, each cathode −I/4 (charge balanced). Ramp state machine: 30s ramp-up → steady → 30s ramp-down, 100ms tick. Safety MCU owns all enable GPIO; impedance check at 1kHz AC before each session; charge density software monitor (95% of 40µC/cm² triggers abort). Concurrent EEG with 200µs blanking on current step edges; SNR ≥20dB specification. FAI-HD01 (15mm localization error, phantom), FAI-HD02 (electrode mapping, software PASS), FAI-HD03 (FWHM ≤25mm, phantom), FAI-HD04 (SNR ≥20dB, bench). Hardware FAI blocking for G3-07 full closure. 7 predefined clinical targets: DLPFC_L/R, VLPFC_L, ACC, MPFC, M1_L/R. Bilateral 4×1 and standard 2-electrode fallback also implemented. BOM delta $0 (Ag/AgCl dual-rated already in T2 cap spec; tACS driver existing T2 hardware). Closes Issue #23.
- **Cervical VNS safety interlock (NP-FW-CVNS-001 Rev A, 2026-05-11):** Dual-processor cardiac interlock architecture. Safety MCU (STM32G071) owns CVNS_ENABLE_L/R GPIO independently of main processor. R-peak signal from main processor PPG detection drives RPEAK_IN GPIO pulse (5ms active-high) to safety MCU. Safety MCU measures R-R interval via TIM2 (1MHz timer); computes instantaneous HR; compares rolling 5s window mean to pre-stimulation baseline. HR change >15 BPM → TIM6 ISR (5ms) asserts enable GPIO LOW within <5.1ms worst-case (spec: <100ms FAI-CV02). Baseline cross-validation: main processor PPG-derived HR and safety MCU GPIO-timer-derived HR must agree within 5 BPM; mismatch blocks enable. Re-enable requires all three: 30s lockout elapsed + explicit app confirmation + repeat impedance check + baseline re-established. Three firmware modules: np_cvns_interlock + np_cvns_stim (biphasic charge-balanced, 10s ramp-up, 5s ramp-down) + np_cvns_session (UHDR/SHDR record). RISK-25 MITIGATED. Closes Issue #24 software gate (G3-08 SOFTWARE BASELINED).
- **Cervical VNS 510(k) Q-Sub (NP-REG-CVNS-001 Rev A, 2026-05-11):** Substantial equivalence argument baselined. Predicate: electroCore gammaCore K163334 + K173323. SE based on same intended use (cluster headache, migraine) and same/safer technology (biphasic charge-balanced, ≤2 mA vs predicate ≤24 mA). Key differences: lower current (more conservative), bilateral option (≤2 mA supported by literature), cardiac interlock (safety enhancement). 6 Q-Sub questions drafted. Type B meeting target Month 20. Standards: IEC 60601-1, IEC 60601-2-10, IEC 62304 Class C + Class B, IEC 62366-1, 21 CFR 820.
- **Privacy remediation program established (NP-PRIV-001 Rev A + NP-PRIV-REM-001 Rev A, 2026-06-02):** Full privacy analysis (18 findings: 2 Critical, 6 High, 7 Medium, 3 Low) and remediation calendar (30 steps, STEP-01 through STEP-30) authored. Direct remediations completed: NP-FW-EMMC-002 Rev A (warranty token architecture, factory reset, two-layer key scheme, Scratch encryption, EDF+ header policy, Mode F spec), NP-SEC-BR-001 Rev A (Breach Response Plan), NP-PROC-POA-001 Rev A (POA Upload Procedure), NP-APP-TELEMETRY-001 Rev A (App Telemetry Policy). Authority submission guides authored for: EU-US DPF, Apple App Store, Google Play, IRB, HIPAA OCR breach, FTC HBNR, GDPR Art. 33, US state breach laws.
- **Adaptive stimulation transparency — locked (2026-06-03, Issue #98):** np_adapt_trigger_t enum (17 classified triggers, 0x00–0x10) + np_adaptation_event_t struct (session_ms, trigger, mod_type, param_id, value_before_x100, value_after_x100, confidence_pct) in firmware/hub_control/include/np_adaptation_log.h. NP_LOG_TAG_UHDR_ADAPT_EVENT (0x18) + np_log_adapt_event() + np_adapt_log_flush() integrated into np_session_log. Adaptation events routed to UHDR only — no SHDR path. iOS: AdaptTrigger Swift enum with plainLanguageDescription computed property (17 cases, 1:1 parity with firmware enum); AdaptationEvent + CompletedSessionSummary models; AdaptiveAdjustmentsCard SwiftUI view (≤5 inline, "and N more" + "View all" sheet, empty state); SessionHistoryView post-session summary screen. GDPR Art. 13(2)(f) §4 in docs/np_priv_notice_001.md. OI-PA-04 OPEN — plain-language copy requires Privacy Lead sign-off before Adaptive Adjustments card ships. T2 NP-API-001 clinical report schema deferred to G3. Enum values are frozen once written to UHDR — additive only, never renumber. ISA: MEMORY/WORK/20260603-adaptive-transparency/ISA.md.
- **`engagement_tier` replaces `session_sequence` in app analytics — locked (2026-06-03):** Raw session count (health-adjacent behavioural data under Washington MHMD and GDPR Art. 9 by inference) replaced with coarsened 3-bucket enum: `"new"` (1–5 app launches), `"active"` (6–50), `"established"` (51+). Counts app launches, not stimulation sessions. Resets on uninstall. Never transmitted as raw integer. Spec: NP-APP-TELEMETRY-001 Rev B §3 and §3.2.
- **NP-FW-EMMC-002 §G authored — locked (2026-06-03):** SHDR accelerometer reclassification spec complete. Permitted SHDR fields: `drop_detected: bool` and `maintenance_alert: bool` only. Prohibited: raw accelerometer series, g-force values, orientation vectors, timestamps, drop counts. Fleet DB schema CI test required (OI-EMMC2-07) — BLOCKING for SHDR schema freeze. On-device module `np_accel_shdr` processes in SRAM only; raw values never written to any partition. Spec: NP-FW-EMMC-002 Rev A §G.
- **Consumable tracker testability refactor + privacy analysis — NP-PRIV-ANALYSIS-003 Rev A (2026-06-08):** `ConsumableCountsProviding` protocol extracted from `NeurOneGATTManager`; `ConsumableTracker` refactored to accept any conforming provider (dependency injection). `MockCountsProvider` (`CurrentValueSubject`-backed) enables synchronous deterministic unit tests. Two model bugs fixed: (1) `intranasalSleeves.lowThreshold` 5→0 (single-use sleeve was permanently `isLow`); (2) `activeReminders` filter operator-precedence bug → `states.filter { $0.isLow }`. Critical init-order bug fixed: `loadPersistedSnooze()` moved before `observeCounts()` — publisher delivers current value synchronously on subscription (wiping persisted snooze state before load). 23 tests, 0 failures. Privacy analysis: 0 Critical/High/Medium, 2 Low (notification lock-screen exposure, permission timing). See `docs/np_priv_analysis_003.md`. ISA ISC-154 VERIFIED (progress: 28/164).
- **Privacy analysis and repair — NP-PRIV-ANALYSIS-002 (2026-06-05, PR #112 + session 2):** Second-pass code-level privacy audit of iOS app. 12 findings (1 Critical, 3 High, 5 Medium, 3 Low); all 12 code-level findings resolved. 6 repaired on branch `privacy/analysis-and-repair-20260605` (PR #112); remaining 6 repaired in session 2: MEDIUM-07 (SessionHistoryStore — `completedAt` replaced by `sessionDay`), MEDIUM-08 (WC bridge — `epoch` excluded), LOW-10 (ConsentDashboardView — contact redaction), LOW-11 (SHDRUploader — SPKI certificate pinning), LOW-12 (SessionProtocolSigner — Keychain load query accessibility filter). Full analysis: `docs/np_priv_analysis_002.md`. OI-PA-03 (BIPA locale detection) resolved: locale gate removed, biometric consent shown to all users unconditionally. OI-PA-04 (Adaptive Adjustments Privacy Lead copy sign-off) remains as a process open item.
- **UHDRKeyManager — Argon2id + PHC reference library implementation locked (2026-06-11):** PBKDF2 placeholder fully replaced. PHC `phc-winner-argon2` (CC0/Apache 2.0) vendored into `app/NeurOneShared/Sources/NeurOneArgon2/phc/` as an SPM C target. `np_bridge.c` wraps `argon2id_hash_raw()` into `np_argon2id_hash()` API. `Argon2Bridge.swift` exports `npArgon2idDeriveKey(password:salt:outputLength:)` with fixed params m=65536 KiB, t=4, p=1 per RFC 9106. `ARGON2_NO_THREADS` compile flag routes to single-threaded path (spec-compliant for p=1). `UHDRKeyManager.swift` derives WKMD → Argon2id → memory-only `UHDRKey(k1:k2:)` (64 bytes = K1+K2 for AES-256-XTS); key never persisted to Keychain or transmitted. Biometric credential: 32-byte `SecRandomCopyBytes` CSPRNG seed protected by `.userPresence` + `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` + `kSecAttrSynchronizable: false`. Salt: separate 32-byte CSPRNG Keychain entry (non-synchronizable, device-bound). Auth via `.deviceOwnerAuthentication` (biometric preferred, passcode fallback) — supports Parkinson's/post-stroke/no-biometric users. `bzero` in `UHDRKey.deinit` + credential defer block. Release build verified: `nm libNeurOneArgon2.a` confirms `np_argon2id_hash_custom` absent in release binary (gated by `#ifdef NP_ARGON2_CUSTOM_ENABLED` in `np_bridge.c`, `.when(configuration: .debug)` in `Package.swift`). PHC SOUP entry + SHA-256 file inventory in `phc/VENDOR.md`. 6/6 `UHDRKeyManagerTests` pass. Supersedes compile-gate placeholder from PR #112.
- **SHDRUploader — opaque Keychain warranty token + backup exclusions locked (2026-06-05, PR #112):** `identifierForVendor` replaced with a 32-byte `SecRandomCopyBytes` Keychain token (`kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`, non-synchronizable); HTTP header renamed `X-NP-Device-Token`. `shdr_staging.bin` now marked `.isExcludedFromBackupKey` before any write. Hub-provisioned TRNG token (NP-FW-EMMC-002 §A) replaces Keychain fallback when GATT warranty characteristic is available.
- **UHDRBackupScheduler — iCloud/iTunes backup exclusion + manifest hardening locked (2026-06-05, PR #112):** `Documents/UHDRBackup/` directory set `.isExcludedFromBackupKey = true` at init — encrypted UHDR archives no longer flow to Apple/iTunes infrastructure. `keyFingerprint: SHA256(K1).prefix(8)` removed from `BackupManifest` struct — no partial key material in any plaintext file.
- **ResearchAnalyticsGate.reset() wired to research consent withdrawal locked (2026-06-05, PR #112; terminology clarified 2026-06-16):** `ResearchAnalyticsGate.reset()` + `SpyAnalyticsBackend.reset()` added; `ConsentStore.revokeResearchAnalytics()` clears the research analytics key (`np.research.consent-granted`) and calls `reset()` atomically. `withdrawBlanketResearchConsent()` calls `revokeResearchAnalytics()` — SDK torn down, not just gated, on blanket research consent withdrawal. *(A post-PR #112 code change incorrectly decoupled these; regression identified in code review 2026-06-16 and restored per authoritative design: blanket research withdrawal implies full data-collection opt-out. Partial withdrawals — specific study or category — do NOT call `revokeResearchAnalytics()`.)* Terminology: "analytics consent" has been replaced by "research consent" (for UHDR/PostHog) and "warranty consent" (for SHDR/fleet telemetry) throughout all code and docs.
- **Two consent subjects — locked (2026-06-16):** Warranty owner consent (SHDR) and user research consent (UHDR) are entirely separate. Warranty owner is the entity that registered the device — may be a clinic, not the user. A clinic activating warranty has NOT consented on behalf of any patient. `SHDRUploader` (gated by `WarrantyAnalyticsGate`) and `ConsentStore` (manages `ResearchAnalyticsGate`) are code-structurally independent. Blanket research consent withdrawal (L3) additionally revokes research analytics (`ResearchAnalyticsGate`); partial research withdrawals do not. Warranty consent withdrawal has no effect on research analytics. See §6.0 for full invariant table. Spec: `app/ios/NeurOne/Consent/ConsentStore.swift` header comment and `docs/np_priv_notice_001.md`.
- **Mode F consent and safety — locked (2026-06-02):** Mode F default-off. Requires separate explicit consent distinct from session consent. Ambient indicator: right temple amber LED triple-pulse pattern (3×150ms on/off, 2s pause) when active — not suppressible by stealth mode. IEC 62471 cumulative daily retinal dose calculated across all Mode F wear time per calendar day (not per session). Firmware build flag `NP_MODE_F_REGULATORY_CLEARED = 0` — gated on RISK-03 Q-13 opinion letter. Spec: NP-FW-EMMC-002 Rev A §F.
- **SHDR warranty token architecture — locked (2026-06-02):** 256-bit TRNG opaque warranty token replaces warranty owner ID as SHDR linkage. Warranty DB and SHDR fleet DB are separate database instances with no cross-DB foreign keys. No-join rule enforced by CI test. Spec: NP-FW-EMMC-002 Rev A §A.
- **UHDR two-layer key scheme — locked (2026-06-02):** UKMD (32-byte TRNG master key) stored in Config partition encrypted by WKMD (Argon2id-derived from biometric/PIN). Biometric change re-encrypts only the 32-byte wrapper — UHDR partition ciphertext unchanged. Spec: NP-FW-EMMC-002 Rev A §C. Supersedes single-layer spec in NP-FW-EMMC-001 Rev A §6 (incorporated into NP-FW-EMMC-001 Rev B).
- **Device factory reset — locked (2026-06-02):** eMMC SANITIZE on UHDR + zero SHDR + clear Config + new TRNG salt + new warranty token. Power-loss resilient (reset_in_progress flag in SNVS_LPGPR1). Spec: NP-FW-EMMC-002 Rev A §B.
- **Cervical VNS (T2 accessory):** Neck-worn tcVNS module, carotid sheath stimulation; safety MCU owns enable with cardiac monitor interlock; 510(k) predicate = electroCore gammaCore K163334/K173323; gel pad consumable; +$35–55 BOM.
- **QMS established at company formation (Issue #33, 2026-05-13):** Six QMS documents released simultaneously — NP-QMS-001 (manual), NP-DHF-001 (DHF index), NP-QMS-DC-001 (design controls), NP-RM-001 (ISO 14971 risk management plan), NP-SW-001 (IEC 62304 software development plan), NP-QMS-CAPA-001 (CAPA procedure). IEC 62304 safety classifications locked: Safety MCU = Class C, Main processor = Class B, iOS/Android app = Class B. All pre-formation design documents (RISK-01..25, all NP-* specifications, CLAUDE.md Rev 1–7) retroactively entered under QMS change control. DHF baseline established. Risk register RISK-01..25 formally under ISO 14971 change control. CAPA log to be created at first trigger event.
- **Research data anonymization architecture:** On-device, per-study, fresh per request. NeurOne never holds or accesses raw UHDR at any point (biometric-derived key never leaves device). Consent withdrawal is immediately effective for all future data flows from any time period. Irreversibility notice given at L3 blanket consent + per-project invitation. Audit trail of contributed studies in SHDR (user-readable, never shared with researchers).
- **Hardware setup wizard — iOS (ISC-114–121, 2026-06-09):** `HardwareSetupManager` (`@MainActor ObservableObject`) with `SetupGATTProviding` protocol abstraction for testability. 11-step `SetupStep` enum (welcome→bleConfirmation→boaDial→electrodePods→zoneModules→impedanceCheck→ads1299Calibration→hydrationCaps→safetyAcknowledgement→protocolSelection→complete). `impedanceResultPublisher: AnyPublisher<UInt16, Never>` backed by a dedicated `PassthroughSubject` in `NeurOneGATTManager` (fired only from `NPUUID.impedanceResult` handler) — prevents false-triggers from unrelated session state updates that would break 0x00 all-fail detection. `minimumImpedancePassCount = 6` hard-coded (safety threshold — UserDefaults configurability removed; configurable threshold was a MEDIUM privacy risk). `setStepForTesting` wrapped in `#if DEBUG`. `safetyAcknowledged` never persisted (GDPR Art. 9 / BIPA — inferred health status). `impedancePassFlags` zeroed when advancing past `impedanceCheck` step. `MainTabView` tab guard replaced `onChange` with `guardedTabSelection: Binding<Int>` (eliminates one-frame flicker). `impedancePassFlags` removed from `SessionState.toWCMessage()` and `from(wcMessage:)` guard — UHDR-class data must not flow over unencrypted WatchConnectivity. `sendImpedanceResult` deleted from `PhoneSessionManager`. Analytics prohibited keys extended with `"imp"`, `"impedance"`, `"impedance_flags"`, `"pass_flags"`. `SetupView.swift` (506 lines): `BLEConnectionStatusCard`, `ImpedanceStatusGrid`, `ZoneModuleStatusGrid`, `SafetyAcknowledgementCard` (6 T1 contraindications), `ProtocolSelectionCard`. 34 unit tests (34/34 pass). ISA progress 36/164 → 44/164.
- **OTA firmware update — iOS + firmware C module (2026-06-09):** `firmware/ota/` C host-testable module: `np_ota_state_t` (magic 0x4E504F54, target_bank, boot_attempts, staged_sha256[32], CRC32 over first 40 bytes); `np_ota_state_validate()` / `np_ota_state_increment_attempts()`; 21 host tests pass. iOS: `OTAModels.swift` (OTAPhase with verified=0x04, FirmwareVersion Comparable+Hashable, FirmwareManifest Decodable, OTASession with integer round-half-up `formattedBytes`); `FirmwareUpdateService.swift` (SPKI SHA-256 certificate pinning — `spkiDERHeader(for:)` prepends correct DER header before hashing; static public manifest URL, no device ID in any request; ephemeral URLSession; `FirmwareUpdateProviding` protocol for mock injection); `OTAManager.swift` (fingerprint-first ordering — `verifySignature(image:)` is the first statement in `beginUpdate(image:)`, before connection guard, before download, before GATT write; INITIATE → chunks → VERIFY → waitForPhase(.verified) → COMMIT → waitForCompletion() per 9-step OTA); `OTAView.swift`. Opcode wire values frozen: initiate=0x01, chunk=0x02, verify=0x03, commit=0x04, abort=0x05, safetyMCUBegin=0x10, safetyMCUChunk=0x11, safetyMCUCommit=0x12. `firmwareVersion` GATT UUID `4E455550-0011-1000-8000-00805F9B34FB` (READ/NOTIFY 4B LE, NOT in NPUUID.all). `applyConnected()` test helper in `NeurOneGATTManager`. 27/27 iOS OTA tests + 21/21 firmware host tests pass. ISA ISC-107–113 VERIFIED (progress: 36/164).
- **BLE GATT layer — iOS app (2026-06-08):** `BLECentral.swift` protocol abstraction; `NeurOneGATTManager.swift` complete rewrite with `applyStateUpdate`, `applyCharacteristicAssignment`, `applyDisconnection`, `applyWarrantyToken` internal apply methods shared by CBCentralManagerDelegate/CBPeripheralDelegate and unit tests; `GATTCharacteristics.swift` extended with `warrantyToken` UUID and corrected characteristic count (14, not 13). 25 unit tests in `NeurOneGATTManagerTests.swift` (25/25 pass). Code review + privacy review completed. 5 findings applied: (1) `applyStateUpdate` default branch calls `applyDisconnection()` — clears stale UHDR state on BLE `.resetting`/`.poweredOff`; (2) `applyCharacteristicAssignment` privacy doc comment; (3) `didUpdateValueFor` SHDR early-return guards for `warrantyToken` and `shdrUploadStatus` before `session = pending`; (4) `requestEDFDownload` explicit `.littleEndian` serialization; (5) `SHDRUploader` Keychain read query `kSecAttrAccessible` filter (mirrors LOW-12 pattern). ISA.md progress: 20/164 → 27/164. ISC-11, 12, 13, 16, 17, 19, 20 verified. OI-BLE-01 open: `SHDRUploader` hub-token upgrade path pending hub firmware OI-WA-03.
- **eMMC partition architecture and encryption (NP-FW-EMMC-001 Rev A, 2026-05-11):** 9-partition GPT user area (8,192 MiB total) + dual hardware boot partitions. Partitions: Firmware Bank A (128 MiB, write-protected production) · Firmware Bank B (128 MiB, OTA target) · Safety MCU FW staging (4 MiB) · Config/Calibration (16 MiB) · SHDR (512 MiB) · UHDR (6,903 MiB, ~84% of user area) · Scratch (500 MiB, zeroed on boot). UHDR: AES-256-XTS, Argon2id key derivation (64 MiB memory, 4 iterations, p=1, 64-byte output → K1+K2), biometric/PIN input, 32-byte TRNG salt in Config partition — NeurOne never holds the key. SHDR: AES-256-XTS, HKDF-SHA256 from NeurOne manufacturing root key + eMMC CID hash, stored in Config under SNVS OTPMK — NeurOne can decrypt for warranty/fleet analytics. LittleFS on UHDR, SHDR, and Config (4 KiB blocks, power-loss resilient, wear-leveled). Scratch: raw blocks, no filesystem, no encryption, no persistent data. Dual-bank OTA: bootloader in eMMC hardware boot partitions 1+2 (primary + backup); Ed25519 signature verification on all firmware images before boot; SNVS_LPGPR0 boot bank flag; 9-step OTA sequence with readback verification and automatic revert; USB-C DFU recovery always available. Safety MCU firmware: never automated-updated via OTA — explicit user confirmation + page-by-page SPI flash with readback required. Session data classification: 27-element table (§12 of NP-FW-EMMC-001 Rev A) governs all UHDR vs SHDR routing decisions. UHDR backup: nightly incremental on USB-C power, same Argon2id-derived AES-256-XTS key, to USB-C local or E2E encrypted cloud — NeurOne cannot decrypt backup archives. Write endurance monitoring: EXT_CSD wear indicator + LittleFS block utilisation logged to SHDR every boot and every 100 sessions; PRE_EOL_INFO=0x03 blocks session start. **NP-COORD-001 G1-16 CLOSED** — NP-FW-EMMC-001 Rev A satisfies the G1 gate requirement that firmware storage architecture spec is baselined before any storage firmware code is authored. Closes Issue #19.
- **Zone module bone conduction announcement firmware (firmware/zone_announce/, 2026-05-11):** Implements RISK-15 Layer 5 (five-layer keying). Five parallel slot state machines drive ZONE_ID LPADC1 detection (10 kΩ pull-up, midpoint ADC thresholds for ZM-01–05) with 3×100 ms debounce (≥2/3 majority, RISK-18). DDS audio synthesis from 256-entry Q15 sine LUT outputs zone-specific tone sequences via SAI3 I2S at 8 kHz to bone conduction piezo: N beeps (440 Hz, N = zone position) + 880 Hz confirmation; unknown zone plays descending 200→150 Hz error pair. Announcement queue serialises simultaneous multi-zone insertions. SHDR auth log written on every debounce exit (no user biology). Insert callback fires twice: before audio (enables PBM zone immediately) and after audio (UX complete). Platform HAL stubs OI-ZA-01 through OI-ZA-04 for LPADC + SAI3 + eDMA. G2-10 CLOSED.
- **HRV biofeedback protocol firmware (firmware/hrv_biofeedback/, 2026-05-11):** Implements all four HRV protocols from CLAUDE.md §3 modality 6. Six-module static library: np_hrv_ppg (adaptive PPG peak detection, RR buffer, RMSSD), np_hrv_coherence (256-pt Welch PSD, radix-2 DIT FFT, LF/HF band extraction, coherence score = LF_peak/(LF_total+HF_total)×10), np_hrv_pacer (breathing pacer, RF personalization sweep 4–7 BPM in 0.5 BPM steps, closed-loop rate set), np_hrv_tavns_sync (RSA slope detection: sliding 6-interval dRR window, ±5 ms/beat hysteresis, inspiration-phase gate, 6 s failsafe, safety MCU veto path), np_hrv_eeg_biofeedback (1024-pt EEG Welch bands, frontal alpha/theta ratio, adaptive step adjusts pacer rate every 30 s to maximise alpha/theta while sustaining coherence ≥ 5.0), np_hrv_session (protocol orchestration, UHDR session record, SHDR coherence trend slope). UHDR data: full RR series + per-update coherence array + np_hrv_session_record_t. SHDR data: coherence trend slope only (no user biology). Platform HAL stubs (taVNS enable/disable, pacer phase notify, Config partition persist) await FW team implementation (OI-HRV-01–05 in NP-FW-HRV-001 Rev A). **NP-COORD-001 G2-12 CLOSED.** Closes Issue #21.
- **Dual-bank OTA bootloader firmware (firmware/bootloader/, 2026-05-11):** First firmware code authored for NeurOne. Implements complete B-1 through B-9 boot sequence per NP-FW-EMMC-001 Rev A §8.1–§8.4. Boot selector uses SNVS_LPGPR0 (persistent through warm resets) with bit fields: [1:0] BOOT_BANK, [7:2] BOOT_ATTEMPTS (6 bits, max 3), [8] OTA_PENDING, [16] DFU_FORCED. eMMC driver targets USDHC2 peripheral with partition switching (boot part 1/2/user area), 8-bit bus, HS timing. Ed25519 verifier is self-contained (no external crypto library): RFC 8032 §5.1.7 verify over message = sha256 || version_le32 || size_le32; includes SHA-256, SHA-512, CRC32, GF(2^255−19) field arithmetic, and constant-time final comparison. OTA state machine: 9-step flow with streaming sector-by-sector SHA-256 (avoids loading full 128 MiB image into SRAM), write-with-readback, np_ota_state_t persisted to Config partition at two mirrored sectors, rollback after 3 failed boot attempts of OTA candidate. DFU recovery: USB DFU 1.1 class on USB1 (USB-HS OTG), writes to Scratch then verifies before writing to inactive bank, Ed25519 gated — no unauthenticated firmware can be flashed. Manufacturing root public key is a placeholder; must be replaced at secure build step before any production image is signed. Closes Issue #20.
- **1064nm smart zone module — Path B architecture locked (NP-FW-PBM1064-001 Rev A, 2026-05-12; hardware NP-HW-FPC-001 Rev E, 2026-05-13):** On-module driver IC selected: Microchip ATtiny402 (I2C slave, SOT-23-8, programmed via UPDI before assembly) + 3× Infineon IRLML6344TRPBF N-channel MOSFETs (SOT-23, RDS(on)=27mΩ, VGS_th fully enhanced at 3.3V). Driver BOM $1.00 — below $2.00 target. Smart module FPC part number NP-FPC-ZM-SM-01; 550 LEDs in tri-wavelength interspersed pattern (200×660nm CH_A + 200×808nm CH_B + 150×1064nm CH_C); 66×78mm footprint unchanged from base module. InGaAs PDs: Hamamatsu G12180-010A (0.90 A/W at 1064nm, <1nA dark current), annular ring PD1/PD2 at X=33.0mm, Y=39.0mm. 1064nm emitters: EPITEX L1064-02AU via Marubeni. BOM delta vs base module: +$23–28. Estimated retail $149–199 per zone / $599–699 all-5-zone kit. mold variant: NP-TOOL-ZM-SM-001 Rev A (rigidizer cavity 24×16×3.5mm internal; mechanical key +2.0mm lateral face width prevents insertion into unmodified hub without TIA gain switch). Smart module ZONE_ID = 3.3kΩ resistor on pin 18 → ADC < 1100 counts (12-bit, 10kΩ pull-up, 3.3V ref). Hub enables LPI2C3 per slot independently (5 separate buses, 400kHz fast mode); I2C probe to address 0x30; ACK within 5ms required. Hub PCB requires Vishay DG2788A analog switch per slot to switch TIA Rf 47kΩ→22kΩ for InGaAs PD gain (OI-PBM-HW-01, BLOCKING). Base modules (ZM-01..ZM-05, ADC 1100–4000 counts) continue via direct-PWM path — unchanged. Duty cycle ceiling 25% unconditionally enforced at hub firmware layer (DUTY_MAX_REG = 0x32). InGaAs PD dose-metering: K_PD1, K_PD2, K_ratio_nom per zone per wavelength in Config partition (factory cal) or firmware defaults (cal_source=DEFAULT in SHDR). Session dose limits: 660/808nm = 60 J/cm², 1064nm = 36 J/cm². Aggregate irradiance ceiling 600 mW/cm² (OI-PBM-05: pending RISK-03 scope expansion). Session descriptor schema v4. EEG-adaptive: uniform mode (default) and cortical gradient mode (Research, OI-SES-02). FAI-SM-01,-02,-03,-05,-09,-10,-11: Software PASS (PR #57). FAI-SM-04,-06,-07,-08: Hardware bench PENDING (require calibrated 1064nm optical bench). Firmware: firmware/pbm_1064nm/. Issues: #53 (firmware), #54 (hardware, closed PR #58), #55 (T2 combined), #56 (regulatory/bibliography).
- **Hub PCB Rev B TIA gain switch architecture locked (NP-HW-HUB-001 Rev B, 2026-05-13):** Vishay DG2788A dual-SPDT analog switch per slot (×5) switches TIA feedback Rf 47kΩ→22kΩ when smart module detected. GAIN_SEL[n] GPIO (i.MX RT1062 GPIO_B0_04..08, one per slot) asserted HIGH after ZONE_ID debounce confirms ADC < 1100, and before I2C mux enable (sequencing requirement, NP-HW-HUB-001 Rev B §5.1). NXP PCA9546A (0x70) provides 4-channel I2C isolation for slots 0–3; slot 4 via GPIO mux. Boot init drives GAIN_SEL[0..4] LOW before zone detection task. On removal: I2C mux disable then GAIN_SEL deassert (HIGH). Firmware: np_tia_gain_t enum, np_sm_slot_ctx_t.tia_gain field, np_pbm1064_hal_tia_gain_set(), np_pbm1064_hal_tia_gain_boot_init() stub. Hub PCB Rev B BOM delta $1.41–1.91. OI-PBM-HW-01 SPECIFIED (hub Gerber build pending). OI-PBM-HW-02 SPECIFIED. FAI-SM-04 and FAI-SM-06 unblocked pending Gerber build.
- **T2 combined 1064+1170nm session architecture locked (NP-SES-1064-001 Rev A, 2026-05-12):** 1064nm zone modules + 1170nm T2 laser coordinated by np_pbm1064_t2_combined. Three-tier penetration: 660nm surface / 1064nm cortical / 1170nm deep. Thermal throttle priority: 1170nm first → CH_C → CH_B, CH_A last. sLORETA coordination for T2 depression protocol (§6.4 NP-SES-1064-001, blocking on T2 prototype). Combined UHDR record: per-zone per-wavelength J/cm² + 1170nm dose + sLORETA MNI coordinate.
- **FPC layout frozen (NP-HW-FPC-001 Rev D, 2026-05-09):** PD2 (RISK-14 Option B) position locked at X = 33.0 mm, Y = 39.0 mm from module reference corner (geometric center of 66 × 78 mm LED array, co-located in XY with PD1 on opposite face). FPC contact pad: 1.6 mm annular ring, scalp-facing copper layer, hard gold ≥ 0.5 µm. F-04 aperture in NP-TOOL-ZM-001 updated with this coordinate (±0.2 mm tolerance). OI-3 closed. NP-COORD-001 G1-15 closed. DRC-18 CAD overlay verified (EEG routing locked — G2-11 closed, OI-09 closed).
- **Lens and goggle assembly tooling (NP-TOOL-LENS-001 Rev B, 2026-05-10):** All five locked design changes consolidated: (1) Sliding rail lens mount F-01/F-02 — T-slot groove in lens rim + tongue in goggle arm, user self-install, ≤5N extraction, 500-cycle rated, eliminates Tier A alignment jig; (2) N42 magnet pockets F-03/F-04 — 6× per lens rim, ≥1mm polymer wall all faces (BLOCKING verification), 10,000-cycle snap durability, polarity pattern enforces shade orientation; (3) EC driver contacts F-05/F-06 — 2× hard gold ≥0.5µm pads in lens rim + BeCu pogo pins in goggle arm, ≤50mΩ, EC transition time >3s flags contact degradation in SHDR; (4) AgNW outer coating P-01 — replaces ITO (ITO 0.5% strain-to-failure vs. AgNW ≥5%), 20–30Ω/sq, ≥82% transmission at 660nm and 808nm (Mode F PBM wavelengths), supplier qualification BLOCKING; (5) Hard coat P-02 — 3–5µm silicone, ≥3H pencil hardness, applied over AgNW (outer) and EC film (inner on EC lens); (6) Inner PDMS diffuser P-03 — plasma-activated anti-fouling, SiO₂ interlayer on PC substrate (separate qualification from PI substrate result); (7) Lens rim guard anchor hooks F-07 (Issue #18) — integral to goggle arm at superior-posterior corner, 4mm ID loop; shade system clearance ≥4mm from hook to S1/S2/S3 shade body confirmed in CAD assembly (MR-19); tether lanyard ≤45mm free length routed along posterior arm surface, must not cross visual field (MR-20); OI-10 tracks CAD sign-off. 20-item mold design review checklist (Rev B adds MR-19 and MR-20); lens rim + goggle arm reviews concurrent; 10 open items tracked.
- **NPPS lexer/serializer bug fixes + regression tests (2026-06-10):** Four lexer bugs corrected in `NPPSLexer` (`app/ios/NeurOne/Protocol/NPProtocolScripting.swift`) and one serializer round-trip corruption fixed: **(1) Compound digit-ident (`660_808nm`, `660_808_1064nm`):** `-` after digit entered `readNumber()` even when followed by a letter; unit-reading backtrack left pos at `_`; outer loop read `_808nm` as a spurious field key → parser threw "Expected ':' or '{'". Fix: after unit backtrack, if pos is at `_`, read a compound ident consuming letters, digits, and `_` (e.g. `"660_808nm"`). **(2) Hyphenated tags (`wind-down`, `all-modalities`):** `-` after an ident unconditionally entered the number branch; `Double("-")` is nil → threw "Invalid number: -". Fix: only enter number branch when `-` is immediately followed by a digit (`pos + 1 < source.count && source[pos + 1].isNumber`). **(3) `mW_cm2` unit reading:** Unit loop stopped at digit `2` producing `"mW_cm"` (not in units set), then backtracked and outer loop re-read `mW_cm2` as an ident field key → parse error. Fix: extend unit loop to include `source[pos].isNumber` in addition to `.isLetter` and `== "_"`. **(4) `NPPSFieldValue.asIdent` broadened:** Extended to also return `.string` cases so quoted wavelength values (`wavelength: "660_808nm"`) work in user-authored scripts. **(5) Serializer round-trip corruption (MEDIUM):** Tags with hyphens were stored as split tokens (`["wind","down"]`) and re-emitted unquoted by the serializer, permanently corrupting the tag across save/reload cycles. Fix: `serializeTag()` helper in `NPPSSerializer` quotes any tag containing characters outside `[A-Za-z0-9_]` (and escapes embedded double-quotes). Both tag emission sites (single protocol and composite) updated to use `serializeTag()`. **4 regression tests added** to `app/ios/NeurOneTests/NPProtocolLibraryTests.swift`: `testLexerCompoundDigitIdentRoundTrip` (660_808nm + mW_cm2 in pbm_transcranial block), `testLexerTriWavelengthCompoundIdent` (660_808_1064nm), `testHyphenatedTagsDoNotThrow` (wind-down, all-modalities), `testHyphenatedTagRoundTripPreservesValue` (serialize→reparse preserves "wind-down" as a single tag). All 19 bundled NPPS protocols now parse without errors. ISA ISC-39 VERIFIED (NPProtocolLibrary loads all 19 templates). ISA ISC-122–125 VERIFIED (PhoneSessionManager WC bridge: isSupported guard, state forwarding, graceful unreachable, no UHDR-class fields in WC payload). ISA progress: 44/164 → 49/164. No UHDR/SHDR impact — lexer changes operate on protocol structure only.

---

## 14. DOCUMENTS GENERATED

| Document | Location | Contents |
|----------|----------|---------|
| Design Brief Revision 1 | `docs/neurone_design_brief_superseded.docx` | SUPERSEDED by R5 — Initial complete design specification |
| Design Brief Revision 2 | `docs/neurone_design_brief_r2_superseded.docx` | SUPERSEDED by R5 — Updated with LED count, irradiance, EEG, EMF decisions |
| Design Brief Revision 3 | `docs/neurone_brief_r3_superseded.docx` | SUPERSEDED by R5 — Adds UHDR/SHDR, consent systems, durability, service network |
| Design Brief Revision 4 | `docs/neurone_brief_r4_superseded.docx` | SUPERSEDED by R5 — zone module engineering package; dual-PD, keying, gasket, lever, PDMS qual, 24-risk register |
| Design Brief Revision 5 | `docs/neurone_design_brief_r5.docx` | **Current** — 12 sections, 28 tables, 50-entry document register. Adds: 1064nm smart zone module complete, Hub PCB Rev B, eMMC partition architecture, 7 firmware modules written, QMS establishment (6 documents), cervical VNS 510(k) pre-sub, shell/lens/hub tooling, provisional accessories (mastoid LRA pad, Apple Watch app), 25-risk register, iOS app roadmap, hub control program (NP-FW-HUB-001 Rev A), NPPS protocol language |
| Clinical Evidence Bibliography | `docs/neurone_bibliography.docx` | 39 entries, 12 modality sections (A–L), DOI links, NeurOne-specific summaries. Section L (entries 34–39): 1064nm PBM — Yao et al. Science Advances 2022 (primary RCT), UT Dallas follow-ons (open item), tissue optics penetration depth basis, CCO mechanism at 1064nm, safety summary, multi-modal combination evidence gap |
| Researcher Candidate List | `docs/neurone_researchers.docx` | 12 researchers, 7 modalities, contact info, cost estimates, funding sources |
| SBIR Phase I Draft | `docs/neurone_sbir_phase1_draft.docx` | NIH SBIR Phase I proposal draft; references Hirose FH34S (not Molex SlimStack) |
| FPC Zone Module Specification | `docs/neurone_fpc_zone_module_spec_revA.docx` | NP-HW-FPC-001 Rev C — 20-pin pinout, dual-PD architecture (§8.4), PDMS bonding (§9), thermal cycling qualification (§9.3), multi-FPC routing (§11.3), five-layer keying (§11.4), gasket (§12) |
| FPC Procurement Requirements | `docs/neurone_fpc_procurement_requirements.docx` | NP-PROC-FPC-001 Rev A — LED Vf binning, Hirose FH34S exclusions, BCR421W spec, RA copper |
| Zone Module Risk Register | `docs/neurone_fpc_zone_module_risks_revA.docx` | 25 risks (RISK-01 through RISK-25); 23 MITIGATED; RISK-03 and RISK-20 OPEN; RISK-25 (cervical VNS cardiac reflex) MITIGATED — pending FAI-CV02 hardware bench |
| Shell FPC Routing Review | `docs/neurone_shell_fpc_routing_review.docx` | NP-DRV-SHELL-001 Rev B (2026-05-10) — bend radius, multi-FPC bundle mgmt (§2.3), EEG cable routing (§2.4), 23-item DRC checklist; §2.4.5 locked routing path (OI-09 CLOSED, DRC-22 CLOSED, DRC-23 CLOSED) |
| FAI Zone Module Checklist | `docs/neurone_fai_zone_module.docx` | NP-FAI-ZM-001 Rev A — §3d PDMS adhesion (FAI-M01–M03), §3e PDMS thermal cycling qualification (FAI-TC01–TC06, TC02 BLOCKING), §4a accessibility (FAI-A09–A15), §4c IPX4 (FAI-IPX-01–04, IPX-02 BLOCKING), §5 lifecycle, §6 system test, §9 risk cross-reference |
| Engineering Coordination Checklist | `docs/neurone_eng_coordination_checklist.docx` | NP-COORD-001 Rev A.9 (2026-06-28) — G1 (16 items), G2 (14 items), G3 (8 items) gate structure. Rev A.9: fixed 8 detail-table rows with incorrect 5-column format (G1-13, G1-14, G2-10, G2-11, G2-12, G2-14, G3-07, G3-08) — all now 7-column matching headers; fixed G1-06 status placement; abbreviated summary dashboard rows G2-02/G2-13/G2-14; added status color coding key (BLOCKING=red, OPEN=amber, PARTIAL/BASELINED=light amber, CLOSED=green); G1-06 summary shading corrected. Prior status: G1-16 CLOSED; G2-10/11/12 CLOSED; G2-13 OPEN; G2-14 PARTIAL; G3-07/G3-08 SOFTWARE BASELINED (hardware FAI pending T2 prototype). |
| Zone Module Tooling Specification | `docs/neurone_tool_zone_module_001.docx` | NP-TOOL-ZM-001 Rev A — 8 mandatory molded features (F-01 through F-08), critical dimensions, 12-item mold design review checklist, FAI cross-reference |
| Supplier Selection Checklist | `docs/neurone_supplier_selection_checklist.docx` | NP-PROC-SUP-001 Rev A — CAT-A (moulding), CAT-B (CFRP shell), CAT-C (PDMS bonding); SUP-M-07 and SUP-B-01 BLOCKING for RISK-20; SUP-C-08 BLOCKING for RISK-04; §9 RISK-20 tracking table |
| Clinical Trials Strategy | `docs/neurone_clinical_trials_strategy.docx` | NP-CLIN-001 Rev A — all-in-one commercial rationale; Vielight trials scope/limitation; individual modality evidence (9 modalities); multi-modal combination evidence; researcher profiles (9 researchers); 6-trial priority plan; key bibliography |
| Additional Modalities | `docs/neurone_additional_modalities_superseded.docx` | SUPERSEDED — NP-MOD-EXT-001 Rev A — all 6 modalities now individually specified: HRV biofeedback (NP-FW-HRV-001 Rev A), 1064nm PBM (NP-FW-PBM1064-001 Rev A), sLORETA HD-tDCS (NP-FW-HD-001 Rev A), cervical VNS (NP-FW-CVNS-001 Rev A), 40Hz vibrotactile (CLAUDE.md §3b), Apple Watch sync (CLAUDE.md §3b + NP-APP-ROADMAP-001 Rev A); tFUS watch program → CLAUDE.md §13.2b |
| Shell Tooling Specification | `docs/neurone_tool_shell_001.docx` | NP-TOOL-SHELL-001 Rev A — 4 mandatory shell features: F-01 zone slot plug anchor posts (×5, color-coded), F-02 accessory port cover anchor posts (×3), F-03 EEG cable routing channel (8×5mm, outer CFRP surface, DRC-18 cross-ref), F-04 temporal wing anchor boss (×2 bilateral, PROVISIONAL pending HOPE Phase 3). 23-item design review checklist (SH-01–SH-23), critical dimensions, FAI cross-reference. Addresses Issue #16 and §13.4 pending decision. |
| Lens and Goggle Assembly Tooling Specification | `docs/neurone_tool_lens_001.docx` | NP-TOOL-LENS-001 Rev B — 7 features (F-01 through F-07 + P-01 through P-03) across lens rim, goggle arm, shade assembly tooling; sliding rail geometry (F-01/F-02); N42 magnet pockets (F-03/F-04, ≥1mm wall BLOCKING); EC driver contacts (F-05/F-06, hard gold, pogo pin); AgNW coating spec (P-01, replaces ITO); hard coat spec (P-02, 3–5µm silicone); inner PDMS diffuser (P-03, plasma-activated); lens rim guard anchor hooks (F-07) — expanded in Rev B with shade clearance ≥4mm (MR-19), tether ≤45mm posterior routing no FOV crossing (MR-20), OI-10 CAD confirmation; 20-item mold design review checklist; 10 open items; FAI cross-reference; risk register cross-reference. Addresses Issue #17 (Rev A) and Issue #18 (Rev B). |
| eMMC Partition Architecture and Storage Encryption | `docs/neurone_fw_emmc_001.docx` | NP-FW-EMMC-001 Rev A (2026-05-11) — 18 sections, 13 tables. eMMC hardware spec (§3); 9-partition GPT layout (§4, 8,192 MiB total, UHDR 6,903 MiB dominant); LittleFS config per partition (§5); UHDR AES-256-XTS + Argon2id key derivation + key lifecycle + session record EDF+ format + HMAC integrity (§6); SHDR AES-256-XTS + HKDF manufacturing key + SNVS OTPMK + file structure (§7); dual-bank OTA bootloader + Ed25519 verification + 9-step OTA sequence + USB-C DFU recovery + Safety MCU firmware update protocol (§8); Config/Calibration partition (§9); Scratch partition zero-on-boot + SANITIZE (§10); boot sequence B-1 through B-9 (§11); 27-element session data classification table UHDR vs SHDR (§12); UHDR nightly incremental backup protocol (§13); write endurance monitoring + predictive maintenance thresholds (§14); research anonymization Scratch workspace (§15); processor ownership table (§16). Closes two CLAUDE.md §13.4 pending decisions: eMMC partition architecture + dual-bank OTA bootloader. Addresses Issue #19. |
| HRV Biofeedback Protocol Firmware | `firmware/hrv_biofeedback/` | Issue #21 (2026-05-11) — Six-module static library (np_hrv_ppg, np_hrv_coherence, np_hrv_pacer, np_hrv_tavns_sync, np_hrv_eeg_biofeedback, np_hrv_session). Coherence algorithm: 256-pt Welch PSD on 4 Hz interpolated RR series, LF peak/(LF+HF) × 10. taVNS timing: RSA slope method, 6-interval dRR window, ±5 ms/beat hysteresis, 6 s failsafe, safety MCU veto path. Dual EEG+HRV: 1024-pt EEG Welch bands, frontal alpha/theta ratio, adaptive pacer rate every 30 s. Spec: NP-FW-HRV-001 Rev A (docs/np_fw_hrv_001.md). G2-12 CLOSED. |
| Zone Module Bone Conduction Announcement Firmware | `firmware/zone_announce/` | Issue #22 (2026-05-11) — Three-module static library (np_zone_detect, np_zone_audio, np_zone_announce). Implements RISK-15 Layer 5 (five-layer keying). Zone detection: LPADC1 ch 12, 10 kΩ pull-up, 1% resistor ladder (ZM-01=10kΩ through ZM-05=220kΩ), midpoint ADC thresholds. Debounce: 3×100 ms reads, ≥2/3 majority (RISK-18). Audio: 256-entry Q15 sine LUT, DDS phase accumulator, SAI3 I2S at 8 kHz, eDMA ping-pong (64 samples/half). Zone sequences: N beeps (440 Hz A4) + rising confirmation (880 Hz A5); total durations 550–1270 ms. Error cue: descending 200→150 Hz pair. Five parallel slot state machines (IDLE→SETTLING→DEBOUNCING→ANNOUNCING→ACTIVE→REMOVING). Announcement queue serialises simultaneous insertions (ZM-01→05 order). SHDR auth logging: zone_id, auth_result, debounce_reads_passed per insertion. Platform HAL stubs OI-ZA-01–04 (LPADC + SAI3 + eDMA). Spec: NP-FW-ZA-001 Rev A (docs/np_fw_za_001.md). **G2-10 CLOSED.** |
| Dual-Bank OTA Bootloader Firmware | `firmware/bootloader/` | Issue #20 (2026-05-11) — NXP i.MX RT1062 (Cortex-M7) bootloader implementing B-1 through B-9 boot sequence from NP-FW-EMMC-001 Rev A. Components: np_main.c (IVT, vector table, boot flow), np_boot_selector.c (SNVS_LPGPR0 bank flag + attempt counter), np_emmc.c (USDHC2 eMMC driver, partition switching, write-verify), np_signature.c (Ed25519 verify + SHA-256 + SHA-512 + CRC32, **self-contained — intentionally NOT using np_crypto**; uses `-nostdlib/-nodefaultlibs` which makes Monocypher unavailable), np_ota.c (9-step OTA state machine, streaming SHA-256 over 128 MiB banks, Config partition state record), np_dfu.c (USB DFU 1.1 class, USB1 peripheral, DFU_DNLOAD/MANIFEST with Ed25519 verification before eMMC write). Linker script targets OCRAM 0x20200000 with 48 KiB bootloader limit. CMake build system with arm-none-eabi toolchain. Public key placeholder in np_signature.c must be replaced at secure build time. |
| Shared Cryptographic Primitives Library | `firmware/crypto/` | OI-SW01-M07-02 CLOSED (2026-06-11) — `np_crypto` static library providing `np_ed25519_verify`, `np_sha256`, `np_sha512`, `np_crc32`. Ed25519 backed by **Monocypher 4.0.2** optional SHA-512 module (RFC 8032 §5.1.7; vendored in `firmware/crypto/vendor/monocypher/`). SOUP record: `firmware/crypto/vendor/monocypher/VERSION` (version 4.0.2, BSD-2-Clause OR CC0-1.0, IEC 62304 §8.1.2). Small-subgroup guard: all-zero pubkey rejected before `crypto_ed25519_check` call (prevents low-order degenerate verification). Build: no CPU flags in library CMakeLists — consuming project supplies them (multi-target: Cortex-M0+, Cortex-M7, x86/arm64 host). Monocypher SOUP sources compiled with `-w`; first-party `np_crypto.c` compiled with `-Wall -Wextra`; `C_EXTENSIONS OFF` enforced on all targets via CMake property. Host tests: 11/11 pass (SHA-256 TV1/TV2, SHA-512 TV1/TV2, CRC32, Ed25519 RFC 8032 TV1/TV2, Ed25519 invalid sig/key/length, all-zero pubkey rejection). IEC 62304 Class C (consumed by SW-01 Safety MCU) + Class B (consumed by SW-02 Main processor bootloader path). |
| sLORETA-guided HD-tDCS Firmware | `docs/np_fw_hd_001.md` + `firmware/sloreta_hdtdcs/` | NP-FW-HD-001 Rev A (2026-05-11) — Issue #23, G3-07 software baselined. Scalar sLORETA: 2447 voxels × 21 channels weight matrix (≈205KB, LPSDR4), 64-epoch 21×21 covariance accumulation, per-voxel quadratic form P[v] = W_v^T C W_v. Electrode mapping: Jurcak 2007 MNI scalp atlas for 21-ch T2 cap; nearest-electrode center anode; 4×1 ring cathodes with ≥2 quadrant angular spread. Current: charge-balanced (anode I, each cathode −I/4). 30s ramp up/down, 100ms tick. Safety MCU SPI for impedance check + enable; software charge density monitor (95% of 40µC/cm² trips abort). Concurrent EEG: 200µs blanking, ≥20dB SNR spec. 7 clinical targets (DLPFC_L/R, VLPFC_L, ACC, MPFC, M1_L/R); bilateral 4×1 and 2-electrode fallback. FAI-HD01 (phantom 15mm accuracy), FAI-HD02 (software PASS all 6 criteria), FAI-HD03 (FWHM ≤25mm phantom), FAI-HD04 (SNR ≥20dB bench). Hardware FAI BLOCKING for full G3-07 closure. BOM delta $0. UHDR: target MNI, montage, current, charge, impedance. SHDR: device metrics only. |
| Cervical VNS Safety Interlock Firmware | `docs/np_fw_cvns_001.md` + `firmware/cervical_vns/` | NP-FW-CVNS-001 Rev A (2026-05-11) — Issue #24, G3-08 software baselined. Safety MCU (STM32G071) owns CVNS_ENABLE_L/R GPIO; monitors R-peak via RPEAK_IN edge GPIO; TIM6 ISR at 200Hz (5ms); HR change >15 BPM within 5s observation window → GPIO LOW within <5.1ms worst-case (spec: <100ms). Baseline cross-validation: main processor PPG-derived HR vs safety MCU GPIO-timer-derived HR must agree within 5 BPM before enable granted. Re-enable requires 30s lockout + explicit app confirmation + repeat impedance check + baseline re-established. Three firmware modules: np_cvns_interlock (Pan-Tompkins R-peak detection, RR buffer, baseline, SPI exchange); np_cvns_stim (biphasic charge-balanced, 10s ramp-up, 5s ramp-down, 100ms tick); np_cvns_session (session orchestration, UHDR/SHDR record). FAI-CV01 (electrode placement, phantom bench), FAI-CV02 (cardiac interlock <100ms, oscilloscope bench — software constants PASS), FAI-CV03 (3 healthy adults, IRB required). Hardware FAI BLOCKING for T2 clinical release. RISK-25 MITIGATED. UHDR: RR series, baseline HR, HR at cutoff, timestamps, impedance raw. SHDR: cutoff flag only (no HR values), impedance pass/fail, fault log (no HR). BOM delta +$35–55 (cervical accessory module). |
| Cervical VNS 510(k) Pre-Submission | `docs/np_reg_cvns_001.md` | NP-REG-CVNS-001 Rev A (2026-05-11) — Issue #24. Q-Sub package for FDA CDRH DNPMD. Predicate: electroCore gammaCore K163334 (cluster headache, 2016) + K173323 (migraine, 2017). Proposed SE argument: same intended use (cluster headache + migraine) + same or safer technology (biphasic charge-balanced, ≤2 mA vs predicate ≤24 mA, 25 Hz within 1–25 Hz range). Key differences mitigated: lower current (more conservative); bilateral option (literature review + bench at ≤2 mA); cardiac interlock (safety enhancement). 6 Q-Sub questions for FDA. Proposed Type B meeting at Month 20 from T1 launch. Standards: IEC 60601-1, IEC 60601-2-10, IEC 62471, IEC 62304 Class C (safety MCU) + Class B (main), IEC 62366-1, 21 CFR 820. Clinical evidence: FAI-CV03 tolerability (3 adults) + predicate program + literature review. Expanded indications (depression, PTSD, post-stroke) planned for subsequent submissions via T2 seeding studies. |
| 1064nm Smart Zone Module Firmware | `docs/np_fw_pbm1064_001.md` + `firmware/pbm_1064nm/` | NP-FW-PBM1064-001 Rev A (2026-05-12) — Issues #53, #54, #55, #56. Smart module detection (3.3kΩ ZONE_ID, ADC < 1100 counts, 3×100ms debounce RISK-18). I2C bus: LPI2C3, 400kHz fast mode, per-slot independent buses, ATtiny402 address 0x30. Three-channel LED control (CH_A=660nm, CH_B=808nm, CH_C=1064nm): registers 0x00–0x0D (STATUS, CH_ENABLE, CUR, PWM_FREQ, DUTY, THERMAL, FAULT_LATCH, CONFIG). Duty ceiling 0x32 (25%) enforced unconditionally. InGaAs PD (Hamamatsu G12180-010A) dose-metering: K_PD1, K_PD2, K_ratio_nom per zone per wavelength from Config partition or firmware defaults (default values: 660nm K_PD1=0.120, 808nm K_PD1=0.105, 1064nm K_PD1=0.088). PD1/PD2 ratio: fouling vs aging disambiguation (RISK-14 Option B). Aggregate irradiance ceiling 600 mW/cm² three-channel combined (OI-PBM-05). Session dose limits: 60 J/cm² for 660/808nm, 36 J/cm² for 1064nm. T2 combined session: np_pbm1064_t2_combined coordinates 1064nm zone modules + 1170nm laser; thermal throttle priority 1170nm first → CH_C → CH_B. FAI status: SM-01,-02,-03,-05,-09,-10,-11 = Software PASS (PR #57); SM-04,-06,-07,-08 = Hardware bench PENDING (require calibrated 1064nm optical bench + hub PCB Rev B with TIA gain switch OI-PBM-HW-01). 8 HAL stubs OI-PBM-01 through OI-PBM-08. |
| 1064nm Multi-Wavelength Session Protocol | `docs/np_ses_1064_001.md` | NP-SES-1064-001 Rev A (2026-05-12) — Issues #53, #55, #56. Session descriptor v4 with smart_module_mask, per-zone np_pbm1064_preset_t, t2_combined_enable, Ed25519 signature extension. T1 three-wavelength session lifecycle (preflight, ramp, steady, ramp-down, UHDR/SHDR). EEG-adaptive frequency: uniform mode (default) and cortical gradient mode (experimental — Research mode label, OI-SES-02). T2 combined 1064+1170nm session: sLORETA coordination for depression protocol (§4.4, requires T2 prototype). Dose limits enforced per channel per zone. UHDR: irradiance time series, dose, NTC, throttle events, EEG-adaptive events, 1170nm dose, sLORETA MNI coordinate. SHDR: PD1/PD2 ratio final, fouling/aging flags, driver fault count, peak NTC (no timestamp), cal source. App display requirements: per-zone per-wavelength irradiance + dose bars, depth tier illustration, module management screen. Clinical evidence: Yao et al. Science Advances 2022 (UT Dallas/Harvard, right prefrontal 1064nm, working memory CDA). 5 open items OI-SES-01 through OI-SES-05. |
| 1064nm PBM Clinical Evidence Bibliography Addendum | `docs/np_bib_1064_001.md` | NP-BIB-1064-001 Rev A (2026-05-13) — Issue #56. Addendum to existing 33-entry bibliography. Six entries: Yao et al. Science Advances 2022 (primary — n=20 RCT, right PFC 1064nm, CDA EEG working memory improvement); UT Dallas follow-on search protocol (open item); tissue optics penetration depth refs (Bashkatov 2005, Yaroslavsky 2002, Strangman 2002 — biophysical basis for depth-tier claim); CCO mechanism at 1064nm (Rojas & Gonzalez-Lima 2013); safety literature (Hamblin 2016); multi-modal combination evidence gap noted. FTC substantiation basis for "deeper than 810nm" and working memory claims. Both this document AND NP-REG-PBM1064-001 Rev A opinion letter required before any 1064nm marketing claim is lifted. |
| RISK-03 Regulatory Opinion Scope Expansion Brief | `docs/np_reg_pbm1064_001.md` | NP-REG-PBM1064-001 Rev A (2026-05-13) — Issue #56. Scope expansion brief to outside regulatory counsel covering four new items beyond existing RISK-03 (Issue #5): (1) 1064nm LED array FDA general wellness pathway + IEC 62471 EH1/EH2 classification; (2) three-channel aggregate irradiance ceiling 600 mW/cm² — IEC 62471 additive assessment and FTC substantiation; (3) T2 combined 1064nm LED + 1170nm laser simultaneous session — dual-standard photobiological hazard and 510(k) coverage; (4) depth-tier penetration claim FTC substantiation adequacy. 12 questions for counsel (Q1–Q12). Marketing gate: all 1064nm irradiance, aggregate, depth-tier, and T2 combined session claims remain gated until opinion letter received. Estimated incremental cost $5,000–10,000 to existing RISK-03 engagement. |
| 1064nm Smart Zone Module Hardware (FPC Variant + Procurement) | `docs/np_hw_fpc_001.md` + `docs/np_proc_fpc_1064_001.md` | NP-HW-FPC-001 Rev E (FPC layout variant NP-FPC-ZM-SM-01): pins 5–8 rerouted from base LED_B to LED_C (1064nm), pins 9–12 to I2C SDA/SCL; 550 LEDs (200+200+150); InGaAs PD1/PD2 (Hamamatsu G12180-010A); rigidizer sub-board 22×14mm for ATtiny402+FETs; TIA gain switch OI-PBM-HW-01 BLOCKING for hub PCB Rev B. NP-PROC-FPC-1064-001 Rev A: ATtiny402 ($0.38) + 3× IRLML6344TRPBF ($0.12 each) + G12180-010A InGaAs PDs + EPITEX L1064-02AU 1064nm LEDs via Marubeni; module BOM sub-total ~$18–22, delta vs base +$23–28, retail $149–199/zone or $599–699 all-5-zone kit. Closes Issue #54. |
| 1064nm Smart Zone Module Tooling Variant | `docs/np_tool_zm_sm_001.md` | NP-TOOL-ZM-SM-001 Rev A — mold variant for smart module. Rigidizer cavity 24×16×3.5mm internal. Asymmetric mechanical key: +2.0mm lateral face width vs base module — prevents physical mis-insertion into un-upgraded hub slot (hub PCB without Vishay DG2788A analog switch would saturate TIA). 5-item DRC checklist. Cross-references NP-HW-FPC-001 Rev E and OI-PBM-HW-01. |
| QMS Manual | `docs/np_qms_001.md` | NP-QMS-001 Rev A (2026-05-13) — Issue #33. QMS established at company formation. Scope: T1 + T2, all modalities, all firmware, iOS/Android app. References: 21 CFR Part 820, ISO 13485:2016, ISO 14971:2019, IEC 62304, IEC 62366-1. Quality policy. Document numbering scheme. QMS process map (design controls, risk management, software, supplier, CAPA, PMS). Management responsibility structure (Quality Lead open, interim CEO). Internal audit cycle. eQMS formalization roadmap (Month 3–24). Closes Issue #33 QMS establishment requirement. |
| Design and Development Plan | `docs/np_dp_001.md` | NP-DP-001 Rev A (2026-05-17) — 20-section formal D&D plan per 21 CFR §820.30(b) and ISO 13485:2016 §7.3.1. Six phases (Phase 0 complete; Phases 1–5 T1 Month 0–18; Phase 6 T2 Month 12–54). Gate schedule: G1 Month 6, G2 Month 10, G3 Month 14. Responsibilities, verification, validation, transfer, change control, risk integration, software integration, supplier management, interim doc control. CLAUDE.md §13.4 NP-DP-001 pending decision CLOSED. |
| Design History File Index | `docs/np_dhf_001.md` | NP-DHF-001 Rev O (2026-06-11) — Rev O: OI-SW01-M07-02 CLOSED; `firmware/crypto/` added to §6 firmware source table; NP-SW-001 SOUP table updated (Monocypher 4.0.2); Rev N → Rev O. Rev N: OTA firmware update — iOS + firmware C module. Rev F updates NP-SIM-001 to v0.3.0 in §5.5; #79 (T2 TMS coil) removed from open sub-issues (CLOSED — branch claude/add-t2-tms-coil-dLbh7); CLAUDE.md updated to Rev 14. Rev E updates NP-SIM-001 to v0.2.0 in §5.5; #77 (WebSocket API) and #78 (probe animation) removed from open sub-issues (both CLOSED — PRs #84, #85); CLAUDE.md updated to Rev 13. Rev D adds NP-DP-001 Rev A to §5.1; design planning completeness upgraded to Good; NP-DP-001 removed from §8 planned additions; CLAUDE.md updated to Rev 12; CEO name (Steve Hickman) added to Author/Approved-by fields. Rev C adds NP-SIM-001 v0.1.0 (Helmet Simulator) to §5.5; new SIM category added; CLAUDE.md updated to Rev 11; open sub-issues #77–#80 and parent Issue #81 / PR #76 recorded. Rev B: relative markdown navigation links to all file entries; NP-DB-005 (R5 design brief) added as ACTIVE; CLAUDE.md updated to Rev 10; NP-MOD-EXT-001 → SUPERSEDED; NP-FW-HUB-001 Rev A (hub_control) added; NPPS Language Reference added; hub_control/ added to firmware source table; NP-COORD-001 → Rev A.8; NP-RISK-001 → Rev B. Rev A (2026-05-13): Master DHF index per 21 CFR §820.30(j); all pre-formation design documents retroactively entered under change control; firmware source code directories declared DHF design output records; DHF completeness assessment (8 design phases); planned additions (NP-DP-001, NP-DT-001, NP-HFE-001, NP-PMS-001). Closes Issue #33 DHF initiation requirement. |
| Design Controls Procedure | `docs/np_qms_dc_001.md` | NP-QMS-DC-001 Rev A (2026-05-13) — Issue #33. Design controls per 21 CFR §820.30 and ISO 13485:2016 §7.3. Design planning (§820.30(b)); design inputs — types, sources, documentation requirements (§820.30(c)); design outputs — types, approval requirements (§820.30(d)); design review — scheduled gate reviews, record requirements, independence requirement (§820.30(e)); design changes — significant change order process and minor change notification (§820.30(i)); design verification — methods, protocol requirements, current pending FAI items (§820.30(f)); design validation — T2 HFE summative, clinical performance, software validation, biocompatibility (§820.30(g)); design transfer (§820.30(h)). Closes Issue #33 design controls process requirement. |
| ISO 14971 Risk Management Plan | `docs/np_rm_001.md` | NP-RM-001 Rev A (2026-05-13) — Issue #33. Risk management plan per ISO 14971:2019. Scope: full device lifecycle, T1 + T2, all accessories. Severity S1–S5 and probability P1–P5 classification tables. 5×5 risk acceptability matrix (ACCEPTABLE / ALARP / UNACCEPTABLE). Full ISO 14971 process map (planning → hazard ID → estimation → evaluation → control → overall residual risk → review → post-market). Intended use and reasonably foreseeable misuse definitions (T1 and T2). 9 hazard categories with primary interlocks. Formal QMS change-control entry of RISK-01..25 (23 MITIGATED, RISK-03 and RISK-20 OPEN, RISK-25 hardware bench pending). Risk control hierarchy (inherent safe design → protective measures → information). Software hazard and cybersecurity risk provisions. Overall residual risk evaluation requirements pre-510(k). Post-market surveillance risk feedback loops. Risk Management Report target Month 18. Closes Issue #33 ISO 14971 risk management plan requirement. |
| IEC 62304 Software Development Plan | `docs/np_sw_001.md` | NP-SW-001 Rev A (2026-05-13) — Issue #33. Software development plan per IEC 62304:2006+AMD1:2015. Three software items classified: SW-01 Safety MCU (STM32G071, bare-metal) = **Class C**; SW-02 Main processor (i.MX RT1062, FreeRTOS) = **Class B**; SW-03 iOS/Android app = **Class B**. Class C rationale: Safety MCU owns all stimulation GPIO, no independent safety backstop. Class B rationale for SW-02/SW-03: Safety MCU provides independent hardware backstop with SPI heartbeat watchdog. V-model for SW-01; iterative V-model for SW-02/SW-03. Full module inventory: 8 SW-01 Class C safety units (SW01-M01..M08); 15 SW-02 modules indexed (includes SW02-M09..M12 hub control: module registry, session runner, session log, safety SPI heartbeat); 7 SW-03 modules indexed. SOUP tracking table (FreeRTOS, LittleFS, Ed25519, CMSIS-DSP). MISRA C:2012 for SW-01 (mandatory) and SW-02 (advisory). Software requirements traceability to NP-DT-001. Safety response time requirements: heartbeat → cutoff ≤50ms; cardiac interlock ≤100ms; photoparoxysmal ≤200ms. IEC 62304 clause compliance table. OTA update process (SW-01 never automated OTA). OS compatibility 7-day SLA. Closes Issue #33 IEC 62304 software plan requirement. |
| Privacy Analysis and Repair | `docs/neurone_privacy_analysis_001.pdf` | NP-PRIV-001 Rev A (2026-06-02) — 18 findings (2 Critical, 6 High, 7 Medium, 3 Low); UHDR/SHDR architecture review; consent engine; research data flows; clinical platform; what looks good; recommended next steps. PDF generated by `generate_privacy_report.py`. |
| Privacy Remediation Master Plan | `docs/np_priv_rem_001.md` | NP-PRIV-REM-001 Rev A (2026-06-02) — 30 remediation steps (STEP-01 through STEP-30); option selections table; master calendar (Phase 0 through Phase 6); capability matrix; authority submission guide (EU-US DPF, Apple App Store, Google Play, IRB, HIPAA OCR, FTC HBNR, GDPR Art. 33, US state breach laws); direct remediations completed index. |
| Breach Response Plan | `docs/np_sec_br_001.md` | NP-SEC-BR-001 Rev A (2026-06-02) — escalation chain; detection signals; severity classification; containment and forensic preservation procedures; regulatory notification decision tree (HIPAA, FTC HBNR, GDPR Art. 33-34, US state laws); individual notification templates (Appendices A-C); annual tabletop exercise procedure. |
| POA Upload Procedure | `docs/np_proc_poa_001.md` | NP-PROC-POA-001 Rev A (2026-06-02) — accepted document types; E2E encrypted upload via signed URL; vault access controls (≤3 named reviewers, full audit log); review procedure and structured review record schema; 30-day document deletion; annual re-verification workflow; capacity restoration procedure; incident response. |
| Research anonymization Engine Firmware Specification | `docs/np_fw_anon_001.md` | NP-FW-ANON-001 Rev A (2026-06-03) — l-diversity (l≥3) + differential privacy (ε≤1.0, δ≤10⁻⁵) Laplace mechanism; on-device k-anonymity grouping; study descriptor schema v1 with prohibited element list; budget tracking per study; FAI-ANON-01 through FAI-ANON-09 test suite; 5 open items (OI-ANON-01 through OI-ANON-05). Addresses NP-PRIV-REM-001 STEP-11. |
| HIPAA BAA Template | `docs/np_legal_baa_001.md` | NP-LEGAL-BAA-001 Rev A (2026-06-03) — DRAFT TEMPLATE requiring legal counsel review before execution. Full HIPAA BAA with: required provisions (45 CFR §164.504(e)); consent revocation deletion cascade (§5.1, 30-day deletion obligation); no onward sharing (§5.2); breach notification within 5 business days (§3.3); 3-tier access table (Monitor/Assess/Full Clinical) with minimum necessary data per tier; BIPA provision for Illinois users (§5.4); Exhibit A scope template. Addresses NP-PRIV-REM-001 STEP-13. |
| FHIR R4 ImplementationGuide | `docs/np_int_fhir_001.md` | NP-INT-FHIR-001 Rev A (2026-06-03) — NeurOne T2 Clinical Profile. Permitted resource types: Patient (opaque MRN only, no name/DOB/address), Observation (EEG band power ratios, HRV RMSSD, coherence score, session dose), DiagnosticReport (weekly session summary), Procedure (Full Clinical tier only). LOINC codes for HRV; NeurOne local codes for EEG spectral features. LSL TLS encryption requirement. FHIR validator CI integration spec. 5 open items (OI-FHIR-01 through OI-FHIR-05). Addresses NP-PRIV-REM-001 STEP-14, NP-COORD-001 G3-09. |
| App Analytics and Crash Reporting Policy | `docs/np_app_telemetry_001.md` | NP-APP-TELEMETRY-001 Rev B (2026-06-03) — Rev B: `session_sequence` (raw integer, health-adjacent) replaced with `engagement_tier` coarsened 3-bucket enum (new/active/established); §3.2 added with implementation requirements. Rev A content: analytics vendor and crash reporter requirements; 9 permitted event properties; prohibited properties; SDK initialization gate (post-consent-flow only); crash reporter configuration; SwiftLint/Android Lint linting rule; annual review cadence. |
| Core iOS App ISA | `app/ios/ISA.md` | E4 ISA for Issue #51 (2026-06-04). 164 ISCs across 17 groups: BLE GATT, session display (Mode 1), protocol upload (Mode 2), session history + EDF (Mode 4), UHDR data, SHDR upload, clinical consent, privacy compliance (age gate, BIPA, HealthKit, analytics gate), consumable tracker, OTA, setup wizard, Watch bridge, App Store readiness. Three known validator safety gaps flagged by unit tests (app-side charge density, PBM dose, zero-duration — all `XCTExpectFailure`-wrapped pending implementation). PR #106 (feature/ios-parallel-integration). |
| iOS App Development Roadmap | `docs/np_app_roadmap_001.md` | NP-APP-ROADMAP-001 Rev B (2026-06-03) — Rev B adds §9 Privacy Constraints (binding engineering constraints): §9.1 HealthKit data residency (no transmission to NeurOne servers); §9.2 minimum age gate (16+ declaration before consent flow); §9.3 BIPA written release screen for Illinois users; §9.4 Adaptive Adjustments card in Session History (plain-language trigger enum); §9.5 SDK init gate cross-reference. New open items OI-WA-06, OI-PA-01 through OI-PA-04. |
| Firmware Privacy Remediation Delta | `docs/np_fw_emmc_002.md` | NP-FW-EMMC-002 Rev A (2026-06-02) — §A warranty token architecture (256-bit TRNG, no-join rule, CI test); §B device factory reset procedure (12-step sequence, SANITIZE, power-loss resilience); §C two-layer UHDR key scheme (UKMD + WKMD, biometric change re-wraps 32 bytes only); §D Scratch partition encryption for research anonymization (AES-256-CTR session key, atomic pipeline, SANITIZE post-run); §E EDF+ patient header policy (opaque token, X fields, header validator in anonymization pipeline); §F Mode F spec (default-off, separate consent, triple-pulse ambient indicator, daily cumulative dose limit, regulatory gate flag). Supersedes conflicting sections of NP-FW-EMMC-001 Rev A; to be incorporated in NP-FW-EMMC-001 Rev B. |
| CAPA Procedure | `docs/np_qms_capa_001.md` | NP-QMS-CAPA-001 Rev A (2026-05-13) — Issue #33. Corrective and Preventive Action procedure per 21 CFR §820.100 and ISO 13485:2016 §8.5.2/§8.5.3. Trigger sources (8 categories: design verification/validation failure, software problem report, internal audit, supplier non-conformance, risk escalation, post-market complaint, regulatory finding, SHDR proactive trend). Severity classification (Critical/Major/Minor) with response time requirements. 8-step CAPA process (initiation → containment → RCA → action planning → implementation → effectiveness verification → closure → management review). Root cause analysis methods per severity (fishbone/FTA for Critical; 5-Why for Major). CAPA log specification and format. CA vs PA distinction. Regulatory reporting obligations: MDR (21 CFR Part 803) within 30 days for death/serious injury; 5-day imminent hazard; Field Safety Corrective Action triggers. Record retention (5 years minimum). Closes Issue #33 CAPA process requirement. |
| iOS App Development Roadmap | `docs/np_app_roadmap_001.md` | NP-APP-ROADMAP-001 Rev A (2026-05-11) — Issue #32. iOS app development roadmap with Apple Watch sync app as Year 1 post-launch deliverable. Core iOS app scope (§3). BLE GATT custom service definition (§5). Watch app: 4-phase development plan — Phase 1 session status + HRV breathing ring (Month 1–2, App Store target Month 3); Phase 2 audio sync to AirPods via WatchConnectivity (Month 3–4); Phase 3 haptic sync 40Hz Core Haptics (Month 4–5, pending thermal characterization OI-WA-01); Phase 4 40Hz visual flicker (Month 6+, blocked on OI-WA-02 screen brightness ≥100 nits at 40Hz). Sub-50ms sync latency target. Regulatory: all Watch functions declared session monitoring / UI aids; App Store Health & Fitness category; no therapeutic claims. BOM delta $0. 5 open items (OI-WA-01–05). Addresses Issue #32 first DoD checkpoint. |
| Hub PCB Rev B — Vishay DG2788A TIA Gain Switch + I2C Mux | `docs/np_hw_hub_001.md` | NP-HW-HUB-001 Rev B (2026-05-13) — Issue #62. OI-PBM-HW-01 CLOSED (SPECIFIED). Five Vishay DG2788A dual-SPDT analog switches (one per zone slot) switch TIA Rf 47kΩ→22kΩ when ZONE_ID debounce confirms smart module (ADC < 1100); GPIO GAIN_SEL[0..4] on i.MX RT1062 GPIO_B0_04..08. Required to prevent InGaAs PD1 TIA saturation (0.90 A/W × 72µA × 47kΩ = 3.38V exceeds 3.3V ADC rail). NXP PCA9546A I2C bus switch (4 channels, 0x70) isolates all 5 smart module slots sharing I2C address 0x30 (OI-PBM-HW-02 CLOSED). 12× 4.7kΩ pull-ups, 10× 47kΩ and 10× 22.1kΩ feedback resistors. Total hub BOM delta $1.41–1.91. Sequencing: ZONE_ID debounce → GAIN_SEL assert (NP_TIA_GAIN_LOW) → 10µs settle → I2C mux enable → I2C probe. Boot init: GAIN_SEL[0..4] driven LOW before zone detection task. Removal: I2C mux disable → GAIN_SEL deassert (NP_TIA_GAIN_HIGH). Firmware changes in firmware/pbm_1064nm/: np_pbm1064_hal_tia_gain_set(), np_pbm1064_hal_tia_gain_boot_init(), np_tia_gain_t enum, np_sm_slot_ctx_t.tia_gain field, np_pbm1064_detect.c sequencing. Hub PCB Rev B Gerber build (OI-HUB-03) required before FAI-SM-04 and FAI-SM-06 optical bench tests. |
| Hub Control Program | `firmware/hub_control/` | NP-FW-HUB-001 Rev A — FreeRTOS application for NXP i.MX RT1062 hub. Four tasks: (1) Module registry (np_hub_module_registry) — probes all 11 zone + accessory slots at boot and on hot-plug; drives zone_announce for bone conduction; maintains slot state table; (2) Session runner (np_hub_session_runner) — deserialises Ed25519-signed .npps session descriptor; dispatches stimulation commands to per-modality HAL drivers; enforces session-level dose limits; (3) Session log (np_hub_session_log) — routes 27-element data classification table to UHDR/SHDR partitions per NP-FW-EMMC-001 Rev A §12; (4) Safety SPI (np_hub_safety_spi) — 200ms heartbeat to Safety MCU; drives 1.5s watchdog; captures fault log to SHDR. Spec: `docs/np_fw_hub_001.md`. SW02-M09..M12 per NP-SW-001. G2-14 PARTIAL. |
| NeurOne Protocol Script (NPPS) Language Reference | `docs/np_npps_ref_001.md` | NPPS v1.0 — text-based session protocol language compiled to binary session descriptors signed by Ed25519. 15 modality blocks (eeg, bes, tdcs, pbm_transcranial, pbm_intranasal, vns_hrv, audio, visual, tms, tacs, hd_tdcs, cervical_vns, pbm_1064nm, hrv_biofeedback, vibrotactile). 19 predefined protocol templates (alpha_train, focus_prime, sleep_deep, gamma_40hz, pbm_vascular, pbm_gamma, combined_standard, combined_deep, emdr_ptsd, tms_depression_rTMS, tms_depression_TBS, ad_prevention, stroke_rehab, pd_motor, anxiety_hrv, pain_vnseeg, cognitive_hd, combined_4modal, t2_depression_full). Compiler validates all fields, dose limits, and interlock requirements before signing. Enables clinical users to script custom protocols without firmware modification. |
| Helmet Simulator | `simulator/` | NP-SIM-001 v0.3.0 (2026-05-17) — Issue #81 / PRs #76, #84, #85. Browser-based interactive 3D simulator (no build step, Three.js 0.164 via importmap CDN). Three purposes: (1) test target for macOS/iOS/Windows apps; (2) marketing and training tool embeddable in any web page; (3) clinician protocol development tool. Three.js helmet geometry based on Neuronic Light dome layout, adapted to NeurOne spec: 5 snap-in LED zone panels (ZM-01–ZM-05) with animated per-LED canvas textures; integrated goggles; over-ear audio cups with bone conduction piezo; occipital Boa arch + dial; control hub with status LEDs (power breathes, session LED pulses at freq/4 per §4.7); 8 spring-decoupled EEG pods (impedance color-coded green/amber/red); VNS auricular clip with PPG; intranasal Y-probe (animated — docked/inserting/inserted/removing; probe tip LEDs sync to active PBM session; hygiene sleeve overlay); mastoid haptic pad; **T2 TMS focal figure-8 coil** (Issue #79 — CLOSED). Wavelength false-color scheme: 660nm shown as actual red (#FF2200); 808nm → orange (#FF7200); 1064nm → amber (#FFBB00); 1170nm → gold (#FFE500) — longer actual wavelength maps to longer apparent visible wavelength. 9 NPPS protocol templates: alpha_calm, focus_prime, gamma_40hz, sleep_deep, anxiety_hrv, combined_4modal, pbm_vascular, pbm_1064_deep, tms_depression. Session engine tick-driven with ramp envelope; HRV breathing ring animates at 0.1Hz; timeline with phase segments; 4 camera presets. WebSocket device API: Node.js server (simulator/server/index.js, ws://localhost:9000); two client roles (controller = external app, display = browser tab); SESSION_START with Ed25519 test-key verification; ZONE_CONFIG; ACCESSORY_CONFIG; TELEMETRY at ≥10 Hz; FAULT; SESSION_COMPLETE; graceful standalone fallback when server absent; API badge in header goes green on connect (Issue #77 — CLOSED PR #84). ACCESSORY_CONFIG message type relays accessory state changes (intranasal probe states) from controller to display (Issue #78 — CLOSED PR #85). TMS coil (Issue #79 — CLOSED): figure-8 geometry at DLPFC_L (phi=0.38π, theta=0.25π); two Torus windings + CFRP housing + tapered handle + grip ridges + cable; CFRP non-conductive window ring at coil-shell interface; blue-white pulse animation with PointLight flash at rTMS rep rate; TMS modality pill in UI status bar; tmsCoil ACCESSORY_CONFIG key; HOWTO updated to v2.2. Open sub-issues: #80 (geometry update pending shell CAD). |
| RISK-03 Regulatory Opinion Scope Expansion — 1064nm | `docs/np_reg_pbm1064_001.md` | NP-REG-PBM1064-001 Rev A (2026-05-13) — Issue #56. Scope expansion brief for outside regulatory counsel extending existing RISK-03 engagement (Issue #5) to cover: (1) 1064nm LED array FDA general wellness pathway + IEC 62471 EH1/EH2 hazard classification (Q1–Q3); (2) three-channel aggregate irradiance ceiling 600 mW/cm² — IEC 62471 additive assessment, classification change risk, FTC substantiation, OI-PBM-05 value confirmation (Q4–Q6); (3) T2 combined 1064nm LED + 1170nm laser simultaneous session — dual-standard photobiological hazard, 510(k) coverage, IEC 60601-2-57 applicability, labeling (Q7–Q9); (4) depth-tier penetration claim FTC substantiation — "deeper than 810nm" and "three independent penetration depths" claims, implied clinical outcome risk (Q10–Q12). Marketing copy gate table for all 1064nm claims. Estimated incremental cost $5,000–10,000. Deliverables: 12-question opinion letter from counsel; explicit standard/statute for each answer. RISK-03 extended scope: MITIGATED pending letter receipt. OI-PBM-05 resolution gated on Q5/Q6. |
| 1064nm PBM Clinical Evidence Bibliography Addendum | `docs/np_bib_1064_001.md` | NP-BIB-1064-001 Rev A (2026-05-13) — Issue #56. Six-entry addendum to `neurone_bibliography.docx` covering the 1064nm PBM evidence base. Entry 12.1: Yao et al. (2022), Science Advances, DOI:10.1126/sciadv.abj7390 — primary evidence for 1064nm working memory protocol; right PFC 1064nm laser, n=20, EEG CDA amplitude significant p<0.01, working memory improvement + fNIRS cerebral oxygenation; NeurOne-specific analysis including LED vs laser caveat, pulsed vs CW limitation, single-site limitation. Entry 12.2: UT Dallas follow-on search guidance (pending 2023–2026 literature search). Entry 12.3: Tissue optics and penetration depth basis — Bashkatov et al. (2005), Yaroslavsky et al. (2002), Strangman et al. (2002) — biophysical foundation for "deeper than 810nm" depth claim; NeurOne two-pronged substantiation strategy (physics + functional human evidence). Entry 12.4: Cytochrome c oxidase mechanism at 1064nm — Rojas & Gonzalez-Lima (2013) review — extends established CCO mechanism from 660/808nm to 1064nm channel. Entry 12.5: 1064nm safety literature (Hamblin 2016) and IEC 62471 open items. Entry 12.6: Multi-modal combination evidence gap — no RCT combines 1064nm with other modalities; claim restriction to technology performance claims only. Summary open items table: 3 DONE (Yao, tissue optics, CCO mechanism); 2 OPEN (UT Dallas follow-ons, multi-modal evidence gap). Both this document AND NP-REG-PBM1064-001 Rev A required to lift marketing gate. |
| Privacy Notice | `docs/np_priv_notice_001.md` | NP-PRIV-NOTICE-001 Rev A (2026-06-03) — Issue #98 / NP-PRIV-REM-001 STEP-33. Full app privacy notice. §2: UHDR/SHDR architecture explained in plain English. §3: app analytics data (engagement_tier enum, SDK gate). §4: **Automated processing and adaptive stimulation** — GDPR Art. 13(2)(f) disclosure covering adaptive algorithm logic, parameters that can change (audio beat, PBM pulse, tACS, VNS phase), what is recorded (classified trigger enum + parameter delta, no raw EEG/HRV), and user's right to view adjustments in Session History. §5: research data architecture (on-device anonymization, consent withdrawal effectiveness). §6: clinician access. §7: GDPR/CCPA rights table with exercise mechanisms. §8: retention table. §9: security summary. OI-PA-04 OPEN — GDPR §4 copy requires Privacy Lead sign-off before first public beta. |
| App Privacy Audit | `docs/np_priv_audit_001.md` | NP-PRIV-AUDIT-001 Rev A (2026-06-03) — STEP-21 of NP-PRIV-REM-001. iOS/Android app pre-beta privacy analysis. Jurisdiction scope: US federal (FTC Act §5, FTC HBNR, HIPAA), US state (BIPA IL, MHMD WA, CCPA/CPRA CA, state omnibus), EU/EEA GDPR, UK GDPR, Canada PIPEDA. 16 findings: 0 Critical, 5 High, 7 Medium, 4 Low. Five High findings: (1) analytics/crash vendor not selected — PrivacyInfo.xcprivacy cannot be completed; (2) SDK initialization gate not CI-verified; (3) App Store privacy nutrition label and Google Play Data Safety not filed; (4) BIPA written release screen not implemented — OI-PA-03 open; (5) Washington MHMD compliance for SHDR device session counts unverified. 16-item compliance checklist (AUDIT-01 through AUDIT-16), all OPEN — BLOCKING for first external beta. Four audit open items (OI-AUDIT-01 through OI-AUDIT-04). Top recommended actions: (1) select analytics + crash vendor, execute DPA/BAA (Month 6); (2) implement + CI-verify SDK init gate before any test build; (3) BIPA + MHMD legal opinions before US activation; (4) commission research consent UX formative study; (5) author adaptive stimulation trigger enum before session runner firmware. |
| Consumable Tracker Privacy Analysis | `docs/np_priv_analysis_003.md` | NP-PRIV-ANALYSIS-003 Rev A (2026-06-08) — Consumable tracker feature audit (ISC-154). Jurisdictions: All. 0 Critical, 0 High, 0 Medium, 2 Low findings. LOW-1: notification lock-screen title reveals NeurOne device ownership and consumable type without device unlock — fix: generic title on lock screen, consumable detail in body (after unlock). LOW-2: `requestNotificationPermission()` called in `ConsumableTracker.init()` at app launch before onboarding — fix: defer to `ConsumableView.onAppear`. What looks good: UHDR/SHDR boundary structurally enforced by `ConsumableCountsProviding` protocol; consumable counts correctly classified as SHDR; `orderURL` uses static enum rawValue (not user data); no outbound network calls; notification `userInfo` contains only `consumableIndex` (Int 0–3). No blocking items for internal beta. |
| Privacy Analysis and Repair (second pass) | `docs/np_priv_analysis_002.md` | NP-PRIV-ANALYSIS-002 Rev A (2026-06-05, PR #112 + session 2) — Code-level privacy audit of iOS app source. Jurisdiction scope: US federal + state (BIPA IL, WA MHMD, CCPA/CPRA), EU/EEA GDPR, UK GDPR, Canada PIPEDA. 12 findings: 1 Critical, 3 High, 5 Medium, 3 Low. All 12 code-level findings resolved. PR #112: (1) UHDRKeyManager PBKDF2 hardcoded password → `#error` compile gate + `.deviceOwnerAuthentication` PIN fallback; (2) SHDRUploader `identifierForVendor` → opaque Keychain warranty token; (3) SHDRUploader staging file excluded from backup; (4) UHDRBackupScheduler backup dir excluded from iCloud/iTunes + `keyFingerprint` removed from manifest; (5) `ResearchAnalyticsGate.reset()` wired to `ConsentStore.revokeResearchAnalytics()`. Session 2: (6) MEDIUM-07 — `SessionRecord.completedAt: Date` → `sessionDay: String` + `insertionIndex: Int`; (7) MEDIUM-08 — `epoch` excluded from WC bridge payload; (8) LOW-10 — `redacted(_:)` helper in `ConsentDashboardView`; (9) LOW-11 — SPKI certificate pinning on `SHDRUploader` fleet session; (10) LOW-12 — `kSecAttrAccessible` filter in `SessionProtocolSigner` Keychain load query. OI-PA-03 (BIPA locale detection) and OI-PA-04 (Adaptive Adjustments Privacy Lead copy sign-off) remain as process open items. |
| SW-01 Safety MCU Unit-Level FMEA | `docs/np_fmea_001.md` | NP-FMEA-001 Rev B (2026-06-15) — IEC 62304 §7.1 Class C FMEA for SW01-M01..M08. 43 failure modes across: GPIO management (M01), SPI heartbeat watchdog (M02), charge density monitor (M03), thermal interlock (M04), cervical VNS cardiac interlock (M05), impedance check (M06), session signature verification (M07), fault latch (M08). ISO 14971 S1–S5 / P1–P5 scoring. Two initial UNACCEPTABLE risks (S×P=10): FMEA-M03-01 (charge accumulator overflow, mitigated by 64-bit uint64_t) and FMEA-M05-02 (HR delta integer underflow, mitigated by int16_t signed arithmetic + MISRA C:2012 Rule 10.1); both residual risk ≤3 (ACCEPTABLE). 5 open items: OI-FMEA-01 (watchdog GPIO cutoff bench ≤50ms), OI-FMEA-02 (cardiac interlock bench <5.1ms), OI-FMEA-03 (Ed25519 key flash CRC at startup), OI-FMEA-04 (MISRA scan), OI-FMEA-05 (update after FAI-CV02 hardware bench). Rev B (OI-SW01-M07-02 CLOSED, PR #132): §3.7 description corrected to state Ed25519 is backed by np_crypto/Monocypher 4.0.2; FMEA-M07-05 mitigation updated to reference Monocypher ct_memcmp. |
| T2 Clinical Scripting API Specification | `docs/np_api_001.md` | NP-API-001 Rev A (2026-06-07, PR #121) — T2 clinical API specification (BAA required; must exist before any T2 API code is written). §2: auth/tiers (Monitor $49/mo, Assess $149/mo, Full Clinical $299/mo, Research $599/mo), UHDR three-condition access gate (ALL THREE: explicit consent + API toggle + use-case scope). §3: 8 REST endpoints — POST /protocols (NPPS compile + Ed25519 sign), POST /devices/{id}/sessions, GET /devices/{id}/status (SHDR-only), GET+POST /patients/{token}/fhir/Observation/DiagnosticReport/Procedure, GET /patients/{token}/export (bulk, separate credential), GET /audit. §4: WebSocket API (controller/display roles; SESSION_START/PAUSE/STOP/ZONE_CONFIG/ACCESSORY_CONFIG/TELEMETRY/FAULT/SESSION_COMPLETE/ERROR); UHDR telemetry fields gated at session-start time. §5: FHIR data model (NP-INT-FHIR-001). §6: privacy governance — SHDR-only default; consent withdrawal → 403 UHDR_ACCESS_DENIED for all data periods; research tier k≥10 l-diversity l≥3 DP ε≤1.0. §7: rate limits (1,000/hr, 10,000/day). §8: error codes (17). §9: TLS 1.3 mandatory; certificate pinning; key vault required; CI boundary tests; independent security audit (NP-SEC-PENTEST-002) before any external clinical key. 7 open items (OI-API-01..07). G1 gate item (NP-PRIV-REM-001 STEP-15). |
| Design Input/Output Traceability Matrix | `docs/np_dt_001.md` | NP-DT-001 Rev A (2026-06-07, PR #122) — Design I/O traceability per 21 CFR §820.30(c)/(d) and ISO 13485:2016 §7.3. §3: 57 design inputs — DI-PERF-01..25 (performance: EEG, PBM, stimulation modalities, UHDR/SHDR, OTA, EMF, fit, zone modules, HRV), DI-SAFE-01..12 (safety: charge density HW limit, SPI watchdog, CVNS cardiac interlock, photoparoxysmal, IEC 62471 MPE, Ed25519 signature, tDCS ramp, thermal cutoff, fault latch, TMS EMF gating, Mode F, impedance check), DI-USE-01..07 (usability), DI-REG-01..12 (regulatory: IEC 60601-1/2-10/62471/62133, FCC Part 15, IEC 62304, ISO 14971, 21 CFR 820, GDPR), DI-INT-01..05 (interface). §4: 35 design outputs (DO-HW-*, DO-FW-*, DO-SW-*, DO-API-*, DO-FHIR-*, DO-RISK-*, DO-REG-*, DO-TEST-*). §5: full DI→DO→VE traceability matrix with status (Traced/Partial/Open). §6: 13 verification evidence entries (VE-01..13). §7: gap analysis — safety-critical blocking gaps identified (hardware bench items, standards lab testing). §8: DHF completeness upgraded from Partial to Good. G2 exit criterion per NP-DP-001 §6.4. 5 open items (OI-DT-01..05). |
| SW-01 Safety MCU Firmware Source (all 8 modules) | `firmware/safety_mcu/` | IEC 62304 Class C — STM32G071 (Cortex-M0+, 64 MHz, bare-metal, no RTOS). Build: `firmware/cmake/stm32g071.cmake` toolchain + `firmware/safety_mcu/CMakeLists.txt` (produces `.elf/.bin/.hex`). Linker script (`startup/stm32g071_flash.ld`): 128 KB Flash at 0x08000000, 36 KB SRAM at 0x20000000, `.fault_latch` NOLOAD **pinned** to top 64 bytes of SRAM via `. = _fault_latch_start;` (not zeroed on warm reset — placement bug fixed 2026-06-08). Startup (`startup_stm32g071xx.s`): copies .data, zeros .bss skipping .fault_latch, calls main. Config (`include/np_safety_config.h`): all GPIO pin assignments, timing constants, limits. Protocol (`include/np_safety_protocol.h`): 8-byte SPI frame types, `NP_SAFETY_STATUS_*` bits, `NP_SESSION_STATUS_*` bits (bit 0 = ACTIVE, bit 1 = CVNS_REENABLE), `np_safety_state_t`. SW01-M01 `np_gpio_mgr`: owns 14 stimulation GPIOs active-LOW (LOW = enabled, HIGH = disabled — startup drives HIGH). SW01-M02 `np_spi_watchdog`: tick on valid frame; fault flags extracted to local variable (ISR race fix); timeout check **every loop iteration** via `np_spi_watchdog_check()` (1500ms cutoff); CUTOFF bit cleared only when no other interlock active. SW01-M03 `np_charge_monitor`: uint64_t per-electrode accumulator (`s_charge_nc[NP_SAFETY_MAX_CHANNELS]`); per-channel limits (`s_limit_nc[]`, default 1000µC, override via `np_charge_monitor_set_channel_limit()`); accumulate() called every heartbeat for granted channels from `current_ua[]` in extended 38-byte frame; OI-CHARGE-01 CLOSED (2026-06-16). `np_charge_monitor_set_channel_area_mcm2()` derives a per-channel limit from electrode AREA (`limit_nc = NP_CHARGE_LIMIT_UC_CM2 × area_mcm2`); called from the main loop when the hub delivers a 34-byte `np_safety_chan_limit_cmd_t` — OI-CHARGE-02 CLOSED (2026-07-07): HD-tDCS CLIN_STIM (ch 13) limited to 3.84µC. Density constant stays on the MCU (hub sends area only). OI-CHARGE-03 CLOSED (2026-07-07): `np_charge_monitor_geom_gate()` blocks CLIN_STIM from granted_mask when the hub asserts `NP_SESSION_STATUS_GEOM_REQUIRED` (heartbeat bit 2) but no valid area has been applied (`geom_applied` latch, reset per session) — fail-closed against a lost area command. Runs before the accumulate loop; scoped to CLIN_STIM (clinical tACS unaffected). SW01-M04 `np_thermal_interlock`: NTC ADC lookup (0–110°C, 1°C resolution), 10ms poll, 62°C cutoff. SW01-M05 `np_cardiac_interlock`: RR buffer (8 intervals); `s_first_beat_seen` guard prevents phantom first interval; `rr_to_bpm()` saturates at INT16_MAX (overflow fix); `current_hr_bpm()` called once per tick (ISR race fix); baseline refresh gated on `!s_cutoff_active` (desensitization fix); rolling baseline refreshed every 5s (NP_CARDIAC_OBS_MS); lockout via elapsed subtraction (wrap-safe at ~49 days); `s_cutoff_active` reset when lockout expires; `np_cardiac_interlock_reenable()` clears CARDIAC status + resets baseline on hub CVNS_REENABLE signal. SW01-M06 `np_impedance_check`: pre-session and post-cutoff impedance check for VNS/tDCS/BES/CVNS; polled every loop iteration; IMPEDANCE status bit cleared on successful recheck (permanent-lock bug fixed). SW01-M07 `np_session_sig`: Ed25519 OTP pubkey verification via `np_crypto` (Monocypher 4.0.2, RFC 8032, SHA-512); `np_session_sig_reset(state*)` sets `NP_SAFETY_STATUS_SIG_PENDING` on session 0→1 transition; `np_session_sig_verify()` clears SIG_PENDING on valid sig; SIG_PENDING in `active_faults` mask blocks `granted_mask` until hub delivers 102-byte `np_safety_sig_cmd_t` frame via `np_safety_spi_send_session_sig()`. OI-SW01-M07-02 CLOSED (2026-06-11). OI-SW01-M07-01 CLOSED (2026-06-16). SW01-M08 `np_fault_latch`: `.fault_latch` section persists through warm reset; magic sentinel 0xDEADBEEF; commit change-gated (count tracks distinct fault transitions, not loop iterations); first-loop overwrite of warm-reset latch prevented by `s_prior_latch_reported` flag in main. Second-pass review: 9 additional bugs corrected 2026-06-08. |
| Firmware Build Super-Project + CI Workflows | `firmware/CMakeLists.txt`, `.github/workflows/` | `firmware/CMakeLists.txt`: dual-mode CMake super-project — `-DNP_BUILD_TESTS=ON` runs host-native tests for anon/edf/factory_reset (returns early, no ARM toolchain required); default cross-compiles all modules for i.MX RT1062. Three new CI workflows: `firmware-host-tests.yml` (Ubuntu, cmake + ctest, triggers on firmware/anon|edf|factory_reset source changes); `web-ci.yml` (Ubuntu, Bun install → tsc → vitest → vite build, triggers on app/web changes); `ios-ci.yml` (macOS-14, xcodebuild test with iPhone 15 simulator, triggers on app/ios changes). `.gitignore` rewritten with project-appropriate rules (firmware ELF/bin/hex/map/su/d, Python pycache, Node modules, Xcode DerivedData, SPM .build, IDE files). **Updated 2026-06-16 (OI-SW01-M07-01):** `add_subdirectory(safety_mcu)` added to host test section; `add_test(NAME np_safety_spi_proto_tests ...)` wired; host test suite now 6 test binaries (+ `np_safety_spi_proto_tests`). `firmware/safety_mcu/CMakeLists.txt` updated with `option(NP_BUILD_TESTS ...)` + early-return host test path (no ARM toolchain required when `-DNP_BUILD_TESTS=ON`). **Updated 2026-06-16 (OI-CHARGE-01):** `add_test(NAME np_charge_monitor_tests ...)` wired; host test suite now 7 test binaries (+ `np_charge_monitor_tests`, 26 tests). CI workflow `firmware-host-tests.yml` trigger path extended to include `firmware/safety_mcu/**` so charge monitor and SPI proto tests run on every safety MCU source change. |
| Safety MCU SPI Protocol — Extended Heartbeat Frame + Charge Monitor (OI-CHARGE-01) | `firmware/common/include/np_spi_wire_types.h`, `firmware/safety_mcu/src/np_charge_monitor.c`, `firmware/safety_mcu/src/np_safety_main.c`, `firmware/safety_mcu/src/np_spi_watchdog.c`, `firmware/hub_control/include/np_safety_spi.h`, `firmware/hub_control/src/np_safety_spi.c`, `firmware/safety_mcu/tests/np_charge_monitor_tests.c`, `firmware/safety_mcu/tests/np_safety_spi_proto_tests.c` | OI-CHARGE-01 CLOSED (2026-06-16). **`np_safety_rx_ext_frame_t`** (38 bytes, packed): `magic[2]` (0xBE 0xA7) + `session_status` + `enable_lo` + `enable_hi` + `channel_count` (repurposes former `reserved` byte, offset 5) + `checksum` (additive sum bytes[0..5], offset 6) + `current_ua[14]` (NP_SAFETY_MAX_CHANNELS × uint16_t LE, commanded µA, offset 8) + `ext_checksum` (additive sum bytes[8..35], offset 36). `np_safety_tx_ext_frame_t` typedef alias for hub-perspective naming. Two compile-time size assertions. **Privacy gate met:** `current_ua[]` = SHDR "commanded current from session descriptor" — classification comment in all 5 production files; NOT ADC-measured actual current (UHDR). **`np_safety_main.c` changes:** `np_hal_spi_get_ext_frame()` replaces legacy `get_frame()`; dual checksum validation (`frame_checksum_ok` + `ext_checksum_ok`) before `valid_frame = true`; interlock ordering fixed — thermal + impedance + cardiac reduce `granted_mask` BEFORE accumulate loop, charge_tick last; accumulate loop bounds: `ch < rx.channel_count && ch < NP_SAFETY_MAX_CHANNELS`. **`np_charge_monitor.c` changes:** `s_charge_nc/limit_nc/limit_reached` arrays sized `NP_SAFETY_MAX_CHANNELS` (code-review fix: no magic literals); `set_channel_limit()` API added; `tick()` uses per-channel `s_limit_nc[]`. **Hub `np_safety_spi_heartbeat()` signature extended:** `const uint16_t *current_ua, uint8_t channel_count` params; builds 38-byte ext frame; `compute_checksum((const uint8_t *)&tx + 8U, NP_SAFETY_MAX_CHANNELS * sizeof(uint16_t))` for `ext_checksum`; receives 38 bytes, `memcpy` first 8 into `np_safety_rx_frame_t mcu_reply`. **26 `np_charge_monitor_tests` (8 test functions):** init, limit enforcement, channel isolation, reset, custom limit, out-of-range, idempotency, precision math. **12 new `np_safety_spi_proto_tests`:** ext frame size + offsets, base + ext checksum round-trip + corruption, magic parity with heartbeat, channel count constant. **76/76 tests pass (0 failures).** FAI bench test with calibrated current source (G2 prototype) still required for full OI-CHARGE-01 closure. |
| Safety MCU SPI Protocol — Session Signature Frame (OI-SW01-M07-01) | `firmware/safety_mcu/include/np_safety_protocol.h`, `firmware/safety_mcu/src/np_session_sig.c`, `firmware/safety_mcu/src/np_spi_watchdog.c`, `firmware/safety_mcu/src/np_safety_main.c`, `firmware/hub_control/include/np_safety_spi.h`, `firmware/hub_control/src/np_safety_spi.c`, `firmware/hub_control/include/np_hub_config.h`, `firmware/hub_control/include/np_hub_types.h`, `firmware/safety_mcu/tests/np_safety_spi_proto_tests.c` | OI-SW01-M07-01 CLOSED (2026-06-16). Two-frame SPI protocol designed and implemented. **`np_safety_sig_cmd_t`** (102 bytes, packed): `cmd_magic[2]` = `{0xC0, 0xDE}`, `cmd_type` = `0x01`, `reserved`, `session_hash[32]` (SHA-256), `session_sig[64]` (Ed25519), `checksum` (additive sum bytes[0..99], wrapping uint16). Compile-time size assertion `typedef char _np_sig_cmd_size_check[...]`. HAL distinguishes from 8-byte heartbeat by NSS-delineated transfer length. **`NP_SAFETY_STATUS_SIG_PENDING` (bit 7)**: set by `np_session_sig_reset(state*)` on session 0→1 transition; cleared by `np_session_sig_verify()` on success; persists + FAULT + CUTOFF (fault_slot=0xFD) on bad sig. Added to `active_faults` mask in `np_spi_watchdog_tick()` — blocks `granted_mask` until verified. **`np_safety_spi_send_session_sig(hash, sig)`**: hub-side API builds 102-byte frame, computes checksum, calls `np_safety_hal_spi_transfer(..., NP_SAFETY_CMD_FRAME_LEN)`; discards MCU's concurrent TX; checks SIG_PENDING in next heartbeat. Sequencing contract: (1) heartbeat(session_active=1, mask=0) → MCU sets SIG_PENDING; (2) hub sends 102-byte sig cmd; (3) MCU verifies → clears SIG_PENDING on success; (4) heartbeat(mask=channels) → MCU grants. **38 host tests** (`np_safety_spi_proto_tests`): 7 test groups covering frame sizes + packed field offsets, magic distinctness, checksum round-trip + corruption + field-flip detection, SIG_PENDING lifecycle (reset/success/failure paths), watchdog grant gating with/without SIG_PENDING, constant values, heartbeat frame unaffected. All 38 pass. 6/6 firmware host test binaries pass end-to-end. |
| OTA Firmware Update — iOS + C module | `app/ios/NeurOne/OTA/`, `app/ios/NeurOne/Models/OTAModels.swift`, `firmware/ota/` | SW-03 iOS OTA implementation + host-testable C module (2026-06-09). ISC-107–113 VERIFIED (ISA progress: 36/164). **`firmware/ota/`**: `np_ota_state.h/.c` — `np_ota_state_t` (packed: magic 0x4E504F54, target_bank uint8, boot_attempts uint8, reserved uint16, staged_sha256[32], crc32); CRC32 IEEE 802.3 reflected polynomial over bytes 0..39; `np_ota_state_validate()` (magic → bank → CRC → attempts); `np_ota_state_increment_attempts()` (returns bool, recomputes CRC); 21 host tests pass (`firmware/ota/tests/np_ota_tests.c`). **`OTAModels.swift`**: `OTAPhase` (idle=0x00 through failed=0xFF, verified=0x04, isBusy/isTerminal); `FirmwareVersion: Comparable, Equatable, Hashable` (String 3-part init, GATT uint32 LE init: bits [23:16]=major, [15:8]=minor, [7:0]=patch); `FirmwareManifest: Decodable` (ISO8601DateFormatter `.withFullDate`); `OTASession` (chunkSize=496, integer round-half-up `formattedBytes` — avoids banker's rounding "0 KB" for <1 KB transfers). **`FirmwareUpdateService.swift`**: SPKI SHA-256 certificate pinning — `spkiDERHeader(for:)` returns correct DER AlgorithmIdentifier prefix for RSA-2048, RSA-4096, EC P-256, EC P-384; computes `spki = Data(header) + rawKey` before SHA-256 (without header the hash would never match a real certificate); ephemeral URLSession; static public manifest URL (no device ID, no user ID, no session context in any request); `FirmwareUpdateProviding` protocol for mock injection in tests. **`OTAManager.swift`**: fingerprint-first ordering — `verifySignature(image:)` is first statement in `beginUpdate(image:)`, before `guard .connected`, before `downloadFirmware()`; unknown fingerprints rejected with `downloadCallCount == 0`; `lastError` set before re-throwing download errors so `OTAView` can display them; 9-step OTA flow: INITIATE → chunks (index prepended, LE uint16, 496-byte payload) → VERIFY → `waitForPhase(.verified, timeout:60)` → COMMIT → `waitForCompletion(timeout:120)`; `abort()` path; Safety MCU flow mirrored. **`OTAView.swift`**: version display from `gatt.hubFirmwareVersion` + `ota.availableUpdate?.version`; byte-level progress `sentBytes / totalBytes`; reconnect wait copy. **`NeurOneGATTManager`** additions: `@Published private(set) var hubFirmwareVersion: String?`; `applyFirmwareVersion(_:)`; `applyConnected()` test helper (sets `.connected` without BLE hardware); `hubFirmwareVersion = nil` on disconnect; `firmwareVersion` GATT UUID `4E455550-0011-…` NOT in `NPUUID.all`. Opcode wire values (frozen — hub firmware contract): initiate=0x01, chunk=0x02, verify=0x03, commit=0x04, abort=0x05, safetyMCUBegin=0x10, safetyMCUChunk=0x11, safetyMCUCommit=0x12. **27/27 `OTAManagerTests` pass.** Privacy analysis: static URL + no device ID (prevents server-side OTA correlation); fingerprint-first ordering (unknown firmware never triggers network download); SPKI DER header fix (pinning was silently non-functional without header). |
| UHDR Argon2id Key Derivation + PHC Library | `app/NeurOneShared/Sources/NeurOneArgon2/`, `app/ios/NeurOne/Data/UHDRKeyManager.swift` | PBKDF2 placeholder fully replaced (2026-06-11). PHC `phc-winner-argon2` (CC0/Apache 2.0) vendored as SPM C target. `np_bridge.c` wraps `argon2id_hash_raw()`. `Argon2Bridge.swift` exports `npArgon2idDeriveKey()` (m=65536 KiB, t=4, p=1, output=64B). `ARGON2_NO_THREADS` enforces single-threaded path. `np_argon2id_hash_custom()` gated to debug builds only (`#ifdef NP_ARGON2_CUSTOM_ENABLED` in `np_bridge.c`; `.define(..., .when(configuration: .debug))` in Package.swift); confirmed absent from release binary (`nm` verified). `UHDRKeyManager.swift` derives memory-only `UHDRKey(k1:k2:)` from biometric/PIN credential + device-bound CSPRNG salt; key never persisted. `.deviceOwnerAuthentication` provides passcode fallback for users without biometric. `phc/VENDOR.md`: SHA-256 file inventory + SOUP record for NP-SW-001. 6/6 `UHDRKeyManagerTests` pass (including `testArgon2idDeterministic` KAT + `testKeyNeverTransmitted` source invariant scan). Cato CR-001–005 resolved. |
| BLE GATT Layer (iOS) | `app/ios/NeurOne/BLE/` | SW-03 iOS App — BLE GATT implementation for NeurOne hub connectivity (2026-06-08). Three files authored/rewritten: (1) **`BLECentral.swift`** — `BLECentralManager` protocol wrapping `CBCentralManager`; `CBCentralManager` zero-cost extension conformance; enables unit testing without real BLE hardware. (2) **`GATTCharacteristics.swift`** (updated) — `warrantyToken` UUID `4E455550-0010-…` added (READ 32B, SHDR-linked TRNG token, NP-FW-EMMC-002 Rev A §A); excluded from `NPUUID.all` — hub firmware not yet shipped (OI-WA-03). `NPUUID.all` count: 14 characteristics (9 NOTIFY + 5 WRITE). (3) **`NeurOneGATTManager.swift`** (rewritten) — complete ISC-11..ISC-20 implementation. `@Published` properties: `connectionState`, `session`, `zoneModules`, `otaStatus`, `shdrUploadPending`, `bluetoothUnavailable`, `allCharacteristicsResolved`, `warrantyToken: Data?`. Internal apply methods for testability: `applyStateUpdate(state:)` (scans on poweredOn; calls `applyDisconnection()` on all other states to clear stale UHDR data — privacy fix), `applyCharacteristicAssignment(discovered:)` (privacy doc comment; boolean output only, `discovered` fingerprint never leaves method), `applyDisconnection()` (clears all UHDR session state, nil warrantyToken, schedules 2s reconnect scan guarded by `central.state == .poweredOn`), `applyWarrantyToken(_:)` (rejects payloads < 32B; truncates > 32B). `didDiscoverCharacteristics`: discovers `NPUUID.all + [NPUUID.warrantyToken]`; `SESSION_STATUS` reads value immediately on discovery (ISC-20 reconnect restore path); `warrantyToken` absence silently tolerated. `didUpdateValueFor`: SHDR characteristics (`warrantyToken`, `shdrUploadStatus`) have structural early-return guards before `session = pending` — UHDR/SHDR boundary is code-structurally enforced, not convention-dependent. `requestEDFDownload`: explicit `.littleEndian` serialization. **Unit tests** (`NeurOneTests/NeurOneGATTManagerTests.swift`): 25 tests, 25/25 pass. `MockBLECentral` injects via `init(mockCentral:)`. Tests cover ISC-11, 12, 13, 16, 17, 19, 20 and warranty token SHDR architecture. **Code review** + **privacy review** completed; 5 findings applied (see §13.5). **OI-BLE-01 (OPEN)**: `SHDRUploader` upgrade path — subscribe to `gatt.$warrantyToken` Combine sink and call `upgradeDeviceToken(_:)` when non-nil; BLOCKING for full SHDR fleet DB no-join architecture (NP-FW-EMMC-002 Rev A §A). |

---

## 15. MARKETING NOTES

### Mastoid LRA pad vs Apple Watch — messaging for marketing literature

**Framing:** Lead with why our add-on is better, not with why Apple Watch is worse. Acknowledge the Apple Watch sync app exists and is available — but be clear about what it does and doesn't do.

---

**Draft marketing copy (vibrotactile accessory page / FAQ):**

*"We offer a free Apple Watch sync app that delivers 40Hz haptic feedback in time with your NeurOne session — and you're welcome to use it. But if you want results that match the science, the NeurOne vibrotactile pad is the right tool.*

*Here's the difference: the mastoid pad is a purpose-built linear actuator positioned at the bone just behind your ear. That placement matters — the mastoid process couples vibration directly into the skull and temporal bone, putting the 40Hz signal right where the brain research was done. The Apple Watch sits on your wrist. Getting a 40Hz vibration from your wrist to your somatosensory cortex means traveling through soft tissue, tendons, and bone across your entire arm and shoulder. Most of the signal doesn't make it.*

*The pad also delivers 40Hz ± 0.5Hz precision. Apple's Taptic Engine was designed for notification taps, not continuous therapeutic vibration — its output at 40Hz is uncharacterised and variable. Our driver IC runs open-loop at a locked frequency with calibrated amplitude, matched to the laboratory protocol.*

*One more practical difference: running continuous haptics on an Apple Watch for 20 minutes will drain a significant portion of its battery. The mastoid pad draws its power from the NeurOne hub.*

*Use the Watch app if you want an extra sensory layer on top of the pad. Use it alone if you're curious. But if 40Hz vibrotactile therapy is the goal, the mastoid pad is what delivers it."*

---

**Key messages (bullet form for web/app copy):**
- Purpose-built for the mastoid — where bone meets skull, where the science was done
- 40Hz ± 0.5Hz locked precision vs uncharacterised wrist haptics
- Powered by the NeurOne hub — doesn't drain your Watch battery
- The Apple Watch sync app is a free bonus, not the full experience

---

## 16. NAMING CONVENTION CHANGES

**Retired term:** "Health Data Record (HDR)" — ambiguous, replaced throughout all documents

**Replacement:**
- `UHDR` = User Health Data Record (user's property, never accessed by NeurOne)
- `SHDR` = System Health Data Record (NeurOne property, device-linked only, never user-linked)

Both terms appear in full on first use in each document, abbreviated thereafter.

---

*This CLAUDE.md is the authoritative project memory file for the NeurOne design program. All decisions marked as "locked" or "decided" have been through full design review. Decisions marked "pending" require resolution before first tooling cut. Update this file when any locked decision changes.*
