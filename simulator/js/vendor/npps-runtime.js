// GENERATED by scripts/build-simulator-runtime.ts — DO NOT EDIT BY HAND.
// A bundle of simulator/src/npps-runtime.ts + app/web/src/lib/nppsParser.ts.
//
// This file contains CODE ONLY. It holds no zones, no conditions and no
// protocols: those are fetched from protocols/predefined/ when the simulator
// loads, per NP-NPPS-REF-001 §1.6 (No build-time cache of protocol content).
// Regenerate with: bun scripts/build-simulator-runtime.ts
// app/web/src/types/protocol.ts
function defaultParams(type) {
  const defaults = {
    pbm_transcranial: {
      zones: "named",
      zoneRefs: ["All"],
      wavelength: "660_808nm",
      intensityPercent: 75,
      frequencyHz: 40,
      dutyCyclePercent: 25
    },
    pbm_intranasal: {
      intensityPercent: 60,
      frequencyHz: 10,
      dutyCyclePercent: 50
    },
    eeg_neurofeedback: {
      channels: "all",
      band: "alpha",
      closedLoopEnabled: true
    },
    bes_tacs: {
      frequencyHz: 40,
      intensityMilliamps: 0.5,
      waveform: "sinusoidal"
    },
    tdcs: {
      intensityMilliamps: 1,
      electrodePairs: [["Fp1", "Fp2"]],
      rampSeconds: 30
    },
    vns_hrv: {
      frequencyHz: 25,
      intensityMilliamps: 0.5,
      hrvProtocol: "standalone",
      resonanceBreathingRate: 6
    },
    audio_entrainment: {
      binauralBeatsHz: 40,
      carrierHz: 200,
      volumePercent: 70,
      eegAdaptive: true,
      boneConductionPacer: false
    },
    visual_stimulation: {
      frequencyHz: 40,
      mode: "binocular",
      emdrCadenceHz: 1,
      enableModeF: false
    },
    qeeg_21ch: {
      montage: "standard_1020",
      sloretaEnabled: true,
      reference: "linked_ear"
    },
    tms: {
      tmsProtocol: "rTMS",
      frequencyHz: 10,
      intensityPercentMT: 110,
      target: "DLPFC_L",
      pulseCount: 3000
    },
    pbm_deep_1170nm: {
      intensityMWcm2: 500,
      frequencyHz: 10,
      dutyCyclePercent: 50
    },
    clinical_tacs: {
      frequencyHz: 40,
      intensityMilliamps: 2,
      channelCount: 8,
      waveform: "sinusoidal"
    },
    hd_tdcs: {
      target: "DLPFC_L",
      montage: "ring_4x1",
      intensityMilliamps: 1.5
    },
    cervical_vns: {
      frequencyHz: 25,
      intensityMilliamps: 1
    },
    vibrotactile_40hz: {
      intensityG: 0.9,
      syncToAudio: true,
      syncToVisual: true
    }
  };
  return defaults[type];
}

// app/web/src/lib/socketMap.generated.ts
var NP_SOCKETS = [
  { id: 1, row: 0, col: 0, x: -1, y: 0, xMm: 127.16, yMm: -26.06, zMm: -8.58 },
  { id: 2, row: 0, col: 1, x: 0, y: 0, xMm: 119.13, yMm: 0, zMm: -34.43 },
  { id: 3, row: 0, col: 2, x: 1, y: 0, xMm: 124.67, yMm: 25.55, zMm: -8.42 },
  { id: 4, row: 1, col: 0, x: -2.5, y: 0.866, xMm: 120.03, yMm: -53.08, zMm: -11.05 },
  { id: 5, row: 1, col: 1, x: -1.5, y: 0.866, xMm: 111.97, yMm: -38.66, zMm: -43.67 },
  { id: 6, row: 1, col: 2, x: -0.5, y: 0.866, xMm: 93.19, yMm: -17.44, zMm: -51.92 },
  { id: 7, row: 1, col: 3, x: 0.5, y: 0.866, xMm: 101.29, yMm: 18.96, zMm: -56.43 },
  { id: 8, row: 1, col: 4, x: 1.5, y: 0.866, xMm: 115.04, yMm: 39.72, zMm: -44.87 },
  { id: 9, row: 1, col: 5, x: 2.5, y: 0.866, xMm: 117.43, yMm: 51.93, zMm: -10.81 },
  { id: 10, row: 2, col: 0, x: -3, y: 1.7321, xMm: 101.35, yMm: -75.45, zMm: -21.7 },
  { id: 11, row: 2, col: 1, x: -2, y: 1.7321, xMm: 84.23, yMm: -53.62, zMm: -48.08 },
  { id: 12, row: 2, col: 2, x: -1, y: 1.7321, xMm: 80.68, yMm: -32.4, zMm: -71.46 },
  { id: 13, row: 2, col: 3, x: 0, y: 1.7321, xMm: 87.38, yMm: 0, zMm: -91.14 },
  { id: 14, row: 2, col: 4, x: 1, y: 1.7321, xMm: 86.04, yMm: 34.56, zMm: -76.21 },
  { id: 15, row: 2, col: 5, x: 2, y: 1.7321, xMm: 86.84, yMm: 55.29, zMm: -49.57 },
  { id: 16, row: 2, col: 6, x: 3, y: 1.7321, xMm: 100.56, yMm: 74.86, zMm: -21.53 },
  { id: 17, row: 3, col: 0, x: -3.5, y: 2.5981, xMm: 78.62, yMm: -98.8, zMm: -46.27 },
  { id: 18, row: 3, col: 1, x: -2.5, y: 2.5981, xMm: 70.82, yMm: -78.84, zMm: -70.31 },
  { id: 19, row: 3, col: 2, x: -1.5, y: 2.5981, xMm: 66.32, yMm: -50.68, zMm: -98.77 },
  { id: 20, row: 3, col: 3, x: -0.5, y: 2.5981, xMm: 65.92, yMm: -16.1, zMm: -117.98 },
  { id: 21, row: 3, col: 4, x: 0.5, y: 2.5981, xMm: 65.13, yMm: 15.91, zMm: -116.58 },
  { id: 22, row: 3, col: 5, x: 1.5, y: 2.5981, xMm: 65.84, yMm: 50.31, zMm: -98.05 },
  { id: 23, row: 3, col: 6, x: 2.5, y: 2.5981, xMm: 66.19, yMm: 73.69, zMm: -65.72 },
  { id: 24, row: 3, col: 7, x: 3.5, y: 2.5981, xMm: 67.62, yMm: 84.98, zMm: -39.8 },
  { id: 25, row: 4, col: 0, x: -4, y: 3.4641, xMm: 45.77, yMm: -109.19, zMm: -35.13 },
  { id: 26, row: 4, col: 1, x: -3, y: 3.4641, xMm: 42.67, yMm: -92.14, zMm: -67.76 },
  { id: 27, row: 4, col: 2, x: -2, y: 3.4641, xMm: 40.08, yMm: -66.75, zMm: -98.78 },
  { id: 28, row: 4, col: 3, x: -1, y: 3.4641, xMm: 40.08, yMm: -37.34, zMm: -124.56 },
  { id: 29, row: 4, col: 4, x: 0, y: 3.4641, xMm: 40.11, yMm: 0, zMm: -134.76 },
  { id: 30, row: 4, col: 5, x: 1, y: 3.4641, xMm: 40.15, yMm: 37.4, zMm: -124.77 },
  { id: 31, row: 4, col: 6, x: 2, y: 3.4641, xMm: 39.55, yMm: 65.87, zMm: -97.49 },
  { id: 32, row: 4, col: 7, x: 3, y: 3.4641, xMm: 40.43, yMm: 87.32, zMm: -64.21 },
  { id: 33, row: 4, col: 8, x: 4, y: 3.4641, xMm: 41.33, yMm: 98.61, zMm: -31.72 },
  { id: 34, row: 5, col: 0, x: -3.5, y: 4.3301, xMm: 7.94, yMm: -105.04, zMm: -65.43 },
  { id: 35, row: 5, col: 1, x: -2.5, y: 4.3301, xMm: 7.32, yMm: -80.6, zMm: -95.31 },
  { id: 36, row: 5, col: 2, x: -1.5, y: 4.3301, xMm: 7.37, yMm: -55.47, zMm: -125.64 },
  { id: 37, row: 5, col: 3, x: -0.5, y: 4.3301, xMm: 7.35, yMm: -20, zMm: -143.92 },
  { id: 38, row: 5, col: 4, x: 0.5, y: 4.3301, xMm: 7.25, yMm: 19.74, zMm: -142.07 },
  { id: 39, row: 5, col: 5, x: 1.5, y: 4.3301, xMm: 7.43, yMm: 55.94, zMm: -126.69 },
  { id: 40, row: 5, col: 6, x: 2.5, y: 4.3301, xMm: 7.37, yMm: 81.13, zMm: -95.95 },
  { id: 41, row: 5, col: 7, x: 3.5, y: 4.3301, xMm: 7.05, yMm: 93.36, zMm: -58.15 },
  { id: 42, row: 6, col: 0, x: -4, y: 5.1962, xMm: -25.18, yMm: -92.72, zMm: -38.83 },
  { id: 43, row: 6, col: 1, x: -3, y: 5.1962, xMm: -26.98, yMm: -88.96, zMm: -73.56 },
  { id: 44, row: 6, col: 2, x: -2, y: 5.1962, xMm: -26.85, yMm: -67.71, zMm: -107.08 },
  { id: 45, row: 6, col: 3, x: -1, y: 5.1962, xMm: -26.91, yMm: -38.24, zMm: -132.04 },
  { id: 46, row: 6, col: 4, x: 0, y: 5.1962, xMm: -26.93, yMm: 0, zMm: -142.16 },
  { id: 47, row: 6, col: 5, x: 1, y: 5.1962, xMm: -27.39, yMm: 38.91, zMm: -134.37 },
  { id: 48, row: 6, col: 6, x: 2, y: 5.1962, xMm: -26.8, yMm: 67.6, zMm: -106.91 },
  { id: 49, row: 6, col: 7, x: 3, y: 5.1962, xMm: -26.82, yMm: 88.43, zMm: -73.12 },
  { id: 50, row: 6, col: 8, x: 4, y: 5.1962, xMm: -27.72, yMm: 102.07, zMm: -42.74 },
  { id: 51, row: 7, col: 0, x: -3.5, y: 6.0622, xMm: -62.39, yMm: -95.64, zMm: -41.54 },
  { id: 52, row: 7, col: 1, x: -2.5, y: 6.0622, xMm: -59.11, yMm: -76.57, zMm: -77.22 },
  { id: 53, row: 7, col: 2, x: -1.5, y: 6.0622, xMm: -59.49, yMm: -53.64, zMm: -108.61 },
  { id: 54, row: 7, col: 3, x: -0.5, y: 6.0622, xMm: -60.02, yMm: -19.98, zMm: -129.49 },
  { id: 55, row: 7, col: 4, x: 0.5, y: 6.0622, xMm: -59.68, yMm: 19.87, zMm: -128.73 },
  { id: 56, row: 7, col: 5, x: 1.5, y: 6.0622, xMm: -59.83, yMm: 53.95, zMm: -109.24 },
  { id: 57, row: 7, col: 6, x: 2.5, y: 6.0622, xMm: -57.66, yMm: 74.7, zMm: -75.33 },
  { id: 58, row: 7, col: 7, x: 3.5, y: 6.0622, xMm: -56.06, yMm: 85.93, zMm: -37.33 },
  { id: 59, row: 8, col: 0, x: -3, y: 6.9282, xMm: -92.8, yMm: -82.36, zMm: -31.99 },
  { id: 60, row: 8, col: 1, x: -2, y: 6.9282, xMm: -84, yMm: -60.83, zMm: -65.81 },
  { id: 61, row: 8, col: 2, x: -1, y: 6.9282, xMm: -86.02, yMm: -36.44, zMm: -96.65 },
  { id: 62, row: 8, col: 3, x: 0, y: 6.9282, xMm: -86.48, yMm: 0, zMm: -109.39 },
  { id: 63, row: 8, col: 4, x: 1, y: 6.9282, xMm: -86.53, yMm: 36.66, zMm: -97.22 },
  { id: 64, row: 8, col: 5, x: 2, y: 6.9282, xMm: -83.91, yMm: 60.77, zMm: -65.75 },
  { id: 65, row: 8, col: 6, x: 3, y: 6.9282, xMm: -82.89, yMm: 73.56, zMm: -28.57 },
  { id: 66, row: 9, col: 0, x: -2.5, y: 7.7942, xMm: -117, yMm: -63.02, zMm: -23.24 },
  { id: 67, row: 9, col: 1, x: -1.5, y: 7.7942, xMm: -105.64, yMm: -45.83, zMm: -50.79 },
  { id: 68, row: 9, col: 2, x: -0.5, y: 7.7942, xMm: -109.72, yMm: -19.54, zMm: -79.52 },
  { id: 69, row: 9, col: 3, x: 0.5, y: 7.7942, xMm: -109.32, yMm: 19.47, zMm: -79.24 },
  { id: 70, row: 9, col: 4, x: 1.5, y: 7.7942, xMm: -105.47, yMm: 45.76, zMm: -50.71 },
  { id: 71, row: 9, col: 5, x: 2.5, y: 7.7942, xMm: -115.23, yMm: 62.07, zMm: -22.88 },
  { id: 72, row: 10, col: 0, x: -2, y: 8.6603, xMm: -118.4, yMm: -36.41, zMm: -2.01 },
  { id: 73, row: 10, col: 1, x: -1, y: 8.6603, xMm: -122.41, yMm: -30.23, zMm: -30.81 },
  { id: 74, row: 10, col: 2, x: 0, y: 8.6603, xMm: -120.86, yMm: 0, zMm: -51 },
  { id: 75, row: 10, col: 3, x: 1, y: 8.6603, xMm: -121.31, yMm: 29.96, zMm: -30.54 },
  { id: 76, row: 10, col: 4, x: 2, y: 8.6603, xMm: -118.32, yMm: 36.38, zMm: -2.01 },
  { id: 77, row: 11, col: 0, x: -1.5, y: 9.5263, xMm: -125.03, yMm: -18.18, zMm: 0 },
  { id: 78, row: 11, col: 1, x: -0.5, y: 9.5263, xMm: -136.55, yMm: -12.06, zMm: -21.62 },
  { id: 79, row: 11, col: 2, x: 0.5, y: 9.5263, xMm: -135.27, yMm: 11.95, zMm: -21.42 },
  { id: 80, row: 11, col: 3, x: 1.5, y: 9.5263, xMm: -124.18, yMm: 18.06, zMm: 0 }
];
var NP_SOCKET_COUNT = 80;
var NP_SOCKET_NUMBERING_BASE = 1;
var NP_SOCKET_ID_MIN = 1;
var NP_SOCKET_ID_MAX = 80;
var BY_ID = new Map(NP_SOCKETS.map((s) => [s.id, s]));
function isValidSocketId(id) {
  return BY_ID.has(id);
}

// app/web/src/lib/socketSet.ts
function socketRangeLabel() {
  return `${NP_SOCKET_ID_MIN}–${NP_SOCKET_ID_MAX}`;
}
var DECIMAL_INTEGER = /^[+-]?\d+$/;
function coerce(value) {
  if (typeof value === "number")
    return value;
  if (typeof value === "string" && DECIMAL_INTEGER.test(value.trim())) {
    return Number(value.trim());
  }
  return NaN;
}
function socketIdProblem(value) {
  const n = coerce(value);
  if (!Number.isInteger(n))
    return "not-an-integer";
  if (n < NP_SOCKET_ID_MIN || n > NP_SOCKET_ID_MAX)
    return "out-of-range";
  if (!isValidSocketId(n))
    return "not-fitted";
  return;
}
function describeInvalid(entry) {
  return typeof entry.raw === "string" ? JSON.stringify(entry.raw) : String(entry.raw);
}
function toSocketSet(raw) {
  const seen = new Set;
  const invalidSeen = new Set;
  const sockets = [];
  const invalid = [];
  const duplicates = [];
  for (const value of raw) {
    const problem = socketIdProblem(value);
    if (problem !== undefined) {
      const key = `${typeof value}:${String(value)}`;
      if (!invalidSeen.has(key)) {
        invalidSeen.add(key);
        invalid.push({ raw: value, problem });
      }
      continue;
    }
    const n = coerce(value);
    if (seen.has(n)) {
      if (!duplicates.includes(n))
        duplicates.push(n);
      continue;
    }
    seen.add(n);
    sockets.push(n);
  }
  sockets.sort((a, b) => a - b);
  return { sockets, invalid, duplicates };
}

// app/web/src/lib/nppsParser.ts
class NPPSParseError extends Error {
  line;
  constructor(message, line) {
    super(line != null ? `Line ${line}: ${message}` : message);
    this.name = "NPPSParseError";
    this.line = line;
  }
}
var KEYWORDS = new Set([
  "protocol",
  "composite",
  "limits",
  "zone",
  "condition",
  "layer",
  "pbm_transcranial",
  "pbm_intranasal",
  "pbm_deep_1170nm",
  "eeg_neurofeedback",
  "bes_tacs",
  "tdcs",
  "vns_hrv",
  "audio_entrainment",
  "visual_stimulation",
  "qeeg_21ch",
  "tms",
  "clinical_tacs",
  "hd_tdcs",
  "cervical_vns",
  "vibrotactile_40hz",
  "id",
  "description",
  "author",
  "version",
  "readonly",
  "tags",
  "duration",
  "interval_count",
  "conditions",
  "references",
  "interval_on",
  "interval_off",
  "repeat",
  "enabled",
  "start",
  "end",
  "intensity_scale",
  "conflict_resolution",
  "level",
  "global",
  "helmet",
  "individual",
  "helmet_id",
  "individual_id",
  "sockets",
  "types",
  "exclude_types",
  "link",
  "code",
  "max_intensity",
  "max_frequency",
  "min_frequency",
  "max_duty_cycle",
  "max_session_dose",
  "max_daily_dose",
  "max_session_duration",
  "max_sessions_per_day",
  "max_sessions_per_week",
  "max_intensity_pct_mt",
  "max_pulses_per_session",
  "max_pulses_per_day",
  "max_binaural_beats",
  "max_isochronic_tones",
  "allowed_modes",
  "allowed_protocols",
  "allowed_targets",
  "allowed_montages",
  "allowed_bands",
  "require_closed_loop",
  "block_high_risk_range",
  "true",
  "false"
]);
var MODALITY_LIMITS_KEYS = new Set([
  "pbm_transcranial",
  "pbm_intranasal",
  "eeg_neurofeedback",
  "bes_tacs",
  "tdcs",
  "vns_hrv",
  "audio_entrainment",
  "visual_stimulation",
  "tms",
  "pbm_deep_1170nm",
  "clinical_tacs",
  "hd_tdcs",
  "cervical_vns",
  "vibrotactile_40hz"
]);
function tokenize(text) {
  const tokens = [];
  let pos = 0;
  let line = 1;
  while (pos < text.length) {
    if (text[pos] === " " || text[pos] === "\t" || text[pos] === "\r") {
      pos++;
      continue;
    }
    if (text[pos] === "#") {
      while (pos < text.length && text[pos] !== `
`)
        pos++;
      continue;
    }
    if (text[pos] === `
`) {
      tokens.push({ type: "NEWLINE", value: `
`, line });
      line++;
      pos++;
      continue;
    }
    if (text[pos] === "{") {
      tokens.push({ type: "LBRACE", value: "{", line });
      pos++;
      continue;
    }
    if (text[pos] === "}") {
      tokens.push({ type: "RBRACE", value: "}", line });
      pos++;
      continue;
    }
    if (text[pos] === "[") {
      tokens.push({ type: "LBRACKET", value: "[", line });
      pos++;
      continue;
    }
    if (text[pos] === "]") {
      tokens.push({ type: "RBRACKET", value: "]", line });
      pos++;
      continue;
    }
    if (text[pos] === ":") {
      tokens.push({ type: "COLON", value: ":", line });
      pos++;
      continue;
    }
    if (text[pos] === ",") {
      tokens.push({ type: "COMMA", value: ",", line });
      pos++;
      continue;
    }
    if (text[pos] === '"') {
      pos++;
      let str = "";
      while (pos < text.length && text[pos] !== '"') {
        if (text[pos] === "\\" && pos + 1 < text.length) {
          const next = text[pos + 1];
          if (next === '"')
            str += '"';
          else if (next === "\\")
            str += "\\";
          else if (next === "n")
            str += `
`;
          else if (next === "t")
            str += "\t";
          else
            str += next;
          pos += 2;
        } else {
          if (text[pos] === `
`)
            line++;
          str += text[pos++];
        }
      }
      if (pos >= text.length)
        throw new NPPSParseError("Unterminated string", line);
      pos++;
      tokens.push({ type: "STRING", value: str, line });
      continue;
    }
    if (text[pos] === "-" && pos + 1 < text.length && text[pos + 1] >= "0" && text[pos + 1] <= "9" || text[pos] >= "0" && text[pos] <= "9") {
      let numStr = "";
      if (text[pos] === "-") {
        numStr = "-";
        pos++;
      }
      while (pos < text.length && (text[pos] >= "0" && text[pos] <= "9" || text[pos] === ".")) {
        numStr += text[pos++];
      }
      if (numStr === "-") {
        throw new NPPSParseError("Stray minus sign", line);
      }
      let unit;
      if (pos < text.length) {
        if (text[pos] === "%") {
          unit = "%";
          pos++;
        } else if (text[pos] === "s" && (pos + 1 >= text.length || !/[a-zA-Z_0-9]/.test(text[pos + 1]))) {
          unit = "s";
          pos++;
        } else if (text[pos] === "m" && (pos + 1 >= text.length || !/[a-zA-Z_0-9]/.test(text[pos + 1]))) {
          unit = "m";
          pos++;
        } else if (text.slice(pos, pos + 2) === "Hz") {
          unit = "Hz";
          pos += 2;
        } else if (text.slice(pos, pos + 2) === "mA") {
          unit = "mA";
          pos += 2;
        }
      }
      if (!unit && pos < text.length && /[a-zA-Z_-]/.test(text[pos])) {
        let rest = numStr;
        while (pos < text.length && /[a-zA-Z0-9_-]/.test(text[pos]))
          rest += text[pos++];
        throw new NPPSParseError(`Unquoted value '${rest}' — values that start with a digit and are not ` + `a plain number must be quoted, e.g. "${rest}"`, line);
      }
      const tok = { type: "NUMBER", value: parseFloat(numStr), line };
      if (unit)
        tok.unit = unit;
      tokens.push(tok);
      continue;
    }
    if (text[pos] >= "a" && text[pos] <= "z" || text[pos] >= "A" && text[pos] <= "Z" || text[pos] === "_") {
      let ident = "";
      while (pos < text.length && /[a-zA-Z0-9_]/.test(text[pos])) {
        ident += text[pos++];
      }
      if (ident === "true") {
        tokens.push({ type: "BOOL", value: true, line });
      } else if (ident === "false") {
        tokens.push({ type: "BOOL", value: false, line });
      } else if (KEYWORDS.has(ident)) {
        tokens.push({ type: "KEYWORD", value: ident, line });
      } else {
        tokens.push({ type: "IDENT", value: ident, line });
      }
      continue;
    }
    throw new NPPSParseError(`Unexpected character: ${text[pos]}`, line);
  }
  tokens.push({ type: "EOF", value: "", line });
  return tokens;
}

class Parser {
  tokens;
  pos = 0;
  zones = [];
  conditions = [];
  constructor(tokens) {
    this.tokens = tokens;
  }
  get current() {
    return this.tokens[this.pos];
  }
  ct() {
    return this.tokens[this.pos].type;
  }
  advance() {
    const t = this.current;
    if (t.type !== "EOF")
      this.pos++;
    return t;
  }
  skipNewlines() {
    while (this.current.type === "NEWLINE")
      this.advance();
  }
  expect(type, value) {
    this.skipNewlines();
    const t = this.current;
    if (t.type !== type) {
      throw new NPPSParseError(`Expected ${value ?? type}, got ${t.type} (${String(t.value)})`, t.line);
    }
    if (value != null && t.value !== value) {
      throw new NPPSParseError(`Expected '${value}', got '${String(t.value)}'`, t.line);
    }
    return this.advance();
  }
  expectIdent(name) {
    this.skipNewlines();
    const t = this.current;
    if (t.type !== "KEYWORD" && t.type !== "IDENT" || t.value !== name) {
      throw new NPPSParseError(`Expected '${name}', got '${String(t.value)}'`, t.line);
    }
    this.advance();
  }
  tryKeyword(name) {
    this.skipNewlines();
    const t = this.current;
    if ((t.type === "KEYWORD" || t.type === "IDENT") && t.value === name) {
      this.advance();
      return true;
    }
    return false;
  }
  readString() {
    this.skipNewlines();
    const t = this.current;
    if (t.type === "STRING") {
      this.advance();
      return t.value;
    }
    if (t.type === "IDENT" || t.type === "KEYWORD") {
      this.advance();
      return t.value;
    }
    throw new NPPSParseError(`Expected string, got ${t.type}`, t.line);
  }
  readNumber() {
    this.skipNewlines();
    const t = this.current;
    if (t.type !== "NUMBER")
      throw new NPPSParseError(`Expected number, got ${t.type} (${String(t.value)})`, t.line);
    this.advance();
    return t.value;
  }
  readBool() {
    this.skipNewlines();
    const t = this.current;
    if (t.type === "BOOL") {
      this.advance();
      return t.value;
    }
    if (t.type === "IDENT" || t.type === "KEYWORD") {
      if (t.value === "true") {
        this.advance();
        return true;
      }
      if (t.value === "false") {
        this.advance();
        return false;
      }
    }
    throw new NPPSParseError(`Expected bool, got ${t.type} (${String(t.value)})`, t.line);
  }
  readTagArray() {
    this.skipNewlines();
    this.expect("LBRACKET");
    const arr = [];
    this.skipNewlines();
    while (this.current.type !== "RBRACKET" && this.current.type !== "EOF") {
      const t = this.current;
      if (t.type === "STRING" || t.type === "IDENT" || t.type === "KEYWORD") {
        arr.push(t.value);
        this.advance();
      } else if (t.type === "NUMBER") {
        arr.push(`${t.value}${t.unit ?? ""}`);
        this.advance();
      } else {
        throw new NPPSParseError(`Expected tag value, got ${t.type}`, t.line);
      }
      this.skipNewlines();
      if (this.current.type === "COMMA") {
        this.advance();
        this.skipNewlines();
      }
    }
    this.expect("RBRACKET");
    return arr;
  }
  readReferences() {
    const arr = this.readGenericArray();
    const out = [];
    for (const el of arr) {
      if (typeof el === "string") {
        out.push(el);
      } else if (Array.isArray(el) && el.length === 2) {
        out.push({ label: String(el[0]), url: String(el[1]) });
      } else if (Array.isArray(el) && el.length === 1) {
        out.push(String(el[0]));
      }
    }
    return out;
  }
  readDurationSeconds() {
    this.skipNewlines();
    const t = this.current;
    if (t.type !== "NUMBER")
      throw new NPPSParseError(`Expected duration, got ${t.type}`, t.line);
    this.advance();
    const val = t.value;
    if (t.unit === "m")
      return val * 60;
    return val;
  }
  skipValue() {
    this.skipNewlines();
    const t = this.current;
    if (t.type === "LBRACKET") {
      this.readGenericArray();
      return;
    }
    if (t.type === "LBRACE") {
      this.readGenericObject();
      return;
    }
    if (t.type === "STRING" || t.type === "NUMBER" || t.type === "BOOL" || t.type === "IDENT" || t.type === "KEYWORD") {
      this.advance();
    }
  }
  blockNameToTypeId(name) {
    const map = {
      pbm_transcranial: "pbm_transcranial",
      pbm_intranasal: "pbm_intranasal",
      eeg_neurofeedback: "eeg_neurofeedback",
      bes_tacs: "bes_tacs",
      tdcs: "tdcs",
      vns_hrv: "vns_hrv",
      audio_entrainment: "audio_entrainment",
      visual_stimulation: "visual_stimulation",
      qeeg_21ch: "qeeg_21ch",
      tms: "tms",
      pbm_deep_1170nm: "pbm_deep_1170nm",
      clinical_tacs: "clinical_tacs",
      hd_tdcs: "hd_tdcs",
      cervical_vns: "cervical_vns",
      vibrotactile_40hz: "vibrotactile_40hz"
    };
    return map[name] ?? null;
  }
  resolveFieldAlias(key) {
    const aliases = {
      frequency: "frequency_hz",
      duty_cycle: "duty_cycle_percent",
      closed_loop: "closed_loop_enabled",
      binaural_hz: "binaural_beats_hz",
      isochronic_hz: "isochronic_tones_hz",
      noise: "noise_type",
      volume: "volume_percent",
      breathing_rate: "resonance_breathing_rate",
      ramp: "ramp_seconds",
      emdr_cadence: "emdr_cadence_hz"
    };
    return aliases[key] ?? key;
  }
  parseTypedModalityBlock(typeName) {
    this.skipNewlines();
    this.expect("LBRACE");
    this.skipNewlines();
    const id = crypto.randomUUID();
    let enabled = true;
    let intervalOnSeconds = 0;
    let intervalOffSeconds = 0;
    let repeatCount;
    const raw = {};
    while (!this.tryBrace()) {
      this.skipNewlines();
      if (this.ct() === "RBRACE")
        break;
      const keyTok = this.current;
      if (keyTok.type !== "KEYWORD" && keyTok.type !== "IDENT") {
        throw new NPPSParseError(`Expected field name, got ${keyTok.type} (${String(keyTok.value)})`, keyTok.line);
      }
      const key = keyTok.value;
      this.advance();
      this.skipNewlines();
      this.expect("COLON");
      if (key === "interval_on") {
        intervalOnSeconds = this.readDurationSeconds();
        this.skipNewlines();
        continue;
      }
      if (key === "interval_off") {
        intervalOffSeconds = this.readDurationSeconds();
        this.skipNewlines();
        continue;
      }
      if (key === "repeat") {
        const t = this.current;
        if ((t.type === "IDENT" || t.type === "KEYWORD") && t.value === "until_end") {
          this.advance();
        } else {
          repeatCount = this.readNumber();
        }
        this.skipNewlines();
        continue;
      }
      if (key === "enabled") {
        enabled = this.readBool();
        this.skipNewlines();
        continue;
      }
      if (key === "intensity") {
        raw["__intensity"] = this.readAnyValue();
        this.skipNewlines();
        continue;
      }
      const canonical = this.resolveFieldAlias(key);
      const val = this.readAnyValue();
      if (canonical === "noise_type" && String(val) === "none") {
        this.skipNewlines();
        continue;
      }
      if (!(canonical !== key && raw[canonical] !== undefined)) {
        raw[canonical] = val;
      }
      this.skipNewlines();
    }
    if ("__intensity" in raw) {
      const intensityVal = raw["__intensity"];
      delete raw["__intensity"];
      const percentTypes = new Set([
        "pbm_transcranial",
        "pbm_intranasal",
        "visual_stimulation"
      ]);
      const mATypes = new Set([
        "bes_tacs",
        "tdcs",
        "vns_hrv",
        "clinical_tacs",
        "hd_tdcs",
        "cervical_vns",
        "tms"
      ]);
      if (percentTypes.has(typeName)) {
        raw["intensity_percent"] = intensityVal;
      } else if (mATypes.has(typeName)) {
        raw["intensity_milliamps"] = intensityVal;
      } else if (typeName === "vibrotactile_40hz") {
        raw["intensity_g"] = intensityVal;
      } else {
        raw["intensity_percent"] = intensityVal;
      }
    }
    const typeId = this.blockNameToTypeId(typeName);
    if (!typeId) {
      throw new NPPSParseError(`Unknown modality block type: '${typeName}'`, this.current.line);
    }
    const params = this.buildModalityParams(typeId, raw, this.current.line);
    const interval = { intervalOnSeconds, intervalOffSeconds };
    if (repeatCount !== undefined)
      interval.repeatCount = repeatCount;
    return { id, modalityParams: params, interval, enabled };
  }
  readKeyValue() {
    this.skipNewlines();
    const t = this.current;
    if (t.type !== "KEYWORD" && t.type !== "IDENT") {
      throw new NPPSParseError(`Expected key, got ${t.type} (${String(t.value)})`, t.line);
    }
    const key = t.value;
    const valueLine = t.line;
    this.advance();
    this.skipNewlines();
    this.expect("COLON");
    return { key, valueLine };
  }
  readLimitsKey() {
    this.skipNewlines();
    const t = this.current;
    if (t.type !== "KEYWORD" && t.type !== "IDENT") {
      throw new NPPSParseError(`Expected key, got ${t.type} (${String(t.value)})`, t.line);
    }
    const key = t.value;
    const valueLine = t.line;
    this.advance();
    this.skipNewlines();
    if (this.ct() === "LBRACE")
      return { key, isBlock: true, valueLine };
    this.expect("COLON");
    return { key, isBlock: false, valueLine };
  }
  parse() {
    const entries = [];
    this.skipNewlines();
    while (this.current.type !== "EOF") {
      this.skipNewlines();
      if (this.ct() === "EOF")
        break;
      if (this.tryKeyword("protocol")) {
        entries.push({ kind: "single", protocol: this.parseProtocol() });
      } else if (this.tryKeyword("composite")) {
        entries.push({ kind: "composite", composite: this.parseComposite() });
      } else if (this.tryKeyword("limits")) {
        this.parseLimitsBlock();
      } else if (this.tryKeyword("zone")) {
        this.zones.push(this.parseZoneBlock());
      } else if (this.tryKeyword("condition")) {
        this.conditions.push(this.parseConditionBlock());
      } else {
        throw new NPPSParseError(`Expected 'protocol', 'composite', 'limits', 'zone', or 'condition', got '${String(this.current.value)}'`, this.current.line);
      }
      this.skipNewlines();
    }
    return entries;
  }
  parseLimitsBlockPublic() {
    return this.parseLimitsBlock();
  }
  parseLimitsBlock() {
    const startLine = this.current.line;
    this.skipNewlines();
    let name = "Untitled Limits";
    if (this.current.type === "STRING") {
      name = this.current.value;
      this.advance();
    } else if (this.current.type === "IDENT") {
      name = this.current.value;
      this.advance();
    }
    this.skipNewlines();
    this.expect("LBRACE");
    this.skipNewlines();
    const now = new Date().toISOString();
    let level = "global";
    let helmetId;
    let individualId;
    let description = "";
    let pbmTranscranial;
    let pbmIntranasal;
    let eegNeurofeedback;
    let besTacs;
    let tdcs;
    let vnsHrv;
    let audioEntrainment;
    let visualStimulation;
    let tms;
    let pbmDeep1170nm;
    let clinicalTacs;
    let hdTdcs;
    let cervicalVns;
    let vibrotactile40hz;
    while (!this.tryBrace()) {
      const { key, isBlock } = this.readLimitsKey();
      if (isBlock !== MODALITY_LIMITS_KEYS.has(key)) {
        throw new NPPSParseError(isBlock ? `'${key}' is a scalar limits field, not a sub-block` : `'${key}' is a per-modality limits sub-block and takes no ':'`, startLine);
      }
      switch (key) {
        case "level": {
          const v = this.readString();
          if (v === "global" || v === "helmet" || v === "individual")
            level = v;
          break;
        }
        case "helmet_id":
          helmetId = this.readString();
          break;
        case "individual_id":
          individualId = this.readString();
          break;
        case "description":
          description = this.readString();
          break;
        case "pbm_transcranial":
          pbmTranscranial = this.parsePBMTranscranialLimits();
          break;
        case "pbm_intranasal":
          pbmIntranasal = this.parsePBMIntranasalLimits();
          break;
        case "eeg_neurofeedback":
          eegNeurofeedback = this.parseEEGNeurofeedbackLimits();
          break;
        case "bes_tacs":
          besTacs = this.parseBESTacsLimits();
          break;
        case "tdcs":
          tdcs = this.parseTDCSLimits();
          break;
        case "vns_hrv":
          vnsHrv = this.parseVNSHRVLimits();
          break;
        case "audio_entrainment":
          audioEntrainment = this.parseAudioEntrainmentLimits();
          break;
        case "visual_stimulation":
          visualStimulation = this.parseVisualStimLimits();
          break;
        case "tms":
          tms = this.parseTMSLimits();
          break;
        case "pbm_deep_1170nm":
          pbmDeep1170nm = this.parseDeepPBMLimits();
          break;
        case "clinical_tacs":
          clinicalTacs = this.parseClinicalTacsLimits();
          break;
        case "hd_tdcs":
          hdTdcs = this.parseHDTdcsLimits();
          break;
        case "cervical_vns":
          cervicalVns = this.parseCervicalVnsLimits();
          break;
        case "vibrotactile_40hz":
          vibrotactile40hz = this.parseVibrotactileLimits();
          break;
        default:
          throw new NPPSParseError(`Unknown limits key: '${key}'`, startLine);
      }
      this.skipNewlines();
    }
    const result = {
      id: crypto.randomUUID(),
      name,
      description,
      createdAt: now,
      modifiedAt: now,
      level
    };
    if (helmetId)
      result.helmetId = helmetId;
    if (individualId)
      result.individualId = individualId;
    if (pbmTranscranial)
      result.pbmTranscranial = pbmTranscranial;
    if (pbmIntranasal)
      result.pbmIntranasal = pbmIntranasal;
    if (eegNeurofeedback)
      result.eegNeurofeedback = eegNeurofeedback;
    if (besTacs)
      result.besTacs = besTacs;
    if (tdcs)
      result.tdcs = tdcs;
    if (vnsHrv)
      result.vnsHrv = vnsHrv;
    if (audioEntrainment)
      result.audioEntrainment = audioEntrainment;
    if (visualStimulation)
      result.visualStimulation = visualStimulation;
    if (tms)
      result.tms = tms;
    if (pbmDeep1170nm)
      result.pbmDeep1170nm = pbmDeep1170nm;
    if (clinicalTacs)
      result.clinicalTacs = clinicalTacs;
    if (hdTdcs)
      result.hdTdcs = hdTdcs;
    if (cervicalVns)
      result.cervicalVns = cervicalVns;
    if (vibrotactile40hz)
      result.vibrotactile40hz = vibrotactile40hz;
    return result;
  }
  parseLimitsSubBlock(fieldMap) {
    this.skipNewlines();
    this.expect("LBRACE");
    this.skipNewlines();
    const result = {};
    while (!this.tryBrace()) {
      const { key } = this.readKeyValue();
      const handler = fieldMap[key];
      if (handler) {
        Object.assign(result, handler(this.readAnyValue()));
      } else {
        this.readAnyValue();
      }
      this.skipNewlines();
    }
    return result;
  }
  parsePBMTranscranialLimits() {
    return this.parseLimitsSubBlock({
      max_intensity: (v) => ({ maxIntensityPercent: Number(v) }),
      max_frequency: (v) => ({ maxFrequencyHz: Number(v) }),
      max_duty_cycle: (v) => ({ maxDutyCyclePercent: Number(v) }),
      max_session_dose: (v) => ({ maxSessionDoseJCm2: Number(v) }),
      max_daily_dose: (v) => ({ maxDailyDoseJCm2: Number(v) })
    });
  }
  parsePBMIntranasalLimits() {
    return this.parseLimitsSubBlock({
      max_intensity: (v) => ({ maxIntensityPercent: Number(v) }),
      max_session_dose: (v) => ({ maxSessionDoseJCm2: Number(v) }),
      max_session_duration: (v) => ({ maxSessionDurationSeconds: Number(v) })
    });
  }
  parseEEGNeurofeedbackLimits() {
    return this.parseLimitsSubBlock({
      allowed_bands: (v) => ({ allowedBands: Array.isArray(v) ? v : [String(v)] }),
      require_closed_loop: (v) => ({ requireClosedLoop: Boolean(v) })
    });
  }
  parseBESTacsLimits() {
    return this.parseLimitsSubBlock({
      max_intensity: (v) => ({ maxIntensityMilliamps: Number(v) }),
      max_frequency: (v) => ({ maxFrequencyHz: Number(v) }),
      min_frequency: (v) => ({ minFrequencyHz: Number(v) }),
      max_session_duration: (v) => ({ maxSessionDurationSeconds: Number(v) }),
      max_sessions_per_day: (v) => ({ maxSessionsPerDay: Number(v) })
    });
  }
  parseTDCSLimits() {
    return this.parseLimitsSubBlock({
      max_intensity: (v) => ({ maxIntensityMilliamps: Number(v) }),
      max_session_duration: (v) => ({ maxSessionDurationSeconds: Number(v) }),
      max_sessions_per_day: (v) => ({ maxSessionsPerDay: Number(v) })
    });
  }
  parseVNSHRVLimits() {
    return this.parseLimitsSubBlock({
      max_intensity: (v) => ({ maxIntensityMilliamps: Number(v) }),
      max_frequency: (v) => ({ maxFrequencyHz: Number(v) }),
      max_session_duration: (v) => ({ maxSessionDurationSeconds: Number(v) }),
      allowed_protocols: (v) => ({ allowedProtocols: Array.isArray(v) ? v : [String(v)] })
    });
  }
  parseAudioEntrainmentLimits() {
    return this.parseLimitsSubBlock({
      max_intensity: (v) => ({ maxVolumePercent: Number(v) }),
      max_frequency: (v) => ({ maxBinauralBeatsHz: Number(v) }),
      max_binaural_beats: (v) => ({ maxBinauralBeatsHz: Number(v) }),
      max_isochronic_tones: (v) => ({ maxIsochronicTonesHz: Number(v) })
    });
  }
  parseVisualStimLimits() {
    return this.parseLimitsSubBlock({
      max_frequency: (v) => ({ maxFrequencyHz: Number(v) }),
      min_frequency: (v) => ({ minFrequencyHz: Number(v) }),
      allowed_modes: (v) => ({ allowedModes: Array.isArray(v) ? v : [String(v)] }),
      block_high_risk_range: (v) => ({ blockHighRiskRange: Boolean(v) })
    });
  }
  parseTMSLimits() {
    return this.parseLimitsSubBlock({
      max_intensity_pct_mt: (v) => ({ maxIntensityPercentMT: Number(v) }),
      max_pulses_per_session: (v) => ({ maxPulsesPerSession: Number(v) }),
      max_pulses_per_day: (v) => ({ maxPulsesPerDay: Number(v) }),
      max_sessions_per_week: (v) => ({ maxSessionsPerWeek: Number(v) }),
      allowed_protocols: (v) => ({ allowedProtocols: Array.isArray(v) ? v : [String(v)] }),
      allowed_targets: (v) => ({ allowedTargets: Array.isArray(v) ? v : [String(v)] })
    });
  }
  parseDeepPBMLimits() {
    return this.parseLimitsSubBlock({
      max_intensity: (v) => ({ maxIntensityMWcm2: Number(v) }),
      max_session_duration: (v) => ({ maxSessionDurationSeconds: Number(v) })
    });
  }
  parseClinicalTacsLimits() {
    return this.parseLimitsSubBlock({
      max_intensity: (v) => ({ maxIntensityMilliamps: Number(v) }),
      max_session_duration: (v) => ({ maxSessionDurationSeconds: Number(v) })
    });
  }
  parseHDTdcsLimits() {
    return this.parseLimitsSubBlock({
      max_intensity: (v) => ({ maxIntensityMilliamps: Number(v) }),
      max_session_duration: (v) => ({ maxSessionDurationSeconds: Number(v) }),
      allowed_montages: (v) => ({ allowedMontages: Array.isArray(v) ? v : [String(v)] })
    });
  }
  parseCervicalVnsLimits() {
    return this.parseLimitsSubBlock({
      max_intensity: (v) => ({ maxIntensityMilliamps: Number(v) }),
      max_session_duration: (v) => ({ maxSessionDurationSeconds: Number(v) })
    });
  }
  parseVibrotactileLimits() {
    return this.parseLimitsSubBlock({
      max_intensity: (v) => ({ maxIntensityG: Number(v) }),
      max_session_duration: (v) => ({ maxSessionDurationSeconds: Number(v) })
    });
  }
  parseProtocol() {
    this.skipNewlines();
    if (this.current.type !== "STRING") {
      throw new NPPSParseError(`Expected protocol name string, got ${this.current.type}`, this.current.line);
    }
    const name = this.current.value;
    this.advance();
    this.skipNewlines();
    this.expect("LBRACE");
    let description = "";
    let author = "NeurOne";
    let version = "1.0";
    let tags = [];
    let timingMode = { type: "duration", seconds: 1200 };
    let modalities = [];
    let parsedId;
    let isReadOnly;
    let conditions;
    let references;
    const now = new Date().toISOString();
    this.skipNewlines();
    while (!this.tryBrace()) {
      this.skipNewlines();
      if (this.ct() === "RBRACE")
        break;
      const keyTok = this.current;
      if (keyTok.type !== "KEYWORD" && keyTok.type !== "IDENT") {
        throw new NPPSParseError(`Expected key, got ${keyTok.type} (${String(keyTok.value)})`, keyTok.line);
      }
      const key = keyTok.value;
      this.advance();
      this.skipNewlines();
      if (this.ct() === "LBRACE") {
        modalities.push(this.parseTypedModalityBlock(key));
        this.skipNewlines();
        continue;
      }
      this.expect("COLON");
      switch (key) {
        case "id":
          parsedId = this.readString();
          break;
        case "description":
          description = this.readString();
          break;
        case "author":
          author = this.readString();
          break;
        case "version":
          version = this.readString();
          break;
        case "readonly":
          isReadOnly = this.readBool();
          break;
        case "tags":
          tags = this.readTagArray();
          break;
        case "duration":
          timingMode = { type: "duration", seconds: this.readDurationSeconds() };
          break;
        case "interval_count":
          timingMode = { type: "interval_count", count: this.readNumber() };
          break;
        case "conditions":
          conditions = this.readTagArray();
          break;
        case "references":
          references = this.readReferences();
          break;
        default:
          this.skipValue();
          break;
      }
      this.skipNewlines();
    }
    const id = parsedId ?? crypto.randomUUID();
    const proto = {
      id,
      name,
      description,
      author,
      version,
      tags,
      createdAt: now,
      modifiedAt: now,
      isPredefined: false,
      timingMode,
      modalities
    };
    if (isReadOnly !== undefined)
      proto.isReadOnly = isReadOnly;
    if (conditions !== undefined)
      proto.conditions = conditions;
    if (references !== undefined)
      proto.references = references;
    if (parsedId !== undefined)
      proto.isPredefined = true;
    return proto;
  }
  tryBrace() {
    this.skipNewlines();
    if (this.current.type === "RBRACE") {
      this.advance();
      return true;
    }
    return false;
  }
  readAnyValue() {
    this.skipNewlines();
    const t = this.current;
    if (t.type === "STRING") {
      this.advance();
      return t.value;
    }
    if (t.type === "NUMBER") {
      this.advance();
      return t.value;
    }
    if (t.type === "BOOL") {
      this.advance();
      return t.value;
    }
    if (t.type === "IDENT" || t.type === "KEYWORD") {
      if (t.value === "true") {
        this.advance();
        return true;
      }
      if (t.value === "false") {
        this.advance();
        return false;
      }
      this.advance();
      return t.value;
    }
    if (t.type === "LBRACKET")
      return this.readGenericArray();
    if (t.type === "LBRACE")
      return this.readGenericObject();
    throw new NPPSParseError(`Unexpected token ${t.type} in value position`, t.line);
  }
  readGenericArray() {
    this.expect("LBRACKET");
    const arr = [];
    this.skipNewlines();
    while (this.current.type !== "RBRACKET" && this.current.type !== "EOF") {
      arr.push(this.readAnyValue());
      this.skipNewlines();
      if (this.current.type === "COMMA") {
        this.advance();
        this.skipNewlines();
      }
    }
    this.expect("RBRACKET");
    return arr;
  }
  readGenericObject() {
    this.expect("LBRACE");
    this.skipNewlines();
    const obj = {};
    while (this.current.type !== "RBRACE" && this.current.type !== "EOF") {
      const { key } = this.readKeyValue();
      obj[key] = this.readAnyValue();
      this.skipNewlines();
      if (this.current.type === "COMMA") {
        this.advance();
        this.skipNewlines();
      }
    }
    this.expect("RBRACE");
    return obj;
  }
  buildModalityParams(typeId, raw, line) {
    const def = defaultParams(typeId);
    function str(key, fallback) {
      return typeof raw[key] === "string" ? raw[key] : fallback;
    }
    function num(key, fallback) {
      return typeof raw[key] === "number" ? raw[key] : fallback;
    }
    function bool(key, fallback) {
      return typeof raw[key] === "boolean" ? raw[key] : fallback;
    }
    function optNum(key) {
      return typeof raw[key] === "number" ? raw[key] : undefined;
    }
    function optStr(key) {
      return typeof raw[key] === "string" ? raw[key] : undefined;
    }
    switch (typeId) {
      case "pbm_transcranial": {
        const d = def;
        const rawZones = raw["zones"];
        let zones = d.zones;
        let zoneRefs = d.zones === "named" ? d.zoneRefs : undefined;
        if (Array.isArray(rawZones)) {
          const els = rawZones;
          if (!els.every((e) => typeof e === "string")) {
            throw new NPPSParseError(`pbm_transcranial: zones must be a list of quoted zone names, ` + `e.g. zones: ["Frontal Left", "Frontal Right"]`, line);
          }
          if (els.length === 0) {
            throw new NPPSParseError(`pbm_transcranial: zones: [] names no zone — a session cannot target nothing`, line);
          }
          zones = "named";
          zoneRefs = els;
        } else if (rawZones !== undefined) {
          if (rawZones !== "clinician_selected") {
            throw new NPPSParseError(`pbm_transcranial: unknown zone selector '${String(rawZones)}'. zones must be a ` + `list of quoted zone names (e.g. zones: ["Frontal"]) or clinician_selected`, line);
          }
          zones = "clinician_selected";
        }
        const params = {
          zones,
          wavelength: str("wavelength", d.wavelength),
          intensityPercent: num("intensity_percent", d.intensityPercent),
          frequencyHz: num("frequency_hz", d.frequencyHz),
          dutyCyclePercent: num("duty_cycle_percent", d.dutyCyclePercent)
        };
        if (zoneRefs)
          params.zoneRefs = zoneRefs;
        return { type: "pbm_transcranial", params };
      }
      case "pbm_intranasal": {
        const d = def;
        return {
          type: "pbm_intranasal",
          params: {
            intensityPercent: num("intensity_percent", d.intensityPercent),
            frequencyHz: num("frequency_hz", d.frequencyHz),
            dutyCyclePercent: num("duty_cycle_percent", d.dutyCyclePercent)
          }
        };
      }
      case "eeg_neurofeedback": {
        const d = def;
        const params = {
          channels: str("channels", d.channels),
          band: str("band", d.band),
          closedLoopEnabled: bool("closed_loop_enabled", d.closedLoopEnabled)
        };
        const customCh = raw["custom_channels"];
        if (Array.isArray(customCh))
          params.customChannels = customCh;
        return { type: "eeg_neurofeedback", params };
      }
      case "bes_tacs": {
        const d = def;
        return {
          type: "bes_tacs",
          params: {
            frequencyHz: num("frequency_hz", d.frequencyHz),
            intensityMilliamps: num("intensity_milliamps", d.intensityMilliamps),
            waveform: str("waveform", d.waveform)
          }
        };
      }
      case "tdcs": {
        const d = def;
        const rawPairs = raw["electrode_pairs"];
        const electrodePairs = Array.isArray(rawPairs) ? rawPairs.map((p) => {
          if (Array.isArray(p) && p.length >= 2)
            return [String(p[0]), String(p[1])];
          return ["Fp1", "Fp2"];
        }) : d.electrodePairs;
        return {
          type: "tdcs",
          params: {
            intensityMilliamps: num("intensity_milliamps", d.intensityMilliamps),
            electrodePairs,
            rampSeconds: num("ramp_seconds", d.rampSeconds)
          }
        };
      }
      case "vns_hrv": {
        const d = def;
        return {
          type: "vns_hrv",
          params: {
            frequencyHz: num("frequency_hz", d.frequencyHz),
            intensityMilliamps: num("intensity_milliamps", d.intensityMilliamps),
            hrvProtocol: str("hrv_protocol", d.hrvProtocol),
            resonanceBreathingRate: num("resonance_breathing_rate", d.resonanceBreathingRate)
          }
        };
      }
      case "audio_entrainment": {
        const d = def;
        const params = {
          carrierHz: num("carrier_hz", d.carrierHz),
          volumePercent: num("volume_percent", d.volumePercent),
          eegAdaptive: bool("eeg_adaptive", d.eegAdaptive),
          boneConductionPacer: bool("bone_conduction_pacer", d.boneConductionPacer)
        };
        const bb = optNum("binaural_beats_hz");
        if (bb !== undefined)
          params.binauralBeatsHz = bb;
        const iso = optNum("isochronic_tones_hz");
        if (iso !== undefined)
          params.isochronicTonesHz = iso;
        const nt = optStr("noise_type");
        if (nt)
          params.noiseType = nt;
        return { type: "audio_entrainment", params };
      }
      case "visual_stimulation": {
        const d = def;
        return {
          type: "visual_stimulation",
          params: {
            frequencyHz: num("frequency_hz", d.frequencyHz),
            mode: str("mode", d.mode),
            emdrCadenceHz: num("emdr_cadence_hz", d.emdrCadenceHz),
            enableModeF: bool("enable_mode_f", d.enableModeF)
          }
        };
      }
      case "qeeg_21ch": {
        const d = def;
        return {
          type: "qeeg_21ch",
          params: {
            montage: str("montage", d.montage),
            sloretaEnabled: bool("sloreta_enabled", d.sloretaEnabled),
            reference: str("reference", d.reference)
          }
        };
      }
      case "tms": {
        const d = def;
        return {
          type: "tms",
          params: {
            tmsProtocol: str("tms_protocol", d.tmsProtocol),
            frequencyHz: num("frequency_hz", d.frequencyHz),
            intensityPercentMT: num("intensity_percent_mt", d.intensityPercentMT),
            target: str("target", d.target),
            pulseCount: num("pulse_count", d.pulseCount)
          }
        };
      }
      case "pbm_deep_1170nm": {
        const d = def;
        return {
          type: "pbm_deep_1170nm",
          params: {
            intensityMWcm2: num("intensity_mw_cm2", d.intensityMWcm2),
            frequencyHz: num("frequency_hz", d.frequencyHz),
            dutyCyclePercent: num("duty_cycle_percent", d.dutyCyclePercent)
          }
        };
      }
      case "clinical_tacs": {
        const d = def;
        return {
          type: "clinical_tacs",
          params: {
            frequencyHz: num("frequency_hz", d.frequencyHz),
            intensityMilliamps: num("intensity_milliamps", d.intensityMilliamps),
            channelCount: num("channel_count", d.channelCount),
            waveform: str("waveform", d.waveform)
          }
        };
      }
      case "hd_tdcs": {
        const d = def;
        return {
          type: "hd_tdcs",
          params: {
            target: str("target", d.target),
            montage: str("montage", d.montage),
            intensityMilliamps: num("intensity_milliamps", d.intensityMilliamps)
          }
        };
      }
      case "cervical_vns": {
        const d = def;
        return {
          type: "cervical_vns",
          params: {
            frequencyHz: num("frequency_hz", d.frequencyHz),
            intensityMilliamps: num("intensity_milliamps", d.intensityMilliamps)
          }
        };
      }
      case "vibrotactile_40hz": {
        const d = def;
        return {
          type: "vibrotactile_40hz",
          params: {
            intensityG: num("intensity_g", d.intensityG),
            syncToAudio: bool("sync_to_audio", d.syncToAudio),
            syncToVisual: bool("sync_to_visual", d.syncToVisual)
          }
        };
      }
      default: {
        const _exhaustive = typeId;
        throw new NPPSParseError(`Unknown modality type: ${String(_exhaustive)}`);
      }
    }
  }
  parseComposite() {
    this.skipNewlines();
    if (this.current.type !== "STRING") {
      throw new NPPSParseError(`Expected composite name string, got ${this.current.type}`, this.current.line);
    }
    const name = this.current.value;
    this.advance();
    this.skipNewlines();
    this.expect("LBRACE");
    this.skipNewlines();
    let description = "";
    let author = "NeurOne";
    let version = "1.0";
    let tags = [];
    let layers = [];
    let conflictResolution = "merge";
    let parsedId;
    let isReadOnly;
    let conditions;
    let references;
    const now = new Date().toISOString();
    while (!this.tryBrace()) {
      this.skipNewlines();
      if (this.ct() === "RBRACE")
        break;
      const keyTok = this.current;
      if ((keyTok.type === "KEYWORD" || keyTok.type === "IDENT") && keyTok.value === "layer") {
        this.advance();
        layers.push(this.parseLayerBlock());
        this.skipNewlines();
        continue;
      }
      const { key } = this.readKeyValue();
      switch (key) {
        case "id":
          parsedId = this.readString();
          break;
        case "description":
          description = this.readString();
          break;
        case "author":
          author = this.readString();
          break;
        case "version":
          version = this.readString();
          break;
        case "readonly":
          isReadOnly = this.readBool();
          break;
        case "tags":
          tags = this.readTagArray();
          break;
        case "conflict_resolution":
          conflictResolution = this.readString();
          break;
        case "conditions":
          conditions = this.readTagArray();
          break;
        case "references":
          references = this.readReferences();
          break;
        default:
          this.skipValue();
          break;
      }
      this.skipNewlines();
    }
    const id = parsedId ?? crypto.randomUUID();
    const composite = {
      id,
      name,
      description,
      author,
      version,
      tags,
      createdAt: now,
      modifiedAt: now,
      isPredefined: false,
      layers,
      conflictResolution
    };
    if (isReadOnly !== undefined)
      composite.isReadOnly = isReadOnly;
    if (conditions !== undefined)
      composite.conditions = conditions;
    if (references !== undefined)
      composite.references = references;
    if (parsedId !== undefined)
      composite.isPredefined = true;
    return composite;
  }
  parseZoneBlock() {
    this.skipNewlines();
    if (this.current.type !== "STRING") {
      throw new NPPSParseError(`Expected zone name string, got ${this.current.type}`, this.current.line);
    }
    const name = this.current.value;
    this.advance();
    this.skipNewlines();
    this.expect("LBRACE");
    this.skipNewlines();
    const zone = { name, sockets: [], isPredefined: false };
    while (!this.tryBrace()) {
      const { key } = this.readKeyValue();
      switch (key) {
        case "id":
          zone.id = this.readString();
          zone.isPredefined = true;
          break;
        case "description":
          zone.description = this.readString();
          break;
        case "sockets": {
          const line = this.current.line;
          const arr = this.readGenericArray();
          const { sockets, invalid } = toSocketSet(arr);
          if (invalid.length > 0) {
            throw new NPPSParseError(`zone "${name}": ${invalid.map(describeInvalid).join(", ")} ` + `${invalid.length === 1 ? "is not a socket" : "are not sockets"} ` + `on this helmet — ids are whole numbers ${socketRangeLabel()} ` + `(${NP_SOCKET_COUNT} sockets, numbered from ${NP_SOCKET_NUMBERING_BASE})`, line);
          }
          zone.sockets = sockets;
          break;
        }
        case "types": {
          const arr = this.readGenericArray();
          zone.types = arr.map(String);
          break;
        }
        case "exclude_types":
          zone.excludeTypes = this.readBool();
          break;
        default:
          this.skipValue();
          break;
      }
      this.skipNewlines();
    }
    return zone;
  }
  parseConditionBlock() {
    this.skipNewlines();
    if (this.current.type !== "STRING") {
      throw new NPPSParseError(`Expected condition name string, got ${this.current.type}`, this.current.line);
    }
    const name = this.current.value;
    this.advance();
    this.skipNewlines();
    this.expect("LBRACE");
    this.skipNewlines();
    let link = "";
    let id;
    let description;
    let code;
    while (!this.tryBrace()) {
      const { key } = this.readKeyValue();
      switch (key) {
        case "link":
          link = this.readString();
          break;
        case "id":
          id = this.readString();
          break;
        case "description":
          description = this.readString();
          break;
        case "code":
          code = this.readString();
          break;
        default:
          this.skipValue();
          break;
      }
      this.skipNewlines();
    }
    const cond = { name, link };
    if (id !== undefined)
      cond.id = id;
    if (description !== undefined)
      cond.description = description;
    if (code !== undefined)
      cond.code = code;
    return cond;
  }
  parseLayerBlock() {
    this.skipNewlines();
    if (this.current.type !== "STRING") {
      throw new NPPSParseError(`Expected layer protocol name string, got ${this.current.type}`, this.current.line);
    }
    const protocolName = this.current.value;
    this.advance();
    this.skipNewlines();
    this.expect("LBRACE");
    this.skipNewlines();
    let startOffsetSeconds = 0;
    let durationSeconds;
    let intensityScale = 1;
    const id = crypto.randomUUID();
    while (!this.tryBrace()) {
      const { key } = this.readKeyValue();
      switch (key) {
        case "start":
          startOffsetSeconds = this.readDurationSeconds();
          break;
        case "end":
          durationSeconds = this.readDurationSeconds() - startOffsetSeconds;
          break;
        case "duration":
          durationSeconds = this.readDurationSeconds();
          break;
        case "intensity_scale":
          intensityScale = this.readNumber();
          break;
        default:
          this.skipValue();
          break;
      }
      this.skipNewlines();
    }
    return { id, protocolName, startOffsetSeconds, durationSeconds, intensityScale };
  }
}
function parseNPPSFile(text) {
  const tokens = tokenize(text);
  const parser = new Parser(tokens);
  const entries = parser.parse();
  return { entries, zones: [...parser.zones], conditions: [...parser.conditions] };
}
function buildNamespace(files) {
  const entries = [];
  const zones = new Map;
  const conditions = new Map;
  const errors = [];
  const collidedZones = new Set;
  const collidedConditions = new Set;
  for (const f of files) {
    entries.push(...f.entries);
    for (const z of f.zones) {
      if (collidedZones.has(z.name))
        continue;
      if (zones.has(z.name)) {
        errors.push(`Duplicate zone name '${z.name}' — defined in more than one file; zone names must be ` + `unique across the protocol directory. The name is left undefined.`);
        zones.delete(z.name);
        collidedZones.add(z.name);
        continue;
      }
      zones.set(z.name, z);
    }
    for (const c of f.conditions) {
      if (collidedConditions.has(c.name))
        continue;
      if (conditions.has(c.name)) {
        errors.push(`Duplicate condition name '${c.name}' — defined in more than one file; condition names ` + `must be unique across the protocol directory. The name is left undefined.`);
        conditions.delete(c.name);
        collidedConditions.add(c.name);
        continue;
      }
      conditions.set(c.name, c);
    }
  }
  return { namespace: { entries, zones, conditions }, errors };
}
function validateNamespaceReferences(ns) {
  const errors = [];
  for (const entry of ns.entries) {
    const def = entry.kind === "single" ? entry.protocol : entry.composite;
    for (const cond of def.conditions ?? []) {
      if (!ns.conditions.has(cond)) {
        errors.push(`Protocol '${def.name}' references undefined condition '${cond}'`);
      }
    }
    if (entry.kind === "single") {
      for (const m of entry.protocol.modalities) {
        if (m.modalityParams.type === "pbm_transcranial") {
          for (const zref of m.modalityParams.params.zoneRefs ?? []) {
            if (!ns.zones.has(zref)) {
              errors.push(`Protocol '${def.name}' references undefined zone '${zref}'`);
            }
          }
        }
      }
    }
  }
  return errors;
}

// simulator/src/npps-runtime.ts
var DEFAULT_BASE_URL = "../protocols/predefined/";
var T2_ONLY_MODALITY_TYPES = new Set([
  "qeeg_21ch",
  "tms",
  "pbm_deep_1170nm",
  "clinical_tacs",
  "hd_tdcs",
  "cervical_vns"
]);
var PBM_PEAK_MW_CM2 = 400;
var PROTOCOLS = {};
var PROTOCOL_IDS = [];
var ZONES = [];
var LOAD_REPORT = { compositesSkipped: 0, duplicateDefinitions: [], unresolvedReferences: [] };
function slugFromFilename(filename) {
  return filename.replace(/\.npps$/, "");
}
function splitWavelength(wl) {
  if (wl === "1064nm" || wl === "1170nm")
    return [wl];
  return wl.replace(/nm$/, "").split("_").map((n) => `${n}nm`);
}
function findParam(modalities, type) {
  const m = modalities.find((m2) => m2.modalityParams.type === type);
  return m ? m.modalityParams.params : undefined;
}
function deriveTier(def) {
  return def.modalities.some((m) => T2_ONLY_MODALITY_TYPES.has(m.modalityParams.type)) ? "T2" : "T1";
}
function derivePhases(durationSeconds) {
  if (durationSeconds < 120) {
    return [{ label: "Session", start: 0, end: durationSeconds, color: "#334466" }];
  }
  const ramp = Math.min(60, Math.round(durationSeconds * 0.05));
  return [
    { label: "Ramp", start: 0, end: ramp, color: "#334466" },
    { label: "Main", start: ramp, end: durationSeconds - ramp, color: "#446688" },
    { label: "Ramp-down", start: durationSeconds - ramp, end: durationSeconds, color: "#334466" }
  ];
}
function buildModalities(def, durationSeconds) {
  const out = {};
  const pbm = findParam(def.modalities, "pbm_transcranial");
  if (pbm) {
    const dutyCycle = pbm.dutyCyclePercent / 100;
    const avgIrradianceMWcm2 = PBM_PEAK_MW_CM2 * (pbm.intensityPercent / 100) * dutyCycle;
    const dose_jcm2 = avgIrradianceMWcm2 * durationSeconds / 1000;
    out.pbm = {
      active: true,
      zones: pbm.zoneRefs ?? [],
      wavelengths: splitWavelength(pbm.wavelength),
      frequency: pbm.frequencyHz,
      dutyCycle,
      dose_jcm2: Math.round(dose_jcm2 * 10) / 10
    };
  }
  const eeg = findParam(def.modalities, "eeg_neurofeedback");
  if (eeg) {
    out.eeg = {
      active: true,
      channels: ["Fp1", "Fp2", "F3", "F4", "C3", "C4", "P3", "P4"],
      band: eeg.band
    };
  }
  const bes = findParam(def.modalities, "bes_tacs");
  if (bes) {
    out.bes = { active: true, frequency: bes.frequencyHz, intensity_ma: bes.intensityMilliamps };
  }
  const tdcs = findParam(def.modalities, "tdcs");
  if (tdcs) {
    out.tdcs = { active: true, intensity_ma: tdcs.intensityMilliamps };
  }
  const vns = findParam(def.modalities, "vns_hrv");
  if (vns) {
    out.vns = {
      active: true,
      frequency: vns.frequencyHz,
      intensity_ma: vns.intensityMilliamps,
      hrv_sync: vns.hrvProtocol !== "standalone"
    };
  }
  const audio = findParam(def.modalities, "audio_entrainment");
  if (audio) {
    out.audio = {
      active: true,
      binaural_hz: audio.binauralBeatsHz ?? audio.isochronicTonesHz ?? 10,
      base_hz: audio.carrierHz,
      type: audio.binauralBeatsHz != null ? "binaural" : "isochronic",
      breathing_pacer: audio.boneConductionPacer,
      breathing_bpm: vns?.resonanceBreathingRate ?? 6
    };
  }
  const visual = findParam(def.modalities, "visual_stimulation");
  if (visual) {
    out.visual = { active: true, frequency: visual.frequencyHz, mode: visual.mode };
  }
  const tms = findParam(def.modalities, "tms");
  if (tms) {
    out.tms = { active: true, frequency: tms.frequencyHz, target: tms.target, pattern: tms.tmsProtocol };
  }
  const hdTdcs = findParam(def.modalities, "hd_tdcs");
  if (hdTdcs) {
    out.hd_tdcs = { active: true, target: hdTdcs.target, intensity_ma: hdTdcs.intensityMilliamps };
  }
  return out;
}
function buildLibrary(files) {
  const parsedFiles = files.map((f) => ({ filename: f.filename, parsed: parseNPPSFile(f.text) }));
  const { namespace, errors } = buildNamespace(parsedFiles.map((p) => p.parsed));
  const zones = [...namespace.zones.values()].map((z) => ({
    name: z.name,
    sockets: z.sockets,
    description: z.description ?? ""
  }));
  const protocols = {};
  let compositesSkipped = 0;
  for (const { filename, parsed } of parsedFiles) {
    if (parsed.entries.length === 0)
      continue;
    const entry = parsed.entries[0];
    if (entry.kind === "composite") {
      compositesSkipped++;
      continue;
    }
    const def = entry.protocol;
    const slug = slugFromFilename(filename);
    const duration = def.timingMode.type === "duration" ? def.timingMode.seconds : 1200;
    protocols[slug] = {
      id: slug,
      name: def.name,
      description: def.description,
      tier: deriveTier(def),
      duration,
      modalities: buildModalities(def, duration),
      phases: derivePhases(duration),
      conditions: def.conditions ?? []
    };
  }
  return {
    protocols,
    zones,
    report: {
      compositesSkipped,
      duplicateDefinitions: errors,
      unresolvedReferences: validateNamespaceReferences(namespace)
    }
  };
}
async function loadLibrary(baseUrl = DEFAULT_BASE_URL) {
  const base = baseUrl.endsWith("/") ? baseUrl : `${baseUrl}/`;
  const manifest = await fetch(`${base}manifest.json`).then((r) => {
    if (!r.ok)
      throw new Error(`manifest.json: HTTP ${r.status}`);
    return r.json();
  });
  const names = [
    ...manifest.zones ?? [],
    ...manifest.conditions ?? [],
    ...manifest.protocols ?? [],
    ...manifest.composites ?? []
  ];
  const files = await Promise.all(names.map(async (filename) => {
    const r = await fetch(`${base}${filename}`);
    if (!r.ok)
      throw new Error(`${filename}: HTTP ${r.status}`);
    return { filename, text: await r.text() };
  }));
  const { protocols, zones, report } = buildLibrary(files);
  PROTOCOLS = protocols;
  PROTOCOL_IDS = Object.keys(protocols);
  ZONES = zones;
  LOAD_REPORT = report;
  if (report.duplicateDefinitions.length) {
    console.error("[NP-SIM] duplicate definitions:", report.duplicateDefinitions);
  }
  if (report.unresolvedReferences.length) {
    console.error("[NP-SIM] unresolved references:", report.unresolvedReferences);
  }
}
export {
  loadLibrary,
  buildModalities,
  buildLibrary,
  ZONES,
  PROTOCOL_IDS,
  PROTOCOLS,
  LOAD_REPORT
};
